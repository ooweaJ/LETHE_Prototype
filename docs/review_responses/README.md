# Legacy Review Responses

This folder is a legacy pointer only. Do not create new review responses here.

Current source-of-truth location:

```text
docs/orchestration/review_responses/
```

Older logs or reports may mention `docs/review_responses/` as historical context. Treat those references as archived evidence, not the current working path.
