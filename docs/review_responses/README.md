# Legacy Review Responses

이 폴더는 마이그레이션 전 경로다. 새 review response를 여기에 만들지 않는다.

현재 기준 위치:

```text
docs/orchestration/review_responses/
```

기존 dated response 파일은 오케스트레이션 폴더로 옮겼다. 과거 로그나 리포트가 `docs/review_responses/`를 언급하는 경우는 당시의 역사 기록으로만 해석한다.
