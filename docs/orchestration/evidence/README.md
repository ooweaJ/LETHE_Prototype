# Evidence

Store or link useful proof here:

- balance summaries,
- browser QA summaries,
- screenshots,
- playtest logs,
- benchmark results,
- generated report artifacts,
- small summary files that point to larger legacy artifacts.

Prefer linking to large existing artifacts instead of duplicating them.

Current legacy evidence locations:

- `docs/balance/`
- `alpha_test/outputs/`
- `playtest_logs/`
- `docs/reports/`
