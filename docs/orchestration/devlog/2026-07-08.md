# 2026-07-08 Devlog

## Memory/Echo Kingmaker VFX and Judgment Pass

- Trigger:
  - jaewoo approved the broader reward-shape design and asked to implement VFX plus matching hitbox/effect behavior so the build can be playtested.
  - Main balance goal: Blood Reflection should not be the only route with obvious power, sustain, VFX, and ultimate payoff.
- Unity connection:
  - AnkleBreaker Unity MCP was available on `LETHE` port `7890`.
  - Scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - MCP menu/code calls sometimes returned `Error polling queue: fetch failed`, but console logs confirmed the QA menus executed.
- Code changes:
  - Added threat-priority helper targeting for Hunter-style effects.
  - Added execution forecast VFX for near-threshold targets.
  - Added stopped-fracture burst VFX/damage helpers.
  - Added Ashen stored guard charge, consume, and release wave helpers.
  - Execution, Shatter, Stopped, Ashen, and Oblivion now have stronger route-specific payoff behavior.
  - Fracture Execution, Stasis Hunt, and Ashen Oblivion were buffed so non-blood ultimates feel more competitive.
  - Dense Dual Blades benchmark-only damage was reduced to prevent kill-chain aftermath from polluting the perf measurement.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity `Assets/Refresh`: success.
  - Unity compilation errors: `0`.
  - VFX Matrix: PASS, `previewMemory=8`, `previewEcho=8`, `fracture=1`, `stasis=1`, `ashen=1`.
  - Echo Matrix Dual Blades: PASS, `total=226`, `state=78`.
  - Echo Matrix Greatsword: PASS, `total=207`, `state=57`.
  - Passive Memory Matrix: PASS, `blood=17`, `ash=6`, `stopped=8`, `oblivion=62`.
  - Utility Ultimate Matrix Dual Blades: PASS, `fracture=28`, `stasis=11`, `ashen=47`.
  - Utility Ultimate Matrix Greatsword: PASS, `fracture=49`, `stasis=22`, `ashen=14`.
  - Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=45`, `activeVfx=26`, `ms=57.58`.
- Notes:
  - Untracked Unity recovery files remain untouched: `LETHE/Assets/_Recovery.meta`, `LETHE/Assets/_Recovery/`.
  - This is still `_dev` validation and does not promote anything to `Assets/Lethe`.
- Next:
  - jaewoo direct-play review should focus on whether Ashen, Stopped, Shatter, Execution, Hunter, and Oblivion now feel like valid routes beside Blood Reflection.

## Kalmuri Echo Weapon Identity Follow-Up

- Trigger:
  - jaewoo pointed out that echoes should keep weapon identity, and that Kalmuri Echo still did not look right.
- Finding:
  - Greatsword and Dual Blades had different numbers/counts, but Kalmuri Echo still shared the same clamp/bite action language.
- Code changes:
  - Added `ResolveGreatswordKalmuriFollowup()`.
  - Greatsword Kalmuri Echo now exits the shared clamp/bite path and uses a falling judgement blade, drop line, ground-rip blade, execution rift, cross rift, ground teeth, and impact core.
  - Dual Blades Kalmuri Echo remains the fast swarm/bite pattern.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: standalone rerun passed with 0 warnings and 0 errors after one temporary DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Echo Matrix Greatsword: PASS, `total=335`, `K=136`, `state=58`.
  - Echo Matrix Dual Blades: PASS, `total=230`, `K=8`, `state=85`.
  - Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=94`, `activeVfx=30`, `ms=87.85`.
- Risk:
  - Greatsword Kalmuri is now much louder. Direct play should decide whether `K=136` feels satisfying or excessive.

## Kalmuri Wound-Reaction Correction

- Trigger:
  - jaewoo pointed out that the awakened Kalmuri Echo design itself was wrong if it reads as "after the weapon attack, one more blade leaves the player body."
  - Design correction: Kalmuri should behave like an echo at the attack/wound point, closer to Blood Reflection's target-side interaction.
- Code changes:
  - `LaunchKalmuriBlade()` now receives weapon/forward context and starts the awakened chain from the struck enemy wound point.
  - Added wound burst, wound scar, chain line, and wound-chain projectile behavior/naming.
  - Dense Dual Blades now suppresses the expensive awakened wound-chain follow-up so dense play keeps the core Kalmuri hit but avoids the perf regression seen during testing.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors after sequential rerun; the first parallel attempt locked the shared DLL.
  - Unity `Assets/Refresh`: success.
  - Unity compilation errors: `0`.
  - Echo Matrix Dual Blades: PASS, `total=230`, `K=8`, `state=86`.
  - Echo Matrix Greatsword: PASS, `total=335`, `K=136`, `state=58`.
  - Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=87`, `activeVfx=25`, `ms=80.59`.
- Notes:
  - The previous dense perf regression was reproduced at `ms=127.47` before suppression, then recovered to `ms=80.59`.
  - Untracked Unity recovery files remain untouched: `LETHE/Assets/_Recovery.meta`, `LETHE/Assets/_Recovery/`.

## Kalmuri Concept Preview Debugger

- Trigger:
  - jaewoo said the wound-chain version still felt like a single blade flying out and asked how to debug the alternate ideas.
  - Candidate concepts to compare: wound-feast, trail-bloom, cross-swarm, and mark-frenzy.
- Code changes:
  - Added `DebugPreviewKalmuriConcept(int concept)`.
  - Added F12 debug panel buttons `K1`, `K2`, `K3`, `K4`.
  - Each button clears/rebuilds the same small enemy pack in front of the player and plays one concept sample:
    - K1 wound-feast: blades collapse into the wound.
    - K2 trail-bloom: weapon trail afterimages multiply into delayed cuts.
    - K3 cross-swarm: surrounding blades cross-cut inward.
    - K4 mark-frenzy: a hungry scar mark forks into nearby enemies.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors on sequential rerun.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity console errors: `0`.
  - Unity `EditorApplication.isCompiling` stayed `true` longer than expected after refresh, so direct click verification is pending.
- Next:
  - jaewoo should run Play Mode, press `F12`, compare `K1` through `K4`, then choose a winner or hybrid for the real Kalmuri Echo implementation.

## Kalmuri Concept Preview Readability Split

- Trigger:
  - jaewoo reported that the first Kalmuri preview buttons all looked the same.
- Finding:
  - The first preview set used similar cyan Kalmuri blade sprites and short target-centered motion, so the concepts did not separate visually enough.
- Code changes:
  - K1 wound-feast now uses red/orange wound disc, bite ring, inward teeth, and scar cuts.
  - K2 trail-bloom now uses blue attack-ribbon afterimages, trail scars, and a delayed rip line.
  - K3 cross-swarm now uses purple/white radial inward cuts and a large X impact.
  - K4 mark-frenzy now uses violet seal/ring/fork links and secondary seals rather than extra flying blades.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors; sequential rerun confirmed no code errors after a parallel DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors: `0`.

## Kalmuri Preview High-HP Dummies

- Trigger:
  - jaewoo could not tell whether the preview update had applied and said the dummy enemies died too quickly to inspect the VFX.
- Code changes:
  - Added an optional `debugHpOverride` parameter to `SpawnEnemy()`.
  - K-preview pack now spawns enemies with HP `9999`.
  - K-preview damage is capped to `1`.
  - K-preview presses now show `K Preview v2 / high HP dummies` above the dummy pack.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors: `0`.
- Note:
  - If the v2 label does not appear, Play Mode is likely still using the old assembly; exit and re-enter Play Mode.

## Kalmuri VFX Hard Reset Handoff

- Trigger:
  - jaewoo said the VFX still does not appear to change and that the current work keeps reusing the old look.
  - Direct instruction: remove the existing reused style and make the next Kalmuri preview entirely different.
- Decision:
  - Stop polishing the current `K1` to `K4` preview set.
  - Treat it as a failed preview because it still carries too much old Kalmuri blade/cyan/short-burst language.
- Next-session implementation note:
  - In `V1GameManager.cs`, delete or bypass the current Kalmuri preview helpers before building the next set.
  - Do not use `KalmuriBladeSprite()` in the new preview candidates.
  - Do not use current Kalmuri helper visuals such as clamp/barrage/surge/awaken blade for the new preview.
  - Build new silhouettes:
    - K1: wound mouth / saw-tooth scar.
    - K2: long ribbon trail / afterimage strip.
    - K3: large geometric X/cross burst.
    - K4: curse-mark seal and forked chain network.
  - Keep high-HP dummies and replace the label with `K Preview HARD RESET`.
- Resume instruction:
  - Start next session from `docs/orchestration/state/CURRENT_TASK.md`, section `Kalmuri VFX Hard Reset Handoff`.
