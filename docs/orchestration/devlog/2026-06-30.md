# 2026-06-30 Devlog

## Boss Pattern / No Reacquire Patch

- Responded to jaewoo direct-play feedback:
  - first Gatekeeper timing is good.
  - second Gatekeeper feels late.
  - boss TTK is too short.
  - boss has no pattern, so the player can free-hit too much.
  - enemy count/HP feels okay.
  - HP bars look wrong.
  - memory reacquire after forgetting should be removed entirely.
  - Hungry Blades appears to pause/stutter when base attacks happen.
- Changed normal Gatekeeper schedule to `150 / 300 / 540 / 900s`.
- Changed normal Gatekeeper HP to `2200 / 4200 / 7600 / 12800`.
- Changed hard cap to `1080s`.
- Added Gatekeeper pulse/guard behavior:
  - periodic pulse VFX.
  - close-range damage/push.
  - short guard window with damage reduction.
- Removed normal post-forget memory reacquire flow:
  - no refill overlay.
  - no `ReacquireLastForgotten` method.
  - result overlay now returns directly to combat.
  - HUD copy no longer asks the player to recover the lost memory.
- Fixed HP bar read by counter-scaling the health-bar root against enemy squash/local scale.
- Improved Hungry Blades continuity and optimization:
  - orbit visual updates continue during hitstop.
  - orbit spawn cadence is throttled with pooled transient sprites.
- Updated:
  - `scripts/balance_curve_v1.js`
  - `scripts/verify_unity_stepped_balance.js`
  - `docs/orchestration/evidence/2026-06-30-boss-pattern-no-reacquire-qa.md`

## Verification

- `node scripts/balance_curve_v1.js`: passed.
- `node scripts/verify_unity_stepped_balance.js`: passed.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after retry.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
- Unity MCP:
  - `Dev_Prototype_v1` active.
  - compile errors `0`.
  - console errors `0`.
  - Play Mode entered and stopped cleanly.
- Limitation:
  - V1QA menu did not produce a captured `[V1QA] PASS` log because MCP bridge restarted/returned a parse error during play-mode automation.
