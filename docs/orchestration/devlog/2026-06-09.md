# 2026-06-09 개발일지

## 최신 마이그레이션 프롬프트 재정렬

- 목표: 덮어써진 `docs/orchestration/MIGRATION_PROMPT.md` 기준에 맞춰 현재 프로젝트 문서 구조를 다시 시험 가능한 상태로 맞춘다.
- 확인한 차이:
  - `reports/index.html`은 unit 목록이 아니라 최신순 날짜 archive여야 한다.
  - `reports/YYYYMMDD/index.html`은 사람이 보는 기본 날짜 리포트 화면이어야 한다.
  - 날짜 리포트는 긴 본문 하나가 아니라 제목 카드 목록으로 작업 단위를 보여줘야 한다.
  - `reports/YYYYMMDD/units/`는 개별 본문 또는 Discord 첨부용 detail page다.
  - Discord handoff는 짧은 한국어 요약과 report path를 Project Orchestrator에 넘기는 규약이 필요하다.
- 변경:
  - `scripts/build_report.js`를 정상 UTF-8 한국어 문자열로 재작성했다.
  - 생성기가 `reports/index.html`을 날짜 archive로 만들도록 했다.
  - 생성기가 `reports/YYYYMMDD/index.html`을 unit-card 페이지로 만들도록 했다.
  - unit HTML에서 날짜 페이지로 돌아가는 링크를 추가했다.
  - report archive 생성기의 프로젝트명 하드코딩을 없애고 `state/PROJECT_BRIEF.md` 기반으로 읽도록 했다.
  - `AGENTS.md`, `docs/orchestration/README.md`, `state/RUNBOOK.md`, `state/CURRENT_TASK.md`의 report/devlog 규칙을 최신 기준에 맞췄다.
  - `docs/DISCORD_REPORTING.md`, `docs/orchestration/MIGRATION_PROMPT.md`, `docs/orchestration/templates/HTML_INTERFACE_TEMPLATE.md`에 Project Orchestrator intake payload 규약을 반영했다.
  - 2026-06-05, 2026-06-06, 2026-06-07 사람용 리포트 원본을 한국어로 재작성했다.
  - 2026-06-05, 2026-06-06, 2026-06-07 개발일지를 한국어로 재정리했다.
  - 깨져 있던 2026-06-09 보고서 원본을 정상 한국어로 다시 작성했다.
- 남은 것:
  - 2026-06-02부터 2026-06-04까지의 대량 과거 원본은 별도 pass로 더 정리할 수 있다.
  - Project Orchestrator intake 실행 명령/API는 아직 이 저장소에 구현되어 있지 않다.
- 커밋:
  - 만들지 않음. 사용자가 테스트할 수 있는 working tree 변경으로 유지한다.
- 검증:
  - `node --check scripts/build_report.js`: 통과.
  - `node scripts\build_report.js --all`: 통과, 2026-06-01부터 2026-06-09까지 날짜 리포트와 unit HTML 재생성.
  - `npm run report:check`: 통과, 최신 날짜 2026-06-09 unit heading 확인.
  - `Get-Content -Encoding UTF8`로 2026-06-05, 2026-06-07, 2026-06-09 HTML 제목과 2026-06-07 unit HTML 제목 정상 한국어 확인.

## 보고서 카드 클릭 라우팅 수정

- 문제: 서버가 보고서 HTML을 `/api/projects/lethe/` 아래에서 보여줄 때, 카드의 `href="units/*.html"`가 `/api/projects/lethe/units/*.html`로 해석되어 `Cannot GET`이 발생했다.
- 변경:
  - 날짜별 보고서 카드 링크를 unit HTML 직접 이동에서 해시 기반 전환으로 바꿨다.
  - `reports/YYYYMMDD/index.html` 안에 unit 본문을 함께 렌더링한다.
  - 카드 클릭은 `#unit-YYYY-MM-DD-NN`로 전환하고, `날짜 리포트로 돌아가기`는 카드 목록을 다시 보여준다.
  - unit HTML 파일은 Discord 첨부와 직접 확인용으로 계속 생성한다.
- 검증:
  - `node --check scripts/build_report.js`: 통과.
  - `node scripts\build_report.js --all`: 통과.
  - `npm run report:check`: 통과, 2026-06-09 unit 2개 확인.
  - `docs/orchestration/reports/20260606/index.html`에서 카드 href가 `#unit-2026-06-06-01` 형태이고 본문 section이 같은 HTML 안에 포함된 것을 확인했다.

## Project Orchestrator Discord 연결

- 목표: `docs/orchestration/MIGRATION_PROMPT.md`의 중앙 Discord intake 규약을 실제 LETHE 명령으로 연결한다.
- 변경:
  - `scripts/send_orchestrator_discord_report.js` 추가.
  - `package.json`에 `report:orchestrator`, `report:orchestrator:dry`, `report:orchestrator:unit`, `report:orchestrator:unit:dry` 추가.
  - `docs/DISCORD_REPORTING.md`, `state/RUNBOOK.md`, `interface/runbook.html`에 Orchestrator 전송 명령 반영.
  - `MIGRATION_PROMPT.md`와 `HTML_INTERFACE_TEMPLATE.md`에 다른 프로젝트가 따라야 할 script 이름, Project Orchestrator payload, date-page hash drill-down 규칙 보강.
- 판단:
  - 현재 `MIGRATION_PROMPT.md`는 다른 프로젝트가 문서 구조를 따라가기 충분하다.
  - 다만 실행 가능성을 위해 Node 프로젝트는 `report:orchestrator:*` 스크립트 또는 동등한 CLI/API 호출을 반드시 추가해야 한다.
- 검증:
  - `node --check scripts/send_orchestrator_discord_report.js`: 통과.
  - `node --check scripts/build_report.js`: 통과.
  - `node scripts\send_orchestrator_discord_report.js --latest-section --dry-run --print-payload`: 통과.
  - `npm run report:orchestrator:unit:dry`: 통과, Orchestrator가 Discord embed payload와 attachment를 생성.
  - `npm run report:orchestrator:unit`: 통과, Orchestrator 응답 `accepted: true`, `sent: true`, attachment `sent: true`.

## v0.12 밸런스 실패 후보 검증

- 목표: `docs/balance/2026-06-08-v012-balance-qa.md`와 `docs/orchestration/review_prompts/2026-06-08-balance-loop.md`를 기준으로 한 번에 하나의 작은 조정만 검증한다.
- 분석:
  - 2026-06-08 실패는 first boss clear `100%`, first boss TTK median `26.42s`, level-ups before first boss median `10`, slot fill median `26.68s`로 초반/보스 지표는 통과했다.
  - 실패 지표는 full clear `20%`, death `60%`였고 death phase는 `망각 전조` 3회로 몰렸다.
  - 따라서 기억 성능, 보스 HP, XP를 건드리지 않고 post-first-cycle `망각 전조` cap만 낮추는 후보를 선택했다.
- 시도한 단일 조정:
  - `src/game.js`의 `laterCycleClimax`를 `46 -> 42`로 낮췄다.
  - 다른 수치, 새 기억, 상점, 메타, 지역, 최종보스 확장은 건드리지 않았다.
- 검증:
  - `npm run qa:balance`: `ITERATE_BALANCE`, first boss clear `100%`, full clear `20%`, death `80%`, first boss TTK median `26.17s`.
  - `npm run balance:loop`: `ITERATE_BALANCE`, first boss clear `100%`, full clear `20%`, death `80%`, first boss TTK median `23.1s`.
  - 생성 리포트: `docs/balance/2026-06-09-v012-balance-qa.md`.
  - 생성 프롬프트: `docs/orchestration/review_prompts/2026-06-09-balance-loop.md`.
  - `npm run report`: 통과, 2026-06-09 보고서 HTML과 4개 unit report 재생성.
  - `npm run report:check`: 통과, 4개 unit heading 확인.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
- 판단:
  - 후보는 실패했다. death rate가 `60% -> 80%`로 악화됐고 death phase도 `망각 전조`만이 아니라 `결손 압박`, `압박 상승`까지 퍼졌다.
  - `laterCycleClimax 46 -> 42`는 accepted tuning으로 채택하지 않는다.
  - `src/game.js`는 기존 `laterCycleClimax: 46`으로 되돌렸다.
- 다음:
  - pure post-cycle climax cap cut은 다음 즉시 후보에서 제외한다.
  - 다음 후보는 refill/transition safety, deficit pressure, early HP erosion, enemy damage timing 중 하나를 증거 기반으로 골라야 한다.

## v0.12 밸런스 기준선 회복

- 목표: cap-only 후보 실패 이후, 한 번에 하나의 작은 레버로 death rate를 `<= 40%`까지 회복한다.
- 기준선 재측정:
  - `npm run balance:loop`: `ITERATE_BALANCE`, first boss clear `80%`, full clear `40%`, death `60%`, first boss TTK median `21.44s`.
  - clear minimum, first boss clear, first boss TTK, level-up, slot fill, top DPS share는 통과했고 death rate만 실패했다.
  - death phase는 `압박 상승`, `망각 전조`, `첫 망각 문지기`로 흩어져 있어 특정 phase cap보다 전체 생존 버퍼 문제로 판단했다.
- 적용한 단일 조정:
  - `src/game.js` player max HP `180 -> 190`.
  - damage, memories, bosses, shop, meta, region, final boss scope는 변경하지 않았다.
- 검증:
  - `npm run balance:loop` pass 1: `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `60%`, death `40%`, first boss TTK median `18.97s`.
  - `npm run balance:loop` pass 2: `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `60%`, death `40%`, first boss TTK median `20.18s`.
  - 최신 리포트: `docs/balance/2026-06-09-v012-balance-qa.md`.
  - 최신 review prompt: `docs/orchestration/review_prompts/2026-06-09-balance-loop.md`.
  - `npm run report`: 통과, 2026-06-09 보고서 HTML과 5개 unit report 재생성.
  - `npm run report:check`: 통과, 5개 unit heading 확인.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
- 판단:
  - HP `190`은 accepted tuning으로 채택한다.
  - 자동 밸런스 게이트는 회복됐다.
  - 다음 blocker는 숫자 튜닝이 아니라 controlled human session evidence다.

## v0.12 사람 테스트 패키지 갱신

- 목표: HP `190` 기준선이 반영된 v0.12를 사람이 바로 테스트할 수 있게 패키지 생성 상태까지 확인한다.
- 검증:
  - `npm run playtest:package:dry`: 통과. `index.html`, `style.css`, `src/game.js`, human playtest guide, notes template, playtest sheet, README 복사 계획 확인.
  - `npm run playtest:package`: 통과. `dist\lethe-v0.12-playtest` 생성.
  - `npm run report`: 통과, 2026-06-09 보고서 HTML과 6개 unit report 재생성.
  - `npm run report:check`: 통과, 6개 unit heading 확인.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
- 판단:
  - 자동 밸런스 게이트와 패키지 생성은 준비됐다.
  - 다음 blocker는 실제 human session log/note 수집이다.
