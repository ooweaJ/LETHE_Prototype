# 2026-06-19 Devlog

## 120-second early fun-loop direction pass

- User asked to list the implementation direction in detail and proceed.
- Added `J. 120초 초반 재미 루프` to `docs/TASK.md`.
  - Scope is not more content.
  - Scope is early fun density: start choice, first rewards, visible core memories, next forget candidate, and echo anticipation inside the first 120 seconds.
- Implementation checklist now tracks:
  - weapon + starting memory selection.
  - four start build cards.
  - core Kalmuri/Blood reward fill.
  - first 20-30 second reward rhythm.
  - 60-90 second memory/forget-candidate readability.
  - direct jaewoo review before broad tuning.

## Runtime changes

- Changed `V1GameManager` start flow:
  - `1`: Dual Blades + Hungry Blades.
  - `2`: Dual Blades + Blood Reflection.
  - `3`: Greatsword + Hungry Blades.
  - `4`: Greatsword + Blood Reflection.
- `BeginRun(V1WeaponId)` now delegates to `BeginRun(V1WeaponId, V1MemoryId)` with Hungry Blades as the debug/default memory.
- The start overlay now uses four build cards instead of two weapon cards.
- Level-up choice generation now prioritizes missing core memories:
  - if the run starts with Blood Reflection, Hungry Blades is preserved as a priority reward candidate.
  - if the run starts with Hungry Blades, Blood Reflection is preserved as a priority reward candidate.
- `NextMissingRewardMemory` now includes Hungry Blades and Blood Reflection before the wider 8-memory rotation.

## Verification

- Initial sandboxed `dotnet build LETHE/Assembly-CSharp.csproj --nologo` failed because dotnet tried to write its first-run sentinel under `C:\Users\dbwid\.dotnet`.
- Re-ran `dotnet build LETHE/Assembly-CSharp.csproj --nologo` with escalation:
  - pass.
  - 7 legacy v0/debug deprecation warnings.
  - 0 errors.
- Unity MCP:
  - active scene: `Dev_Prototype_v1`.
  - compile error count: 0.
  - Play Mode reached `isPlaying=true`.
  - console error log count: 0.
  - Play Mode stopped successfully.
- Attempted camera-based Game View screenshots, but the capture path saved only the camera background rather than the OnGUI overlay.
- The unusable black screenshot files were removed and should not be treated as visual UI evidence.

## Next

- jaewoo should directly run `Dev_Prototype_v1`, choose each start build card at least once, and judge the first 120 seconds.
- Main review questions:
  - Does the start card communicate the run direction?
  - Does the missing Kalmuri/Blood card appear quickly enough?
  - Does the first level-up rhythm arrive fast enough?
  - Does the next forget candidate feel like a build decision rather than just HUD text?
- Based on that review, choose exactly one next pass: reward cadence, attack readability, forgetting UX, spawn pressure, or art replacement.

## Five-pass follow-up

User asked to continue through all five next passes and keep commits separate.

Separate commits created:

- `695771f feat: 초반 보상 속도 보정`
  - Early non-boss kills grant +1 XP during the first 120 seconds.
  - Floating XP text is cyan during the boosted early window.
- `fbf0f0f feat: 무기 타격 확인 피드백 추가`
  - Weapon hits now spawn a small confirm ring and impact core in addition to the existing hit spark/damage number.
  - Greatsword uses a larger, warmer confirm pulse than dual blades.
- `111cdab feat: 망각 결과 UX 강화`
  - Forgetting result overlay now explains lost memory, remaining echo, awakening, overcharge if present, deficit survival, and resonance next action.
  - Added floating loss/echo text at the transformation moment.
- `b19a1b6 feat: 초반 스폰 압박 보정`
  - Added a first-120-second spawn profile for the first cycle.
  - Spawn radius is closer during the early review window.
  - Early cap is set to 28 to keep pressure visible without matching later-cycle density.
- `081e13b feat: 적 역할 실루엣 보정`
  - Drifting Eye, Split One, Void Priest, and Gatekeeper now use distinct procedural silhouettes.
  - This is not final art, but it should read better than role-colored circles.

Verification:

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo` passed after each pass.
- Each build retained 7 legacy v0/debug deprecation warnings and 0 errors.
- Final Unity MCP check:
  - compile error count 0.
  - Play Mode entered.
  - console error count 0.
  - Play Mode stopped.

Remaining risk:

- These were implementation passes, not human fun proof.
- Direct play is still required to decide which of the five axes actually improved the first 120 seconds most.

## Direct Codex smoke-test follow-up

User asked Codex to test the current build directly.

Added an editor smoke menu:

- `LETHE/V1 Smoke/Start Dual Kalmuri`
- `LETHE/V1 Smoke/Start Dual Blood`
- `LETHE/V1 Smoke/Start Great Kalmuri`
- `LETHE/V1 Smoke/Start Great Blood`
- `LETHE/V1 Smoke/M2 Loop`

The menu starts Play Mode, invokes the target start build or M2 debug loop, logs `DebugSnapshot()`, then stops Play Mode. `SessionState` is used so the queued smoke target survives Unity domain reload when entering Play Mode from edit mode.

Verification:

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed, 0 warnings, 0 errors.
- Unity AssetDatabase refresh: success.
- Unity compile error count: 0.
- Start-build smoke snapshots:
  - DualBlades + HungryBlades -> weapon `절단쌍검`, memories `[HungryBlades:1]`, result/refill/death false.
  - DualBlades + BloodReflection -> weapon `절단쌍검`, memories `[BloodReflection:1]`, result/refill/death false.
  - Greatsword + HungryBlades -> weapon `장송대검`, memories `[HungryBlades:1]`, result/refill/death false.
  - Greatsword + BloodReflection -> weapon `장송대검`, memories `[BloodReflection:1]`, result/refill/death false.
- M2 loop smoke snapshot:
  - memories `[BloodReflection:3,HungryBlades:3]`.
  - echoes `[HungryBlades:5,BloodReflection:5]`.
  - enemies `10`.
  - storm `True`.
  - result overlay `True`.
  - death `False`.
- Unity console error log count after M2 smoke: 0.
- Unity missing references:
  - scene: 0.
  - assets: 0.

Remaining risk:

- This confirms technical initialization and debug-loop wiring.
- It still does not replace direct human feel review of the first 120 seconds.
