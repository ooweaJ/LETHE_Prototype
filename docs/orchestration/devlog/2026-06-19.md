# 2026-06-19 Devlog

## 120-second early fun-loop direction pass

- User asked to list the implementation direction in detail and proceed.
- Added `J. 120초 초반 재미 루프` to `docs/TASK.md`.
  - Scope is not more content.
  - Scope is early fun density: start choice, first rewards, visible core memories, next forget candidate, and echo anticipation inside the first 120 seconds.
- Implementation checklist now tracks:
  - weapon + starting memory selection.
  - four start build cards.
  - core Kalmuri/Blood reward fill.
  - first 20-30 second reward rhythm.
  - 60-90 second memory/forget-candidate readability.
  - direct jaewoo review before broad tuning.

## Runtime changes

- Changed `V1GameManager` start flow:
  - `1`: Dual Blades + Hungry Blades.
  - `2`: Dual Blades + Blood Reflection.
  - `3`: Greatsword + Hungry Blades.
  - `4`: Greatsword + Blood Reflection.
- `BeginRun(V1WeaponId)` now delegates to `BeginRun(V1WeaponId, V1MemoryId)` with Hungry Blades as the debug/default memory.
- The start overlay now uses four build cards instead of two weapon cards.
- Level-up choice generation now prioritizes missing core memories:
  - if the run starts with Blood Reflection, Hungry Blades is preserved as a priority reward candidate.
  - if the run starts with Hungry Blades, Blood Reflection is preserved as a priority reward candidate.
- `NextMissingRewardMemory` now includes Hungry Blades and Blood Reflection before the wider 8-memory rotation.

## Verification

- Initial sandboxed `dotnet build LETHE/Assembly-CSharp.csproj --nologo` failed because dotnet tried to write its first-run sentinel under `C:\Users\dbwid\.dotnet`.
- Re-ran `dotnet build LETHE/Assembly-CSharp.csproj --nologo` with escalation:
  - pass.
  - 7 legacy v0/debug deprecation warnings.
  - 0 errors.
- Unity MCP:
  - active scene: `Dev_Prototype_v1`.
  - compile error count: 0.
  - Play Mode reached `isPlaying=true`.
  - console error log count: 0.
  - Play Mode stopped successfully.
- Attempted camera-based Game View screenshots, but the capture path saved only the camera background rather than the OnGUI overlay.
- The unusable black screenshot files were removed and should not be treated as visual UI evidence.

## Next

- jaewoo should directly run `Dev_Prototype_v1`, choose each start build card at least once, and judge the first 120 seconds.
- Main review questions:
  - Does the start card communicate the run direction?
  - Does the missing Kalmuri/Blood card appear quickly enough?
  - Does the first level-up rhythm arrive fast enough?
  - Does the next forget candidate feel like a build decision rather than just HUD text?
- Based on that review, choose exactly one next pass: reward cadence, attack readability, forgetting UX, spawn pressure, or art replacement.
