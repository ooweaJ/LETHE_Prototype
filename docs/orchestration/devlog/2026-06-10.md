# 2026-06-10 Devlog

## Unit: HTML forgetting model gate

User decision received: run `ITERATE_BEFORE_TEST` before Unity. The next validation build should change only the forgetting model inside the existing HTML scope: highest-level memory forgetting, echo `+5` cap with overcharge overflow, and reacquisition resonance.

Implementation notes:

- Replaced the active forgetting selection path with highest-level memory selection while keeping `forgetWeightedMemory`/`forgetMostDependent` wrappers for existing callers.
- Added player tie selection for normal play. Automated QA/debug paths prefer the most recently upgraded tied memory.
- Added memory and echo `+5` caps.
- Added echo overflow-to-overcharge burst and logs containing `overcharge` and `ultimateGauge`.
- Added reacquisition resonance: echoed memories return at `min(5, 1 + floor(echoLevel / 2))`; the echo is not consumed.
- Added HUD/slot visibility for next forgetting candidates, echo `+5` awakening, overcharge, and ultimate gauge.
- Added debug demonstration controls: immediate forgetting, echo `+5`, and ultimate marker setup.
- Synced the alpha simulator's deletion choice to highest-level memory with echo stack cap support.

Files touched:

- `src/game.js`
- `index.html`
- `style.css`
- `alpha_test/src/simulator.js`
- `docs/orchestration/review_responses/2026-06-10-forgetting-model-gate.md`
- `docs/orchestration/state/STATUS.md`
- `docs/orchestration/state/CURRENT_TASK.md`
- `docs/orchestration/state/NEXT_TASKS.md`
- `docs/orchestration/state/DECISION_LOG.md`
- `docs/orchestration/state/PROMPT_CONTEXT.md`
- `docs/orchestration/state/PROJECT_BRIEF.md`
- `docs/orchestration/state/SCOPE_GUARD.md`
- `docs/orchestration/reports/20260610/index.md`

Verification:

- `node --check src/game.js`: pass.
- `node --check alpha_test/src/simulator.js`: pass.
- First `npm run qa:balance` after new model: pass, `GO_BALANCE_BASELINE`, full clear `60%`, death `40%`.
- First `npm run balance:loop` after new model: failed, `ITERATE_BALANCE`, full clear `20%`, death `80%`.
- Tried a reacquisition candidate-priority lever; it worsened the loop to full clear `0%`, death `100%`, so that agent change was reverted.
- Applied the accepted balance lever: player max HP `190 -> 210`.
- `npm run qa:balance` after HP `210`: pass, `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `60%`, death `40%`, first boss TTK median `20.19s`.
- `npm run balance:loop` after HP `210`: pass, `GO_BALANCE_BASELINE`, first boss clear `80%`, full clear `60%`, death `40%`, first boss TTK median `23.91s`.
- `npm run playtest:package:dry`: pass.
- `npm run playtest:package`: pass, wrote `dist\lethe-v0.12-playtest`.
- `npm run report`: pass, regenerated `docs\orchestration\reports\20260610\index.html`, one unit report, and `docs\orchestration\reports\index.html`.
- `npm run report:check`: pass, one unit heading ok.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.

## Unit: Forgetting feel design concretization

User feedback after the HTML model change: the play experience did not feel like a big change. Interpreted as a planning/design gap rather than a reason to keep adding isolated code.

Actions:

- Added `docs/design/LETHE_FORGETTING_FEEL_SPEC.md`.
- Reframed the loop around four player-facing moments: loss, echo combat shift, resonance reacquisition, and ultimate echo goal.
- Updated design README, overview, core systems plan, and Unity slice spec to point to the feel spec.
- Updated orchestration `CURRENT_TASK.md`, `NEXT_TASKS.md`, `STATUS.md`, `PROJECT_BRIEF.md`, `PROMPT_CONTEXT.md`, and `SCOPE_GUARD.md` so the next step is design concretization.
- `npm run report`: pass, regenerated 2026-06-10 daily report and 2 unit reports.
- `npm run report:check`: pass, 2 unit headings ok.

No gameplay code changed in this unit.

## Unit: Weapon, memory, and echo hype design

User clarified the real issue: echoes currently feel like basic attacks with a `잔향!` label, not like a powerful build fantasy. This is a design concretization task, not a code task.

Actions:

- Added `docs/design/LETHE_WEAPON_MEMORY_ECHO_SPEC.md`.
- Defined dual blades as fast, frequent, chained echo delivery.
- Defined greatsword as slow, heavy, conditional echo detonation.
- For the current 8 memories, documented active memory behavior, normal echo behavior, awakened echo behavior, resonance rider, and intended hype moment.
- Detailed the first ultimate echo target, `피의 칼폭풍`, and backup directions for `처형 각인`, `정지 추적`, and `성채 파문`.
- Updated design README, overview, combat design, content tables, and forgetting feel spec to point to the new concrete spec.
- Updated orchestration state/dashboard/command to make weapon-memory-echo design review the next step.

No gameplay code changed in this unit.

## Unit: Unity echo system PRD

User asked to turn the newly concrete weapon/memory/echo design into a Unity structure/PRD: classes, roles, prefabs, and assets.

Actions:

- Added `docs/design/LETHE_ECHO_FORM_TRANSFORMATION_SPEC.md`.
- Defined the key grammar: active memory is an independent force, echo is a weapon habit, +5 echo is semi-awakened, resonance makes active and echo coexist, ultimate makes two echoes feed each other.
- Added event boundaries for `WeaponHit`, `EchoHit`, `UltimateHit`, `Kill`, `DamageTaken`, and `ShieldBreak` so echo systems do not recurse into infinite on-hit loops.
- Added `docs/design/LETHE_UNITY_ECHO_SYSTEM_PRD.md`.
- Defined Unity data ScriptableObjects: `WeaponDefinition`, `MemoryDefinition`, `EchoDefinition`, `EchoLevelData`, `EchoSynergyDefinition`, and `FeedbackProfile`.
- Defined runtime classes for build state, forgetting, resonance, ultimate echo unlocks, hit events, echo routing, proc limiting, echo runtimes, feedback, pooling, UI, and debug.
- Defined first-slice prefabs for dual blades, hungry blades, blood reflection, Kalmuri echo, blood echo, blood blade storm, test enemies, HUD, forget result panel, resonance card, and debug panel.
- Updated design README and orchestration state/dashboard/report to point to PRD review and implementation-surface choice.

No gameplay code changed in this unit.

## Unit: First sprite/VFX concept sheet

User asked whether Codex could generate sprite images before Unity setup and document how they should be used with MCP later.

Actions:

- Used the `imagegen` skill and built-in image generation to create a first echo showcase concept sheet.
- Copied the generated image into the project at `docs/design/assets/lethe-first-echo-showcase-concept.png`.
- Added `docs/design/LETHE_VISUAL_ASSET_PLAN.md`.
- Mapped concept sheet regions to Unity prefab candidates: dual blades, Kalmuri orbit blades, delayed slash trails, blood marks, heal threads, and Blood Blade Storm.
- Documented the future Unity MCP role: import concept art, import final sprites, configure Sprite import settings, connect prefabs, and place them in `Slice_EchoShowcase.unity`.
- Updated design README and orchestration state/report/dashboard to include visual asset planning.

No gameplay code changed in this unit.

## Unit: Unity asset binding map

User clarified that the generated image is mostly weapon/effect direction and asked for a document that links character files, map files, sword/effect files, prefabs, and MCP execution steps.

Actions:

- Added `docs/design/LETHE_UNITY_ASSET_BINDING_PLAN.md`.
- Marked `docs/design/assets/lethe-first-echo-showcase-concept.png` as Unity-importable reference art, not a final runtime sprite atlas.
- Defined the file-to-prefab map for player, map, dual blades, Kalmuri echo, Blood echo, Blood Blade Storm, test enemies, UI, and ScriptableObjects.
- Marked missing character/map/enemy art as placeholder-ready for the first slice.
- Defined Unity MCP execution order: status, folders, concept import, placeholder sprites, runtime sprite import, materials, prefabs, SOs, scene, debug verification.
- Updated design README, visual asset plan, Unity echo PRD, and orchestration state/report/dashboard.

No gameplay code changed in this unit.

## Unit: Unity skeleton and AnkleBreaker MCP cleanup

User confirmed the Unity project is open and asked to verify MCP, remove unnecessary files/settings, and commit/push the Unity baseline.

Actions:

- Confirmed the active Unity MCP server path: `C:\jaewoo\unity-mcp-server\src\index.js`.
- Confirmed it is AnkleBreaker Studio Unity MCP (`anklebreaker-unity-mcp`), not the earlier gamelovers/coplaydev-style registration.
- Registered Codex MCP server `anklebreaker-unity` with `UNITY_BRIDGE_PORT=7890`.
- Confirmed Unity bridge port `127.0.0.1:7890` responds.
- Confirmed Unity instance registry contains project `LETHE` at `C:/jaewoo/LETHE_Prototype/LETHE`, Unity `6000.3.10f1`.
- Removed obsolete Codex MCP registrations `mcp-unity` and `unityMCP`.
- Added Unity generated-file ignores to `.gitignore` and committed them separately as `20c8381`.
- Removed obsolete untracked `LETHE/ProjectSettings/McpUnitySettings.json`.
- Prepared to commit only the Unity project skeleton under `LETHE/Assets`, `LETHE/Packages`, and `LETHE/ProjectSettings`.

No Unity gameplay code changed in this unit.

## Unit: Unity runtime foundation scripts

Actions:

- Added `_dev` ScriptableObject definitions and shared enums for weapons, memories, echoes, echo synergies, echo level data, feedback profiles, hit source types, trigger families, echo styles, and echo behavior flags.
- Added runtime contracts and abstract base classes for weapon, active memory, echo, ultimate echo, and pooled object implementations.
- Added `RunBuildState` with active memory levels, echo levels, synergy unlocks, highest-memory lookup, and +5 echo cap/overflow handling.
- Added hit event structs, `HitResolver`, `EchoTriggerRouter`, and `EchoProcLimiter`.
- Added `Health`, `WeaponHitEmitter`, `PoolService`, `PooledBehaviour`, `FeedbackService`, and `SpriteFlashReceiver`.
- Kept core routing id-agnostic: hit resolver and echo router do not branch on `Echo_Kalmuri` or `Echo_Blood`.

Verification:

- AnkleBreaker MCP `unity_execute_menu_item(menuPath="Assets/Refresh")`: success.
- AnkleBreaker MCP `unity_get_compilation_errors(port=7890, severity="all")`: `count: 0`, `isCompiling: false`.
- `npm.cmd run report`: pass, generated 13 unit reports.
- `npm.cmd run report:check`: pass, 13 unit headings ok.
- `node scripts\send_orchestrator_discord_report.js --latest-section --dry-run --print-payload`: pass, latest unit payload points to `20260610/units/2026-06-10-13-unity-런타임-기반-스크립트.html`.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`; Project Orchestrator intake still appears unavailable on the configured localhost endpoint.

No scene or prefab assembly changed in this unit.

## Unit: Unity slice image production plan

User clarified that the next step should be a Superpowers-like plan for the Unity slice: identify the map, character, enemies, weapons, and echo images, generate them with Codex imagegen, then use MCP to assemble the slice.

Actions:

- Added `docs/design/LETHE_UNITY_SLICE_ASSET_PRODUCTION_PLAN.md`.
- Added the slice visual concept lock, imagegen prompt pack, and image-to-prefab-to-class matrix to the Unity slice asset production plan.
- Defined the first Unity slice as an image-first echo-feel test, not a full game slice.
- Scoped experimental Unity work to `Assets/_dev` until the slice earns promotion to `Assets/Lethe`.
- Broke image creation into batches: basic readability, dual blades, Kalmuri echo, Blood echo, and Blood Blade Storm.
- Defined the first 5 generated images: player silhouette, walker enemy, dark floor tile, and left/right dual blades.
- Defined the next 5 echo VFX images: Kalmuri slash, orbit blade, launch blade, blood mark, and heal thread tip.
- Added locked concept notes, reusable imagegen prompt packs, and a prefab/class connection matrix.
- Defined the MCP sequence: create `_dev` subfolders, import PNGs, confirm via asset list, set import settings, create `Dev_EchoSlice.unity`, then assemble debug states.
- Updated design README, visual asset plan, Unity asset binding plan, orchestration state, dashboard, and command page.

No Unity gameplay code changed in this unit.

## Unit: Agent rules transition to Unity game development

User noted that the old HTML agent rules are no longer the default, while report rules must remain. User also clarified that image generation is only one part of the workflow; the agent rules should describe the whole Unity game-development process.

Actions:

- Updated `AGENTS.md` so Codex's default role is Unity 2D game slice development: resource production, AnkleBreaker MCP operation, C# implementation, prefab/scene assembly, verification, git, and reporting.
- Added a concrete Unity Game Development Workflow to `AGENTS.md`: confirm rules/MCP, create `_dev` structure, produce needed resources, implement runtime foundations, build combat/memory/echo loops, assemble prefabs/scenes, run debug states, then run jaewoo review.
- Clarified that imagegen is a resource pipeline step, not the entire workflow.
- Marked HTML prototype work as legacy unless explicitly requested.
- Preserved report/devlog/orchestration/git rules, including the eight report items.
- Updated `PROMPT_CONTEXT.md`, `CURRENT_TASK.md`, and `DECISION_LOG.md` to match the new default workflow.

No gameplay code changed in this unit.

## Unit: Unity architecture contract audit

User asked to verify whether the technical/development docs are concrete enough, OOP-compliant, and open to adding new weapons, memories, echoes, images, and prefabs before starting implementation. User also requested Discord notifications while documenting.

Audit result:

- Direction is correct: the current design already separates weapon, memory, echo, synergy, feedback, prefab, and image concepts.
- Gap 1: the PRD listed classes but did not explicitly forbid id-based branching in core services.
- Gap 2: the asset binding plan still read like `Assets/Lethe` was the immediate target, while current work should happen in `Assets/_dev`.
- Gap 3: Discord notification was documented as available, but not as a current Unity development habit after meaningful doc/dev units.

Actions:

- Added OOP/data extension rules to `LETHE_UNITY_ECHO_SYSTEM_PRD.md`.
- Added interface/base-class guidance: `IWeaponRuntime`, `IActiveMemoryRuntime`, `IEchoRuntime`, `IUltimateEchoRuntime`, `IPooledObject`, and matching abstract `MonoBehaviour` bases.
- Defined which core services must not know specific echo ids: `HitResolver`, `EchoTriggerRouter`, `ForgetService`, `UltimateEchoService`, and related services.
- Added a first-slice data-prefab-resource matrix for `Weapon_DualBlades`, `Memory_HungryBlades`, `Memory_BloodReflection`, `Echo_Kalmuri`, `Echo_Blood`, and `Synergy_BloodBladeStorm`.
- Added `_dev` staging vs `Assets/Lethe` promotion rules to `LETHE_UNITY_ASSET_BINDING_PLAN.md`.
- Updated `SCOPE_GUARD.md` to describe the current Unity `_dev` gate.
- Updated `RUNBOOK.md` so meaningful Unity document/resource/MCP/C# units should send Discord notification.
- Ran `npm.cmd run report:orchestrator:unit:dry`; blocked by `fetch failed`.
- Ran `npm.cmd run report:discord:unit:dry`; fallback payload rendered correctly.
- Attempted real fallback Discord send with approval, but policy rejected it because the payload would export report contents and an extra review-prompt attachment to Discord. Do not retry without explicit user approval after the risk is understood.

No Unity gameplay code changed in this unit.

## Unit: Unity _dev folder setup

Actions:

- Created the Unity development staging folder structure under `LETHE/Assets/_dev`.
- Added leaf `.gitkeep` files so the empty Unity development folders stay versioned before assets/scripts/prefabs exist.
- Verified AnkleBreaker MCP connection with `unity_editor_ping(port=7890)`.
- Verified these folders through AnkleBreaker MCP `unity_asset_list`: `Assets/_dev`, `Assets/_dev/Art`, `Assets/_dev/Art/Sprites`, `Assets/_dev/Art/Sprites/Weapons`, `Assets/_dev/Prefabs`, `Assets/_dev/Scripts`, `Assets/_dev/Data`, and `Assets/_dev/Scenes`.
- Updated `NEXT_TASKS` so the next concrete step is basic slice resource production.
- Ran `npm.cmd run report:discord:unit:dry`; latest Discord fallback payload rendered, but it still includes an extra review prompt attachment. Real Discord send was not retried after the previous policy rejection.

No Unity gameplay code changed in this unit.

## Unit: Unity basic slice resources

Actions:

- Generated the first 5 basic readability images with Codex imagegen: player silhouette, walker enemy, dark floor tile, and left/right dual blades.
- Preserved generated chroma-key source images under `LETHE/Assets/_dev/Art/Source`.
- Produced runtime PNGs under `LETHE/Assets/_dev/Art/Sprites/Characters`, `Enemies`, `Map`, and `Weapons`.
- Cleaned chroma-key backgrounds for player, enemy, and dual blades into transparent PNGs. The floor tile remains opaque by design.
- Imported the 5 runtime sprites through AnkleBreaker MCP `unity_asset_import(port=7890)`.
- Configured the 5 runtime sprites with `unity_texture_set_sprite` as Sprite/Single at 100 pixels per unit.
- Imported the 4 chroma source textures through AnkleBreaker MCP so Unity `.meta` files are tracked.
- Added `tmp/` to `.gitignore` because MCP import staging uses temporary copied source files.
- Updated `LETHE_UNITY_ASSET_BINDING_PLAN.md` with the concrete `_dev` file names and first-slice use.

Verification:

- Alpha validation: player/enemy/dual blade corner alpha is `0`; floor tile corner alpha is `255`.
- `unity_asset_list(folder="Assets/_dev/Art/Sprites", type="Texture")`: 5 Texture2D assets found.
- `unity_asset_list(folder="Assets/_dev/Art/Source", type="Texture")`: 4 Texture2D assets found.
- `unity_texture_info` for `spr_player_echo_silhouette_01.png`: Texture Type `Sprite`, Sprite Mode `Single`, alpha transparency on, 100 PPU.
- `npm.cmd run report`: pass, generated 12 unit reports.
- `npm.cmd run report:check`: pass, 12 unit headings ok.
- `node scripts\send_orchestrator_discord_report.js --latest-section --dry-run --print-payload`: pass, latest unit payload points to `20260610/units/2026-06-10-12-unity-기본-slice-리소스-생성-import.html`.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.
- `npm.cmd run report:orchestrator:unit:dry` with escalation: failed with `fetch failed`.
- `Test-NetConnection 127.0.0.1 -Port 4317`: `TcpTestSucceeded: False`.

No Unity gameplay code changed in this unit.
