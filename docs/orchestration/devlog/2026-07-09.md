# 2026-07-09 Devlog

## Weapon-Specific Echo VFX Readability Pass

- User feedback:
  - Greatsword Echo VFX should be larger and feel more like a Greatsword.
  - Dual Blades Echo VFX is getting hidden by the weapon slash effects.
  - Kalmuri should be more visible.
  - Other Echoes should also split Greatsword and Dual Blades identity more clearly.
- Implementation:
  - Raised transient sorting for Kalmuri, Echo, EchoGreat, EchoDual, and Ultimate VFX so Echoes are not buried under ordinary weapon slashes.
  - Added `KalmuriEchoVisibilityScale` and enlarged Kalmuri blade sprites/trails.
  - Increased Kalmuri Hunger Echo blue rifts, blade pulls, bite scars, and secondary bites.
  - Kept Dense Dual Blades budget-safe by reducing only dense-specific Kalmuri trail/scar counts after the first QA pass exceeded the perf budget.
  - Enlarged Greatsword Echo VFX across Blood, Execution, Hunter, Shatter, Stopped, Ashen, and Oblivion.
  - Slightly enlarged Dual Blades utility Echo VFX with brighter stacked cuts, clearer links, and larger short marks.
- Tuning note:
  - First Kalmuri Perf run failed at `totalKalmuri=428` against a limit of `420`.
  - Renamed the auxiliary blue pulse to `HungryEcho_*` and reduced dense-only Kalmuri pieces; final Kalmuri Perf passed at exactly `totalKalmuri=420`.
  - First Dense Dual run failed at `ms=121.26`; dense-only Kalmuri trail/scar count was reduced and the final run passed at `ms=99.50`.
- Verification:
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `totalKalmuri=420`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `hits=18`, `suppressed=15`, `transient=156`, `activeVfx=73`, `ms=99.50`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `total=232`, `K=8`, `state=87`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `total=231`, `K=8`, `state=59`.
- Next:
  - jaewoo should directly compare both weapons and report any Echo family that still feels hidden, small, or too noisy.

## LETHE Intro Weapon Selection Screen

- User request:
  - Add one intro page and decide whether it should be click-to-start or weapon selection.
- Direction:
  - Chose weapon selection as the intro.
  - Reason: LETHE's first meaningful action should be crossing the river with a chosen weapon, not pressing a generic start button.
- Implementation:
  - Added `DrawLetheIntroOverlay()` in `V1GameManager`.
  - Routed the opening `OnGUI` branch to the new intro overlay.
  - Added a dark river / mist-line / memory-shard background treatment.
  - Added two clickable weapon cards:
    - `절단쌍검`: fast paired cuts and frequent echo rhythm.
    - `장송대검`: heavy cleaves and larger echo rhythm.
  - Preserved existing `1`, `2`, and card-click start behavior through `BeginRun`.
- Unity / MCP notes:
  - LETHE Unity instance connected on port `7890`.
  - AssetDatabase refresh completed successfully.
- Verification:
  - Unity compilation errors: `0`.
  - Play Mode initial state: `weaponSelectOverlay=True`, `runStarted=False`, `GameplayPaused=True`.
  - Direct selection call: `weaponSelectOverlay=False`, `runStarted=True`, `GameplayPaused=False`.
  - `LETHE/V1 QA/Start Dual Blades`: invoked successfully.
  - `LETHE/V1 QA/Start Greatsword`: invoked but failed with `liveEnemies=2`; this is tracked as a start-smoke expectation or Greatsword start-sim mismatch, not an intro-routing blocker.
- Next:
  - jaewoo should visually inspect the first screen in Game view.
  - Then fix Greatsword start QA as a separate small task.

## Default Kalmuri Hunger Echo Runtime

- User request:
  - Make the effect fit Kalmuri after the design direction settled on a K2/K1 hybrid.
  - The old read of a blade simply flying out after an attack should no longer be the default fantasy.
- Implementation:
  - Updated `V1GameManager` default Kalmuri Echo route to call `ResolveKalmuriHungerFollowup`.
  - Added `SpawnKalmuriHungerEchoVfx`:
    - Dual Blades: fast inward teeth, gnaw scars, pack-bite secondary marks.
    - Greatsword: wound pool, scent ring, jaw closure, drawn-to-wound trails, splinter scars.
  - Replaced default +5 awakened `LaunchKalmuriBlade` call with `SpawnKalmuriAwakenHungerBloom`.
  - Kept F12 K1-K4 prototype modes available for comparison; only default mode is now converted.
- Unity / MCP notes:
  - LETHE Unity instance connected on port `7890`.
  - `unity_execute_menu_item` intermittently returned queue `fetch failed`; bridge ping stayed healthy and retry succeeded.
- Verification:
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `orbit=44`, `bite=72`, `return=24`, `hunting=16`, `echoSurge=0`, `echoBarrage=0`, `totalKalmuri=340`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `hits=18`, `suppressed=15`, `transient=134`, `activeVfx=52`, `ms=8.56`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `total=231`, `K=8`, `state=87`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `total=207`, `K=8`, `state=58`.
- Next:
  - jaewoo should direct-play default Kalmuri with F12 prototype mode off and judge whether the wound/scent/teeth grammar feels satisfying.

## Kalmuri Blue Memory-Lineage VFX Pass

- User feedback:
  - The previous default Kalmuri direction still felt off because the original memory was blue Hungry Blades.
  - The Echo should feel like that same blue blade memory remained, became more detailed, and gained polish.
- Implementation:
  - Shifted default Kalmuri Hunger VFX from red/orange wound tones to cyan/blue/white memory-blade tones.
  - Removed the large red/orange circle-style wound pool from Dual Blades default Kalmuri.
  - Replaced generic inward tooth/bite sprites with `KalmuriBladeSprite()` so pulls and bite marks read as actual spectral blades.
  - Greatsword keeps larger blue blade-wake and blade-jaw closure to preserve heavy identity.
  - +5 awakened Kalmuri now uses blue spectral blade devour pulls.
- Verification:
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `totalKalmuri=356`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `hits=18`, `suppressed=15`, `transient=120`, `activeVfx=58`, `ms=86.60`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `total=232`, `K=8`, `state=86`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `total=207`, `K=8`, `state=58`.
- Next:
  - Direct-play default Kalmuri and judge whether it now feels like a polished blue Hungry Blades Echo.
