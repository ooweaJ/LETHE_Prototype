# 2026-07-20 Devlog

## Spatial Hash Targeting Optimization

- Trigger:
  - jaewoo asked whether the project already uses algorithms/data structures, then asked Codex to implement the useful optimization.
- Code work:
  - Added a per-frame living-enemy spatial hash grid to `V1GameManager`.
  - Added reusable query buffers for weapon target candidates, weapon hits, target-evaluation hits, radius queries, sorted target helpers, and separation queries.
  - Replaced hot full-list scans in:
    - `FindWeaponTarget`,
    - `CollectWeaponHits`,
    - `EchoTargetsInRadius`,
    - `EchoTargetsInCone`,
    - `EchoChainTargets`,
    - `FindVoidPriestHealTargets`,
    - `EnemySeparationForce`,
    - `CountLiveEnemies`.
  - Added spatial cache invalidation when enemies spawn, die, are debug-cleared, gatekeepers are removed, or null cleanup removes entries.
- Efficiency expectation:
  - Range queries now inspect only relevant grid cells plus exact distance checks.
  - Greatsword densest-arc targeting no longer evaluates each candidate direction against the entire enemy list.
  - Enemy separation now uses local-neighborhood candidates instead of a broad scan over all enemies for each enemy.
  - Several core helper paths avoid LINQ `Where` / `OrderBy` / `ToList` allocation in dense combat.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
  - `npm run report`: passed and generated `docs/orchestration/reports/20260720/index.html`.
  - `npm.cmd run report:check`: passed.
  - Unity MCP `unity_editor_state`: failed because no Unity Editor instance was detected.
- Remaining QA:
  - Run Dense Dual Blades Perf Matrix.
  - Run Echo Matrix Dual Blades and Echo Matrix Greatsword.
  - Direct-play dense packs to confirm targeting/separation feel did not regress.
