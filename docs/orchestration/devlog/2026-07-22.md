# 2026-07-22 Devlog

## Blood Visibility Regression Fix / Remaining Echo VFX Plan

- Trigger:
  - jaewoo reported that Blood Echo appears once and then stops appearing.
  - jaewoo asked to organize the remaining Echo / VFX direction with examples.
- Diagnosis:
  - Greatsword Blood accent could be skipped when the base weapon hit killed the target before the Echo accent ran.
  - Dense Dual Blades still applied Blood marks, but recent dense VFX throttling suppressed almost all visible Blood read after the first obvious trigger.
  - There was also an impossible legacy branch guarded by `bloodLevel < 0`, so the fallback Blood pulse never ran.
- Applied:
  - Greatsword Blood accent now can spawn VFX on a dead target position, so kill hits still show the Blood vortex read.
  - Dead-target Greatsword Blood is VFX-only, preventing it from killing the Echo Matrix targets before later Echo state checks run.
  - Dense Dual Blades now spawns a lightweight repeated Blood mark read on the first allowed dense hit: pulse, two suture cuts, and a +5 bloom hint.
  - Removed the dead `bloodLevel < 0` fallback branch.
  - Generated and saved a remaining Echo / Ultimate VFX concept board at `docs/orchestration/evidence/2026-07-22-remaining-echo-vfx-concept-board.png`.
  - Wrote the remaining VFX direction at `docs/orchestration/evidence/2026-07-22-remaining-echo-vfx-plan.md`.
- Verification:
  - Runtime C# build passed with 7 existing legacy warnings and 0 errors.
  - Editor C# build passed with 7 existing legacy warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors after final QA: `0`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `B=303`, `stateH=1`, `stateSt=20`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `B=83`, `state=86`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `transient=46`, `activeVfx=33`, `ms=91.56`.
- Notes:
  - Greatsword Blood count is intentionally higher in the matrix because kill-hit VFX now appears instead of being skipped.
  - Direct play should confirm whether this fixes the perceived "first time only" issue.
