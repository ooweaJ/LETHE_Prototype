# 2026-07-22 Devlog

## Procedural Motif VFX Rework

- Trigger:
  - jaewoo approved the memory/echo/weapon silhouette board and asked to implement those motifs in the actual game VFX.
  - The previous implementation still depended too much on rings, boxes, and simple support marks, so the in-game first read did not match the concept-board promise.
- Applied:
  - Added generated sprite motifs in `V1GameManager`:
    - Stopped clock seal: gear teeth, tick marks, inner rings, frozen scratches, and clock hands.
    - Execution judgement: dark sentence body, guillotine/blade core, gold verdict bars, and shard accents.
    - Shatter slam: vertical downstrike, impact core, ground shadow, jagged cracks, and chips.
    - Oblivion brand: torn void spine, purple cross strokes, broken runes, and fragments.
    - Ashen holy fire: white/gold flame tongues, grey ash bed, embers, and ward cross.
  - Added shared procedural pixel helpers for soft line/disc/ring/diamond/ellipse drawing.
  - Rewired memory previews and normal Echo paths so these generated motifs are the primary silhouettes.
  - Rewired Fracture Execution, Stasis Hunt, and Ashen Oblivion previews/ultimate support layers to use motif language instead of only prompt rings.
  - Copied the approved concept board into orchestration evidence for traceability.
- Verification:
  - `dotnet build` from repository root returned `MSB1003` because there is no root solution/project file; Unity compile was used as the valid C# check for this Unity project state.
  - Unity editor state: `Dev_Prototype_v1`, not compiling.
  - Unity compilation errors: `0`.
  - Unity console errors after Play Mode QA: `0`.
  - `DebugPreviewAllUtilityVfx()` invoked successfully.
  - `DebugRunEchoMatrix(DualBlades)` completed with all 8 Echoes at `+5`, `kills=31`, `storm=True`.
  - `DebugRunEchoMatrix(Greatsword)` completed with all 8 Echoes at `+5`, `kills=57`, `storm=True`.
- Evidence:
  - `docs/orchestration/evidence/2026-07-22-memory-echo-weapon-vfx-silhouette-board.png`
  - `docs/orchestration/evidence/2026-07-22-vfx-motif-preview-slow.png`
  - `docs/orchestration/evidence/2026-07-22-vfx-motif-dual-echo-matrix.png`
  - `docs/orchestration/evidence/2026-07-22-vfx-motif-great-echo-matrix.png`
- Notes:
  - The all-on debug screenshots are deliberately noisy and should not be treated as final composition. They prove the motifs render and survive weapon-specific paths.
  - Next direct-play pass should tune scale, alpha, lifetime, and spawn count before adding more mechanics.

## Stopped Field / Non-Circle VFX Readability Pass

- Trigger:
  - jaewoo clarified that Stopped Second should freeze enemies entering the space while it exists, and the field should render above the map but behind monsters.
  - jaewoo also called out the broader issue: Execution, Shatter, Oblivion, and Ashen were still reading as too much "circle play" instead of distinct memory/Echo identities.
- Applied:
  - Added `activeStoppedFields` to keep Stopped areas alive as gameplay volumes and pulse-freeze enemies that enter during the lifetime.
  - Cleared active stopped fields on debug VFX cleanup.
  - Routed Stopped field/clock/dome VFX to `sortingOrder=12`, while enemy state badges remain above monsters.
  - Execution memory/Echo now spawns a judgement/condemnation stamp with a dark sentence plate, gold sentence bars, vertical blade stamp, nail core, and burst cuts.
  - Shatter memory field now spawns a down-slam pressure plate, vertical impact, cracks, and lifted shards; the SFX was tuned lower and heavier.
  - Oblivion memory/Echo now uses torn void-brand glyphs, cross strokes, erased tears, and fragments instead of purple ring seals.
  - Ashen memory/Echo now uses holy ash fire language: white/gold flame tongues, grey ash bed, embers, and guard/counter plates.
  - Debug Echo Matrix now triggers Hunter before Execution to avoid forced Greatsword execution kills deleting the Hunter state-mark opportunity.
- Verification:
  - `dotnet build LETHE/LETHE.sln`: passed with 7 existing legacy warnings and 0 errors.
  - Unity `Assets/Refresh`: success.
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Passive Memory Matrix`: PASS, `blood=17`, `ash=5`, `stopped=8`, `oblivion=180`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `total=1210`, `Ex=171`, `Sh=175`, `St=160`, `A=190`, `O=248`.
  - `LETHE/V1 QA/Utility Ultimate Matrix Dual Blades`: PASS, `fracture=51`, `stasis=27`, `ashen=75`.
  - `LETHE/V1 QA/Utility Ultimate Matrix Greatsword`: PASS, `fracture=80`, `stasis=43`, `ashen=53`.
  - Direct `DebugRunEchoMatrix(Greatsword)` object count confirmed new Greatsword Echo families spawn: `total=1240`, `execution=176`, `shatter=144`, `stoppedFields=433`, `ashen=424`, `oblivion=472`.
  - Stopped field reflection/layer check returned `activeStoppedFields=8`; field/clock samples were `sortingOrder=12`, while `EchoState_StoppedSecond` stayed at `52`.
- Notes:
  - MCP queue polling remains intermittent for menu/code execution, but console logs and direct manager calls confirmed execution.
  - Direct play remains the taste gate: these changes intentionally move identity apart, but scale/noise/timing may still need hand tuning.

## Memory / Echo / Ultimate Dopamine Rework

- Trigger:
  - jaewoo asked to rework memories, Echoes, and Ultimate Echoes so they feel dopamine-rich rather than like size/color variants.
  - The earlier concept board was treated as the 4-ultimate motif reference while Ashen and Oblivion normal Echoes were brought up to the new baseline.
- Applied:
  - Ashen memory now adds a cracked guard-plate/halo so the passive shield reads as stored defense.
  - Ashen Echo now spawns a normal-density shield-break/counter burst:
    - Dual Blades: quick parry-cut read near the struck enemy.
    - Greatsword: cracked bulwark shock front near the player.
  - Oblivion memory +5 and Echo now add void-core, brand-ring, crack, and lifted-fragment erase VFX.
  - Blood Blade Storm opening and climax now layer an outer shock ring, white-hot core, and orbit blade shards on top of the blood vortex.
  - Fracture Execution now adds a sentence/verdict ground mark before the execution hit.
  - Stasis Hunt now adds an ultimate clock burst and stronger second-hand snap.
  - Ashen Oblivion now layers shield plate, ash wall, void break, and guard-collapse burst.
  - Dense Dual Blades initially exceeded the VFX budget, so dense branches now skip the newest non-essential Ashen/Oblivion ornament layers.
- Verification:
  - Runtime C# build passed with 7 existing legacy warnings and 0 errors.
  - Editor C# build passed with 0 warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors after final QA: `0`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `total=1028`, `A=110`, `O=146`, `state=88`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `total=991`, `A=120`, `O=103`, `state=45`.
  - `LETHE/V1 QA/Passive Memory Matrix`: PASS, `blood=17`, `ash=6`, `stopped=8`, `oblivion=60`.
  - `LETHE/V1 QA/Utility Ultimate Matrix Dual Blades`: PASS, `fracture=28`, `stasis=11`, `ashen=47`.
  - `LETHE/V1 QA/Utility Ultimate Matrix Greatsword`: PASS, `fracture=49`, `stasis=26`, `ashen=12`.
  - First dense run failed at `transient=170`, `activeVfx=96`, `ms=146.61`; final `Dense Dual Blades Perf Matrix` passed at `transient=51`, `activeVfx=38`, `ms=98.03`.
- Notes:
  - MCP `unity_execute_code` often returned `Error polling queue: fetch failed`, but the QA menu methods executed and PASS/FAIL lines were confirmed through Unity console logs.
  - Direct play is still the real taste gate for whether these effects feel premium instead of noisy.

## Blood Visibility Regression Fix / Remaining Echo VFX Plan

- Trigger:
  - jaewoo reported that Blood Echo appears once and then stops appearing.
  - jaewoo asked to organize the remaining Echo / VFX direction with examples.
- Diagnosis:
  - Greatsword Blood accent could be skipped when the base weapon hit killed the target before the Echo accent ran.
  - Dense Dual Blades still applied Blood marks, but recent dense VFX throttling suppressed almost all visible Blood read after the first obvious trigger.
  - There was also an impossible legacy branch guarded by `bloodLevel < 0`, so the fallback Blood pulse never ran.
- Applied:
  - Greatsword Blood accent now can spawn VFX on a dead target position, so kill hits still show the Blood vortex read.
  - Dead-target Greatsword Blood is VFX-only, preventing it from killing the Echo Matrix targets before later Echo state checks run.
  - Dense Dual Blades now spawns a lightweight repeated Blood mark read on the first allowed dense hit: pulse, two suture cuts, and a +5 bloom hint.
  - Removed the dead `bloodLevel < 0` fallback branch.
  - Generated and saved a remaining Echo / Ultimate VFX concept board at `docs/orchestration/evidence/2026-07-22-remaining-echo-vfx-concept-board.png`.
  - Wrote the remaining VFX direction at `docs/orchestration/evidence/2026-07-22-remaining-echo-vfx-plan.md`.
- Verification:
  - Runtime C# build passed with 7 existing legacy warnings and 0 errors.
  - Editor C# build passed with 7 existing legacy warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors after final QA: `0`.
  - `LETHE/V1 QA/Echo Matrix Greatsword`: PASS, `B=303`, `stateH=1`, `stateSt=20`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: PASS, `B=83`, `state=86`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `transient=46`, `activeVfx=33`, `ms=91.56`.
- Notes:
  - Greatsword Blood count is intentionally higher in the matrix because kill-hit VFX now appears instead of being skipped.
  - Direct play should confirm whether this fixes the perceived "first time only" issue.
