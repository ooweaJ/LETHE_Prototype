# 2026-07-21 Devlog

## Stopped / Hunter Readability Follow-up

- Trigger:
  - jaewoo reported that Dual Blades Stopped Echo was hard to see.
  - jaewoo liked the Dual Blades Hunter concept, but the blade size was too small to read quickly.
  - jaewoo reported that Greatsword Hunter had an unwanted fan/cone VFX.
  - jaewoo suggested Stopped Echo should show clock second-hand motion during the frozen-time window.
- Applied:
  - Dual Blades Hunter:
    - increased ricochet blade visual height from `0.62` to `0.82`;
    - added immediate ricochet preview links/marks so the bounce route reads before the moving blades finish;
    - stabilized moving ricochet hit checks so high-speed blades do not skip targets.
  - Greatsword Hunter:
    - removed the green pressure cone/fan sector.
  - Stopped Echo:
    - Dual Blades now gets a small clock lock, second-hand sweep, clock ticks, and stronger tick cut outside dense throttle;
    - Greatsword now gets a larger second-hand sweep over its clock field.
  - Dense Dual Blades:
    - reduced clockwork and ricochet preview extras in dense throttle after one perf regression.
- Verification:
  - Runtime C# build passed.
  - Editor C# build passed.
  - Unity compilation errors: `0`.
  - Unity console errors after final QA: `0`.
  - Dense Dual Blades Perf Matrix: PASS, `transient=139`, `activeVfx=82`, `ms=91.05`.
  - Echo Matrix Dual Blades: PASS, `total=946`, `H=175`, `St=160`, `stateH=19`, `stateSt=11`.
  - Echo Matrix Greatsword: PASS, `total=671`, `H=51`, `St=168`, `stateH=3`, `stateSt=19`.
- Remaining concept report:
  - Kalmuri, Blood, Hunter, and Stopped are now conceptually usable.
  - Next concept implementation should target Shatter first, Ashen second, Oblivion third.

## Hunter Echo / Greatsword Blood Readability Pass

- Trigger:
  - jaewoo reported that Greatsword Blood Echo was hard to see.
  - jaewoo also pointed out that most Echoes still risk feeling like size differences, and suggested a clearer Hunter Echo concept: Dual Blades should throw two bouncing blades, while Greatsword should throw one large piercing blade.
- Applied:
  - Greatsword Blood Echo:
    - enlarged the blood-iaido radius and target cap;
    - added stronger crescent layers, a shadow crescent, impact zone, blood bloom, radial blood petals, and longer cut line;
    - increased hitstop/camera shake slightly so the event has more weight.
  - Hunter Echo:
    - Dual Blades now spawns two green ricochet blade objects that bounce between enemies;
    - Greatsword now spawns a single green thrown-greatsword pierce line/area;
    - both variants keep the old Hunter green language so players read it as the same Echo family after the rework.
- Verification:
  - Runtime C# build passed.
  - Editor C# build passed.
  - Unity compilation errors: `0`.
  - Unity console errors: `0`.
  - Echo Matrix Dual Blades: PASS, `total=802`, `H=136`, `state=82`.
  - Echo Matrix Greatsword: PASS, `total=500`, `B=31`, `H=30`, `state=51`.
  - Dense Dual Blades Perf Matrix: PASS, `transient=109`, `activeVfx=82`, `ms=87.70`.
- Concept note:
  - Stopped Second should keep its current mechanic for now, but its visual language should move toward a premium clockwork/watch read.
  - Shatter, Ashen, and Oblivion should be reviewed from the active-memory fantasy first. If the memory fantasy is unclear, VFX scale will only make the same problem louder.

## Dual Blades Kalmuri Visibility Pass

- Trigger:
  - jaewoo reported that Dual Blades Kalmuri Echo looked too similar to the basic Dual Blades attack color and lacked visibility.
- Diagnosis:
  - Basic Dual Blades uses bright cyan/white slash language.
  - Dual Blades Kalmuri Hunger Echo also used cyan/blue/white colors, so it read as another basic slash instead of a separate Echo.
- Applied:
  - `V1GameManager.SpawnKalmuriHungerEchoVfx()`:
    - Dual Blades branch now uses dark indigo, violet-blue, and blue-edge colors.
    - Core/pulse/bite/gnaw/link lifetimes were slightly extended.
    - Greatsword Kalmuri branch was kept stable.
  - `Weapon_DualBlades.asset`:
    - `followupBaseDelay` changed from `0.035` to `0.085`.
    - `followupStagger` changed from `0.012` to `0.018`.
- Verification:
  - Runtime C# build passed on final rerun.
  - Editor C# build passed.
  - Unity compilation errors: `0`.
  - Unity console errors: `0`.
  - Dense Dual Blades Perf Matrix: PASS, `transient=103`, `activeVfx=81`, `ms=87.49`.
  - Kalmuri Perf Matrix: PASS, `totalKalmuri=396`.
  - Echo Matrix Dual Blades: PASS, `total=803`, `K=8`, `state=82`.
  - Echo Matrix Greatsword: PASS, `total=499`, `K=8`, `state=51`.
- Notes:
  - First parallel build hit a temporary DLL write lock; runtime build passed cleanly when rerun alone.
  - Direct play should judge whether the new read succeeds emotionally: bright basic slash, then darker Kalmuri bite.

## Spatial Hash Unity QA Follow-up

- Trigger:
  - jaewoo said MCP should be connected and asked Codex to check again.
- Unity MCP state:
  - Auto-connected to LETHE on port `7890`.
  - Project path: `D:/LETHE_Prototype/LETHE`.
  - Active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - Unity version: `6000.3.10f1`.
  - `isPlaying=false`, `isCompiling=false`, `sceneDirty=false`.
- Verification:
  - Unity compilation errors: `0`.
  - Unity console errors before and after QA: `0`.
  - Dense Dual Blades Perf Matrix: PASS.
    - `hits=18`, `suppressed=15`, `transient=141`, `activeVfx=87`, `ms=43.11`.
  - Echo Matrix Dual Blades: PASS.
    - `prefix=EchoDual_`, `total=803`, `state=82`.
  - Echo Matrix Greatsword: PASS.
    - `prefix=EchoGreat_`, `total=501`, `state=53`.
- MCP note:
  - `unity_execute_menu_item` and some `unity_execute_code` calls intermittently returned `Error polling queue: fetch failed`.
  - The QA methods still executed; PASS lines were confirmed by reading Unity console logs.
- Remaining review:
  - Direct-play dense packs to confirm spatial hash candidate ordering did not change target feel.
  - Continue Utility Echo and Kalmuri direct-play review.
