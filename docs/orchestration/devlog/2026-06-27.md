# 2026-06-27

## Unit: Unity v1 beta-play preparation

User clarified that build/export can wait, but wanted the prototype moved closer to a beta-playable shape through runtime asset cataloging, scene/prefab promotion preparation, and player-facing UI cleanup. User explicitly asked to keep the memory/echo debug review UI because VFX edits are likely coming next.

Changed:

- Added `V1ContentCatalog`.
- Created `Assets/_dev/Data/V1_ContentCatalog.asset` and wired 46 sprite references, Korean font, and two weapon definitions.
- Wired `Dev_Prototype_v1` `V1_GameManager` to the content catalog and both weapon definitions.
- Added `Assets/Lethe/` promotion-prep folders and `Assets/Lethe/README.md`.
- Created `Assets/Lethe/Scenes/Lethe_BetaPreview.unity` as a beta-facing candidate scene copy.
- Added current echo summary and a short run objective line to the HUD.
- Kept F12 debug / memory / echo review UI intact.

Verification:

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: `0`.
- Unity scene missing references: `0`.
- Unity Play Mode entered.
- `V1GameManager.DebugSnapshot()` returned a valid v1 start state.
- Unity console error count after stop: `0`.

Notes:

- Build/export was intentionally not touched.
- The new `Assets/Lethe` area is a promotion-prep structure, not a claim that all `_dev` content is ready for final promotion.
- Next useful pass is direct play review of the HUD/objective line plus memory/echo VFX review using the retained debug panel.

## Unit: Unity v1 MCP automated play/QA

User asked Codex to run what it can through MCP instead of waiting for manual play. The pass focused on technical playability, smoke routes, memory/echo VFX wiring, ultimate readiness, and error checks.

Checked:

- Confirmed AnkleBreaker Unity MCP targets `LETHE` on port `7890`.
- Confirmed active scene `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
- Confirmed scene root objects are still `V1_GameManager` and `Main Camera`.
- Ran Unity compile and missing-reference checks.
- Ran smoke menu routes:
  - `LETHE/V1 Smoke/Start Dual Blades`.
  - `LETHE/V1 Smoke/Start Greatsword`.
  - `LETHE/V1 Smoke/M2 Loop`.
- Entered Play Mode directly and invoked `BeginRun(DualBlades)` by reflection.
- Spawned all 8 memory preview ids and all 8 echo preview ids by reflection.
- Spawned utility memory/echo previews and the 3 non-blood ultimate previews.
- Forced all 8 echoes to +5 and confirmed Blood Blade Storm readiness.

Results:

- Unity compile error count: `0`.
- Unity scene missing references: `0`.
- Dual-blades smoke initialized `weapon=절단쌍검` with no console errors.
- Greatsword smoke initialized `weapon=장송대검`; after 2.2 seconds it reported `enemies=10`.
- M2 smoke injected `HungryBlades:5`, `BloodReflection:5`, and `storm=True`.
- Direct dual-blades run advanced to `elapsed=6.2`, `level=2`, `kills=2`, `enemies=26`, `timeScale=1`.
- Individual preview probe returned `M1/E1` for all 8 memory ids.
- Utility preview probe returned `PreviewMemory=6`, `PreviewEcho=6`, `PreviewFracture=1`, `PreviewStasis=1`, `PreviewAshen=1`.
- Ultimate readiness probe returned 8 echoes at +5 and `storm=True`.
- Final Unity console error count after stopping Play Mode: `0`.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors.

Notes:

- The old smoke-menu result for dual blades once showed `elapsed=0.0`, but a direct Play Mode reflection test confirmed the actual dual-blades runtime loop advances normally.
- MCP QA can verify that the game can initialize, spawn runtime objects, wire VFX ids, and avoid exceptions.
- It cannot replace direct jaewoo judgment for hand-feel, VFX clutter, readability, or whether the run feels beta-ready.

## Unit: Unity v1 reliable QA line

User asked Codex to keep going until the pre-play verification/test line felt complete enough to trust before direct play. The focus was to fix ambiguous smoke output and add explicit automated coverage for Blood Blade Storm and the 8 memory / 8 echo VFX surface.

Changed:

- Reworked `V1SmokeTestMenu` so it logs `[V1QA] PASS/FAIL` based on real conditions instead of always logging a delayed result snapshot.
- Existing smoke menu paths now use stronger pass criteria:
  - `LETHE/V1 Smoke/Start Dual Blades`.
  - `LETHE/V1 Smoke/Start Greatsword`.
  - `LETHE/V1 Smoke/M2 Loop`.
- Added new QA menu paths:
  - `LETHE/V1 QA/VFX Matrix`.
  - `LETHE/V1 QA/Blood Blade Storm`.
- Start-weapon QA waits for actual runtime progress: `elapsed >= 2.0`, live enemies, `timeScale=1`, and no overlay state.
- M2 QA confirms Hungry/Blood echoes at +5, `BloodBladeStormReady`, result overlay, and live enemies.
- VFX Matrix QA confirms all 8 memory previews, all 8 echo previews, and 3 non-blood ultimate previews.
- Blood Blade Storm QA forces Hungry/Blood +5, ticks `UpdateEchoUltimate`, and requires actual `BloodBladeStorm*` objects.

Verification:

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
- Unity compile error count after `AssetDatabase.Refresh`: `0`.
- Unity scene missing references: `0`.
- Unity MCP QA:
  - Dual blades: `[V1QA] PASS`, `elapsed=2.0`, `liveEnemies=8`.
  - Greatsword: `[V1QA] PASS`, `elapsed=2.0`, `liveEnemies=8`.
  - M2 loop: `[V1QA] PASS`, `HungryBlades:5`, `BloodReflection:5`, `storm=True`, `result=True`.
  - VFX Matrix: `[V1QA] PASS`, `previewMemory=8`, `previewEcho=8`, `fracture=1`, `stasis=1`, `ashen=1`.
  - Blood Blade Storm: `[V1QA] PASS`, `stormObjects=77`, `hungryEcho=5`, `bloodEcho=5`.
- Unity console error count after QA: `0`.

Notes:

- MCP menu execution sometimes reports `fetch failed` while Unity still executes the menu item; the reliable source of truth is the Unity console `[V1QA] PASS/FAIL` line.
- The technical line is now strong enough for pre-play verification. Remaining uncertainty is player perception: hand-feel, VFX scale/noise, echo identity, and HUD readability.
