# 2026-06-27

## Unit: Unity v1 beta-play preparation

User clarified that build/export can wait, but wanted the prototype moved closer to a beta-playable shape through runtime asset cataloging, scene/prefab promotion preparation, and player-facing UI cleanup. User explicitly asked to keep the memory/echo debug review UI because VFX edits are likely coming next.

Changed:

- Added `V1ContentCatalog`.
- Created `Assets/_dev/Data/V1_ContentCatalog.asset` and wired 46 sprite references, Korean font, and two weapon definitions.
- Wired `Dev_Prototype_v1` `V1_GameManager` to the content catalog and both weapon definitions.
- Added `Assets/Lethe/` promotion-prep folders and `Assets/Lethe/README.md`.
- Created `Assets/Lethe/Scenes/Lethe_BetaPreview.unity` as a beta-facing candidate scene copy.
- Added current echo summary and a short run objective line to the HUD.
- Kept F12 debug / memory / echo review UI intact.

Verification:

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: `0`.
- Unity scene missing references: `0`.
- Unity Play Mode entered.
- `V1GameManager.DebugSnapshot()` returned a valid v1 start state.
- Unity console error count after stop: `0`.

Notes:

- Build/export was intentionally not touched.
- The new `Assets/Lethe` area is a promotion-prep structure, not a claim that all `_dev` content is ready for final promotion.
- Next useful pass is direct play review of the HUD/objective line plus memory/echo VFX review using the retained debug panel.
