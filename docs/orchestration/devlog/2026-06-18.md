# 2026-06-18 Devlog

## 30-minute runtime stability pass

- Started from the current `Dev_Prototype_v1` worktree after the Blood Bloom collection-modified fix.
- Unity MCP checks at the start of the pass showed:
  - active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - compile errors: 0.
  - short Play Mode smoke after clearing console: no runtime exceptions, only AB-MCP startup logs.
- After Play Mode, AnkleBreaker MCP tool calls began returning `Transport closed`.
  - Unity Editor was still running and listening on port `7890`.
  - Post-patch Unity Editor recheck could not be completed through MCP in this session.

## Code QA

- Searched `V1GameManager` for enemy-list enumeration patterns that could still be affected when `DealDamage` kills enemies.
- Area-effect loops are now snapshot-based with `.ToList()`.
- Added two low-risk null guards:
  - Hungry Blades target selection now checks `e != null`.
  - enemy-cap counting now checks `e != null`.

## Verification

- Final `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass.
- Result: 0 warnings, 0 errors.
- `npm.cmd run report`: pass.
- `npm.cmd run report:check`: pass after rerun. The first check was started in parallel with report generation and raced the new `units` directory.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.
- Unity post-patch Play Mode recheck: blocked by AnkleBreaker MCP `Transport closed`.

## Next

- Restore/restart the AnkleBreaker MCP session, then run a post-patch Play Mode smoke on `Dev_Prototype_v1`.
- If Unity console remains clean, continue the next gameplay-facing pass: hit feedback, weapon identity, and M2 pacing review.

## Hungry Blades / Kalmuri readability follow-up

- User feedback: `굶주린 칼무리` did not read as a blade swarm, and the user asked whether all other memory effect VFX exist.
- Inventory answer:
  - `_dev/Data` contains the full prototype content set: 8 memory definitions, 8 echo definitions, and 4 ultimate definitions.
  - Dedicated sprite VFX is not complete for all memory families.
  - Dedicated PNG VFX currently exists mainly for Kalmuri, Blood, and Blood Blade Storm.
  - Execution, Hunter, Shatter, Stopped, Ashen, Oblivion, and the non-Blood Blade Storm ultimates currently use procedural runtime VFX.
- Changed `V1GameManager.UpdateHungryBlades`:
  - orbit count increased from 2-6 faint short-lived blades to 6-14 blade sprites.
  - orbit now uses two radii so it reads as a swarm occupying space, not a single ring.
  - tick cadence is slightly faster for readability.
  - damage ticks now call `SpawnHungryBladeBite` to spawn target-local bite blades and a small halo.
- Changed `ResolveKalmuriFollowup`:
  - Kalmuri echo now calls `SpawnKalmuriEchoBarrage`.
  - The existing ring/slash follow-up remains, but an explicit cluster of Kalmuri blade sprites appears at the impact origin.
- Added shared helpers:
  - `SpawnKalmuriBlade`
  - `KalmuriBladeSprite`

## Verification - Kalmuri readability follow-up

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass.
- Result: 7 legacy v0/debug deprecation warnings, 0 errors.
- Unity MCP:
  - active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - Unity compile errors: 0.
  - Play Mode entered successfully despite one `fetch failed` response on the play request.
  - Console error log after short Play Mode entry: 0.
  - Play Mode stopped successfully.
- Limitation:
  - This was not a full visual smoke because the run begins behind the weapon-select overlay.
  - jaewoo still needs to review the new Kalmuri swarm in real play.

## Stage/balance shell and object pooling pass

- User direction:
  - VFX/art feedback will come later.
  - First ensure every memory works.
  - Implement the stage and balance shell from the planning docs.
  - Optimize VFX/object churn with object pooling.
- Runtime audit:
  - 8 active memories have first-pass behavior:
    - Hungry Blades, Blood Reflection, Execution Flash, Hunter Oath, Shatter Wave, Stopped Second, Ashen Shield, Oblivion Brand.
  - 8 echoes have first-pass reactions:
    - Kalmuri, Blood, Execution, Hunter, Shatter, Stopped, Ashen, Oblivion.
  - 4 ultimate branches have first-pass runtime:
    - Blood Blade Storm, Fracture Execution, Stasis Hunt, Ashen Oblivion.
  - Dedicated art/VFX remains incomplete and intentionally deferred.
- Stage/balance shell changes:
  - Normal run duration uses 600s.
  - Gatekeeper schedule uses 180/340/490/600s.
  - First boss HP uses 2050.
  - Deficit survival uses 54s.
  - Fast/debug paths keep compressed smoke timing.
  - Review-only auto grants now run only when `fastDebugRun` is true.
  - Spawn pressure now follows the design-table phases:
    - lull.
    - rising.
    - gate breath.
    - climax.
    - deficit breath.
    - deficit trial.
  - Enemy caps now follow the design caps for first cycle, deficit, and later cycle.
  - Level-up choices now include all six run stats.
- Pooling changes:
  - Procedural transient sprites are pooled.
  - Floating text is pooled.
  - Damage numbers are pooled.
  - XP orbs are pooled.
  - Enemy/projectile pooling is still pending.

## Verification - Stage/balance shell and pooling

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 0 warnings, 0 errors.
- Unity MCP:
  - Initial compile query showed stale errors from the previous constant rename.
  - `Assets/Refresh`: success.
  - compile error count after refresh: 0.
  - short Play Mode entry: success.
  - console error count during short entry: 0.
  - Play Mode stop: success.
- Reporting still needs to be regenerated after this devlog/report update.
