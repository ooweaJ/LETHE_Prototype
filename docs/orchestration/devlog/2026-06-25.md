# 2026-06-25 Devlog

## Greatsword VFX Missing-Read Fix

- Responded to jaewoo feedback: "뭐야 대검 vfx 어디감?"
- Checked greatsword weapon/VFX assets:
  - `Weapon_Greatsword.asset` still references `VFX_Weapon_Greatsword`.
  - `VFX_Weapon_Greatsword.asset` still contains profile entries for AoE crescent, primary crescent, target shock, cut point, assist crescent, Kalmuri followup, ultimate slash, and hit spark.
- Root risk:
  - Greatsword visible slash was too dependent on profile-driven slash entries.
  - If the profile entry is skipped or visually buried, the player reads only the phantom weapon swing and feels like the VFX disappeared.
- Fix:
  - Added `SpawnGreatswordGuaranteedSlash()`.
  - Every successful greatsword hit now spawns:
    - one large cleave arc at the calculated swing tip,
    - one secondary bright cleave arc around hit center,
    - one cut line aligned to the swing direction.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after Unity refresh.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.

## One-by-One Memory / Echo Debug

- Responded to jaewoo request to test one memory and one echo at a time from the panel.
- Added `DebugSetSelectedMemoryOnly()`.
- Debug panel flow now uses one shared selected id:
  - `Prev` / `Next`: cycle through the 8 ids.
  - `Mem One`: clear echoes/ultimate state and enable only the selected memory at +5.
  - `Echo One`: enable only the selected echo at +5 with ultimate suppression.
  - `DB Rev` / `GS Rev`: keep the integrated all-echo review shortcuts.
- Purpose:
  - Compare each memory with its matching echo directly.
  - Find which ids are invisible, too similar, underpowered, overpowered, or too visually noisy.

## One-by-One Memory / Echo Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after Unity refresh.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.
- Remaining review: jaewoo direct play should confirm that the greatsword cleave is visible again and not too stacked with profile VFX.

## Integrated Review Presets

- Proceeded to the next task after the greatsword VFX fix: make direct integrated review faster.
- Added `DebugIntegratedReview(V1WeaponId weaponId)`.
- Added debug panel buttons:
  - `DB Rev`
  - `GS Rev`
- Each button:
  - starts the run if needed,
  - switches to the selected weapon,
  - spawns 18 nearby enemies,
  - sets all 8 echoes to +5,
  - enables Echo Only mode so utility ultimates do not hijack the review,
  - sets Gatekeeper warning timer to 20 seconds.

## Integrated Review Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after Unity refresh.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.

## Integrated Feel Pass After Deferring Single Tests

- Jaewoo decided to leave one-by-one memory/echo testing for later and asked to proceed with the next work.
- Treated the reported greatsword issue as duplicate/stacked VFX rather than missing VFX.
- Greatsword VFX:
  - Normal profile slashes remain the main visible cleave.
  - Guaranteed fallback now becomes only a subdued tip afterglow and cut line when profile slashes exist.
  - If profile slashes are missing, fallback still creates a readable cleave.
  - Greatsword slash delay and minimum lifetime were shortened.
- Weapon data:
  - Greatsword cadence `1.02 -> 0.92`, range `3.75 -> 3.85`, arc `96 -> 102`, max targets `6 -> 5`, knockback/hitstop/shake increased.
  - Dual blades cadence `0.36 -> 0.32`, damage `15 -> 13.5`, range `2.8 -> 2.72`, max targets `7 -> 6`, hitstop/shake shortened.
- Early run/readability:
  - First Gatekeeper timing moved from `150s` to `135s`; schedule is now `135 / 285 / 435 / 600`.
  - Warning lead moved from `18s` to `22s`, with larger/longer warning rings.
  - First-120s spawn pressure increased.
  - Enemy role markers are larger/brighter.
  - Terrain backdrop/floor are darker and memory landmarks are larger/brighter.

## Integrated Feel Pass Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.
