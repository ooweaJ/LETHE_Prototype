# Devlog

AI가 이어서 읽을 수 있는 날짜별 작업 기록을 남긴다.

파일명은 `YYYYMMDD.md`를 기본으로 쓴다.

각 항목은 아래 기준으로 짧게 작성한다.

- 오늘 만든 것
- 테스트 결과
- 결정한 것
- 다음에 볼 것

사람이 읽는 보고서는 `docs/orchestration/reports/YYYYMMDD/index.html`에 둔다.
devlog는 내부 연속성용이므로 영어 요약도 허용한다.

