# 2026-06-06 개발일지

## v0.12 브라우저 첫 보스 TTK 종료 조건 수정

- 목표: 첫 보스 HP `2800`에서 브라우저 `first_boss_ttk` accepted sample 수집을 안정화한다.
- `src/game.js`를 수정해 `balanceScenario=first_boss_ttk`가 첫 보스 TTK 샘플을 기록하는 즉시 `complete`로 종료되게 했다.
- `scripts/run_browser_balance_qa.js`가 timeout wrapper에서도 마지막 page QA payload를 보존하게 했다.
- `first_boss_ttk` 시나리오가 전체 런 clear/level-up/slot-fill 체크 대신 첫 보스 전용 체크를 쓰도록 했다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - `node scripts\run_browser_balance_qa.js --scenario first_boss_ttk --runs 3 --run-sec 230 --timeout-ms 60000 --report docs\balance\2026-06-06-v012-browser-first-boss-ttk-terminal.md --out alpha_test\outputs\balance-browser-ttk-terminal`: `GO_BALANCE_BASELINE`, 3/3 accepted, 첫 보스 클리어 `100%`, TTK 중앙값 `25.76s`.
  - `npm run qa:balance`: `ITERATE_BALANCE`, 첫 보스 클리어 `80%`, 사망 `20%`, 첫 보스 TTK 중앙값 `27.79s`, 첫 보스 전 레벨업 중앙값 `11`, 전체 클리어 `0%`.
- 결정: 첫 보스 TTK 샘플링은 더 이상 blocker가 아니다. HP `2800`을 유지하고 다음 밸런스 작업은 보스 이후와 전체 런 흐름으로 옮긴다.
- 리스크: 전체 브라우저 밸런스는 전체 클리어 `0%`로 실패하며, 한 run은 망각 경고 구간 `156.09s` 근처에서 사망했다.

## v0.12 보스 이후 밸런스 기준선

- 목표: 첫 보스 TTK 안정화 이후 보스 이후와 전체 런 흐름으로 넘어간다.
- `resolveBalanceInterrupts()`에서 cycle result continue 버튼을 먼저 누르게 해, `questions` mode에서 active memory 0개 guard가 QA 루프를 멈추는 문제를 고쳤다.
- 보스 이후 spawn cap을 추가했다: deficit breath `16`, deficit trial `22`, later-cycle default `58`.
- 최종 보스가 `600s`에 등장하므로 브라우저 밸런스 QA 기본 실행 창을 `608s`에서 `690s`로 늘렸다.
- 전체 QA 보스 이후 흐름은 통과했지만 첫 보스 TTK 중앙값이 30s 목표를 넘어서, 첫 보스 HP를 `2800`에서 `2500`으로 낮췄다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_boss_ttk_harness.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - boss-only HP `2500`: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK 중앙값 `15.62s`.
  - 브라우저 `first_boss_ttk` HP `2500`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK 중앙값 `21.05s`.
  - `npm run qa:balance`: `GO_BALANCE_BASELINE`, 첫 보스 클리어 `100%`, 전체 클리어 `40%`, 사망 `60%`, 첫 보스 TTK 중앙값 `22.24s`.
- 결정: v0.12는 자동 밸런스 기준선을 갖췄다. 다음은 결손 시험 사망 집중이 허용 가능한 긴장인지, 과한 처벌인지 해석해야 한다.
- 리스크: 사망 `60%`가 결손 시험에 집중되어 있다. 자동 clear-rate gate는 통과하지만 인간 준비 완료로 보기 전 디자인 판단이 필요하다.

## v0.12 결손 시험 생존 조정

- 목표: 사망 `60%`를 `<= 40%`로 낮추면서 전체 클리어 `35-80%`, 첫 보스 TTK `15-30s`를 유지한다.
- 결손 지속시간 `75s -> 60s`.
- 결손 시험 cap `22 -> 16`.
- pre-boss XP multiplier `1.75 -> 1.95`.
- 리필은 HP를 최소 `85%`까지 회복시키고 shield `18`을 부여한다.
- survival stat은 max HP `+16`, heal `+28`, damage reduction `+5%`로 강화했다.
- 저HP level-up에서는 survival을 아직 선택하지 않았다면 강제로 제시한다.
- balance QA는 HP `78%` 아래에서 survival을 고르게 했다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_boss_ttk_harness.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - boss-only TTK: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK 중앙값 `15.62s`.
  - 브라우저 `first_boss_ttk`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK 중앙값 `21.04s`.
  - `npm run qa:balance`: `GO_BALANCE_BASELINE`, 첫 보스 클리어 `100%`, 전체 클리어 `80%`, 사망 `20%`, 첫 보스 TTK 중앙값 `27.84s`.
- 결정: 사망 목표는 만족했지만 전체 클리어가 자동 상한에 정확히 닿았다.
- 리스크: 의도한 긴장에 비해 쉬워졌을 수 있으므로 reviewer/GPT가 accept할지, late pressure를 작게 되돌릴지 판단해야 한다.

## v0.12 결손 시험 리뷰 후속 조정

- 목표: 전체 클리어 `80%` 기준선이 너무 쉬운지 검토하고 필요할 때만 튜닝한다.
- 외부 Claude review를 시도했지만, workspace-derived prompt를 외부 Claude 서비스로 보내는 것이 잠재적 데이터 반출로 판단되어 승인 검토에서 차단됐다.
- local fallback review를 `docs/review_responses/2026-06-06-balance-baseline-deficit-trial-codex.md`에 저장했다. 결정은 `ITERATE_DEFICIT_TRIAL`.
- 첫 보스 HP `2500`은 유지했다.
- 결손 trial cap `16`, refill HP floor `85%`, shield `18`도 유지했다. 더 작은 안전 마진은 시험 run에서 사망을 과하게 올렸다.
- balance QA choice behavior를 바꿔 첫 보스 전 survival 과우선순위를 줄이고, post-first-boss survival threshold `78%`는 유지했다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - boss-only final: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK 중앙값 `15.62s`.
  - 브라우저 `first_boss_ttk` final: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK 중앙값 `19.82s`.
  - `npm run qa:balance`: `GO_BALANCE_BASELINE`, 첫 보스 클리어 `80%`, 전체 클리어 `60%`, 사망 `40%`, 첫 보스 TTK 중앙값 `25.79s`.
- 결정: 이 기준선을 인간/reviewer 피드백 전 현재 자동 밸런스 기준선으로 받아들인다. 쉬운 쪽 끝이 아니라 목표 band 안쪽에 있다.
- 리스크: 사망이 허용 상한 `40%`에 정확히 걸려 있어, 인간 테스트에서 보스 이후 압박이 여전히 강하다고 나올 수 있다.
- 보고/Discord:
  - `npm run report`: 통과.
  - `npm run report:check`: 통과.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: 승인 검토에서 차단됨.
  - `npm run report:discord:unit:dry`: 통과.
  - trusted-local 명령: `npm run report:discord:unit`.
