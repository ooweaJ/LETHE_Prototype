# 2026-06-22 Devlog

## Generated VFX Runtime Wiring

- User asked to proceed with next tasks 1 and 2: wire generated VFX into runtime, then make hit/VFX size and natural game progression ready for direct review.
- Read current orchestration state, task board, test/changelog, and the Unity MCP skill instructions.
- Changed `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`.

## Implementation Notes

- Added generated VFX asset path constants for:
  - dual-blade swing arcs, greatsword cleave arc, cyan/red hit sparks.
  - six utility active-memory sprites.
  - six utility echo sprites.
  - three non-blood ultimate sprites.
- Added prompt-sprite helper methods:
  - `SpawnPromptSprite`
  - `ScaleSpriteToWorldWidth`
  - `MemoryVfxSprite`
  - `EchoVfxSprite`
  - `EchoTransformSprite`
- Replaced procedural-first VFX calls for:
  - Execution Flash.
  - Hunter Oath projectile.
  - Shatter Wave.
  - Stopped Second.
  - Ashen Shield.
  - Oblivion Brand.
  - utility echo reactions for Shatter, Execution, Hunter, Stopped, Ashen, Brand.
  - Fracture Execution, Stasis Hunt, Ashen Oblivion.
- Updated `MakeSlashSprite` so `WeaponVfxProfile` entries prefer generated sprites:
  - Crescent -> dual blade arc PNGs or Kalmuri echo slash.
  - WideCrescent -> greatsword cleave PNG.
  - ImpactDiamond -> cyan/red hit spark PNG.
- Added scale multipliers/world-width targets so 1254px prompt-sheet sprites render near the old procedural world-size budget.
- Left procedural generation as fallback if a PNG is missing.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity MCP:
  - selected LETHE instance on port 7890.
  - active scene: `Dev_Prototype_v1`.
  - `Assets/Refresh`: success.
  - compile error count: 0.
  - Play Mode smoke attempts: console error count 0.
  - Play Mode stopped successfully.

## Limitations

- Unity Game View and Scene View capture still returned solid-color images, matching the known screenshot issue.
- Because visual capture is unreliable, final VFX scale/timing/readability still needs jaewoo direct play review.
- Manual runtime sprite counting did not catch transient VFX after smoke because the smoke route either auto-stopped or did not advance enough through live combat frames inside MCP. This is a tooling/evidence limitation, not a compile/runtime error.

## Next

- jaewoo should run `Dev_Prototype_v1`, choose both start weapons, and review the first 120 seconds.
- The next implementation pass should be chosen from one weak axis only: VFX scale/timing, attack feel, reward cadence, forgetting UX, spawn pressure, or enemy/memory readability.

## Blood Blade Storm Payoff / Player Movement Pass

- User review:
  - Kalmuri and Blood Blade Storm are not bad.
  - Blood Blade Storm should feel extremely strong, but currently reads like Kalmuri with a different effect.
  - Player walking still feels unnatural.
- Changed `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`.

## Implementation Notes

- Player movement:
  - Added short acceleration/deceleration smoothing instead of applying raw input directly to position every frame.
  - Stored `PlayerVisual` as a stable child transform and added subtle walk bob/tilt.
  - Smoothed weapon-anchor rotation during movement so direction changes do not snap as hard.
  - Added a defensive `BeginRun` guard so debug/smoke calls create the player if called before the runtime player reference exists.
- Blood Blade Storm:
  - Split the old always-on storm logic into an opening cue, continuous storm pressure, and periodic burst pulses.
  - Dual-blade storm now uses faster 8-blade orbit ticks plus 12-blade periodic burst rings.
  - Greatsword storm now uses slower/heavier rings, 4 directional heavy slashes, larger burst damage, stronger shake, and heavier knockback.
  - Storm hits now mark enemies, tug nearby enemies inward during the storm, burst outward on pulses, heal the player, and draw blood-heal threads.
  - Damage source strings in the storm path now use ASCII `BloodBladeStorm` to avoid mojibake-string fragility in compile/smoke work.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity MCP:
  - active scene: `Dev_Prototype_v1`.
  - Unity compile error count: 0.
  - Play Mode entered; Unity console error count: 0.
  - Direct M2 state injection reached `memories=[BloodReflection:3,HungryBlades:3]`, `echoes=[HungryBlades:5,BloodReflection:5]`, `storm=True`, `result=False`.
  - Manual reflection ticks through `UpdateEchoUltimate(0.12f)` created `bloodStormObjects=124`, killed nearby spawned enemies (`kills=14`), and produced console error count 0.
  - Play Mode stopped successfully.

## Limitations

- MCP Play Mode time did not advance normally in this session (`elapsed=0.0` stayed fixed), so the final storm check used direct reflection ticks instead of natural timed gameplay.
- Visual scale and walking feel still need jaewoo direct play review because the known screenshot path is unreliable.

## Held Weapon Silhouette / Attack VFX Scale Tune

- User review:
  - Greatsword is too large.
  - Dual blades are too small.
  - Asked whether dual-blade/greatsword attack sprites are necessary.
- Changed `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`.

## Implementation Notes

- Added held-weapon scale constants:
  - `DualBladeVisualScale = 0.43`.
  - `GreatswordVisualScale = 0.34`.
- Dual blades:
  - runtime scale changed from `0.30~0.33` to `0.43~0.475`.
  - local positions tightened to around `x=±0.19`, `y=-0.035~-0.04`.
  - swing rotation widened slightly so the larger blades still read as active cuts.
- Greatsword:
  - runtime scale changed from `0.44~0.51` to `0.34~0.375`.
  - local position tightened to around `x=0.03`, `y=-0.09`.
  - swing travel/rotation reduced so it does not dominate the player silhouette.
- Attack VFX:
  - generated dual-blade slash PNG scale factor changed `0.153 -> 0.192`.
  - generated greatsword cleave PNG scale factor changed `0.225 -> 0.182`.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: 0.
- Play Mode runtime transform check:
  - dual blade scale `0.430`, positions `(-0.19, -0.04)` and `(0.19, -0.04)`.
  - greatsword scale `0.340`, position `(0.03, -0.09)`.
- Unity console error count: 0.
