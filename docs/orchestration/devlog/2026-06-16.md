# 2026-06-16 Devlog

## Pause / hitstop movement fix

- User review:
  - Monsters kept moving while choosing cards.
  - Greatsword/basic attack/Kalmuri echo hitstop made the character feel like movement briefly stopped.
- Diagnosis:
  - `V1GameManager` returned during overlays, but `V1Enemy.Update()` and projectile updates still ran independently.
  - `hitstopTimer` returned before player/camera/weapon visual updates, so player-side responsiveness felt interrupted.
- Implemented:
  - Added `V1GameManager.GameplayPaused` for blocking overlays:
    - start weapon selection.
    - level-up card selection.
    - forgetting result.
    - resonance refill.
    - death overlay.
  - Added `V1GameManager.HitstopActive` for combat impact freeze.
  - `V1Enemy`, `V1Projectile`, and `V1EnemyShot` now early-return when gameplay is paused or hitstop is active.
  - Hitstop now still updates player, camera, and weapon visuals before returning from the manager loop.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `pauseDistance=0.0000`
    - `unpauseDistance=0.0240`
    - `weaponAnimAfterHitstop=0.220`
    - `hitstopAfterUpdate=0.060`
    - `gameplayPaused=False`
    - `hitstopActive=True`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 1 unit heading ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next

- Jaewoo should re-test:
  - card selection while enemies are on screen.
  - greatsword hitstop while moving.
  - Kalmuri echo follow-up while moving.
- If movement still feels interrupted, the next lever is to remove player-side hitstop entirely and keep only camera shake/enemy freeze/VFX emphasis.

## Crescent slash feedback pass

- User review:
  - Pause and movement fixes are working.
  - Hit feel is still missing.
  - Dual blades should show two half-moon/crescent slashes.
  - Greatsword should show one large crescent so nearby AoE damage is readable.
- Implemented:
  - Added procedural crescent slash sprites:
    - `MakeCrescentSlashSprite`
    - `MakeWideCrescentSprite`
  - Dual blade primary hit now spawns:
    - `DualBladeCrescent_A`
    - `DualBladeCrescent_B`
  - Dual blade assist hits use smaller crescent slashes.
  - Greatsword hit now spawns:
    - `GreatswordCrescent_Aoe`
    - `GreatswordCrescent_Primary`
    - `GreatswordTargetShock`
  - Kalmuri follow-up slashes now use crescent sprites instead of thin iai lines.
  - Greatsword Blood Blade Storm heavy slashes now use wide crescent sprites.
  - Hit spark was changed to an impact diamond so it does not compete with slash shapes.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `dualCrescent=6`
    - `kalmuriCrescent=10`
    - `greatCrescent=6`
    - `heavyKalmuri=1`
    - `shock=1`
  - Evidence:
    - `LETHE/Assets/_dev/Evidence/v1_crescent_slash_feedback_20260616.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 2 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next after crescent pass

- Jaewoo should review whether:
  - dual blades now read as two quick crescent cuts.
  - greatsword crescent reads as an AoE slash.
  - Kalmuri echo follow-up feels like a slash continuation instead of a random effect.
- If still weak, next lever is not timing but stronger authored art/sprite replacement and possibly sound placeholders.

## Crescent slash size / timing tune

- User review:
  - Greatsword crescent was too thick and read like a fan.
  - Dual blade crescents were too small.
  - VFX lifetime was too short, making it hard to tell attacks happened.
- Implemented:
  - Dual blade crescent A scale/lifetime: `0.62 / 0.13s -> 0.78 / 0.21s`.
  - Dual blade crescent B scale/lifetime: `0.54 / 0.15s -> 0.68 / 0.23s`.
  - Dual blade assist scale/lifetime: `0.38 / 0.10s -> 0.50 / 0.15s`.
  - Greatsword AoE crescent scale/alpha/lifetime reduced: `1.02 / 0.40 / 0.28s -> 0.88 / 0.32 / 0.24s`.
  - Greatsword primary crescent scale reduced: `0.78 -> 0.66`.
  - `MakeWideCrescentSprite` was thinned by reducing blade-edge width, inner fill, and glow.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `dualCrescent=6`
    - `dualMaxScale=0.78`
    - `kalmuriCrescent=10`
    - `greatCrescent=6`
    - `greatMaxScale=0.88`
    - `shock=1`
  - Evidence:
    - `LETHE/Assets/_dev/Evidence/v1_crescent_slash_timing_size_20260616.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 3 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next after size / timing tune

- Jaewoo should compare:
  - whether dual blade attacks are now visible enough.
  - whether greatsword is now a slash arc instead of a fan.
- If still weak, stop tweaking procedural textures and generate/author dedicated slash sprites.

## Greatsword range / damage feedback / ranged caster pass

- User review:
  - Greatsword crescent should not be made smaller; it should be large enough to match the actual damage range while staying crescent-shaped.
  - Greatsword VFX lifetime was still too short.
  - Hit feedback should include Vampire Survivors-like white enemy flash and readable damage numbers.
  - Ranged enemies should not kite backward; once in range they should stop and keep casting.
- Implemented:
  - Greatsword AoE crescent scale/lifetime: `0.88 / 0.24s -> 1.24 / 0.42s`.
  - Greatsword primary crescent scale/lifetime: `0.66 / 0.20s -> 1.02 / 0.34s`.
  - Kept the thinned crescent texture from the previous pass, so the larger VFX should read as a big slash arc rather than a filled fan.
  - Added `V1DamageNumber` for floating damage UI.
  - Enemy hit flash now uses pure white and lasts longer:
    - weapon hits: `0.105s`.
    - non-weapon hits: `0.075s`.
  - `DriftingEye` now approaches until cast range, then stays still and fires `EyeShot`; it no longer backs away.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `greatCrescent=6`
    - `greatMaxScale=1.24`
    - `damageNumbers=5`
    - `whiteEnemies=5`
    - `eyeMovedAtRange=0.0000`
    - `eyeShots=1`
  - Evidence:
    - `LETHE/Assets/_dev/Evidence/v1_damage_feedback_ranged_cast_20260616.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 4 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next after damage feedback pass

- Jaewoo should review:
  - whether greatsword now reads as a large range hit without becoming a fan.
  - whether white flash + damage numbers make enemy hits obvious enough.
  - whether DriftingEye stationary casting feels more like a readable ranged enemy.
- If hit feel is still weak, the next lever is authored slash sprite art plus sound placeholders, not more procedural scale tuning.
