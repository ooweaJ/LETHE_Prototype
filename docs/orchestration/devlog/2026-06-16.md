# 2026-06-16 Devlog

## Pause / hitstop movement fix

- User review:
  - Monsters kept moving while choosing cards.
  - Greatsword/basic attack/Kalmuri echo hitstop made the character feel like movement briefly stopped.
- Diagnosis:
  - `V1GameManager` returned during overlays, but `V1Enemy.Update()` and projectile updates still ran independently.
  - `hitstopTimer` returned before player/camera/weapon visual updates, so player-side responsiveness felt interrupted.
- Implemented:
  - Added `V1GameManager.GameplayPaused` for blocking overlays:
    - start weapon selection.
    - level-up card selection.
    - forgetting result.
    - resonance refill.
    - death overlay.
  - Added `V1GameManager.HitstopActive` for combat impact freeze.
  - `V1Enemy`, `V1Projectile`, and `V1EnemyShot` now early-return when gameplay is paused or hitstop is active.
  - Hitstop now still updates player, camera, and weapon visuals before returning from the manager loop.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `pauseDistance=0.0000`
    - `unpauseDistance=0.0240`
    - `weaponAnimAfterHitstop=0.220`
    - `hitstopAfterUpdate=0.060`
    - `gameplayPaused=False`
    - `hitstopActive=True`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 1 unit heading ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next

- Jaewoo should re-test:
  - card selection while enemies are on screen.
  - greatsword hitstop while moving.
  - Kalmuri echo follow-up while moving.
- If movement still feels interrupted, the next lever is to remove player-side hitstop entirely and keep only camera shake/enemy freeze/VFX emphasis.

## Crescent slash feedback pass

- User review:
  - Pause and movement fixes are working.
  - Hit feel is still missing.
  - Dual blades should show two half-moon/crescent slashes.
  - Greatsword should show one large crescent so nearby AoE damage is readable.
- Implemented:
  - Added procedural crescent slash sprites:
    - `MakeCrescentSlashSprite`
    - `MakeWideCrescentSprite`
  - Dual blade primary hit now spawns:
    - `DualBladeCrescent_A`
    - `DualBladeCrescent_B`
  - Dual blade assist hits use smaller crescent slashes.
  - Greatsword hit now spawns:
    - `GreatswordCrescent_Aoe`
    - `GreatswordCrescent_Primary`
    - `GreatswordTargetShock`
  - Kalmuri follow-up slashes now use crescent sprites instead of thin iai lines.
  - Greatsword Blood Blade Storm heavy slashes now use wide crescent sprites.
  - Hit spark was changed to an impact diamond so it does not compete with slash shapes.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `dualCrescent=6`
    - `kalmuriCrescent=10`
    - `greatCrescent=6`
    - `heavyKalmuri=1`
    - `shock=1`
  - Evidence:
    - `LETHE/Assets/_dev/Evidence/v1_crescent_slash_feedback_20260616.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 2 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Next after crescent pass

- Jaewoo should review whether:
  - dual blades now read as two quick crescent cuts.
  - greatsword crescent reads as an AoE slash.
  - Kalmuri echo follow-up feels like a slash continuation instead of a random effect.
- If still weak, next lever is not timing but stronger authored art/sprite replacement and possibly sound placeholders.
