# 2026-07-06 Devlog

## Echo State Mark Readability Pass

- Jaewoo reported that non-blood memories/echoes do not clearly feel different, while Blood Reflection reads strongest.
- Direction:
  - Do not start with raw number buffs.
  - First make affected monsters visibly show what state they are in.
- Implemented:
  - `V1Enemy.ApplyEchoStateMark()`.
  - `V1EnemyStateBadge`, a short-lived pulsing/rotating marker attached to affected enemies.
  - Temporary enemy color tint for utility echo states.
  - State marks for:
    - `ExecutionFlash`
    - `HunterOath`
    - `ShatterWave`
    - `StoppedSecond`
    - `AshenShield`
    - `OblivionBrand`
  - Echo Matrix QA now reports and requires `EchoState_*` markers.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
- Blocker:
  - AnkleBreaker Unity MCP still returns `Transport closed`, so Echo Matrix rerun is pending.

## Next Echo-State Step

- Retry `LETHE/V1 QA/Echo Matrix Dual Blades`.
- Retry `LETHE/V1 QA/Echo Matrix Greatsword`.
- Direct-play check whether utility echoes read through monster marks before text.

## Gatekeeper Raid Telegraph Pass

- Jaewoo reported that Gatekeeper cone/circle patterns still look odd and should feel like raid-game mechanics.
- Direction:
  - Keep patterns simple.
  - Make them read as `red zone appears -> zone fills -> bang`.
- Implemented:
  - `V1RaidTelegraphFill`, a small component for transient danger-zone fill animation.
  - Meteor tells now show the outer danger ring immediately, grow the internal fill, then flash on impact.
  - Cone tells now show a filled sector charging up, a center line, and a bright impact flash.
  - Ring tells now show the danger boundary and growing fill before the shockwave.
  - Gatekeeper warning duration clamps to at least `0.92s`.
  - `SpawnTransientSpriteScaled()` disables stale raid-fill components when pooled objects are reused.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: first parallel run hit a DLL lock, standalone rerun passed with 0 warnings and 0 errors.
- Blocker:
  - AnkleBreaker Unity MCP still returns `Transport closed`, so Gatekeeper Pattern Matrix rerun is pending.

## Next Gatekeeper Step

- Retry `LETHE/V1 QA/Gatekeeper Pattern Matrix`.
- Retry `LETHE/V1 QA/Gatekeeper Jump`.
- Visually inspect whether red zones now read as simple raid telegraphs.

## Dense Dual-Blade VFX Churn Pass

- Jaewoo reported that dual blades can lag when many enemies are present.
- Added a dense dual-blade QA/perf matrix:
  - `DebugRunDenseDualBladePerfMatrix()`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`.
  - counters for hits, suppressed echo chains, transient sprite spawns, active VFX, and synchronous stress time.
- The first QA attempts reproduced the problem:
  - `hits=24`, `suppressed=4`, `transient=954`, `activeVfx=506`, `ms=947.64`.
  - after first throttles: `hits=24`, `suppressed=10`, `transient=550`, `activeVfx=307`, `ms=561.77`.
  - follow-up run: `hits=30`, `suppressed=15`, `transient=627`, `activeVfx=361`, `ms=644.39`.
- Added final throttles after those failed baselines:
  - dense Kalmuri follow-ups now store dense state at scheduling time.
  - dense Kalmuri follow-ups use fewer surge blades and skip extra flash/barrage.
  - dense Blood Reflection keeps mark/pulse readability but suppresses bloom/accent spam.
  - dense utility echoes rotate one utility family per primary hit.
  - secondary dual-blade slash/echo VFX is capped in dense waves.
- Replaced VoidPriest target discovery with `FindVoidPriestHealTargets()` over the manager enemy list instead of whole-scene scans.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity compile error count before MCP transport closure: `0`.
- Blocker:
  - After `Assets/Refresh`, AnkleBreaker Unity MCP transport closed. The `LETHE` Unity editor process stayed alive and OS-responsive, but MCP calls failed immediately with `Transport closed`.
  - Final Dense QA rerun after the last throttle is pending.

## Next Dense-Pass Step

- Retry `LETHE/V1 QA/Dense Dual Blades Perf Matrix`.
- Rerun `LETHE/V1 QA/Void Priest Heal Matrix`.
- Rerun `LETHE/V1 QA/M2 Loop`.
- If dense QA still fails, tune remaining object churn before any new VFX spectacle.

## Gatekeeper Heal Diagnosis

- Jaewoo reported that the first boss did not seem to die after boss spawn, while normal adds kept spawning during a dual-blades + Hungry Blades run.
- Code inspection found no dedicated Gatekeeper self-heal.
- Actual issue found:
  - `VoidPriest` healed nearby `V1Enemy` targets every 1.4 seconds.
  - The target filter did not exclude `V1EnemyKind.Gatekeeper`.
  - Therefore a priest near the boss could restore boss HP, which matched the playtest feeling.

## Gatekeeper Pattern Pass

- Excluded `Gatekeeper` from `VoidPriest` heal targets.
- Replaced the old immediate boss pulse with delayed, red telegraphed attacks.
- Added rank-based Gatekeeper pattern identity:
  - rank 0: meteor circle under/near the player.
  - rank 1: cone slash plus occasional meteor.
  - rank 2: ring burst plus meteor.
  - rank 3: cone plus paired meteor pressure.
- Added procedural sprite support:
  - red disc telegraph.
  - cone sector telegraph.
  - Gatekeeper meteor shard.
  - Gatekeeper cleave sector.
  - rank-varying Gatekeeper fallback body/sigil colors and marks.
- Added `DebugRunGatekeeperPatternMatrix()`.
- Added Unity menu `LETHE/V1 QA/Gatekeeper Pattern Matrix`.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`:
  - First parallel run hit a transient DLL lock from Unity/dotnet concurrency.
  - Standalone rerun passed with 0 warnings and 0 errors.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`:
  - Passed with 7 legacy warnings and 0 errors.
- Unity compile error count: `0`.
- Unity QA:
  - `LETHE/V1 QA/Gatekeeper Pattern Matrix`: `[V1QA] PASS`, `boss=4`, `meteor=15`, `cone=4`, `ring=3`.
  - `LETHE/V1 QA/M2 Loop`: `[V1QA] PASS`, `hungryEcho=5`, `bloodEcho=5`, `storm=True`.
- Unity console error count after QA: `0`.
- Remaining non-gameplay warning:
  - Unity/Visual Studio UDP port warning appeared in console. It is unrelated to gameplay runtime.

## Next

- Jaewoo should replay the first Gatekeeper and judge:
  - red telegraph readability,
  - fairness and dodge timing,
  - first boss HP/TTK after healer exclusion,
  - whether rank-based boss concepts are distinct enough.

## Enemy Soft Separation

- Jaewoo asked whether normal enemies are supposed to overlap, and whether Vampire Survivors also allows overlap.
- Direction chosen:
  - keep survivor-style dense crowd pressure,
  - avoid hard collision/body blocking,
  - add a soft visual/gameplay separation nudge so enemies do not collapse into one unreadable stack.
- Implemented `V1GameManager.EnemySeparationForce(V1Enemy self)`.
- Applied separation after normal enemy behavior:
  - Gatekeeper is pushed less to keep boss presence stable.
  - Normal enemies are pushed more strongly away from Gatekeeper.
  - DriftingEye separates even while holding its ranged casting position.
  - exact same-position overlaps use a deterministic fallback direction.
- Added `DebugRunEnemySeparationMatrix()`.
- Added Unity menu `LETHE/V1 QA/Enemy Separation Matrix`.

## Enemy Separation Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`:
  - A first parallel run hit a transient DLL lock because runtime/editor builds were launched together.
  - Standalone rerun passed with 0 warnings and 0 errors.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`:
  - Passed with 7 legacy warnings and 0 errors.
- Unity compile error count: `0`.
- Unity QA:
  - `LETHE/V1 QA/Enemy Separation Matrix`: `[V1QA] PASS`, overlap pairs `91 -> 4`.
  - `LETHE/V1 QA/M2 Loop`: `[V1QA] PASS`, `hungryEcho=5`, `bloodEcho=5`, `storm=True`.
- Remaining non-gameplay warning:
  - Unity/Visual Studio UDP port warning appeared in console. It is unrelated to gameplay runtime.

## Next For Separation

- Jaewoo should direct-play a dense wave and judge:
  - whether the crowd still feels pressuring,
  - whether the blob stacking is reduced enough,
  - whether boss-adjacent adds create readable spacing,
  - whether separation makes enemies feel too loose.

## Gatekeeper Body Visual Repair

- Jaewoo rejected the updated boss body as looking degraded and slime-like.
- Cause:
  - the previous pass prioritized rank/pattern identity,
  - but `EnemySprite(Gatekeeper)` used the new procedural rank fallback directly,
  - so the original angular Gatekeeper identity was effectively bypassed.
- Fix direction:
  - restore the boss as an authored body sprite,
  - keep rank differences through separate PNGs,
  - keep procedural fallback only as a last resort.
- Implemented:
  - replaced `spr_boss_gatekeeper_01.png` with a sharper gate/mask body,
  - added `spr_boss_gatekeeper_02.png`,
  - added `spr_boss_gatekeeper_03.png`,
  - added `spr_boss_gatekeeper_04.png`,
  - added `BossGatekeeperRankPaths`,
  - changed Gatekeeper sprite loading to use rank-specific PNGs first,
  - reworked procedural fallback to preserve an angular gate/mask silhouette.

## Gatekeeper Body Verification

- Unity `AssetDatabase.Refresh()` imported new PNGs and `.meta` files.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`:
  - Passed with 7 existing legacy warnings and 0 errors.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`:
  - A parallel run hit a transient DLL lock.
  - Standalone rerun passed with 0 warnings and 0 errors.
- Unity compile error count: `0`.
- Unity QA:
  - `LETHE/V1 QA/Gatekeeper Pattern Matrix`: `[V1QA] PASS`, `boss=4`, `meteor=15`, `cone=4`, `ring=3`.
  - `LETHE/V1 QA/M2 Loop`: `[V1QA] PASS`, `hungryEcho=5`, `bloodEcho=5`, `storm=True`.

## VoidPriest Heal Stacking / Readability

- Jaewoo reported that healer-supported waves can feel impossible to kill unless Blood Reflection is involved.
- Cause:
  - `VoidPriest` healing was invisible.
  - Multiple priests could heal the same target on the same beat.
  - That made sustain scale with priest overlap, so sustained damage builds felt much better than burst/control builds.
- Implemented:
  - `VoidPriest` heal VFX:
    - source pulse,
    - target pulse/core,
    - source-to-target heal thread,
    - floating heal amount.
  - Heal cadence changed to `1.05s`.
  - Heal amount changed to `2.4`.
  - Per-priest target cap set to `3`.
  - Per-target receiver lockout set to `0.95s`.
  - Priests prioritize wounded nearby non-boss enemies.
  - Added `DebugRunVoidPriestHealMatrix()`.
  - Added Unity menu `LETHE/V1 QA/Void Priest Heal Matrix`.

## Echo / Memory / Ultimate Interaction Audit

- Blood Reflection currently has the strongest feel because it combines:
  - frequent marks,
  - visible wound/pulse VFX,
  - DoT,
  - healing,
  - bloom,
  - echo payoff,
  - and Blood Blade Storm progression.
- Non-blood memories/echoes pass QA but often read weaker because:
  - they are lower-count,
  - conditional,
  - defensive/control-biased,
  - or show area VFX without persistent enemy-state change.
- Added audit handoff:
  - `docs/orchestration/review_prompts/2026-07-06-echo-memory-monster-interaction-audit.md`

## VoidPriest / Interaction Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`:
  - Passed with 7 existing legacy warnings and 0 errors.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`:
  - A parallel run hit a transient DLL lock.
  - Standalone rerun passed with 0 warnings and 0 errors.
- Unity compile error count: `0`.
- Unity QA:
  - `LETHE/V1 QA/Void Priest Heal Matrix`: `[V1QA] PASS`, `attempts=12`, `accepted=4`, `vfx=16`.
  - `LETHE/V1 QA/M2 Loop`: `[V1QA] PASS`, `hungryEcho=5`, `bloodEcho=5`, `storm=True`.
  - `LETHE/V1 QA/Echo Matrix Dual Blades`: `[V1QA] PASS`, `total=240`, `K=8`, `B=56`, `Ex=64`, `H=24`, `Sh=8`, `St=8`, `A=32`, `O=40`.
  - `LETHE/V1 QA/Passive Memory Matrix`: `[V1QA] PASS`, `blood=17`, `ash=6`, `stopped=8`, `oblivion=36`.
  - `LETHE/V1 QA/Utility Ultimate Matrix Dual Blades`: `[V1QA] PASS`, `fracture=22`, `stasis=9`, `ashen=47`.

## Next For Interactions

- Do not simply buff every non-blood number.
- First make non-blood effects produce visible monster-state changes:
  - cracked,
  - exposed,
  - frozen,
  - locked,
  - shield-broken,
  - branded.
- Add QA that tracks damage/heal/control contribution by family, not only object counts.

## Gatekeeper Jump Debug / VFX-Performance Triage

- Jaewoo requested a debug route that jumps directly to the boss.
- Implemented:
  - `F6` now calls `DebugJumpToGatekeeper()`.
  - `F6` works from the weapon-select overlay.
  - F12 debug panel now includes a `Boss` button.
  - `DebugJumpToGatekeeper()` starts the run if needed, clears overlays, removes existing Gatekeepers, seeds an empty run with `HungryBlades:3`, `BloodReflection:2`, `StoppedSecond:1`, spawns review enemies, shows the warning cue, and spawns one Gatekeeper.
  - Added `LETHE/V1 QA/Gatekeeper Jump`.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity QA `Gatekeeper Jump`: `[V1QA] PASS`, `boss=1`, `liveEnemies=15`, `memories=[HungryBlades:3,BloodReflection:2,StoppedSecond:1]`.
  - Unity console error count after QA: `0`.
- Triage:
  - VFX identity is still low in actual play.
  - Kalmuri echo still feels conceptually wrong, not just underpowered.
  - Dual blades appear to hitch in dense waves.
  - Likely next hotspots: dense-hit transient VFX churn, Kalmuri echo object counts, LINQ-heavy enemy queries, and VoidPriest whole-scene heal scans.
- Next:
  - Prioritize a dense dual-blade performance/readability pass, then Kalmuri echo concept redesign.

## Kalmuri Echo Clamp/Rip Readability

- Jaewoo's feedback remained that Hungry Blades / Kalmuri echo did not fit the concept; more cyan blade clutter was not enough.
- Implemented `SpawnKalmuriEchoClampRip()`:
  - two opposing Kalmuri blades close into the target point,
  - a bite/rip core reads at the center,
  - a wound slash remains after the clamp.
- Reduced old generic support visuals:
  - smaller/lower-alpha range ring,
  - lower-alpha flash,
  - lower-alpha cut trace.
- Dense dual-blade path:
  - keeps the new clamp/rip shape,
  - skips the old surge blade loop,
  - skips extra core/flash support to avoid undoing the dense VFX churn pass.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - AnkleBreaker Unity MCP reconnect attempt still failed with `Transport closed`.
- Remaining:
  - Retry `LETHE/V1 QA/Kalmuri Perf Matrix` when MCP routing is stable.
  - Direct-play +1/+3/+5 Kalmuri echo and dense dual blades to judge both concept read and object churn.

## MCP QA Recovery / Dense Dual-Blade Final Pass

- MCP recovered and found `LETHE` on port `7890`.
- Ran the pending QA sequence through AnkleBreaker Unity MCP.
- Echo / Gatekeeper results:
  - `Echo Matrix Dual Blades`: PASS, `total=240`, `state=73`.
  - `Echo Matrix Greatsword`: PASS, `total=223`, `state=69`.
  - `Gatekeeper Pattern Matrix`: PASS, `boss=4`, `meteor=20`, `cone=6`, `ring=3`.
  - `Gatekeeper Jump`: PASS, `boss=1`, `liveEnemies=15`.
- Dense Dual-Blade first rerun failed:
  - `hits=24`, `suppressed=10`, `transient=711`, `activeVfx=0`, `ms=570.01`.
- Implemented final dense optimization:
  - primary-only dual-blade slash VFX in dense fights,
  - dense Kalmuri skips support range ring,
  - dense Kalmuri skips moving dive trails and extra slash entries,
  - dense Kalmuri keeps two clamp blades plus one rip line,
  - repeated generated circle/ring/box/impact-diamond sprites now use `generatedSpriteCache`.
- Dense rerun passed:
  - `hits=30`, `suppressed=25`, `transient=64`, `activeVfx=42`, `ms=55.73`.
- Additional QA:
  - `Kalmuri Perf Matrix`: PASS, `totalKalmuri=0` at the 2s limit check.
  - `Void Priest Heal Matrix`: PASS, `attempts=12`, `accepted=4`, `vfx=16`.
  - `M2 Loop`: PASS, `hungryEcho=5`, `bloodEcho=5`.
- Final build/console:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 0 warnings, 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 0 warnings, 0 errors.
  - Unity compilation errors: `0`.
  - Unity console errors after final QA: `0`.
- Note:
  - MCP menu requests often returned `Error polling queue: fetch failed`, but the Unity console showed the menu action executed and produced `[V1QA] PASS`/`FAIL` results. Treated console QA output as source of truth.
