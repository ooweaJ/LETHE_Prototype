# 2026-06-17 Devlog

## A-I core prototype EPIC start

- User asked to proceed with the structured A-I work package:
  - A data contracts.
  - B hit feel / echo readability.
  - C real M2 loop.
  - D sprite/VFX replacement.
  - E 8 memories.
  - F 8 echoes.
  - G 4 ultimates.
  - H full play review.
  - I first balance/readability tuning.
- Created the root project-doc entry set:
  - `docs/PRD.md`
  - `docs/TECH.md`
  - `docs/TASK.md`
  - `docs/TEST.md`
  - `docs/CHANGELOG.md`
- Updated `AGENTS.md` so future meaningful work reads those root docs before orchestration state docs.
- Rewrote `docs/orchestration/state/NEXT_TASKS.md` because the previous Korean text was mojibake and no longer useful as a task board.

## A. Data contract pass

- Expanded `LETHE/Assets/_dev/Scripts/Core/DefinitionTypes.cs`.
- Added content taxonomy enums:
  - `MemoryEffectKind`
  - `EchoFormKind`
  - `EnemyRole`
  - `EncounterSpawnMode`
  - `UltimateTriggerMode`
- Added `EncounterWaveData`.
- Expanded `MemoryDefinition` with card text, effect kind, trigger family, base values, targeting radius, feedback, and active-form notes.
- Expanded `EchoDefinition` with card text, form kind, awakened text, preferred weapons, weapon interaction note, and overflow note.
- Added `UltimateEchoDefinition` for the 4 ultimate-echo direction.
- Kept and expanded `EchoSynergyDefinition` so existing Blood Blade Storm assets/runtime references remain compatible.
- Expanded `EnemyDefinition` with role, attack values, projectile values, XP, spawn cost, prefab, and feedback.
- Added `EncounterDefinition` for the M2 real-play loop pacing.
- Expanded `RewardPoolDefinition` with `ultimates` and `encounters`.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
- The warnings are existing legacy v0/debug `Object.FindObjectOfType<T>()` deprecation warnings.
- Unity MCP Editor verification was not run because the current tool list exposed UE-MCP, not AnkleBreaker Unity MCP.

## Next

- Continue B: hit feel / echo readability.
- Start with weapon and echo behavior using the new data-contract language, then migrate runtime values gradually instead of adding new hardcoded branches.

## B. Hit feel / echo readability first pass

- Implemented a first readability pass in `V1GameManager`.
- Kalmuri echo:
  - `ResolveKalmuriFollowup` now spawns a short target-local range ring before applying follow-up damage.
  - This should make Kalmuri read as "the hit point produced a follow-up echo" instead of a stray character-side line.
- Blood echo:
  - Hitting an already blood-marked enemy now sends a red thread back to the player and heals a small amount.
  - +5 Blood Bloom also emits a heal thread.
  - Added a small player-side pulse when the heal is large enough.
- Hit feedback:
  - Enemy knockback cap increased from `4.6` to `6.2` so greatsword feedback is not flattened as much.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 0 warnings, 0 errors.
  - Unity MCP Editor verification still not run because Unity MCP tools are not exposed in the current tool list.

## C. Real M2 loop readability first pass

- Added `M2LoopText()` to the v1 HUD.
- The HUD now tells the player which M2 loop state is active:
  - fill three memories.
  - next forget candidate / first boss.
  - deficit survival countdown.
  - resonance reacquire.
  - ultimate ready.
- Expanded level-up card choices:
  - after Blood Reflection is acquired, the player can pick `멈춘 1초` as a third memory if a slot is open.
  - this reduces reliance on automatic review pacing for filling all three active memory slots.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 0 warnings, 0 errors.
  - `npm.cmd run report`: pass, 4 unit reports generated.
  - `npm.cmd run report:check`: pass, 4 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.
  - Unity MCP Editor verification still pending.

## E/F/G. First content expansion pass

- Added first-pass active memory behaviors beyond Hungry Blades and Blood Reflection:
  - `ExecutionFlash`: periodically strikes low-health enemies with a bright execution diamond.
  - `HunterOath`: fires a homing shot at a nearby target.
  - `ShatterWave`: creates target-local shockwave damage around dense enemy groups.
  - `StoppedSecond`: emits a player-centered freeze/damage pulse.
  - `AshenShield`: shows a guard ring and reduces incoming player damage.
  - `OblivionBrand`: periodically brands random enemies with purple pulse damage.
- Added first-pass weapon-hit echo reactions for the non-Kalmuri/Blood echoes:
  - Shatter ripple.
  - Execution extra hit.
  - Hunter follow-up shot.
  - Stasis mini-freeze.
  - Ashen small heal/guard pulse.
  - Oblivion pulse.
- Added first-pass extra ultimate branches:
  - `FractureExecutionReady`: Shatter Wave + Execution Flash +5.
  - `StasisHuntReady`: Stopped Second + Hunter Oath +5.
  - `AshenOblivionReady`: Ashen Shield + Oblivion Brand +5.
- HUD now uses `UltimateReadyName()`/`UltimateGoalText()`.
- Level-up rewards can surface a rotating missing memory candidate.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity MCP Editor verification still pending.
