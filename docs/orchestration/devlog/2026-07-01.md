# 2026-07-01 Devlog

## Kalmuri Living Swarm Motion Pass

- Jaewoo rejected both C and D candidate-image directions.
- Changed direction from "better emblem image" to "better blade motion grammar."
- Updated `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`:
  - removed runtime C/D candidate references.
  - restored original Kalmuri blade sprite as the primary visual.
  - changed active orbit into irregular multi-speed, multi-radius blade sweeps.
  - added target-seeking hunting lunges from the orbit toward nearby enemies.
  - added higher-level recoil shards so blades feel like they return after biting.
  - rebuilt active bite hits as converging blade dives plus crossing wound cuts.
  - rebuilt Kalmuri echo follow-ups as surge/fan blade strokes.
  - rebuilt Hungry Blades memory-gain feedback as an outward blade spiral.
- Visual reference intent:
  - high-readability action hit strokes.
  - survival-game swarm density.
  - LETHE cyan-white forgotten-blade recoil and return motion.

## Living Swarm Verification

- `rg` confirmed no `PredatorBite`, `CrescentPack`, `candidate_c`, or `candidate_d` runtime references remain in `V1GameManager.cs`.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity MCP compile error count: `0`.
- Unity MCP console error count: `0`.
- Unity `LETHE/V1 Smoke/M2 Loop` executed successfully.
- Editor log includes `[V1QA] PASS M2 Loop` with:
  - `HungryBlades:5`
  - `BloodReflection:5`
  - `storm=True`
  - live enemies `10`

## Living Swarm Next

- Jaewoo should direct-play +1/+3/+5.
- If the swarm is still weak, tune one of: lunge frequency, trail alpha, blade count, or hit convergence scale.

## Kalmuri D-Only Runtime Follow-up

- Jaewoo feedback after the C/D wiring: D was not felt enough.
- Updated `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs` again:
  - removed runtime C / Crescent Pack usage.
  - replaced the active C aura with D / Predator Bite orbit frames around the player.
  - reduced the supporting small orbit blade count/alpha so D is not buried.
  - enlarged/brightened the D bite on active Hungry Blades hit.
  - added a faint D afterimage to active Hungry Blades hit.
  - enlarged/brightened the D bite on Kalmuri echo follow-up.
  - added an extra side D bite from echo level +3 onward.
  - changed Hungry Blades memory-gain and echo transform feedback to D.
- `rg` confirmed no C runtime references remain in `V1GameManager.cs`.

## D-Only Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity MCP compile error count: `0`.
- Unity MCP console error count: `0`.
- Unity `LETHE/V1 Smoke/M2 Loop` executed successfully.
- Editor log includes `[V1QA] PASS M2 Loop` with:
  - `HungryBlades:5`
  - `BloodReflection:5`
  - `storm=True`
  - live enemies `10`

## D-Only Next

- Jaewoo direct play should judge D-only at +1/+3/+5.
- If still too subtle, tune only D size/alpha/frequency next.

## Kalmuri C/D Runtime Wiring

- Continued from the 2026-06-30 Kalmuri C/D candidate asset pass.
- Decision for implementation:
  - C / Crescent Pack is the active Hungry Blades aura around the player.
  - D / Predator Bite is the enemy-side bite / hit-frame read.
  - Existing small orbit blades stay as support detail so the effect still has motion and edge sparkle.
- Updated `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`:
  - added constants for the Kalmuri base/candidate sprite paths.
  - spawned C during Hungry Blades orbit visuals.
  - added a +3 counter-rotating C aura layer.
  - spawned D during active Hungry Blades bite hits.
  - spawned D during Kalmuri echo follow-up impacts.
  - spawned C during Hungry Blades memory-gain feedback.
  - changed Hungry Blades echo transform sprite to prefer C.
- Dual-blade VFX check:
  - no new dual-blade change was made today.
  - the smaller perceived attack comes from previous work:
    - 2026-06-22 hit-point phantom weapon pass disabled held dual-blade renderers during normal play.
    - 2026-06-25 integrated feel tuning reduced dual-blade slash profile scale/lifetime and slightly reduced range/max targets.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity MCP compile error count: `0`.
- Unity `LETHE/V1 Smoke/M2 Loop` executed successfully.
- Editor log includes `[V1QA] PASS M2 Loop` with:
  - `HungryBlades:5`
  - `BloodReflection:5`
  - `storm=True`
  - live enemies `10`
- Attempted Unity Game View screenshots, but they captured the intro/solid frame rather than the forced Kalmuri combat frame. Deleted the misleading evidence files.

## Next

- Jaewoo direct play should judge whether C as aura plus D as bite makes Hungry Blades feel readable, hungry, and exciting.
- If it is too noisy, reduce aura alpha/frequency first before changing damage or Kalmuri rules.
