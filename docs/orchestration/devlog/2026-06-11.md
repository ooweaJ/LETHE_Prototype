# Devlog - 2026-06-11

## Unit: Unity core echo VFX sprites

Goal:

- Produce the first Kalmuri, Blood, and Blood Blade Storm VFX sprites needed for the Unity `_dev` playable slice.
- Import the sprites into Unity, configure them as Sprite assets, and create placeholder VFX prefabs that Task 4 can wire into the debug loop.

Actions:

- Generated 8 project-bound VFX images with Codex imagegen:
  - `spr_kalmuri_echo_slash_01.png`
  - `spr_kalmuri_orbit_blade_01.png`
  - `spr_kalmuri_launch_blade_01.png`
  - `spr_blood_mark_01.png`
  - `spr_heal_thread_tip_01.png`
  - `spr_blood_bloom_01.png`
  - `spr_blood_blade_storm_ring_01.png`
  - `spr_blood_blade_storm_blade_01.png`
- Preserved chroma-key source images under `LETHE/Assets/_dev/Art/Source`.
- Created runtime alpha PNGs under:
  - `LETHE/Assets/_dev/Art/Sprites/Echoes/Kalmuri`
  - `LETHE/Assets/_dev/Art/Sprites/Echoes/Blood`
  - `LETHE/Assets/_dev/Art/Sprites/Ultimates`
- The bundled imagegen chroma-key helper could not run because Pillow is not installed in the active Python environment.
- Used Windows `System.Drawing` to remove the flat green key locally. This is acceptable for these opaque VFX assets because the subject colors avoid green.
- Used AnkleBreaker MCP `unity_execute_code(port=7890)` to refresh Unity, configure the 8 textures as Sprite/Single/100 PPU, and create 7 VFX prefabs:
  - `Assets/_dev/Prefabs/Echoes/Kalmuri/Echo_Kalmuri_DelayedSlash.prefab`
  - `Assets/_dev/Prefabs/Echoes/Kalmuri/Memory_HungryBlades_Ring.prefab`
  - `Assets/_dev/Prefabs/Echoes/Kalmuri/Echo_Kalmuri_LaunchBlade.prefab`
  - `Assets/_dev/Prefabs/Echoes/Blood/Echo_Blood_Mark.prefab`
  - `Assets/_dev/Prefabs/Echoes/Blood/Echo_Blood_HealThread.prefab`
  - `Assets/_dev/Prefabs/Echoes/Blood/Echo_Blood_Bloom.prefab`
  - `Assets/_dev/Prefabs/Ultimates/Ultimate_BloodBladeStorm.prefab`
- Created evidence contact sheet:
  - `docs/orchestration/evidence/2026-06-11-echo-vfx-contact-sheet.png`

Verification:

- Alpha validation: all 8 runtime PNGs have corner alpha `0,0,0,0`.
- Unity MCP `unity_asset_list(folder="Assets/_dev/Art/Sprites/Echoes", type="Texture")`: 6 echo Texture2D assets found.
- Unity MCP `unity_asset_list(folder="Assets/_dev/Art/Sprites/Ultimates", type="Texture")`: 2 ultimate Texture2D assets found.
- Unity MCP `unity_asset_list(folder="Assets/_dev/Prefabs/Echoes", type="Prefab")`: 6 echo prefab assets found.
- Unity MCP `unity_asset_list(folder="Assets/_dev/Prefabs/Ultimates", type="Prefab")`: 1 ultimate prefab asset found.
- Unity MCP `unity_get_compilation_errors(port=7890, severity="all")`: `count: 0`, `isCompiling: false`.
- `npm.cmd run report`: pass, generated the 2026-06-11 daily report.
- `npm.cmd run report:check`: pass, 1 unit heading ok.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`; Project Orchestrator intake is still unavailable from this repo session.

Notes:

- The first `unity_execute_code` attempt failed because `using` declarations were placed inside the MCP method-body wrapper. The retry used fully qualified type names and succeeded.
- These sprites are first playable-slice VFX, not final art. They are sufficient for morning review because they establish distinct white-blue Kalmuri, red Blood, and white/red storm silhouettes.

## Unit: Unity playable echo debug loop

Goal:

- Wire the imported VFX prefabs into `Dev_EchoSlice.unity` so jaewoo can press Play and immediately review the echo states.

Actions:

- Added `LETHE/Assets/_dev/Scripts/Debug/DevEchoSliceDebugController.cs`.
- The debug controller supports:
  - `1`: base dual blades
  - `2`: Kalmuri +1 delayed slash
  - `3`: Kalmuri +5 orbit + launch blade
  - `4`: Blood +5 mark + bloom + heal thread
  - `5`: Blood Blade Storm
  - `Space`: force one attack
- Added compact OnGUI buttons for mouse-driven review.
- Wired the controller onto the scene `Services` object.
- Connected scene references: `RunBuildState`, `HitResolver`, `PoolService`, `DualBladesController`, player, enemy, echo anchor, Kalmuri prefabs, Blood prefabs, and Blood Blade Storm prefab.
- Saved `Assets/_dev/Scenes/Dev_EchoSlice.unity`.
- Captured Play Mode evidence:
  - `docs/orchestration/evidence/2026-06-11-dev-echo-slice-play.png`

Verification:

- First Play Mode run found a real blocker: `UnityEngine.Input.GetKeyDown` throws when the project uses Input System-only handling.
- Fixed input to use `UnityEngine.InputSystem.Keyboard` under `ENABLE_INPUT_SYSTEM`, with legacy input guarded under `ENABLE_LEGACY_INPUT_MANAGER`.
- Unity MCP `unity_get_compilation_errors(port=7890, severity="all")`: `count: 0`, `isCompiling: false`.
- Unity MCP Play Mode runtime check:
  - `controller`: true
  - `stormVisible`: true
  - `orbitVisible`: true
  - `healThreadVisible`: true
  - `enemyHealth`: `6` after forced attack in the final run
- Unity console error log after final Play Mode run: `count: 0`.
- Unity editor state after stopping Play Mode: `isPlaying: false`, `sceneDirty: false`.
- Scene missing reference scan: `totalFound: 0`.
- `npm.cmd run report`: pass, generated 2 unit reports.
- `npm.cmd run report:check`: pass, 2 unit headings ok.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`; Project Orchestrator intake is still unavailable from this repo session.

Notes:

- The debug controller intentionally lives under `_dev/Scripts/Debug`; it is not production echo runtime yet.
- It uses the existing `PoolService` for transient prefab VFX and direct `Instantiate` only for persistent debug orbit/storm state. Promotion to `Assets/Lethe` should replace the debug-only persistent path with production pooling/runtime classes.
