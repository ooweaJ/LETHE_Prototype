# 2026-07-23 Devlog

## Brand Overhead / Ashen Field Identity Follow-up

- Trigger:
  - jaewoo asked for Brand to show over the monster's head.
  - jaewoo also said Ashen still lacks identity from the memory/attack structure and suggested an ash/fire zone with DoT as a clearer direction.
- Applied:
  - Added `SpawnOblivionOverheadBrand`.
  - Brand overhead markers now spawn for memory inscription, Echo inscription, delayed erase, and spread.
  - Ashen memory now creates a scorched holy-ash field near the watched/threatened enemy.
  - The Ashen field persists briefly, burns enemies inside with DoT ticks, and feeds a little guard charge.
  - Existing Ashen guard/counter behavior remains, but the active memory's offensive identity is now spatial.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after retrying a transient Unity DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing deprecation warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Play Mode Passive + Dual Echo Matrix smoke: `memoryOverhead=4`, `echoOverhead=11`, `ashenField=10`, `ashenDots=63`.
  - Unity console errors after delayed coroutine wait: `0`.
  - Dense Dual Blades matrix reflection call completed.
- Notes:
  - Direct play should decide if the overhead Brand needs larger scale/lifetime.
  - Ashen may now need frequency/radius/DPS tuning based on feel.

## Echo Rule Identity Rework

- Trigger:
  - jaewoo clarified that Shatter feels acceptable, but Ashen, Brand, and Execution still feel like similar area attacks despite different VFX.
  - The target is now rule identity, not another VFX-only layer.
- Applied:
  - Ashen memory no longer periodically damages nearby enemies from the player body.
  - Ashen memory now keeps guard/threat-watch reads and releases damage only from stored guard charge.
  - Ashen stored release chooses high-threat counter targets instead of all enemies in range.
  - Great Ashen Echo now uses a holy-wall lane target rule instead of a cone.
  - Oblivion Brand memory now inscribes first, then resolves delayed erase and delayed spread through coroutines.
  - Oblivion Echo now uses light inscription damage first, then delayed deletion/collapse as the real hit.
  - Execution Echo now focuses condemned low-health targets and limited verdict witnesses.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after retrying a transient Unity DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing deprecation warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Play Mode Dual / Great / Passive / Dense matrix calls completed.
  - Unity console errors after delayed Brand coroutine wait: `0`.
- Notes:
  - Shatter remains the current reference for "the rule itself explains the VFX."
  - Direct play should judge whether the other three now stop collapsing into generic AoE.

## Echo Hit Readability Follow-up

- Trigger:
  - jaewoo confirmed the new Echo VFX quality is better, but the damage 판정 is still hard to read.
  - Ashen is the clearest issue: it starts from the player body/guard, but the route to enemy damage is not obvious.
- Applied:
  - Added `SpawnEchoHitRead` in `V1GameManager`.
  - Ashen memory counter and stored guard wave now add target-local consecration marks and visible counter-return links from source to victim.
  - Great Ashen holy wall now adds wall-to-victim rays and target marks for each cone target.
  - Execution / Shatter / Oblivion Echo damage loops now add target-local verdict/fault/erase hit confirmations before damage.
  - Dense Dual Blades keeps extra hit-read layers throttled.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after retrying a transient Unity DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing deprecation warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Play Mode `DebugRunEchoMatrix(DualBlades)`: reflection call completed.
  - Play Mode `DebugRunEchoMatrix(Greatsword)`: reflection call completed.
  - Unity console errors: `0`.
- Notes:
  - This pass intentionally changes readability, not damage balance.
  - Direct play should judge whether source -> path -> victim is now readable, especially for Ashen.

## Execution / Shatter / Ashen / Oblivion Echo Redesign Pass

- Trigger:
  - jaewoo clarified that remade memory VFX must not be reused as normal Echo VFX.
  - The target is "memory = symbol, Echo = separate combat event."
- Applied:
  - Generated two image atlas attempts with the built-in image generation tool.
  - Selected the darker LETHE-style atlas and saved the source under `_dev/Art/Source`.
  - Cut 8 transparent Echo-only sprites:
    - Dual Execution verdict shred.
    - Dual Shatter fault hop.
    - Dual Ashen parry return.
    - Dual Oblivion erasure scars.
    - Great Execution execution gate.
    - Great Shatter ground collapse.
    - Great Ashen holy wall.
    - Great Oblivion void collapse.
  - Set Unity import settings for the atlas and all 8 sprites.
  - Updated `V1GameManager`:
    - added dedicated sprite paths;
    - prevented Execution Echo from using the memory motif condemnation sprite;
    - changed Great Shatter to target-centered collapse with center damage bonus;
    - changed Great Execution to a target-centered execution gate area;
    - changed Great Ashen to a forward holy wall cone;
    - changed Great Oblivion to use the void collapse crater with center bonus;
    - stamped Dual Echo sprites across chain/parry/erasure targets.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing deprecation warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing deprecation warnings and 0 errors.
  - Unity import settings: `configured=9`.
  - Unity compilation errors: `0`.
  - `DebugRunEchoMatrix(DualBlades)`: all 8 Echoes at `+5`, `dualReworkObjects=64`.
  - `DebugRunEchoMatrix(Greatsword)`: all 8 Echoes at `+5`, `greatReworkObjects=32`.
  - `DebugRunDenseDualBladePerfMatrix()`: `hits=18`, `echoesSuppressed=15`, `transient=46`, `ms=18.30`.
- Evidence:
  - `docs/orchestration/evidence/2026-07-23-echo-rework-sprite-contact-sheet.png`
  - `docs/orchestration/evidence/2026-07-23-echo-rework-dual-matrix.png`
  - `docs/orchestration/evidence/2026-07-23-echo-rework-great-matrix.png`
  - `docs/orchestration/evidence/2026-07-23-echo-rework-unity-isolated-preview.png`
- Notes:
  - The all-on matrix remains visually noisy because Blood/Stopped/Hunter also fire. The sprite contact sheet is the clean read of the new art direction; direct play should judge timing/scale/noise.
