# 2026-07-07 Devlog

## Direct Feedback VFX Action Pass

- Trigger:
  - jaewoo reported that dual-blade VFX was not visible, Gatekeeper meteor/cone patterns looked like static red zones without actual attack bodies, boss damage feedback was unclear, and Hungry Blades / Kalmuri looked like wobble instead of circular orbit-to-target attacks.
- Unity connection:
  - AnkleBreaker Unity MCP was available on `LETHE` port `7890`.
  - Unity scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
- Code changes:
  - Added guaranteed dual-blade slash cuts and a normal-density hit spark.
  - Kept dense dual-blade fallback to one lightweight cut and no extra spark.
  - Added Gatekeeper falling meteor body/trail/shadow/debris.
  - Added Gatekeeper cone charge blade/edge-line cue and slash wave.
  - Added boss-hit player damage cue: flash/ring/text/SFX/camera shake.
  - Reworked Kalmuri orbit visual into coherent circular lanes plus orbit-exit, lock-line, and lunge.
  - Added dense-specific transient snapshot so Dense Dual Blades Perf Matrix no longer reads the global transient counter after gameplay continues.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity `Assets/Refresh`: success.
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Dense Dual Blades Perf Matrix`: PASS, `hits=18`, `suppressed=15`, `transient=62`, `activeVfx=27`, `ms=85.10`.
  - `LETHE/V1 QA/Gatekeeper Pattern Matrix`: PASS, `boss=4`, `meteor=20`, `cone=6`, `ring=3`.
  - `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `orbit=44`, `bite=72`, `return=24`, `hunting=16`, `totalKalmuri=268`.
- Notes:
  - MCP `unity_execute_code` intermittently returned `Error polling queue: fetch failed`, but the requested Unity QA menu was invoked and logs confirmed results.
  - Untracked Unity recovery files remain untouched: `LETHE/Assets/_Recovery.meta`, `LETHE/Assets/_Recovery/`.
- Next:
  - jaewoo direct-play review should decide whether the new VFX language is sufficient or whether one specific element needs timing/scale tuning.

## Kalmuri Orbit-To-Lunge Continuity Follow-Up

- Trigger:
  - jaewoo noted that the orbiting Kalmuri blades and the flying attack blades still looked unrelated.
- Code change:
  - Kalmuri visual update now chooses the orbit blade slot whose endpoint is closest to the target direction.
  - That slot is highlighted as `KalmuriLivingOrbitBlade_Hunter`.
  - The lunge starts from the exact orbit endpoint instead of from an independently computed arc point.
  - Added a small `KalmuriHuntReleaseSpark` and retained the lock line from launch point to target.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: standalone rerun passed with 0 warnings and 0 errors after a temporary DLL lock during parallel build.
  - Unity compilation errors: `0`.
  - `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `orbit=44`, `bite=72`, `return=24`, `hunting=16`, `totalKalmuri=270`.

## Memory/Echo/Enemy Identity Pass

- Trigger:
  - jaewoo asked to identify and immediately improve weak areas in the game, especially bosses, monsters, and the lack of distinctive memory/echo identity.
- Finding:
  - Functionality is present, but several echoes communicate through similar short marks.
  - Enemy role markers exist but are static and easy to miss in dense waves.
  - Gatekeeper has better attack patterns now, but persistent boss-body identity can still be stronger.
- Code changes:
  - Added `SpawnEchoIdentityBurst()` behind `MarkEnemyEchoState()`.
  - Utility echo state marks now create family-specific bursts:
    - gold verdict/crack for ExecutionFlash,
    - green lock/needle for HunterOath,
    - cyan fracture/fault for ShatterWave,
    - gold clock clamp/ticks for StoppedSecond,
    - pale ward/shards for AshenShield,
    - purple brand/seal for OblivionBrand.
  - Passive memories now apply matching enemy state marks when they directly affect enemies.
  - Hunter projectile hits now mark targets.
  - Added `V1EnemyRoleMarker` and animated role symbols for VoidPriest, DriftingEye, SplitOne, and Gatekeeper.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: standalone rerun passed with 0 warnings and 0 errors after a temporary DLL lock.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Echo Matrix Dual Blades: PASS, `total=240`, `state=72`.
  - Echo Matrix Greatsword: PASS, `total=223`, `state=70`.
  - Passive Memory Matrix: PASS, `blood=17`, `ash=6`, `stopped=8`, `oblivion=37`.
  - Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=114`, `activeVfx=27`, `ms=104.35`.
  - Gatekeeper Pattern Matrix: PASS, `boss=4`, `meteor=20`, `cone=6`, `ring=3`.
- Risk:
  - Dense Dual Blades is still within budget but closer to the threshold. If direct play reports hitching, reduce dense-only identity bursts first.
