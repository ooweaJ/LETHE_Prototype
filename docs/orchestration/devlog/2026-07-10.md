# 2026-07-10 Devlog

## Project Thumbnail and Intro Key-Art Pass

- Trigger:
  - jaewoo asked for a project thumbnail and an in-game intro screen that fits LETHE's mood.
- Asset work:
  - Generated a square project thumbnail/key visual:
    - `LETHE/Assets/_dev/Art/Sprites/UI/spr_lethe_project_thumbnail_01.png`
  - Generated a wide in-game intro background:
    - `LETHE/Assets/_dev/Art/Sprites/UI/spr_lethe_intro_background_01.png`
  - Unity imported both as Sprite textures and created `.meta` files.
- Runtime work:
  - Added `IntroBackgroundPath` and `ProjectThumbnailPath` to `V1GameManager`.
  - Updated `DrawLetheIntroOverlay()` to use the generated intro key art as the first-screen background.
  - Added dark readability bands and lighter glass-panel treatment so the title and weapon cards remain readable without hiding the art.
  - Added both UI sprites to `V1_ContentCatalog.asset`.
- Evidence:
  - Captured Game View evidence:
    - `LETHE/Assets/_dev/Evidence/lethe_intro_keyart_screen_20260710.png`
- Verification:
  - Unity `Assets/Refresh`: success.
  - Unity compilation errors: `0`.
  - Unity console errors after Play Mode capture: `0`.
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing legacy warnings and 0 errors.
- Remaining review:
  - jaewoo should direct-play the first screen and decide whether the title/card composition is ceremonial enough or needs less copy, stronger title treatment, or a weapon-pick animation.

## Portfolio Documentation Direction

- Trigger:
  - jaewoo asked to memo the final boss visual direction from the current key art and decide how to present LETHE on a portfolio site.
- Documentation work:
  - Added a durable final-boss visual decision:
    - `DEC-2026-07-10-01` in `docs/orchestration/state/DECISION_LOG.md`
  - Added a portfolio presentation plan:
    - `docs/orchestration/state/PORTFOLIO_SITE_PLAN.md`
- Direction:
  - Public portfolio should be a case-study page, not a raw document dump.
  - Planning/GDD and technical/TDD documents should exist as deeper supporting documents.
  - The story should connect problem -> design choice -> Unity implementation -> test evidence -> iteration -> current result.
- Future final boss memo:
  - Treat the last boss as a colossal Lethe gatekeeper / forgotten-god figure matching the key art: ruined gate silhouette, halo/crown, black water, cyan memory shards, and crimson echo blade energy.
  - Keep it out of current `_dev` scope until explicitly approved.

## Final Gatekeeper Boss Prototype

- Trigger:
  - jaewoo asked to actually implement the final boss from the key art, including mechanics and motion sprites.
- Asset work:
  - Generated a 4-frame final boss sprite sheet inspired by the key art:
    - `LETHE/Assets/_dev/Art/Sprites/Enemies/Bosses/sheet_boss_lethe_gatekeeper_final_4f.png`
  - Removed chroma background locally and imported the result as a Sprite.
  - Registered the sprite in `LETHE/Assets/_dev/Data/V1_ContentCatalog.asset`.
- Runtime work:
  - The 4th Gatekeeper/final boss now uses the new sheet and runtime frame animation.
  - Final boss visual scale/spawn offset are separate from normal Gatekeepers so the large sprite stays in frame.
  - Added final-only raid mechanics:
    - crimson judgment lane,
    - cyan cross judgment lane,
    - cyan shard cage circle,
    - layered on top of the existing final-rank cone/meteor pattern.
  - Added `Final` to the F12 debug panel.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 existing warnings and 0 errors.
  - Unity compilation errors: `0`.
  - Play Mode call `DebugJumpToFinalGatekeeper()` succeeded.
  - Evidence captured:
    - `LETHE/Assets/_dev/Evidence/lethe_final_boss_gatekeeper_screen_20260710_final.png`
- Remaining review:
  - jaewoo should direct-play F12 -> `Final` and judge whether the boss body is readable enough while Blood Blade Storm and dense enemy VFX are active.

## Weapon Echo VFX Board

- Trigger:
  - jaewoo asked to see which VFX each Echo will use by weapon with images visible.
- Documentation work:
  - Added a visual HTML board:
    - `docs/orchestration/evidence/2026-07-10-weapon-echo-vfx-board.html`
- Contents:
  - Shows real sprite thumbnails for the Echo families.
  - Separates Dual Blades and Greatsword VFX reads for Kalmuri, Blood, Execution, Hunter, Shatter, Stopped Second, Ashen Shield, and Oblivion Brand.
  - Notes which parts are base sprites and which parts are procedural line/ring/fan/chain VFX from `V1GameManager`.
- Verification:
  - Checked every image link in the board resolves to an existing workspace PNG.
