# 2026-06-15 Devlog

## V1 target-local slash / Kalmuri follow-up implementation

- Continued from DEC-2026-06-12-05.
- Changed `V1GameManager` twin-blade basic attack VFX:
  - removed player-origin fan-like slash spawning from the attack path.
  - primary target now receives two `TargetLocalSlash_*` iai/slash marks.
  - cleave targets receive smaller `TargetLocalSlash_Assist` marks.
- Changed Kalmuri echo:
  - `TriggerTwinBladeKalmuriEcho` now queues `PendingKalmuriFollowup`.
  - `UpdatePendingKalmuriFollowups` resolves the follow-up after a short delay.
  - `ResolveTwinBladeKalmuriFollowup` spawns `KalmuriFollowup` cuts from the weapon hit origin.
- Reduced twin-blade hitstop from `0.025s` to `0.018s` so echo follow-up feels less like a character pause.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Warnings are legacy v0/debug deprecated API usage.
- Unity MCP:
  - instance: `LETHE`, port `7890`.
  - active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - compilation errors: `count=0`.
- Play Mode targeted smoke:
  - `noAirAfter=0`
  - `targetLocalSlash=3`
  - `playerFanArc=0`
  - `kalmuriFollowup=6`
  - `hitSpark=6`
- Evidence:
  - `LETHE/Assets/_dev/Evidence/v1_target_local_slash_echo_followup_20260615.png`
  - `LETHE/Assets/_dev/Evidence/v1_target_local_slash_echo_followup_20260615_scene.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 1 unit heading ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `404 Not Found`, `project not found`.

## Next

- Jaewoo should review `Dev_Prototype_v1` in Play Mode and judge target-local basic attack + Kalmuri follow-up as `GO`, `ITERATE`, or `NO-GO`.
- If accepted, next implementation is greatsword attack/echo rhythm prep.
- If not accepted, tune slash size/origin, Kalmuri delay, and hitstop before expanding.

## Weapon spec / greatsword prep implementation

- Listed the next tasks from `NEXT_TASKS.md`:
  - play review is the top gate but requires jaewoo judgment.
  - twin-blade feel tuning depends on that review.
  - the next Codex-executable task is greatsword attack structure prep.
- Refactored `V1GameManager` weapon combat around `WeaponRuntimeSpec`.
- Added runtime weapon fields aligned with the design docs:
  - targeting mode.
  - echo size/damage scale.
  - echo proc style.
  - ultimate pattern.
  - hitstop/shake/knockback tuning.
- Default remains `절단쌍검`.
- Added debug `장송대검` path:
  - `DensestArc` targeting.
  - slower interval and bigger range/damage.
  - `SingleHeavy` Kalmuri follow-up.
  - `F9` toggles weapon for review/debug.
- Fixed first smoke result where `SingleHeavy` was firing once per hit. It now fires once per swing from the primary hit path.

## Verification - weapon spec / greatsword prep

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Warnings are legacy v0/debug deprecated API usage.
- Unity MCP:
  - active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - compilation errors: `count=0`.
- Play Mode targeted smoke:
  - `noAir=0`
  - `dualSlash=3`
  - `dualOldFan=0`
  - `dualFollow=6`
  - `greatSlash=4`
  - `heavyFollow=1`
  - `multiFollowStill=0`
- Evidence:
  - `LETHE/Assets/_dev/Evidence/v1_weapon_spec_greatsword_prep_20260615.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 2 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `404 Not Found`, `project not found`.

## Next after weapon spec prep

- Jaewoo still needs to review twin-blade target-local slash + echo follow-up.
- The F9 greatsword path is structure proof only; a later pass must add proper greatsword sprite/animation/readability before judging feel.

## One-pass feedback batch

- User asked to run the whole pending list so feedback can be given once.
- Implemented a combined review pass:
  - twin-blade target-local slash readability pass.
  - greatsword weapon visual/animation behavior.
  - M2 review pacing toward memory growth -> Gatekeeper -> forgetting -> deficit -> resonance -> +5 echoes -> Blood Blade Storm.
  - echo awakening HUD and ultimate readiness text.
  - weapon-patterned Blood Blade Storm.
  - denser enemy pressure for the review run.
- Important implementation details:
  - `ReviewFirstBossSeconds = 62`.
  - `ReviewDeficitSeconds = 22`.
  - review pacing grants/boosts Blood Reflection, Hungry Blades, and Stopped Second before the first Gatekeeper.
  - Gatekeeper HP is reduced for the review loop.
  - Gatekeeper gets a small post-74s fracture damage assist so the first forgetting gate does not stall.
  - resonance reacquire now spawns `공명` VFX.
  - dual-blade storm emits small frequent blades.
  - greatsword storm emits slower heavy blood slashes.

## Verification - one-pass feedback batch

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Warnings are legacy v0/debug deprecated API usage.
- Unity MCP:
  - active scene: `Assets/_dev/Scenes/Dev_Prototype_v1.unity`.
  - compilation errors: `count=0`.
- Play Mode targeted smoke:
  - `resultAfterGate=True`
  - `activeMemories=3`
  - `dualSlash=3`
  - `greatSlash=4`
  - `multiFollow=6`
  - `heavyFollow=1`
  - `dualStorm=6`
  - `greatStorm=3`
  - `resonance=12`
  - snapshot: `scene=v1 weapon=장송대검 elapsed=87.0 hp=210.0/210.0 level=1 xp=0/5 kills=5 memories=[HungryBlades:3,StoppedSecond:1,BloodReflection:2] echoes=[BloodReflection:5,HungryBlades:5] enemies=0 storm=True result=False refill=False death=False`
- Evidence:
  - `LETHE/Assets/_dev/Evidence/v1_full_feedback_batch_20260615.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 3 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `404 Not Found`, `project not found`.

## Next after one-pass batch

- Jaewoo should play `Dev_Prototype_v1` and give one combined feedback pass.
- Use `F9` during review to compare dual blades and greatsword.
- Next implementation should fix the weakest reviewed area only.

## Weapon select / hit feedback pass

- User review: there was no real choice between dual blades and greatsword, and enemy attack interaction felt strange, likely because hit feedback/knockback was too weak.
- Reference direction:
  - Vampire Survivors-like runs use automatic weapons, but the player still needs a clear start identity.
  - LETHE should treat the weapon as the automatic trigger and echoes as follow-up effects at the weapon hit origin.
- Implemented:
  - Added run-start weapon selection overlay.
  - `1` selects `절단쌍검`; `2` selects `장송대검`; clicking either card also starts the run.
  - The run no longer starts until a weapon is chosen.
  - `F9` remains as post-start debug/review weapon toggle.
  - Greatsword visual now uses a procedural blade silhouette instead of the temporary rectangular sprite.
  - Twin-blade weapon hit VFX now uses sharper target-local iai slash sprites.
  - Greatsword hit VFX now uses a heavier slash sprite and impact diamond marker.
  - Weapon knockback was increased, especially for greatsword.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 7 warnings, 0 errors.
  - Unity compilation errors: `count=0`.
  - Play Mode targeted smoke:
    - `beforeOverlay=True`
    - `afterOverlay=False`
    - `dualSlash=5`
    - `dualSpark=4`
    - `dualKnock=1.78`
    - `greatSlash=5`
    - `greatShock=1`
    - `greatKnock=5.53`
    - snapshot: `scene=v1 weapon=장송대검 elapsed=0.0 hp=210.0/210.0 level=1 xp=0/5 kills=1 memories=[HungryBlades:1] echoes=[] enemies=7 storm=False result=False refill=False death=False`
- Evidence:
  - `LETHE/Assets/_dev/Evidence/v1_weapon_select_hit_feedback_20260615.png`
- Reporting:
  - `npm.cmd run report`: pass.
  - `npm.cmd run report:check`: pass, 4 unit headings ok.
  - `npm.cmd run report:orchestrator:unit:dry`: failed with `404 Not Found`, `project not found`.

## Next after weapon select / hit feedback

- Jaewoo should review the start weapon selection and compare:
  - dual blades: quick target-local multi-slash, light repeated knockback.
  - greatsword: slow heavy hit, stronger push, larger hit VFX.
- Next likely pass after review:
  - tune enemy spacing/contact response if enemies still feel like they slide through hits.
  - or replace generated/procedural weapon sprites with dedicated art if silhouette is still not enough.
