# 2026-07-02 Devlog

## Production Gap Audit

- User asked to stop treating direct jaewoo validation as the next task and find concrete shortcomings to build next.
- Read orchestration state and inspected the current Unity v1 runtime around active memories, utility echoes, ultimates, weapon runtime specs, data assets, and smoke tests.
- Main finding: the next production gap is echo identity across weapons.
  - `TriggerUtilityEchoes` still centralizes most utility echo behavior in one weapon-hit path.
  - `WeaponRuntimeSpec` already carries `MultiSmall` vs `SingleHeavy`, so dual-blade and greatsword behavior can diverge without making duplicate echo definitions.
  - `BloodBladeStorm` already demonstrates a stronger weapon-specific split than the other three ultimate branches.
- Updated:
  - `docs/orchestration/state/NEXT_TASKS.md`
  - `docs/orchestration/state/CURRENT_TASK.md`

## Next Build Direction

1. Implement weapon-specific echo patterns for all 8 echoes.
2. Strengthen passive-feeling active memories: Blood Reflection, Ashen Shield, Stopped Second, Oblivion Brand.
3. Improve the forgetting/resonance UX so the build shift reads through action and VFX.
4. Bring Fracture Execution, Stasis Hunt, and Ashen Oblivion up to Blood Blade Storm's weapon-specific standard.
5. Move echo proc/VFX knobs out of the large manager path and into data or small specs with deterministic QA.

## Weapon-Specific Echo Pass

- Implemented the first next-task sequence item.
- Added runtime dual/greatsword echo branches while keeping one echo definition per memory.
- New object families:
  - `EchoDual_*`
  - `EchoGreat_*`
- Behavior direction:
  - Dual blades: fast bleed stacks, ripple tells, chain cuts, fan shots, micro time clamps, parry returns, brand stacks.
  - Greatsword: blood cleave pools, fracture cleaves, execution stamps, spear-like hunter shots, stasis domes, counter-waves, brand detonations.
- Added QA menu items:
  - `LETHE/V1 QA/Echo Matrix Dual Blades`
  - `LETHE/V1 QA/Echo Matrix Greatsword`
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Dual echo matrix: `[V1QA] PASS`, `EchoDual_ total=240`, all 8 echo families counted.
  - Greatsword echo matrix: `[V1QA] PASS`, `EchoGreat_ total=221`, all 8 echo families counted.
- Next sequence item: passive-feeling active memory reinforcement.

## Passive Memory Reinforcement Pass

- Implemented the second next-task sequence item.
- Goal: make `BloodReflection`, `AshenShield`, `StoppedSecond`, and `OblivionBrand` readable as active memory behaviors before they are forgotten into echoes.
- Runtime changes:
  - `BloodReflection` now has a periodic active pulse that blooms from a marked or nearby target, marks victims, deals pulse damage, heals, adds +3 tether threads, and adds +5 awakened lash/bloom feedback.
  - `AshenShield` now keeps the defensive shield read, then adds +3 counter slashes/counter damage and a +5 awakened shield wave with small recovery.
  - `StoppedSecond` now adds a visible memory beat, +3 aftercut lines, and a +5 wider freeze dome extension.
  - `OblivionBrand` now adds target tethering, +3 fork links/splash damage, and +5 awakened seal feedback.
- Added QA menu:
  - `LETHE/V1 QA/Passive Memory Matrix`
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors.
  - Final editor build after cleanup: passed with 0 warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Passive memory matrix: `[V1QA] PASS`, `blood=13`, `ash=5`, `stopped=6`, `oblivion=24`.
- Next sequence item: forgetting/resonance UX production pass.

## Forget / Resonance UX Pass

- Implemented the third next-task sequence item.
- Goal: make memory loss, echo gain, resonance bonus, +5 awakening, and ultimate readiness read as a visible transition before the player reads the overlay text.
- Runtime changes:
  - Added readable Korean forget-result overlay copy after the old overlay setup.
  - Added `ForgetFlow_*` transition VFX: lost memory marker, break ring, gained echo marker, bridge link, echo-level ring, resonance target/thread, awaken stamp/burst, and ultimate bridge.
  - Added `DebugRunForgetResonanceFlow()` to force Hungry Blades +5 memory into Hungry echo +5 while Blood echo is already +5, producing Blood Blade Storm readiness.
- Added QA menu:
  - `LETHE/V1 QA/Forget Resonance Flow`
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Forget resonance flow: `[V1QA] PASS`, `forgetFlow=15`, `echoTransform=2`, `ultimateReady=3`, `hungryEcho=5`, `bloodEcho=5`, `storm=True`, `result=True`.
- Next sequence item: four ultimate weapon-pattern expansion.

## Utility Ultimate Weapon Pattern Pass

- Implemented the fourth next-task sequence item.
- Goal: bring `FractureExecution`, `StasisHunt`, and `AshenOblivion` closer to `BloodBladeStorm` by giving each dual-blade and greatsword-specific rhythms.
- Runtime changes:
  - Added `UltDual_*` and `UltGreat_*` object families.
  - `FractureExecution`: dual rapid execution cuts/marks; greatsword execution stamp, cleave, verdict burst.
  - `StasisHunt`: dual small stasis field, micro clamps, fast hunter shots; greatsword target-centered stasis dome, frozen cleaves, spear read.
  - `AshenOblivion`: dual guard return/parry/brand links; greatsword guard-break wave and heavy brand links.
- Added QA menu items:
  - `LETHE/V1 QA/Utility Ultimate Matrix Dual Blades`
  - `LETHE/V1 QA/Utility Ultimate Matrix Greatsword`
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after retry. First attempt hit a transient DLL file lock from Unity/dotnet concurrency.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Dual utility ultimate matrix: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Greatsword utility ultimate matrix: `[V1QA] PASS`, `fracture=8`, `stasis=22`, `ashen=16`.
- Next sequence item: echo/ultimate dataization and QA counter cleanup.

## Kalmuri Performance Optimization

- User reported heavy lag when `Hungry Blades / Kalmuri` appears across many enemies, and also suspected +5 blade count was too high.
- Direction chosen:
  - Keep the fantasy as a blade swarm, but shift +5 from "many small blades" to "fewer larger blades with clearer bite/echo accents".
  - Preserve total damage feel by increasing per-blade damage compensation while reducing object count.
- Runtime changes:
  - Added Kalmuri caps for orbit blades, bite targets, bite blades, and pending followups.
  - Reduced active memory tick cadence and +5 bite fan-out.
  - Reduced orbit spawn frequency/count while increasing blade scale/alpha.
  - Reduced bite blades, return shards, hunting recoil spam, echo surge blades, echo barrage blades, and helper ring/flash lifetimes.
  - Added a short +5 awakened launch cooldown to avoid projectile/ring bursts on every dense hit.
- QA changes:
  - Added `DebugRunKalmuriPerfMatrix()`.
  - Added `LETHE/V1 QA/Kalmuri Perf Matrix`.
  - Perf QA clears previous Kalmuri objects before running and requires at least 2 seconds of sustained state.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Final QA: `[V1QA] PASS Kalmuri Perf Matrix`, `orbit=44`, `bite=72`, `return=24`, `hunting=14`, `echoSurge=64`, `echoBarrage=32`, `totalKalmuri=374`.
  - First measured failing stress count was `totalKalmuri=690`; final pass is about 46% lower.
- Follow-up:
  - Direct-play is still needed for feel: confirm +5 still feels rich enough after reducing density.

## Echo Tuning Spec / QA Counter Cleanup Pass

- Implemented the first pass of the current highest-priority next task: echo dataization and QA counter cleanup.
- Scope intentionally stayed inside existing runtime behavior:
  - No new memory/echo/ultimate content.
  - No promotion out of `_dev`.
  - No full ScriptableObject migration yet.
- Runtime cleanup:
  - Added compact helper/spec functions for utility echo proc chance, radius, target limit, damage multiplier, freeze duration, and execution threshold.
  - Rewired Shatter, Hunter, Stopped, Ashen, Oblivion, and Execution echo code to use those helpers for their repeated tuning formulas.
  - This makes the next migration to `_dev/Data` smaller because the scattered formulas now have one intermediate home.
- QA cleanup:
  - Added shared count metric and count limit helpers in `V1SmokeTestMenu`.
  - Refactored Echo Matrix, Passive Memory Matrix, Kalmuri Perf Matrix, and Utility Ultimate Matrix to use shared count formatting/checking.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 0 warnings and 0 errors after retrying one transient DLL lock.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Echo Matrix Dual: `[V1QA] PASS`, `total=240`.
  - Echo Matrix Great: `[V1QA] PASS`, `total=223`.
  - Utility Ultimate Dual: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Utility Ultimate Great: `[V1QA] PASS`, `fracture=8`, `stasis=26`, `ashen=16`.
  - Kalmuri Perf Matrix: `[V1QA] PASS`, `totalKalmuri=374`.
- Next:
  - Move compact helper/spec values into a serializable table or `_dev/Data` asset path.
  - Then remove old fallback utility echo code once the data route is stable.

## Utility Echo Serializable Tuning Table

- Continued the echo dataization sequence.
- Runtime changes:
  - Added serialized `UtilityEchoTuningSpec[] utilityEchoTuningSpecs` to `V1GameManager`.
  - Added a static default table for `ExecutionFlash`, `HunterOath`, `ShatterWave`, `StoppedSecond`, `AshenShield`, and `OblivionBrand`.
  - Moved proc chance, first-hit gating, radius, target limit, damage multiplier, freeze duration, and execution health threshold reads through table methods.
  - Kept safe fallback values so scenes with an empty or partial serialized table preserve behavior.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Echo Matrix Dual: `[V1QA] PASS`, `total=240`.
  - Echo Matrix Great: `[V1QA] PASS`, `total=219`.
  - Utility Ultimate Dual: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Utility Ultimate Great: `[V1QA] PASS`, `fracture=8`, `stasis=26`, `ashen=16`.
  - Kalmuri Perf Matrix: `[V1QA] PASS`, `totalKalmuri=0` in the current post-optimization smoke run.
- Next:
  - Move this serializable table into `_dev/Data` as a dedicated ScriptableObject/data contract.
  - Apply the same data path to ultimate and VFX timing constants.

## Utility Echo Tuning Data Asset Migration

- Continued the highest-priority dataization task.
- Runtime/data changes:
  - Added `V1UtilityEchoTuningTable` as a ScriptableObject.
  - Added `Assets/_dev/Data/Echoes/UtilityEcho_Tuning.asset`.
  - Connected `V1_ContentCatalog.asset` to the tuning asset.
  - Added `utilityEchoTuningTable` to `V1ContentCatalog` and `V1GameManager`.
  - Updated `V1SceneBuilder` so freshly rebuilt v1 scenes wire the same data asset.
  - `V1GameManager.EchoTuning()` now checks the asset table before manager serialized fallback and static defaults.
- Verification:
  - Unity compile error count: `0`.
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors after retrying a transient DLL lock from a parallel build.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Echo Matrix Dual: `[V1QA] PASS`, `total=240`.
  - Echo Matrix Great: `[V1QA] PASS`, `total=221`.
  - Utility Ultimate Dual: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Utility Ultimate Great: `[V1QA] PASS`, `fracture=8`, `stasis=22`, `ashen=16`.
- Next:
  - Continue echo/ultimate runtime cleanup with compact specs for remaining repeated effect constants.

## Utility Echo Legacy Fallback Removal

- Continued the echo/ultimate runtime cleanup task after moving utility echo tuning into `_dev/Data`.
- Removed the unreachable old inline fallback branch inside `V1GameManager.TriggerUtilityEchoes`.
- The route now explicitly returns on `enemy == null` and dispatches through the current per-echo handlers only:
  - `TriggerShatterEcho`
  - `TriggerExecutionEcho`
  - `TriggerHunterEcho`
  - `TriggerStoppedEcho`
  - `TriggerAshenEcho`
  - `TriggerOblivionEcho`
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Echo Matrix Dual: `[V1QA] PASS`, `total=240`.
  - Echo Matrix Great: `[V1QA] PASS`, `total=221`.
  - Utility Ultimate Dual: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Utility Ultimate Great: `[V1QA] PASS`, `fracture=8`, `stasis=22`, `ashen=16`.
  - Kalmuri Perf Matrix: `[V1QA] PASS`, `totalKalmuri=374`.
- Note:
  - One first attempt at removing the fallback damaged file encoding, so the file was restored to HEAD and the final range replacement was repeated with explicit UTF-8 output before verification.
- Next:
  - Continue compacting repeated echo/ultimate effect constants, then proceed to passive memory feel tuning.

## Passive Memory Feel Tuning

- Tuned the four passive-feeling memories that needed stronger readability before forgetting:
  - `BloodReflection`: faster cadence, wider radius, seven-target +5 cap, stronger bloom, awakened draw thread.
  - `StoppedSecond`: faster cadence, wider field, stronger aftercut, longer +5 freeze reach.
  - `AshenShield`: faster shield pulse, larger counter radius, stronger counter damage, clearer +5 guard wave.
  - `OblivionBrand`: faster cadence, four +5 brands, stronger fork links, target-centered awakened seals.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Passive Memory Matrix: `[V1QA] PASS`, `blood=17`, `ash=6`, `stopped=8`, `oblivion=36`.
  - Echo Matrix Dual: `[V1QA] PASS`, `total=240`.
  - Forget Resonance Flow: `[V1QA] PASS`, `forgetFlow=15`, `echoTransform=2`, `ultimateReady=3`.
  - Kalmuri Perf Matrix: `[V1QA] PASS`, `totalKalmuri=374`.
- Next:
  - Continue with forget/resonance UX tuning.

## Forget / Resonance UX Compression

- Tuned the forget/resonance result flow after passive memory tuning.
- Runtime changes:
  - Replaced the readable overlay with a compact `memory lost -> echo gained -> resonance start -> return` summary.
  - Rebuilt `EchoTransform` into a shorter burst with explicit `EchoTransformShard` objects.
  - Moved `ForgetFlow` memory and echo symbols closer to the player.
  - Shortened `ForgetFlow` memory/echo/resonance/awaken/ultimate bridge lifetimes.
  - Reduced the `UltimateReady` cue size, lifetime, and floating text height.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
  - Forget Resonance Flow: `[V1QA] PASS`, `forgetFlow=15`, `echoTransform=14`, `ultimateReady=3`.
  - Utility Ultimate Dual: `[V1QA] PASS`, `fracture=19`, `stasis=9`, `ashen=34`.
  - Kalmuri Perf Matrix: `[V1QA] PASS`, `totalKalmuri=374`.
- Next:
  - Tune non-blood ultimate feel.
