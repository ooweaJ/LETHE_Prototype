# 2026-06-07 개발일지

## v0.12 인간 플레이테스트 패키지

- 목표: 자동 밸런스 기준선을 받아들인 뒤, 추가 블라인드 튜닝 대신 인간 테스트 패키지로 넘어간다.
- `docs/HUMAN_PLAYTEST_GUIDE.md`를 v0.12 기준 5-8명 통제 세션용으로 갱신했다.
- `docs/PLAYTEST_NOTES.md`에 초반 루프, 첫 보스, 망각, 결손, 리필 관찰 항목을 분리한 v0.12 세션 템플릿을 넣었다.
- `docs/playtest/2026-06-07-v012-human.md`를 추가했다.
- `scripts/prepare_playtest_build.js`가 이전 v0.7 solo sheet 대신 `V012_HUMAN_PLAYTEST_SHEET.md`를 포함하도록 수정했다.
- 검증:
  - `node --check scripts/prepare_playtest_build.js`: 통과.
  - `npm run playtest:package:dry`: 통과, 출력 대상 `dist\lethe-v0.12-playtest`.
  - `npm run playtest:package`: 통과, `dist\lethe-v0.12-playtest` 생성.
  - 패키지 파일 확인: `HUMAN_PLAYTEST_GUIDE.md`, `PLAYTEST_NOTES_TEMPLATE.md`, `V012_HUMAN_PLAYTEST_SHEET.md`, `PLAYTEST_SUMMARY_README.md`, `README.md`, `index.html`, `style.css`, `src/`.
- 결정: v0.12를 현재 통제 인간 테스트 후보로 포장했다.
- 리스크: 인간 세션에서 사망 `40%`와 패배 후 압박이 여전히 강하게 느껴질 수 있다. 인간 증거 없이 다시 수치만 만지지 않는다.
- 보고/Discord:
  - `npm run report`: 통과.
  - `npm run report:check`: 통과.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: 승인 검토에서 외부 Discord webhook 전송이 잠재적 데이터 반출로 판단되어 차단됨.
  - `npm run report:discord:unit:dry`: 통과.
  - trusted-local 명령: `npm run report:discord:unit`.

## v0.12 밸런스 루프 게이트 수정

- 목표: v0.12 밸런스 목표와 루프 기본값/게이트가 어긋난 문제를 고친다.
- `npm run autopilot:preflight:local`: 20 pass / 1 warn / 0 fail.
- PowerShell/npm 경로에서 `npm run balance:loop -- --runs ...` 인자가 스크립트에 위치 인자로 들어와, 루프가 예전 기본값 `608s`를 조용히 쓰고 있던 문제를 확인했다.
- `scripts/run_balance_loop.js` 변경:
  - 기본 실행 창 `608s -> 690s`.
  - 위치 인자 허용.
  - dry-run에서 run-sec, timeout, out, report path를 출력.
- `scripts/run_browser_balance_qa.js`에 사망률 최대 `<= 40%` 게이트를 추가했다.
- 밸런스 변경:
  - 첫 보스 HP `2500 -> 2050`.
  - 결손 지속시간 `60s -> 54s`.
  - 적 피해 스케일 완화.
  - 첫 사이클 상승 cap과 이후 사이클 cap 완화.
  - 리필 HP floor와 shield 증가.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - `node --check scripts/run_balance_loop.js`: 통과.
  - `npm run balance:loop:dry`: 통과, `run-sec 690` 확인.
  - 브라우저 `first_boss_ttk`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK 중앙값 `18.61s`.
  - 최종 `npm run balance:loop -- 5 690 60000 alpha_test\outputs\balance-loop-2026-06-07-final-hp2050 docs\balance\2026-06-07-v012-balance-loop.md`: `GO_BALANCE_BASELINE`, 첫 보스 클리어 `100%`, 전체 클리어 `60%`, 사망 `40%`, 첫 보스 TTK 중앙값 `20.73s`.
- 밸런스 변경 후 `npm run playtest:package`로 `dist\lethe-v0.12-playtest`를 다시 생성했다.
- 보고/Discord:
  - `npm run report`: 통과.
  - `npm run report:check`: 통과.
  - `npm run doctor`: 통과, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: 승인 검토에서 차단됨.
  - `npm run report:discord:unit:dry`: 통과.
  - trusted-local 명령: `npm run report:discord:unit`.
