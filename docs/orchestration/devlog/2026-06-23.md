# 2026-06-23

## Greatsword Blade-Tip Alignment

- Responded to jaewoo feedback that the greatsword handle should point toward the player body and that slash VFX should be calculated from the blade tip.
- Updated `V1GameManager` so greatsword phantom attacks calculate the desired blade-tip path first, then offset the weapon sprite center backward from the tip.
- Added greatsword-specific slash anchor compensation so `GreatswordCrescent_Primary` lands on the calculated blade tip instead of the weapon center.
- Kept the previous phantom timing rule: the weapon sweep appears first, then slash / spark / hit-confirm VFX follows after the short greatsword delay.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity `Assets/Refresh`: success.
- Unity compile error count: 0.
- Forced greatsword attack Play Mode check:
  - `handleCloser=True`
  - tip distance from player: `1.67`
  - handle distance from player: `0.16`
  - desired slash tip and `GreatswordCrescent_Primary` position distance: `0.000`
- Unity console error count: 0.

## Remaining Review

- Jaewoo still needs to directly play the greatsword route and judge whether the 45-degree sweep, handle direction, blade-tip VFX, and attack readability feel natural.

## Greatsword Handle-Pivot Sweep Follow-Up

- Responded to jaewoo feedback that the greatsword slash VFX was reversed and the sword should rotate around the handle like a real cut.
- Reworked greatsword phantom motion so `V1WeaponPhantomSweep` can run in a handle-pivot mode.
- Greatsword phantom now keeps a player-facing handle pivot and rotates blade direction from `-28` to `+28` degrees.
- Greatsword crescent VFX now uses the sweep end blade direction plus a `180` degree facing correction.

## Follow-Up Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: 0.
- Play Mode forced greatsword attack:
  - `usePivot=True`
  - handle distance from player: `0.13`
  - strike center distance from player: `0.61`
  - blade sweep: `-28.0 -> 28.0`
- Direct slash VFX check:
  - end blade angle: `28.0`
  - VFX rotation: `208.0`
  - tip alignment error: `0.000`
- Unity console error count: 0.

## Greatsword Spectacle Follow-Up

- Responded to jaewoo feedback that the greatsword cut still lacked heat and flash.
- Increased `GreatswordSwingHalfAngle` from `28` to `45`, making the pivot sweep a full `90` degrees.
- Increased greatsword wide crescent prompt-sprite scale factor from `0.150` to `0.175`.
- Enlarged greatsword weapon-hit VFX profile values:
  - AoE crescent scale/lifetime: `1.24 / 0.42 -> 1.65 / 0.50`.
  - Primary crescent scale/lifetime: `1.02 / 0.34 -> 1.38 / 0.42`.
  - Shock, cut point, and assist crescent also increased slightly.

## Spectacle Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: 0.
- Play Mode forced greatsword attack:
  - `usePivot=True`
  - blade sweep: `-45.0 -> 45.0`
  - total sweep: `90.0`
  - AoE scale: `1.65`
  - Primary scale: `1.38`
- Direct slash VFX check:
  - end blade angle: `45.0`
  - VFX rotation: `225.0`
  - generated bounds: `(4.28, 4.28)`
  - tip alignment error: `0.000`
- Unity console error count: 0.

## Greatsword Timing / Coverage Review Loop

- Responded to jaewoo feedback that the greatsword VFX still appeared while the weapon was swinging and could feel too fast.
- Increased greatsword slash delay from `0.18s` to `0.22s`.
- Kept greatsword sweep at `0.28s`, so slash VFX now appears at roughly `78.6%` of the swing.
- Increased greatsword phantom lifetime to `0.42s` and minimum slash lifetime to `0.62s`.
- Split greatsword VFX placement along the 90-degree tip arc:
  - AoE crescent: `58%` of the sweep arc.
  - Primary crescent: `78%`.
  - Assist crescent: `72%`.
  - Shock/cutpoint: final tip.
- Unity inline Game View capture succeeded on a frozen review frame with the sword held around `85%` and the long-lived VFX visible.

## Dual-Blade Direction Note

- Dual blades should use the same readability principle, but not the same scale.
- Recommended next dual-blade pass: weapon sprites cross first, then two shorter staggered slash VFX appear, with a tighter lifetime than greatsword.

## Greatsword Slash Timing Tighten

- Responded to jaewoo feedback that the greatsword slash VFX felt slightly slow.
- Reduced greatsword slash delay from `0.22s` to `0.20s`.
- With the current `0.28s` sweep, slash VFX appears at roughly `71.4%` of the motion instead of `78.6%`.
- Kept the longer slash lifetime and arc-based VFX placement from the previous pass.

## Dual Blades / Blood Blade Storm / First-120 Pass

- Completed the three next work items requested by jaewoo.
- Dual blades:
  - Added per-entry VFX delay so A slash, cut flash, and B slash do not fire on the same frame.
  - Runtime delays: A `0.045s`, cut flash `0.067s`, B `0.085s`, assist `0.045s`.
  - Slightly increased dual-blade slash/spark profile scales and lifetimes.
- Blood Blade Storm:
  - Increased opening cue scale/lifetime.
  - Increased heavy and fast burst cadence, ring scale, blade count, pressure damage, heal, hitstop, and shake.
  - Smoke result: `stormReady=True`, `stormObjects=187`, `burstObjects=45`, `bladeObjects=187`, `kills=21`.
- First 120 seconds:
  - Opening spawn interval `0.52 -> 0.46`.
  - 35-80s interval `0.58 -> 0.52`.
  - 80-120s profile `0.50 x3 -> 0.46 x4`.
  - Early enemy cap `28 -> 32`.
  - Early XP multiplier `1.95 -> 2.15`.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Unity console error count: 0.
