# 2026-06-23

## Greatsword Blade-Tip Alignment

- Responded to jaewoo feedback that the greatsword handle should point toward the player body and that slash VFX should be calculated from the blade tip.
- Updated `V1GameManager` so greatsword phantom attacks calculate the desired blade-tip path first, then offset the weapon sprite center backward from the tip.
- Added greatsword-specific slash anchor compensation so `GreatswordCrescent_Primary` lands on the calculated blade tip instead of the weapon center.
- Kept the previous phantom timing rule: the weapon sweep appears first, then slash / spark / hit-confirm VFX follows after the short greatsword delay.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity `Assets/Refresh`: success.
- Unity compile error count: 0.
- Forced greatsword attack Play Mode check:
  - `handleCloser=True`
  - tip distance from player: `1.67`
  - handle distance from player: `0.16`
  - desired slash tip and `GreatswordCrescent_Primary` position distance: `0.000`
- Unity console error count: 0.

## Remaining Review

- Jaewoo still needs to directly play the greatsword route and judge whether the 45-degree sweep, handle direction, blade-tip VFX, and attack readability feel natural.

## Greatsword Handle-Pivot Sweep Follow-Up

- Responded to jaewoo feedback that the greatsword slash VFX was reversed and the sword should rotate around the handle like a real cut.
- Reworked greatsword phantom motion so `V1WeaponPhantomSweep` can run in a handle-pivot mode.
- Greatsword phantom now keeps a player-facing handle pivot and rotates blade direction from `-28` to `+28` degrees.
- Greatsword crescent VFX now uses the sweep end blade direction plus a `180` degree facing correction.

## Follow-Up Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: 0.
- Play Mode forced greatsword attack:
  - `usePivot=True`
  - handle distance from player: `0.13`
  - strike center distance from player: `0.61`
  - blade sweep: `-28.0 -> 28.0`
- Direct slash VFX check:
  - end blade angle: `28.0`
  - VFX rotation: `208.0`
  - tip alignment error: `0.000`
- Unity console error count: 0.

## Greatsword Spectacle Follow-Up

- Responded to jaewoo feedback that the greatsword cut still lacked heat and flash.
- Increased `GreatswordSwingHalfAngle` from `28` to `45`, making the pivot sweep a full `90` degrees.
- Increased greatsword wide crescent prompt-sprite scale factor from `0.150` to `0.175`.
- Enlarged greatsword weapon-hit VFX profile values:
  - AoE crescent scale/lifetime: `1.24 / 0.42 -> 1.65 / 0.50`.
  - Primary crescent scale/lifetime: `1.02 / 0.34 -> 1.38 / 0.42`.
  - Shock, cut point, and assist crescent also increased slightly.

## Spectacle Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity compile error count: 0.
- Play Mode forced greatsword attack:
  - `usePivot=True`
  - blade sweep: `-45.0 -> 45.0`
  - total sweep: `90.0`
  - AoE scale: `1.65`
  - Primary scale: `1.38`
- Direct slash VFX check:
  - end blade angle: `45.0`
  - VFX rotation: `225.0`
  - generated bounds: `(4.28, 4.28)`
  - tip alignment error: `0.000`
- Unity console error count: 0.

## Greatsword Timing / Coverage Review Loop

- Responded to jaewoo feedback that the greatsword VFX still appeared while the weapon was swinging and could feel too fast.
- Increased greatsword slash delay from `0.18s` to `0.22s`.
- Kept greatsword sweep at `0.28s`, so slash VFX now appears at roughly `78.6%` of the swing.
- Increased greatsword phantom lifetime to `0.42s` and minimum slash lifetime to `0.62s`.
- Split greatsword VFX placement along the 90-degree tip arc:
  - AoE crescent: `58%` of the sweep arc.
  - Primary crescent: `78%`.
  - Assist crescent: `72%`.
  - Shock/cutpoint: final tip.
- Unity inline Game View capture succeeded on a frozen review frame with the sword held around `85%` and the long-lived VFX visible.

## Dual-Blade Direction Note

- Dual blades should use the same readability principle, but not the same scale.
- Recommended next dual-blade pass: weapon sprites cross first, then two shorter staggered slash VFX appear, with a tighter lifetime than greatsword.

## Greatsword Slash Timing Tighten

- Responded to jaewoo feedback that the greatsword slash VFX felt slightly slow.
- Reduced greatsword slash delay from `0.22s` to `0.20s`.
- With the current `0.28s` sweep, slash VFX appears at roughly `71.4%` of the motion instead of `78.6%`.
- Kept the longer slash lifetime and arc-based VFX placement from the previous pass.

## Dual Blades / Blood Blade Storm / First-120 Pass

- Completed the three next work items requested by jaewoo.
- Dual blades:
  - Added per-entry VFX delay so A slash, cut flash, and B slash do not fire on the same frame.
  - Runtime delays: A `0.045s`, cut flash `0.067s`, B `0.085s`, assist `0.045s`.
  - Slightly increased dual-blade slash/spark profile scales and lifetimes.
- Blood Blade Storm:
  - Increased opening cue scale/lifetime.
  - Increased heavy and fast burst cadence, ring scale, blade count, pressure damage, heal, hitstop, and shake.
  - Smoke result: `stormReady=True`, `stormObjects=187`, `burstObjects=45`, `bladeObjects=187`, `kills=21`.
- First 120 seconds:
  - Opening spawn interval `0.52 -> 0.46`.
  - 35-80s interval `0.58 -> 0.52`.
  - 80-120s profile `0.50 x3 -> 0.46 x4`.
  - Early enemy cap `28 -> 32`.
  - Early XP multiplier `1.95 -> 2.15`.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Unity console error count: 0.

## Utility VFX / Background / Movement Pass

- Responded to jaewoo feedback:
  - Greatsword slash VFX can appear a bit faster.
  - Game feel is promising.
  - Other memories, especially Stopped Second, do not feel visible yet.
  - A first real background pass is now needed.
- Greatsword:
  - Reduced slash delay from `0.20s` to `0.18s`.
  - With the current `0.28s` sweep, slash VFX appears at about `64.3%`.
- Utility memories/echoes:
  - Enlarged and lengthened Execution, Hunter, Shatter, Stopped, Ashen, and Brand active/echo VFX.
  - Added stronger secondary cues such as impact cores, muzzle rings, outer shock rings, brand slashes, shield cores, and clock hands.
  - Changed Stopped Second from player-centered to nearest-enemy-cluster centered.
- Debug panel:
  - Added `Mem A`, `Mem B`, `Echo A`, `Echo B`, `Ult 3`, and `VFX` buttons.
  - These are review conveniences; active memory rules still keep the 3-slot cap.
- Arena/movement:
  - Added runtime dark backdrop, boundary bands, memory cracks, and outer markers.
  - Softened player acceleration/deceleration, walk frame cadence, bob, and tilt.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Play Mode utility smoke: `greatDelay=0.18`, `sweep=0.28`, `activeMemories=3`, `bgDecor=30`, `utilityVfx=36`, `enemies=14`.
  - Play Mode echo/ultimate smoke: `echoCount=6`, `previewUlt=6`, `clockHands=21`.
  - Unity console error count: 0.

## Execution / Stopped Second Readability Follow-up

- Responded to jaewoo feedback that Execution Flash was still too small and Stopped Second needed an obvious clock/floor telegraph.
- Execution Flash:
  - Active memory target width `1.30 -> 1.95`.
  - Lifetime `0.24s -> 0.38s`.
  - Added a crack burst helper with vertical, horizontal, and diagonal fracture lines plus a bright core.
  - Execution Echo target width `1.08 -> 1.48` and now uses the same crack helper.
- Stopped Second:
  - Replaced subtle clock-hand-only feedback with a full clock-field floor telegraph.
  - Field includes a clock face, outer/inner rings, 12 tick marks, hands, and center core.
  - Stopped Echo and Stasis Hunt preview/ultimate paths now reuse the clock-field language.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Play Mode smoke: `executionCracks=16`, `executionVfx=24`, `clockFaces=5`, `clockTicks=60`, `clockHands=15`, `stoppedVfx=79`.
  - Unity console error count: 0.

## Stopped Second Gold Clock-Field Follow-up

- Responded to jaewoo feedback:
  - Hunter Oath appears green/yellow-green.
  - Stopped Second should read as a yellow time/clock field.
  - The field should remain visible during the one-second stop, closer to `1.5s` visually, because the game needs more readable spectacle.
- Reference:
  - Looked up Eternal Return Henry's Chrono Field description and used it only as a readability reference: a persistent time field around the character that clearly communicates slow/stop space.
- Implementation:
  - Kept Hunter Oath in the green/yellow-green projectile family.
  - Changed Stopped Second active/echo/ultimate colors to gold/yellow.
  - Increased active Stopped Second freeze to up to `1.0s`.
  - Increased active Stopped Second VFX lifetime to `1.50s`.
  - Increased Stopped Echo field to `0.90s`.
  - Increased Stasis Hunt field readability with `1.20s~1.50s` gold fields.
  - Enlarged/brightened the clock face, outer/inner rings, 12 ticks, hands, core, and added a rotating pulse ring.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Play Mode smoke: `clockFaces=2`, `totalClockFaces=3`, `clockTicks=24`, `totalClockTicks=36`, `clockPulses=2`, `clockHands=6`, `goldFaces=2`, `frozenNear1s=5`.
  - Unity console error count: 0.

## Hunter Oath Value Follow-up

- Responded to jaewoo feedback that Hunter Oath felt worse than the basic attack.
- Problem found:
  - Runtime Hunter Oath was effectively one weak homing shot per cooldown.
  - Design says Hunter Oath should be tracking multi-projectile pressure, with projectile count/speed as the level-up fantasy.
- Implementation:
  - Active Hunter Oath now fires 2/3/4 shots at levels 1/3/5.
  - Cooldown changed to `max(0.62, 1.25 - level*0.11)`.
  - Projectile speed changed to `9.4 + level*0.85`.
  - Projectile damage changed to `13 + level*4.8`.
  - Added target-lock VFX and a lock-on impact burst.
  - Hunter Echo proc chance and damage were increased.
  - Hunter Echo +5 can fire two echo shots.
  - Homing shots retarget if their original target dies.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Unity Play Mode entry reached `isPlaying=true`.
  - Unity console error count: 0.

## Lingering VFX / Echo Readability Follow-up

- Responded to jaewoo feedback:
  - Shatter Wave and Stopped Second should persist briefly instead of disappearing immediately.
  - Echo VFX may exist technically, but it does not yet read clearly during play.
  - First boss at 180s may feel loose, but keep it for now until the first three minutes are visually readable enough to judge.
- Implementation:
  - Added a Shatter Wave field helper with prompt sprite, outer/inner hold rings, and radial fracture spokes.
  - Shatter Wave active field lifetime is now `1.05s`.
  - Shatter Echo uses the same language for `0.90s`.
  - Stopped Second active clock field now lasts `1.75s`.
  - Stopped Echo field is larger and lasts `1.25s`.
  - Stasis Hunt prompt/field now uses the gold time-stop language more consistently.
  - Execution, Hunter, Ashen, and Oblivion echo VFX lifetimes/alpha/scale were increased.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: 0.
  - Unity Play Mode entry reached `isPlaying=true`.
  - Unity console error count: 0.

## Enemy / Boss Sprite Insertion

- Responded to jaewoo asking about enemy/boss sprite insertion.
- Generated first-pass prototype sprites:
  - Drifting Eye: floating eye with tendrils.
  - Split One: amber dividing blob.
  - Void Priest: hooded caster with green casting cue.
  - Gatekeeper: large armored guardian / door motif.
- Files:
  - `LETHE/Assets/_dev/Art/Sprites/Enemies/Eye/sheet_enemy_eye_4dir.png`
  - `LETHE/Assets/_dev/Art/Sprites/Enemies/Splitter/sheet_enemy_splitter_4dir.png`
  - `LETHE/Assets/_dev/Art/Sprites/Enemies/VoidPriest/sheet_enemy_voidpriest_4dir.png`
  - `LETHE/Assets/_dev/Art/Sprites/Enemies/Bosses/spr_boss_gatekeeper_01.png`
  - matching chroma source PNGs under `LETHE/Assets/_dev/Art/Source`.
- Added `scripts/generate_enemy_boss_sprites.ps1` so the files can be regenerated.
- Runtime:
  - `V1GameManager.EnemySprite()` now loads the new enemy/boss assets before procedural fallback.
- Verification:
  - Local image view sanity checks opened the generated PNGs.
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity `Assets/Refresh`: success.
  - Unity compile error count: 0.
  - Unity Play Mode entry reached `isPlaying=true`.
  - Unity console error count: 0.
