# 2026-06-05 개발일지

## v0.12 밸런스 루프 테스트

- 목표: 감정 proxy가 아니라 실제 브라우저 QA 수치로 v0.12 밸런스를 판단한다.
- 초반 생존 압력, 플레이어 기본 HP/속도, 적 피해, 무기 로직, spawn 압력, 첫 보스 HP를 조정했다.
- `qa=balance` 자동 조작을 보스 접근과 보스 집중 공격 중심으로 보강했다.
- `npm run qa:balance`: `ITERATE_BALANCE`, 첫 보스 클리어 `0%`, 전체 클리어 `0%`, 사망 `100%`, 사망 시점 중앙값 `133.97s`.
- 결정: 다음 루프는 추가 상향보다 `deathPhase`, `maxEnemies`, `hpSamples`, `pressureSegments`, `bossPostCycleState` 계측 보강을 우선한다.

## v0.12 밸런스 5개 표면 분리 루프

- 목표: 단일 평균값 대신 생존, TTK, 무기 편차, 결손, 전체 클리어율을 분리해 병목을 찾는다.
- 결과:
  - prelude 10회: 사망 `60%`, 첫 보스 클리어 `0%`.
  - TTK 5회: 첫 보스 클리어 `0%`, TTK 미측정.
  - twin+hungry: 첫 보스 클리어 `20%`, 사망 `60%`.
  - greatsword+execution: 첫 보스 클리어 `0%`, 사망 `100%`.
  - post-loss fast QA: complete, survived true, refill complete.
  - full-run 5회: 전체 클리어 `0%`, 사망 `80%`.
- 결정: 첫 보스 전 생존과 대검 시작 안정성이 최우선 병목이다.

## 대검 시작 안정화와 진단 로그 보강

- 목표: 대검 시작 안정성을 개선하고 사망 원인을 데이터로 남긴다.
- Balance QA 결과 JSON에 `deathPhase`, `deathEnemyCount`, `maxEnemies`, `pressureSegments`, `hpSamples`, `lowHpSamples`, `bossPostCycleState`를 추가했다.
- telemetry sample에 `hpRate`, `pressurePhase`, `bossActive`를 추가했다.
- 대검 기본 공격에 전방 cleave를 추가했다.
- spawn cap과 cleave 재측정 결과 대검 death rate는 100%에서 40%까지 내려왔지만 first boss clear는 여전히 `0%`였다.
- 결정: 밀도만 더 낮추지 말고 HP 붕괴 시점을 기준으로 다음 조정을 판단한다.

## HP 붕괴 지표와 첫 보스 스폰 버그 수정

- 목표: first boss clear `0%`가 수치 문제인지, 보스 스폰/타겟팅 문제인지 분리한다.
- QA 보고서에 HP 60% / 40% / 20% 이하 첫 진입 시점을 추가했다.
- balance QA per-run retry와 Chrome 안정화 flag를 추가했다.
- 보스가 기본 공격 사거리 안에 있으면 잡몹보다 우선 공격하도록 했다.
- `updateSpawning()`에서 cap에 막혀 보스가 스폰되지 않던 순서 버그를 수정했다.
- 결정: 보스 스폰 수정은 유지한다. 다음은 장거리 balance QA 안정화와 first boss damage/TTK 표본 확보가 우선이다.

## 스폰 수정 후 5개 표면 재측정

- 목표: 보스 스폰 수정 후 같은 5개 표면을 다시 재고 가장 앞단의 병목을 확인한다.
- 결과:
  - prelude: 5 runs / 4 gameplay, 사망 `50%`, 사망 시점 중앙값 `159.8s`, HP <= 20% 중앙값 `137.76s`.
  - TTK: sample이 보스 피해 전 `163-179s`에 사망해 blocked.
  - twin+hungry: 3/3 gameplay, 사망 `33.3%`.
  - greatsword+execution: 첫 보스가 `180.02s`에 스폰되고 10초 동안 `647.87 / 780` 피해를 받음.
  - post-loss fast gate: 통과.
  - full clear: partial sample이 `178.35s` `망각 전조`에서 사망.
- 결정: 다음 조정은 first-cycle `망각 전조` 생존에 집중한다.

## 망각 전조 생존 보정

- 목표: 첫 보스 직전 `망각 전조`에서 HP가 너무 빨리 무너지는 문제를 줄인다.
- 약 `169s`부터 `문지기 힌트` 완충 구간을 추가했다.
- first-cycle `망각 전조` cap을 `38 -> 32`로 낮췄다.
- spawn rate를 `0.90s -> 1.08s`로 늦췄다.
- enemy pool에서 추가 `drifting_eye`, `split_one` 가중치를 제거했다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_browser_balance_qa.js`: 통과.
  - climax soften + gate breath: 5/5 gameplay, 사망 `20%`, HP <= 40% 중앙값 `160.58s`, HP <= 20% 중앙값 `169.73s`.
- 결정: `망각 전조` 생존 보정은 유지한다. 다음은 TTK 측정 경로 안정화다.

## 첫 보스 TTK 측정 시나리오

- 목표: prelude 생존과 boss HP 측정을 분리한다.
- CDP 읽기를 긴 `Runtime.evaluate` Promise 대신 500ms polling으로 바꿨다.
- `--scenario first_boss_ttk` 옵션과 `balanceScenario=first_boss_ttk` 전용 상태를 추가했다.
- TTK scenario smoke는 report/gameplay sample 전 timeout되어 실제 표본은 만들지 못했다.
- 결정: 이 상태에서 boss HP를 조정하면 근거 없는 튜닝이므로, 더 안정적인 측정 경로가 필요하다.

## 첫 보스 TTK 보스 전용 하네스

- 목표: Chrome/CDP 불안정을 우회하는 deterministic boss-only TTK 측정 경로를 만든다.
- `scripts/run_boss_ttk_harness.js`를 추가했다.
- `npm run qa:boss-ttk`, `npm run qa:boss-ttk:dry`를 추가했다.
- harness의 `echo.attackSpeed` baseline이 `NaN`이 되지 않도록 고쳤다.
- 첫 보스 HP는 `780 -> 3500 -> 2800`으로 조정했다.
- 검증:
  - `node --check src/game.js`: 통과.
  - `node --check scripts/run_boss_ttk_harness.js`: 통과.
  - HP `3500`: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK 중앙값 `21.92s`.
  - HP `2800`: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK 중앙값 `17.8s`.
  - 브라우저 `first_boss_ttk` HP `2800`: 1/3 accepted, TTK `22.59s`, 2/3 incomplete.
- 결정: HP `2800`을 다음 브라우저 밸런스 QA 전 현재 첫 보스 값으로 본다. boss-only TTK는 전체 런 밸런스 증거가 아니다.
- 다음: HP `2800`으로 브라우저 `first_boss_ttk` accepted sample rate를 안정화하고, 그 뒤 `npm run qa:balance`를 다시 실행한다.
