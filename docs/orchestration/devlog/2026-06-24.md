# 2026-06-24

## Release-Prep Map / Background Pass

- Responded to jaewoo direction that the background should be redone, the map feels too small, and the project should move from prototype presentation toward release preparation.
- Added `scripts/generate_world_sprites.ps1` to generate a reproducible first-pass world art set using local .NET drawing APIs.
- Generated four stone floor tiles and one large arena backdrop:
  - `LETHE/Assets/_dev/Art/Sprites/Map/tile_lethe_stone_01.png`
  - `LETHE/Assets/_dev/Art/Sprites/Map/tile_lethe_stone_02.png`
  - `LETHE/Assets/_dev/Art/Sprites/Map/tile_lethe_stone_03.png`
  - `LETHE/Assets/_dev/Art/Sprites/Map/tile_lethe_stone_04.png`
  - `LETHE/Assets/_dev/Art/Sprites/Map/spr_lethe_arena_backdrop_01.png`
- Saved source PNG copies under `LETHE/Assets/_dev/Art/Source/`.
- Updated `V1GameManager` world generation:
  - Expanded player bounds from `x +/-12`, `y -8.5..8.5` to `x +/-24`, `y +/-16`.
  - Expanded floor placement from `11x9` to `21x15`.
  - Added the generated backdrop sprite behind runtime floor tiles.
  - Increased boundary, crack, and outer marker coverage to match the larger world.
  - Increased camera size from `6.1` to `6.8` and clamped camera follow within arena bounds.
  - Increased enemy spawn radius and adjusted the debug Gatekeeper spawn offset for the larger space.
  - Added texture fallback loading in `LoadSprite()` so generated PNGs still render if imported as generic textures.

## Verification

- Local visual sanity check opened representative generated floor tile/backdrop PNGs.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console error count: `0`.

## Remaining Review

- Jaewoo direct play should decide whether this arena scale is enough for release-facing feel.
- If scale is approved, the next art pass should replace the generated first-pass background with more authored landmarks, stronger boundary identity, and better production polish.

## Lethe Terrain Sprite Follow-Up

- Responded to jaewoo feedback that the first release-prep background still felt like an artificial field.
- Used built-in image generation to create a Vampire Survivors-readable top-down terrain concept sheet for LETHE.
- Saved the generated source sheet as `LETHE/Assets/_dev/Art/Source/spr_lethe_terrain_sheet_01_source.png`.
- Reworked `scripts/generate_world_sprites.ps1`:
  - Crops the source sheet into eight terrain tiles.
  - Produces `spr_lethe_terrain_backdrop_01.png`.
  - Keeps a procedural fallback if the source sheet is missing.
- Updated `V1GameManager`:
  - Loads `tile_lethe_terrain_01..08.png`.
  - Uses `spr_lethe_terrain_backdrop_01.png`.
  - Removes artificial outer marker rings.
  - Adds marsh edge, Lethe water seam, drowned root, and memory gravel runtime dressing.

## Terrain Follow-Up Verification

- Local visual sanity check opened terrain tile 01, tile 03, tile 06, and the terrain backdrop.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console error count: `0`.

## Terrain Continuity Follow-Up

- Responded to jaewoo feedback that the terrain did not feel connected and each tile looked like a separate patch.
- Changed the generation rule so all eight terrain tiles derive from the same wet black stone base instead of using eight different terrain categories as floor tiles.
- Kept water/root details as rare variants and runtime dressing, not as the main ground language.
- Updated runtime tile selection so most floor tiles use the connected base set and high-character variants appear rarely.
- Reduced runtime water seam, drowned root, and memory gravel counts.
- Increased floor tile scale slightly to overlap hard seams.

## Continuity Verification

- Local visual check opened regenerated tile 01, tile 02, tile 03, and the backdrop.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy v0/debug warnings and 0 errors.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console error count: `0`.

## 8 Echo VFX Readability Follow-Up

- Responded to jaewoo asking whether the current 8 echoes actually have VFX because they were hard to notice.
- Confirmed the hooks existed, then changed the runtime emphasis from "small proc flash" to "readable combat event".
- Updated `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`:
  - Kalmuri Echo: stronger range ring, added inner flash ring, larger/longer blade barrage, larger awakened launch blade, stronger awakened launch damage/speed.
  - Blood Echo: added mark pulse ring, enlarged Blood Bloom, extended Blood Bloom lifetime, added red bloom ring, slightly increased bloom radius.
  - Execution Echo: enlarged gold prompt, added halo ring, enlarged/lengthened execution burst.
  - Hunter Echo: enlarged echo projectile, increased target-lock ring size/alpha/lifetime, slightly increased echo proc chance.
  - Shatter Echo: increased proc chance, field radius, field lifetime, ring alpha, and fracture spoke visibility.
  - Stopped Echo: increased proc chance, short freeze duration, gold clock field radius, and field lifetime.
  - Ashen Echo: increased proc chance, prompt scale/lifetime, and added player guard ring.
  - Oblivion Echo: increased proc chance, purple brand scale/lifetime, and added slash accent.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity Play Mode entry reached `isPlaying=true`.
  - Unity console error count: `0`.
- Remaining review:
  - Jaewoo direct play should confirm whether each echo is now identifiable by color/shape at combat speed.
  - Watch for over-spam if multiple echoes are stacked at +5 on the larger terrain.

## Echo Only Debug Follow-Up

- Responded to jaewoo asking whether there was a way to test echoes without ultimate awakenings.
- Existing debug buttons were not precise enough:
  - `Echo A/B` could set +5 echo pairs that also satisfied ultimate conditions.
  - `VFX` preview intentionally included ultimate previews.
- Added `echoOnlyDebugMode` to `V1GameManager`.
  - When enabled, `UpdateEchoUltimate()` is skipped even if echo pairs are at +5.
  - `Echo A` now tests Execution / Hunter / Stopped echoes at +5 with ultimates suppressed.
  - `Echo B` now tests Shatter / Ashen / Oblivion echoes at +5 with ultimates suppressed.
  - Added `Echo All` and `F10` to test all 8 echoes at +5 with ultimates suppressed.
  - `Ult 3` remains available as a separate ultimate test button.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.

## Combat VFX / Attack Coverage Scale-Up

- Responded to jaewoo feedback that VFX readability may be failing because the effects and attack coverage are simply too small.
- Updated `V1GameManager`:
  - Increased fallback dual blades range from `94px` to `112px`, arc from `119` to `132`, and fallback max targets from `6` to `7`.
  - Increased fallback greatsword range from `126px` to `150px`, arc from `82` to `96`, and fallback max targets from `5` to `6`.
  - Increased Hungry Blades base radius from `72px` to `86px`.
  - Added `CombatVfxVisibilityScale = 1.18f` and applied it to transient sprite VFX and weapon phantom sweeps.
  - Increased Shatter Echo radius and Stopped Echo clock/clamp radius so the utility area is easier to read.
- Updated weapon data assets:
  - Dual blades: range `2.35 -> 2.8`, arc `119 -> 132`, max targets `6 -> 7`, engage multiplier `1.15 -> 1.2`, echo size scale `0.8 -> 1.05`.
  - Greatsword: range `3.15 -> 3.75`, arc `82 -> 96`, max targets `5 -> 6`, engage multiplier `1.08 -> 1.12`, echo size scale `1.8 -> 2.15`.
- Updated weapon VFX profile assets:
  - Dual-blade crescents, cut flash, assist crescent, Kalmuri followup, and hit spark are larger.
  - Greatsword AoE/primary crescents, target shock/cut point, assist crescent, heavy Kalmuri followup, Blood Blade Storm slash, and hit spark are larger.

## Combat VFX / Attack Coverage Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after Unity refresh.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.
- Remaining review: jaewoo direct play should judge whether the enlarged effects are now satisfying or too visually crowded.

## Prototype Gap Pass

- Responded to jaewoo asking to list the still-lacking parts and proceed.
- Current weak points listed:
  - Enlarged VFX still needs direct scale/noise judgment.
  - Early run could feel loose before the first boss.
  - Enlarged map needed directional landmarks.
  - Enemy/boss sprite readability needed stronger role reads during combat.
  - Scaled-up echo stacks may now create clutter.
- Implemented the first pass on the gaps that Codex can safely address without human feel input:
  - First Gatekeeper timing moved from `180s` to `150s`.
  - Boss schedule changed to `150 / 300 / 450 / 600`.
  - First Gatekeeper HP reduced from `2050` to `1750`.
  - Added an 18-second pre-boss warning VFX and `문지기 접근` callout.
  - Added five low-contrast memory landmarks to the arena floor.
  - Added low-alpha role markers for Drifting Eye, Split One, Void Priest, and Gatekeeper.

## Prototype Gap Pass Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 0 warnings and 0 errors after Unity refresh.
- Unity `Assets/Refresh`: success.
- Unity compile error count: `0`.
- Unity Play Mode entry reached `isPlaying=true`.
- Unity console contained only MCP server info logs and no gameplay errors/warnings.
- Remaining review: jaewoo should judge whether the first boss now arrives at a better time, whether landmarks help map identity, and whether role markers improve readability without clutter.

## Echo Identity / Greatsword Timing Follow-Up

- Responded to jaewoo feedback that echoes may still be either hard to see or too similar to active memories, and that greatsword VFX appears too late.
- Greatsword slash VFX:
  - Changed `GreatswordSlashDelay` from `0.18f` to `0.045f`.
  - This makes the cleave VFX appear almost immediately after the greatsword swing starts.
- Echo identity separation:
  - Added reusable hit-site accent helpers:
    - `SpawnEchoWoundSlash`
    - `SpawnShatterEchoScar`
    - `SpawnEchoLink`
    - `SpawnStoppedEchoClamp`
    - `SpawnOblivionEchoBrand`
  - Kalmuri Echo now adds a cyan cut trace at the hit origin.
  - Blood Echo now adds a red wound slash on the marked enemy.
  - Execution Echo now adds a gold execution cut line.
  - Hunter Echo now adds an origin mark and visible aim-line links before homing shots.
  - Shatter Echo now adds fracture scar lines before the field pulse.
  - Stopped Echo now adds a small clock clamp directly on the struck enemy and uses a slightly tighter support field.
  - Ashen Echo now reads as hit seal -> return thread -> smaller player guard instead of mostly a player-centered ring.
  - Oblivion Echo now adds a purple brand seal and brand lines on the target.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity Play Mode entry reached `isPlaying=true`.
  - Unity console error count: `0`.

## Single Echo Debug Review Flow

- Responded to jaewoo asking for the next task list and to proceed.
- Listed the next practical work as:
  1. isolate each echo one by one,
  2. judge which echo is invisible / memory-like / too noisy,
  3. only then continue map, enemy, and background contrast review.
- Added per-echo debug controls to `V1GameManager`:
  - `Prev`: select previous echo in the 8-echo list.
  - `Next`: select next echo in the 8-echo list.
  - `Echo One`: sets only the selected echo to +5 and keeps ultimate updates suppressed.
  - `F11`: same as `Echo One`.
  - `F12`: cycle to the next echo.
- Existing controls remain:
  - `Echo All` / `F10`: all 8 echoes at +5 with ultimate suppressed.
  - `Echo A/B`: grouped utility echo review.
  - `Ult 3`: utility ultimate preview.
- Verification:
  - `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: passed with 7 legacy warnings and 0 errors.
  - Unity compile error count: `0`.
  - Unity console error count: `0`.
