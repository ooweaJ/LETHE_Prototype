# 2026-06-12 Devlog

## Dev_Prototype_v1 reset

- User rejected the previous Unity prototype direction and asked to restart from the new consolidated design docs.
- Read the new source set: `docs/design/README.md`, `LETHE_DESIGN_00..07`.
- Decision: stop patching `Dev_Prototype_v0`; create a fresh v1 scene and script namespace.
- Added:
  - `LETHE/Assets/_dev/Scripts/PrototypeV1/V1GameManager.cs`
  - `LETHE/Assets/_dev/Scripts/PrototypeV1/Editor/V1SceneBuilder.cs`
  - `LETHE/Assets/_dev/Scenes/Dev_Prototype_v1.unity`
- v1 runtime includes:
  - player movement.
  - separated `WeaponAnchor` dual blades.
  - arena generation.
  - camera follow.
  - dual blades baseline from docs.
  - enemy pressure/spawn roles.
  - XP/level-up overlay.
  - HUD.
  - highest-level forgetting.
  - echo cap +5.
  - resonance reacquire.
  - Kalmuri/Blood/Blood Blade Storm debug path.
- Fixed issues found during MCP smoke:
  - Unity Input System-only project threw on `UnityEngine.Input`; added Input System + legacy input compatibility.
  - player/enemy 8x4 sheets rendered as whole sheets; added runtime frame cropping.

## Verification

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 0 warnings, 0 errors.
- Unity MCP selected `LETHE` port `7890`.
- `unity_get_compilation_errors(port=7890, severity="all")`: `count=0`.
- Opened `Assets/_dev/Scenes/Dev_Prototype_v1.unity` successfully.
- Hierarchy: `V1_GameManager` + `Main Camera`.
- Play Mode smoke: `player=True`, `enemies=2`, `renderers=107`, `playing=True`, `paused=False`.
- Console after fixes: no v1 runtime exceptions.
- Game capture confirms single-frame player/enemy rendering instead of whole sprite sheet.
- Scene saved with `unity_scene_save(port=7890)`.
- `npm.cmd run report`: pass.
- `npm.cmd run report:check`: pass.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Notes

- `Dev_Prototype_v1` is an M1 reset shell, not the complete 8-memory prototype.
- Next step is game-feel pass on v1 shell, then M2 Gatekeeper -> forgetting -> deficit -> resonance.
- Project Orchestrator Discord intake still appears unavailable from this repo session.

## Dev_Prototype_v1 M1/M2 compressed loop

- Continued v1 instead of returning to `Dev_Prototype_v0`.
- Added debug smoke paths to `V1GameManager`:
  - `DebugRunM1Smoke()`
  - `DebugRunM2Smoke()`
  - `DebugSnapshot()`
  - OnGUI `M1 Smoke`, `M2 Loop`, `Continue` buttons.
  - keyboard shortcut `F8` for M2 compressed smoke.
- Changed enemy kills to spawn XP orbs so XP collection has a visible interaction point instead of direct kill-to-XP accounting.
- Added player damage routing for ranged enemy shots.
- M2 smoke now injects the intended one-core loop quickly:
  - active Hungry Blades / Blood Reflection setup.
  - highest-level forgetting.
  - result continuation.
  - resonance reacquire.
  - Kalmuri/Blood echoes at +5.
  - Blood Blade Storm active.
- Fixed a runtime exception found during smoke: XP orb collection modified the list while iterating, so `UpdateXpCollection` now iterates a snapshot.

## Verification - v1 M1/M2 compressed loop

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: pass, 0 warnings, 0 errors.
- Unity refresh: pass.
- `unity_get_compilation_errors(port=7890, severity="all")`: `count=0`.
- Play Mode M2 smoke after 120 forced frames:
  - `scene=v1 elapsed=8.5 hp=155.2/210.0 level=2 xp=7/9 kills=10 memories=[BloodReflection:3,HungryBlades:3] echoes=[HungryBlades:5,BloodReflection:5] enemies=24 storm=True result=False refill=False death=False`
- Unity console errors after smoke: `count=0`.
- Screenshot saved: `Assets/_dev/Evidence/v1_m2_smoke_20260612.png`.
- `npm.cmd run report`: pass.
- `npm.cmd run report:check`: pass, 2 unit headings ok.
- `npm.cmd run report:orchestrator:unit:dry`: failed with `fetch failed`.

## Notes - v1 M1/M2 compressed loop

- The loop is functional but compressed by debug injection. It proves the state transitions and combat wiring, not the final pacing.
- Next useful human check: open `Dev_Prototype_v1`, run `M1 Smoke` and `M2 Loop`, then decide whether v1 is `GO`, `ITERATE`, or `NO-GO`.
