# 2026-07-09-09 - 대검 혈반 잔향 이중 반달 크기 보정

## 1. 현재 빌드 상태

- 대검 혈반 잔향의 혈발도 VFX가 대검 사거리 끝에 더 크게, 얇은 두 겹 반달로 나가도록 보정됐다.
- `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- 혈발도 중심을 적 타격점 근처에서 대검 스윙 tip/end 위치로 옮겼다.
- 단일 반달 대신 `EchoGreat_BloodIaidoCrescentOuter`, `EchoGreat_BloodIaidoCrescentInner` 두 겹을 추가했다.
- 두 반달은 같은 방향으로 살짝 어긋나게 배치해서 `((`처럼 보이도록 했다.
- 끝점 기준 피해 범위도 더 키웠다.

## 3. 테스트 결과와 근거

- Unity 컴파일 에러: `0`.
- `Echo Matrix Greatsword`: PASS, `total=368`, `K=8`, `B=27`, `Ex=64`, `H=22`, `Sh=56`, `St=16`, `A=80`, `O=95`, `state=51`.
- `Dense Dual Blades Perf Matrix`: PASS, `hits=18`, `suppressed=15`, `transient=139`, `activeVfx=75`, `ms=104.66`.

## 4. 결정한 것

- 대검 혈반은 타격점 위 작은 표식보다 사거리 끝을 긁는 큰 후속 베기가 맞다.
- 두껍고 큰 덩어리보다 얇은 반달 두 겹이 혈발도 감성에 더 맞다.

## 5. 문제 또는 리스크

- 직접 플레이에서 아직 작게 보이면 스케일을 한 번 더 키워야 한다.
- Dense Dual QA는 PASS지만 `ms=104.66`이라 성능 여유가 아주 넓지는 않다.

## 6. GPT/Claude 인계 요약

- 대검 혈반 잔향은 이제 대검 사거리 끝에서 얇은 빨간 반달 두 겹이 나가는 구조다.
- 리뷰 기준은 위치가 끝자락에 충분히 닿는지, 두 겹이 `((`로 보이는지, 얇지만 존재감이 있는지다.

## 7. 다음 Codex 작업

- jaewoo 직접 플레이 후 크기/거리/두께가 부족하면 `EchoGreat_BloodIaidoCrescentOuter/Inner` 스케일과 위치를 추가 조정한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 대검 혈반 혈발도가 작고 타격점 근처에 붙어 대검 사거리감이 약했다.
- 방향: 대검 끝자락에서 얇은 반달 두 겹으로 긁는 형태로 바꾼다.
- 행동: VFX 중심을 swing tip/end로 옮기고 outer/inner crescent를 추가했다.
- 결과: 대검 Echo Matrix와 Dense Dual 회귀 QA가 PASS했다.
