# 2026-06-09-09 - docs 현재 상태와 잔향 시스템 동기화

## 1. 현재 빌드 상태

- HTML v0.12는 자동 밸런스 근거 빌드로 유지한다.
- 현재 프로젝트 초점은 Unity 전환용 시스템 기획이다.
- 새 기준은 `docs/design/LETHE_CORE_SYSTEMS_UNITY_PLAN.md`다.

## 2. 오늘 바뀐 것

- `docs/CODEX_STATUS.md` 상단을 현재 상태로 갱신했다.
- `docs/NEXT_TASKS.md` 상단을 Unity vertical-slice backlog 중심으로 갱신했다.
- 일반 docs에서도 기억/잔향 `+5` max, 망각 시 잔향 누적, 재획득 공명, 각성 잔향, 궁극 잔향 시너지가 바로 보이게 했다.

## 3. 테스트 결과와 근거

- 이번 작업은 문서 동기화다.
- 보고서 생성 후 `npm run report`, `npm run report:check`, `npm run doctor`로 문서 파이프라인을 확인한다.

## 4. 결정한 것

- 오케스트레이션 문서가 source of truth지만, 사람이 직접 열어볼 수 있는 legacy docs 상단도 최신 상태를 보여야 한다.
- `docs/CODEX_STATUS.md`와 `docs/NEXT_TASKS.md`는 상세 archive 성격을 유지하되, 맨 위에는 최신 방향을 둔다.

## 5. 문제 또는 리스크

- legacy docs 본문 아래쪽에는 과거 HTML 단계 기록이 길게 남아 있다.
- 따라서 최신 판단은 항상 문서 상단과 `docs/orchestration/state/`를 우선해서 읽어야 한다.

## 6. GPT/Claude 인계 요약

일반 docs 문서도 Unity 전환 방향으로 동기화했다. 현재 기준은 HTML 추가 튜닝이 아니라 memory/echo/resonance system을 Unity vertical slice backlog로 쪼개는 것이다.

## 7. 다음 Codex 작업

- `docs/design/LETHE_CORE_SYSTEMS_UNITY_PLAN.md`를 기준으로 Unity vertical-slice backlog를 작성한다.
- 첫 각성 잔향 2개와 궁극 잔향 1개를 선택한다.

## 8. 포트폴리오 메모

- 문제: 최신 방향은 오케스트레이션에 반영됐지만 일반 docs 상단은 과거 HTML 단계처럼 보일 수 있었다.
- 방향: 사용자가 직접 여는 docs에서도 현재 판단을 바로 볼 수 있게 한다.
- 행동: 상태 문서와 다음 작업 문서의 상단을 Unity 전환/잔향 시스템 기준으로 갱신했다.
- 결과: 문서 진입점들이 같은 방향을 가리키게 됐다.
