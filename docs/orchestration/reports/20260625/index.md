# 2026-06-25-01 - 대검 VFX 보장 수정

## 1. 현재 빌드 상태

대검 데이터와 VFX 프로필은 남아 있었지만, 실제 플레이에서는 대검 베기 VFX가 사라진 것처럼 보일 수 있었다. 특히 프로필 기반 slash entry가 스킵되거나 다른 시각 요소에 묻히면, 플레이어 입장에서는 무기 잔상만 보이고 베기 이펙트가 없는 것처럼 느껴진다.

## 2. 오늘 바뀐 것

- 대검 타격 성공 시 무조건 뜨는 보장형 cleave VFX를 추가했다.
- `SpawnGreatswordGuaranteedSlash()`를 새로 만들었다.
- 대검이 적을 맞추면 기존 VFX 프로필과 별개로 다음이 항상 생성된다:
  - 큰 대검 cleave arc 1장
  - 밝은 보조 cleave arc 1장
  - 칼끝 방향에 맞춘 cut line 1개
- 위치는 현재 대검 스윙의 손잡이/칼끝 계산을 그대로 사용해서, 무기 움직임과 베기 VFX가 따로 놀지 않게 했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, warning 0개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console에는 MCP 서버 정보 로그만 있었고 게임 런타임 오류/경고는 없었다.

## 4. 결정한 것

대검은 공격 속도가 느리고 한 방이 커서, VFX가 조건부로 사라져 보이면 무기 정체성이 바로 무너진다. 그래서 프로필 VFX와 별개로 최소 한 번은 확실한 베기 효과를 보장하는 구조로 바꿨다.

## 5. 문제 또는 리스크

기존 대검 프로필 VFX도 계속 살아 있기 때문에, 직접 플레이에서는 cleave가 너무 많이 겹치는지 봐야 한다. 안 보이는 문제를 막는 대신 과밀 위험이 조금 생겼다.

## 6. GPT/Claude 인계 요약

Codex가 대검 타격 시 보장형 cleave fallback을 추가했다. 다음 판단 기준은 "대검 VFX가 다시 보이는가"와 "기존 프로필 VFX와 겹쳐 과하지 않은가"다.

## 7. 다음 Codex 작업

1. jaewoo가 대검으로 직접 플레이한다.
2. 대검 베기 VFX가 다시 보이는지 확인한다.
3. 너무 겹치면 기존 프로필 VFX 또는 보장형 cleave의 alpha/scale/lifetime만 줄인다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 대검 VFX가 사라진 것처럼 보여 무기 타격감이 무너졌다.
- 방향: 대검은 조건부 VFX보다 항상 보이는 최소 베기 효과를 보장한다.
- 행동: 대검 타격 성공 시 cleave arc와 cut line을 무조건 생성하는 fallback을 추가했다.
- 결과: 기술 검증은 통과했고, 직접 플레이로 시각 밀도만 확인하면 된다.

# 2026-06-25-02 - 통합 리뷰 프리셋 추가

## 1. 현재 빌드 상태

대검 VFX는 보장형 cleave로 다시 보이게 만들었지만, 이제 남은 문제는 직접 리뷰를 빠르게 반복하는 것이다. 기존에는 대검/쌍검, 잔향 과밀, 적 판독, 문지기 예고를 보려면 플레이를 길게 진행해야 했다.

## 2. 오늘 바뀐 것

- 디버그 패널에 `DB Rev`, `GS Rev` 버튼을 추가했다.
- 버튼을 누르면 바로 통합 리뷰 상태로 들어간다:
  - 필요한 경우 런 시작
  - 선택 무기로 전환
  - 주변에 리뷰용 적 18마리 배치
  - 8잔향 전체 +5 설정
  - 궁극은 Echo Only 모드로 억제
  - 문지기 예고 타이밍을 20초로 설정
- 디버그 패널 높이를 늘려 버튼과 로그가 겹치지 않게 했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, warning 0개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console에는 MCP 서버 정보 로그만 있었고 게임 런타임 오류/경고는 없었다.

## 4. 결정한 것

지금 단계에서는 감각 판단을 빨리 반복하는 도구가 필요하다. `DB Rev`와 `GS Rev`는 일반 플레이를 대체하는 모드가 아니라, 무기 VFX/잔향 과밀/적 판독/문지기 예고를 한 화면에서 빠르게 확인하기 위한 리뷰 프리셋이다.

## 5. 문제 또는 리스크

리뷰 프리셋은 의도적으로 과밀한 상황을 만든다. 실제 150초 플레이보다 더 복잡하게 보일 수 있으니, 이 버튼에서 문제를 찾은 뒤 일반 플레이에서도 같은 문제가 반복되는지 확인해야 한다.

## 6. GPT/Claude 인계 요약

Codex가 직접 리뷰 반복을 줄이기 위해 `DB Rev` / `GS Rev` 디버그 프리셋을 추가했다. 다음 판단 기준은 이 프리셋에서 대검 VFX, 쌍검 VFX, 잔향 과밀도, 적 표식, 문지기 예고가 한눈에 읽히는가다.

## 7. 다음 Codex 작업

1. jaewoo가 `GS Rev`로 대검 VFX와 과밀도를 본다.
2. jaewoo가 `DB Rev`로 쌍검 VFX와 과밀도를 본다.
3. 문제 있는 축만 scale, alpha, lifetime, enemy marker, boss warning timing 순서로 조정한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 통합 감각을 보려면 플레이 시간이 길어 리뷰 반복이 느렸다.
- 방향: 검증해야 하는 화면을 빠르게 만드는 디버그 프리셋을 제공한다.
- 행동: `DB Rev`와 `GS Rev` 버튼을 추가하고, 무기/잔향/적/보스예고를 동시에 띄우는 상태를 만들었다.
- 결과: 기술 검증은 통과했고, 이제 직접 플레이 리뷰 속도를 높일 수 있다.

# 2026-06-25-03 - 기억/잔향 단일 테스트 패널

## 1. 현재 빌드 상태

디버그 패널에서 8종 기억/잔향을 한 종류씩 골라 테스트할 수 있게 되었다. 이제 통합 화면에서 "뭔가 안 보인다"를 느끼는 단계에서 멈추지 않고, 같은 id의 기억과 잔향을 따로 켜서 비교할 수 있다.

## 2. 오늘 바뀐 것

- `Prev` / `Next`는 테스트할 기억/잔향 id를 선택한다.
- `Mem One` 버튼을 추가했다.
- `Mem One`은 주변 리뷰 적을 만들고, 잔향/궁극 상태를 지운 뒤 선택한 기억만 +5로 켠다.
- `Echo One`은 그대로 유지해서 선택한 잔향만 +5로 켠다.
- `DB Rev` / `GS Rev`는 전체 잔향 통합 리뷰용으로 유지했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, warning 0개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console에는 MCP 서버 정보 로그만 있었고 게임 오류/경고는 없었다.

## 4. 결정한 것

기억과 잔향의 가시성 문제는 한꺼번에 보면 원인을 분리하기 어렵다. 그래서 같은 선택 슬롯으로 `Mem One`과 `Echo One`을 번갈아 켜서 "기억 자체가 약한지", "잔향만 약한지", "둘이 너무 비슷한지"를 확인하는 흐름으로 바꿨다.

## 5. 문제 또는 리스크

기술 검증은 통과했지만, 실제 판단은 직접 플레이가 필요하다. 특히 각 id별로 색, 범위, 유지시간, 피해 가치가 기억과 잔향 사이에서 충분히 다르게 느껴지는지 봐야 한다.

## 6. GPT/Claude 인계 요약

Codex가 패널에 `Mem One`을 추가했고 기존 `Echo One`과 같은 선택 id를 공유하게 만들었다. 다음 리뷰는 8종을 하나씩 돌면서 기억/잔향의 차별성, 밸류, 가시성을 표로 적는 방식이 좋다.

## 7. 다음 Codex 작업

1. jaewoo가 `Prev` / `Next`로 8종을 하나씩 고른다.
2. 각 id마다 `Mem One`과 `Echo One`을 번갈아 눌러본다.
3. 약한 id는 scale, alpha, lifetime, proc, damage, field radius 순서로 조정한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 잔향/기억 가시성 피드백이 전체 화면에서는 원인 분리가 어려웠다.
- 방향: 같은 id를 단일 조건으로 켜서 비교 가능한 테스트 루프를 만든다.
- 행동: `Mem One` 버튼과 단일 기억 세팅 함수를 추가하고, 기존 `Echo One`과 나란히 배치했다.
- 결과: 이제 8종 기억/잔향을 하나씩 검수할 수 있는 디버그 패널이 생겼다.

# 2026-06-25-04 - 전투 체감 통합 패스

## 1. 현재 빌드 상태

기억/잔향 단일 테스트는 나중으로 미루고, 지금은 대검 VFX 중복감과 전체 전투 진행감을 먼저 정리했다. 대검은 이제 프로필 참격이 메인이고, 보장형 VFX는 겹쳐 보이는 큰 두 번째 참격이 아니라 칼끝 잔광/컷라인으로 낮췄다.

## 2. 오늘 바뀐 것

- 대검 보장형 VFX를 상황별로 나눴다.
  - 프로필 VFX가 있으면 얇은 tip afterglow와 cut line만 나온다.
  - 프로필 VFX가 없을 때만 fallback cleave가 크게 나온다.
- 대검은 `1.02s -> 0.92s`로 조금 빨라졌고, 사거리/각도/넉백/히트스톱/흔들림은 더 묵직하게 정리했다.
- 쌍검은 `0.36s -> 0.32s`로 더 빠르게 만들고, 단일 피해/범위/히트스톱은 줄여 빠르고 가벼운 무기 쪽으로 밀었다.
- 첫 문지기 타이밍은 `150s -> 135s`, 보스 스케줄은 `135 / 285 / 435 / 600`으로 바꿨다.
- 문지기 경고는 22초 전에 더 크고 오래 보이게 바꿨다.
- 초반 120초 스폰 압력을 올리고, 적 역할 마커와 맵 랜드마크를 더 잘 보이게 했다.
- 배경/바닥 톤을 조금 어둡게 내려 적과 VFX가 더 위에 뜨게 했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, 기존 v0/debug 경고 7개, error 0개.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console에는 MCP 서버 정보 로그만 있었고 게임 오류/경고는 없었다.

## 4. 결정한 것

대검 문제는 VFX 부재가 아니라 VFX 위계 문제로 본다. 그래서 대검의 “맛”은 프로필 참격과 무기 스윙이 맡고, 보장형 VFX는 안전장치 겸 칼끝 확인선으로 낮췄다. 초반 루즈함은 새 시스템을 추가하기보다 보스 타이밍, 스폰 압력, 역할 마커, 맵 대비를 먼저 조정했다.

## 5. 문제 또는 리스크

직접 플레이에서 135초 첫 보스가 너무 빠르게 느껴질 수 있다. 또 대검 fallback을 낮춘 만큼, 프로필 VFX가 실제 화면에서 충분히 선명한지 다시 봐야 한다.

## 6. GPT/Claude 인계 요약

Codex가 전투 체감 통합 패스를 적용했다. 다음 판단 기준은 대검이 한 번의 큰 베기로 읽히는지, 쌍검이 더 빠르고 가볍게 느껴지는지, 첫 문지기 135초가 목표감을 주는지, 어두워진 지형이 가시성을 살리는지다.

## 7. 다음 Codex 작업

1. jaewoo가 일반 플레이로 쌍검/대검을 각각 확인한다.
2. `GS Rev`로 대검 VFX가 두 번 터지는 느낌이 사라졌는지 본다.
3. 첫 문지기 135초가 빠르면 145초로 되돌리고, 괜찮으면 초반 보상/적 밀도 조정을 이어간다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 대검 VFX가 사라진 문제를 고쳤더니 이번에는 두 겹으로 나오는 느낌이 생겼다.
- 방향: VFX를 더 넣는 대신 메인/보조 위계를 정하고, 초반 흐름을 출시 준비형 게임 템포에 가깝게 만든다.
- 행동: 대검 fallback을 보조 잔광으로 낮추고, 무기 데이터/보스 타이밍/스폰/맵 대비/역할 마커를 조정했다.
- 결과: 기술 검증은 통과했고, 이제 직접 플레이에서 전투 체감만 판단하면 된다.
