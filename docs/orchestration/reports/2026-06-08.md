# 2026-06-08-01 - Orchestration Interface Adoption

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work did not change gameplay.

## 2. What changed

- Added `docs/orchestration/` as the shared Codex project-management interface.
- Added the Orchestration Interface section to `AGENTS.md`.
- Created current-state, current-task, next-task, scope, runbook, prompt context, and decision index files.
- Added initial orchestration devlog/report records.

## 3. Test results and evidence

- Existing source docs inspected: `AGENTS.md`, `docs/CODEX_STATUS.md`, `docs/NEXT_TASKS.md`.
- Read `docs/orchestration/README.md`: pass.
- Read `docs/orchestration/STATUS.md`: pass.
- Read `docs/orchestration/CURRENT_TASK.md`: pass.
- Read `docs/orchestration/NEXT_TASKS.md`: pass.
- `npm run report`: pass.
- `npm run report:check`: pass after rerun; the first parallel check raced report generation.
- `npm run report:discord:unit:dry`: pass.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Gameplay and balance tests were not rerun because this is a documentation/interface adoption task.

## 4. Decisions made

- Root `AGENTS.md` remains the top-level rulebook.
- `docs/orchestration/` is now the quick resume and operating interface.
- Markdown remains the source of truth; HTML dashboard generation is deferred until the Markdown interface proves useful.

## 5. Problems or risks

- Some legacy docs are much more detailed than the orchestration summaries; users should follow links for deep history.
- The existing legacy `docs/NEXT_TASKS.md` is still long and may conflict with the intentionally short orchestration task list if not maintained carefully.
- `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session. Next trusted-local command: `npm run report:discord:unit`.

## 6. GPT handoff summary

The project now has a reusable orchestration interface suitable for testing the cross-project Codex management pattern. The current project state remains v0.12 human-test candidate; next evidence should come from human sessions.

## 7. Next Codex tasks

- Verify orchestration files by reading `README.md`, `STATUS.md`, `CURRENT_TASK.md`, and `NEXT_TASKS.md`.
- Optionally add a generated HTML dashboard after the Markdown interface is validated.

## 8. Portfolio notes

- Problem: Codex project state was spread across legacy docs, status files, devlogs, and reports.
- Direction: introduce a reusable orchestration interface without deleting legacy history.
- Action: created `docs/orchestration/` and linked it from `AGENTS.md`.
- Result: future Codex sessions can resume from a small standard document set.

# 2026-06-08-02 - Dashboard Current-State Refresh

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work updated only the orchestration dashboard and source Markdown.

## 2. What changed

- Updated `docs/orchestration/index.html` top summary cards for the current v0.12 human-test gate.
- Updated `docs/orchestration/STATUS.md` to mention the dashboard exists.
- Updated `docs/orchestration/NEXT_TASKS.md` so the low-priority dashboard task is now automation/refresh, not initial creation.

## 3. Test results and evidence

- Dashboard content was refreshed from the current orchestration state.
- `npm run report`: pass.
- `npm run report:check`: pass.
- `npm run report:discord:unit:dry`: pass.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Gameplay and balance tests were not rerun because this is a documentation/dashboard update.

## 4. Decisions made

- Keep Markdown as the source of truth.
- Treat `docs/orchestration/index.html` as a readable view until a generator is added.

## 5. Problems or risks

- The dashboard is currently hand-maintained, so it can drift from Markdown if future updates skip it.
- `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session. Next trusted-local command: `npm run report:discord:unit`.

## 6. GPT handoff summary

The orchestration dashboard now reflects the current project state: v0.12 controlled human-test candidate, `balance:loop` passing, human evidence missing, and next work focused on controlled sessions plus playtest summary.

## 7. Next Codex tasks

- Run controlled human sessions.
- Place logs in `playtest_logs/`.
- Run `npm run playtest:summary`.
- Later, add a generator for `docs/orchestration/index.html` if the dashboard shape feels useful.

## 8. Portfolio notes

- Problem: Markdown orchestration docs existed, but the human-readable dashboard still showed placeholder summary values.
- Direction: make the dashboard useful as a fast project status view.
- Action: refreshed summary cards and next-task copy.
- Result: the dashboard now communicates current status, latest verification, blocker, and next work.

# 2026-06-08-03 - Korean Human-Facing Dashboard Normalization

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work updated only orchestration documentation and human-facing HTML.

## 2. What changed

- Rebuilt `docs/orchestration/index.html` as a Korean command dashboard.
- Added a copyable recommended prompt section.
- Added user-facing sections for current state, next action, last completed report, evidence, blockers, scope guard, and document links.
- Added `docs/orchestration/reports/index.html`.
- Added `docs/orchestration/devlog/index.html`.
- Updated orchestration Markdown docs so this dashboard rule is explicit.

## 3. Test results and evidence

- HTML files were read and checked structurally through file inspection.
- `npm run report`: pass.
- `npm run report:check`: pass.
- `npm run report:discord:unit:dry`: pass.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Gameplay and balance tests were not rerun because this is documentation/dashboard work.

## 4. Decisions made

- Orchestration HTML should be Korean-first.
- The dashboard reports the last completed state, not live in-progress Codex thoughts.
- Markdown remains the source of truth.
- HTML pages are readable views and should later be generated automatically.

## 5. Problems or risks

- The HTML dashboard and list pages are currently hand-maintained and can drift from Markdown.
- A generator is still needed before using this pattern across many projects.
- `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session. Next trusted-local command: `npm run report:discord:unit`.

## 6. GPT handoff summary

The orchestration dashboard is now shaped as a Korean report screen that helps the user decide the next prompt. It also links report/devlog HTML-style index pages so development documentation can be inspected directly from the project folder.

## 7. Next Codex tasks

- Use the dashboard to decide the next prompt.
- Run controlled human sessions and `npm run playtest:summary` when logs exist.
- Later, build a dashboard generator from Markdown if this view is useful.

## 8. Portfolio notes

- Problem: Codex state usually stays inside the app conversation, making project progress hard to inspect outside the chat.
- Direction: expose project state and development documentation as project-local, human-readable HTML.
- Action: normalized the orchestration dashboard and added report/devlog list pages.
- Result: the user can inspect project status and choose the next prompt from the repository itself.

# 2026-06-08-04 - HTML Interface Contract Alignment

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work updated only orchestration documentation and human-facing HTML.

## 2. What changed

- Aligned the current HTML changes with the updated `EXISTING_PROJECT_MIGRATION_PROMPT.md`.
- Restored `docs/orchestration/index.html` as the Korean human-facing project dashboard.
- Added `docs/orchestration/command.html` as the compact next-instruction block.
- Added `docs/orchestration/runbook.html` as the operating-procedure block.
- Added an orchestration-local report HTML under `docs/orchestration/reports/`.
- Updated AGENTS/orchestration Markdown so generated HTML is treated as the human-facing interface, while Markdown remains the source of truth.

## 3. Test results and evidence

- `npm run report`: pass, generated 5 unit reports.
- `npm run report:check`: pass, 5 report units.
- `npm run report:discord:unit:dry`: pass, latest unit 05 summary generated.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- `npm run report:discord:unit`: blocked by approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook without explicit user approval for that exfiltration. Next trusted-local command: `npm run report:discord:unit`.

## 4. Decisions made

- Required human-facing HTML interface: `index.html`, `command.html`, `runbook.html`.
- Optional browse pages: `reports/index.html`, `devlog/index.html`.
- Reports can live inside orchestration as user-facing progress records, while legacy generated reports remain linked.

## 5. Problems or risks

- The HTML interface is still hand-maintained and can drift until a generator exists.
- External Discord actual send was blocked by approval policy in this Codex session.

## 6. GPT handoff summary

The orchestration interface now matches the updated migration prompt: Markdown is source of truth, and the three HTML files are the local human-facing project interface for dashboard, next instruction, and runbook.

## 7. Next Codex tasks

- Run controlled human sessions.
- Place logs in `playtest_logs/`.
- Run `npm run playtest:summary`.
- Later, automate generation of the orchestration HTML interface from Markdown.

## 8. Portfolio notes

- Problem: project state and user-facing reports need to be inspectable outside the Codex chat.
- Direction: make `docs/orchestration/` the project-local operating interface.
- Action: aligned dashboard, command, runbook, and report HTML with the new migration contract.
- Result: the user can open local HTML files to understand current state, next prompt, repeated commands, and latest progress reports.

# 2026-06-08-05 - Project Dashboard Compaction

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work changed only orchestration documentation and HTML.

## 2. What changed

- Rebuilt `docs/orchestration/index.html` as a shorter 30-second project dashboard.
- Removed dashboard content that duplicated `command.html`, including the long copyable prompt and detailed completion criteria.
- Removed broad document browsing and Codex-comparison sections from the main dashboard.
- Kept only current state, latest verification, blocker, next gate, current conclusion, recent completion, and links to next instruction, runbook, and reports.
- Updated orchestration Markdown docs to preserve the role split.

## 3. Test results and evidence

- `npm run report`: pass, generated 6 unit reports.
- `npm run report:check`: pass, 6 report units.
- `npm run report:discord:unit:dry`: pass, latest unit 06 summary generated.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- `npm run report:discord:unit`: blocked by approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook without explicit user approval for that exfiltration. Next trusted-local command: `npm run report:discord:unit`.

## 4. Decisions made

- `index.html` is for 30-second status.
- `command.html` is for the next Codex instruction.
- `runbook.html` is for repeated commands.
- `reports/` is for detailed explanation and portfolio-facing records.

## 5. Problems or risks

- HTML is still hand-maintained and can drift until a generator exists.
- The dashboard may need another pass after the user tries it in a real session.
- Discord actual send was blocked by approval policy in this Codex session.

## 6. GPT handoff summary

The main project dashboard is now shorter and avoids duplicating the next-instruction page. It should act as a top-level status surface, while command/report details live elsewhere.

## 7. Next Codex tasks

- Run controlled human sessions.
- Place logs in `playtest_logs/`.
- Run `npm run playtest:summary`.
- Later, automate generation of the HTML interface from Markdown.

## 8. Portfolio notes

- Problem: the dashboard was becoming a long mixed-purpose document.
- Direction: split the interface into status, instruction, operation, and report surfaces.
- Action: compacted the project dashboard and moved detail out by role.
- Result: the user can understand project state faster and open detailed pages only when needed.

# 2026-06-08-06 - Project Dashboard Surface Trim

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work changed only orchestration HTML and documentation.

## 2. What changed

- Renamed the dashboard heading from `30초 상태 요약` to `상태 요약`.
- Removed the subtitle explaining that next prompts live in `command.html` and details live in `reports/`.
- Removed the bottom link cards for next instruction, runbook, and detailed reports.
- Kept the dashboard focused on current state, current conclusion, goal, next judgment, recent completion, and date.

## 3. Test results and evidence

- `npm run report`: pass, generated 7 unit reports.
- `npm run report:check`: pass, 7 report units.
- `npm run report:discord:unit:dry`: pass, latest unit 07 summary generated.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- `Select-String` dashboard check: only `상태 요약` remained; removed target strings were absent.
- `npm run report:discord:unit`: blocked by approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook, which is untrusted by default and not explicitly approved by the user. Next trusted-local command: `npm run report:discord:unit`.

## 4. Decisions made

- The project dashboard should not explain the interface.
- Human-facing detail belongs in HTML reports.
- AI-facing continuity should remain in Markdown source files and devlogs.

## 5. Problems or risks

- HTML is still hand-maintained and can drift until a generator exists.
- The exact dashboard density may need user review in the browser.
- Discord actual send was blocked by approval policy in this Codex session.

## 6. GPT handoff summary

The dashboard has been trimmed to status-only content. Navigation and explanation were removed from the main view.

## 7. Next Codex tasks

- Run controlled human sessions.
- Place logs in `playtest_logs/`.
- Run `npm run playtest:summary`.
- Later, automate generation of the HTML interface from Markdown.

## 8. Portfolio notes

- Problem: a dashboard can become noisy if it explains its own navigation.
- Direction: make the visible surface feel like a concise AI plugin dashboard.
- Action: removed explanatory copy and extra cards from the main dashboard.
- Result: the main HTML now behaves more like a status widget, while reports keep detail.

# 2026-06-08-07 - Plugin-Oriented Migration Prompt Update

## 1. Current build status

LETHE v0.12 remains the current controlled human-test candidate. This work changed only orchestration migration guidance.

## 2. What changed

- Updated `EXISTING_PROJECT_MIGRATION_PROMPT.md` for a reusable personal AI plugin-style structure.
- Defined `docs/orchestration/interface/` for the required human-facing HTML files:
  - `index.html`,
  - `command.html`,
  - `runbook.html`.
- Defined `docs/orchestration/state/` for AI-facing Markdown source-of-truth files.
- Defined `docs/orchestration/reports/` as people-facing HTML work-unit reports.
- Defined `docs/orchestration/devlog/` as AI/internal Markdown continuity.
- Added legacy migration rules so old docs become migrated, archived, linked, or pointer-only.

## 3. Test results and evidence

- `npm run report`: pass, generated 8 unit reports.
- `npm run report:check`: pass, 8 report units.
- `npm run report:discord:unit:dry`: pass, latest unit 08 summary generated.
- `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- `npm run report:discord:unit`: blocked by approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook, which is untrusted by default and not explicitly approved by the user. Next trusted-local command: `npm run report:discord:unit`.

## 4. Decisions made

- People-facing documentation should be HTML-first.
- AI-facing continuity should stay in concise Markdown under `state/` and `devlog/`.
- Existing docs outside orchestration should stop being normal source-of-truth files after migration.

## 5. Problems or risks

- This updates the shared prompt only; the current LETHE folder layout still needs the physical migration to `interface/` and `state/`.
- Deleting old docs should still require explicit approval; migration should archive or point first.
- Discord actual send was blocked by approval policy in this Codex session.

## 6. GPT handoff summary

The migration prompt now describes the orchestration system as a local personal AI plugin: people read HTML, AI reads Markdown, and old project-management docs are absorbed into orchestration.

## 7. Next Codex tasks

- Migrate LETHE's current flat orchestration files into the new `interface/` and `state/` folder structure.
- Add a migration map under `docs/orchestration/legacy/`.
- Then update AGENTS and local links to the new paths.

## 8. Portfolio notes

- Problem: the previous orchestration layout mixed HTML and Markdown at one level.
- Direction: make the structure reusable across projects as a personal AI plugin convention.
- Action: updated the migration prompt with separate human-facing and AI-facing folders.
- Result: future projects can adopt the same interface contract more cleanly.
