# 2026-06-08-02 - 오케스트레이션 인터페이스 도입

## 1. 현재 빌드 상태

- `docs/orchestration/`을 프로젝트 관리 인터페이스로 도입했다.
- 기존 `AGENTS.md` 규칙은 유지했다.

## 2. 오늘 바뀐 것

- 오케스트레이션용 상태, 작업, 실행 절차, 범위 제한, 결정 로그 문서를 만들었다.
- 루트 `AGENTS.md`에 오케스트레이션 사용 규칙을 추가했다.

## 3. 테스트 결과와 근거

- 오케스트레이션 핵심 문서를 읽어 구조를 확인했다.
- `npm run report`: 통과.
- `npm run report:check`: 통과.
- `npm run doctor`: 통과.

## 4. 결정한 것

- `AGENTS.md`는 프로젝트 최상위 규칙으로 남기고, 오케스트레이션은 관리 인터페이스 역할을 맡긴다.

## 5. 문제 또는 리스크

- 기존 `docs/NEXT_TASKS.md`와 새 오케스트레이션 작업 목록이 한동안 중복될 수 있다.

## 6. GPT/Claude 인계 요약

새 세션은 먼저 오케스트레이션 문서를 읽고 현재 상태와 다음 작업을 파악해야 한다.

## 7. 다음 Codex 작업

- 실제 작업에서 오케스트레이션 문서가 충분한지 검증한다.
- 사람이 읽는 HTML 화면을 추가한다.

## 8. 포트폴리오 메모

- 문제: 프로젝트 진행 상태가 여러 문서로 흩어져 있었다.
- 방향: 반복 가능한 프로젝트 관리 인터페이스를 만든다.
- 행동: 오케스트레이션 문서 세트를 도입했다.
- 결과: Codex가 이어서 읽을 짧은 진입점이 생겼다.
