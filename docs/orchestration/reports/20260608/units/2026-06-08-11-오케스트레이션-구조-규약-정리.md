# 2026-06-08-11 - 오케스트레이션 구조 규약 정리

## 1. 현재 빌드 상태

- 오케스트레이션 구조의 기준 위치를 `docs/orchestration/`로 고정했다.
- review prompt/response도 실제 파일까지 오케스트레이션 아래로 옮겼다.
- Discord 보고는 최신 날짜의 unit HTML과 summary JSON을 기준으로 보낸다.

## 2. 오늘 바뀐 것

- `AGENTS.md`, `EXISTING_PROJECT_MIGRATION_PROMPT.md`, `docs/DISCORD_REPORTING.md`, `docs/CODEX_RUNBOOK.md`의 경로 규약을 새 구조로 정리했다.
- `docs/review_prompts/*.md`를 `docs/orchestration/review_prompts/`로 옮겼다.
- `docs/review_responses/*.md`를 `docs/orchestration/review_responses/`로 옮겼다.
- 기존 review 폴더에는 legacy pointer README만 남겼다.
- `scripts/ask_claude_review.js`, `scripts/ask_codex_review.js`, `scripts/send_discord_report.js`, `scripts/run_balance_loop.js`, `scripts/run_planning_pipeline.js`, `scripts/check_local_pipeline.js`, `scripts/run_autonomous_dev_loop.js`, trusted QA 스크립트를 새 경로 우선으로 바꿨다.
- `docs/orchestration/legacy/MIGRATION_MAP.md`를 추가했다.
- 날짜별 리포트를 전체 재생성해 `latest.json` 경로가 새 구조를 가리키도록 정리했다.

## 3. 테스트 결과와 근거

- 날짜별 전체 리포트 재생성: 통과.
- `docs/orchestration/reports/index.html`가 새 날짜별 구조를 기준으로 다시 생성됐다.
- `node --check scripts/ask_claude_review.js`: 통과.
- `node --check scripts/ask_codex_review.js`: 통과.
- `node --check scripts/send_discord_report.js`: 통과.
- `node --check scripts/run_balance_loop.js`: 통과.
- `node --check scripts/run_planning_pipeline.js`: 통과.
- `node --check scripts/run_autonomous_dev_loop.js`: 통과.
- `npm.cmd run report`: 통과, 11개 단위 리포트 생성.
- `npm.cmd run report:check`: 통과, 11개 단위 확인.
- `npm.cmd run report:discord:unit:dry`: 통과, 최신 첨부가 `docs/orchestration/reports/20260608/units/2026-06-08-11-오케스트레이션-구조-규약-정리.html`을 가리킴.
- `npm.cmd run doctor`: 통과, 49 pass / 1 warn / 0 fail. Codex CLI 없음은 warning.
- `npm.cmd run report:discord:unit`: 승인 정책에서 차단. 외부 Discord webhook으로 workspace 생성 보고서와 첨부를 보내는 것은 고위험 데이터 전송으로 판단됨.

## 4. 결정한 것

- 새 source of truth는 `docs/orchestration/` 아래에만 둔다.
- `docs/reports/`, `docs/devlog/`, `docs/review_prompts/`, `docs/review_responses/`는 레거시 포인터 또는 역사 기록으로만 본다.
- review prompt/response는 새 작업부터 반드시 `docs/orchestration/review_prompts/`, `docs/orchestration/review_responses/`에 저장한다.
- Discord는 알림과 첨부 채널일 뿐이고, 기록의 기준은 Markdown/HTML 파일이다.

## 5. 문제 또는 리스크

- 과거 devlog와 report 본문 안에는 당시 사용한 레거시 경로가 역사 기록으로 남아 있다.
- 실제 Discord 전송은 이 Codex 세션의 승인 정책에서 차단됐다. 우회하지 않는다. 다음 trusted-local 명령은 `npm run report:discord:unit`이다.

## 6. GPT/Claude 인계 요약

오케스트레이션은 프로젝트별 문서 폴더가 아니라 공통 개인 AI 플러그인 규약으로 확정한다. 사람은 HTML을 보고, AI는 `state/`, `devlog/`, `review_prompts/`, `review_responses/` Markdown을 읽는다.

## 7. 다음 Codex 작업

- `npm run report`로 이번 단위 HTML을 생성한다.
- `npm run report:check`로 단위 제목과 생성 파일을 검증한다.
- `npm run report:discord:unit:dry`로 최신 Discord 첨부가 새 경로를 가리키는지 확인한다.
- `npm run doctor`로 로컬 파이프라인을 점검한다.
- 이후 원래 작업인 v0.12 밸런스 실패 리뷰로 돌아간다.

## 8. 포트폴리오 메모

- 문제: 여러 프로젝트에 같은 방식으로 적용할 문서/보고/AI 연속성 규약이 필요했다.
- 방향: 오케스트레이션 폴더 하나를 플러그인형 프로젝트 관리 인터페이스로 표준화했다.
- 행동: 문서 구조, 스크립트 기본 경로, Discord 보고 기준, legacy map을 한 번에 정렬했다.
- 결과: 다음 작업부터 상태, 개발로그, 리포트, AI 리뷰, Discord 첨부가 같은 구조로 쌓인다.
