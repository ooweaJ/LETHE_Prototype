# 2026-06-08-05 - HTML 인터페이스 규약 정렬

## 1. 현재 빌드 상태

- 오케스트레이션 HTML 3종의 역할을 정했다.

## 2. 오늘 바뀐 것

- `index.html`, `command.html`, `runbook.html`을 필수 사람용 인터페이스로 정의했다.
- 마이그레이션 프롬프트와 현재 HTML 구성을 맞췄다.

## 3. 테스트 결과와 근거

- `npm run report`: 통과.
- `npm run report:check`: 통과.
- `npm run report:discord:unit:dry`: 통과.
- `npm run doctor`: 통과.

## 4. 결정한 것

- 프로젝트 상태, 다음 지시, 운영 절차를 서로 다른 HTML 화면으로 분리한다.

## 5. 문제 또는 리스크

- 실제 폴더는 아직 `interface/`와 `state/`로 완전히 옮겨지지 않았다.

## 6. GPT/Claude 인계 요약

다른 프로젝트에도 같은 구조를 적용하려면 프롬프트 규약과 실제 폴더 구조가 일치해야 한다.

## 7. 다음 Codex 작업

- 실제 LETHE 폴더도 규약대로 물리 이동한다.
- report/devlog 중복을 제거한다.

## 8. 포트폴리오 메모

- 문제: 인터페이스 규약과 실제 파일 배치가 달랐다.
- 방향: 사람이 보는 HTML과 AI가 읽는 Markdown을 분리한다.
- 행동: HTML 역할을 명시했다.
- 결과: 플러그인형 구조의 기준이 잡혔다.
