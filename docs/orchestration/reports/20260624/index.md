# 2026-06-24-01 - 출시 준비형 맵/배경 1차 확장

## 1. 현재 빌드 상태

`Dev_Prototype_v1`은 계속 `_dev` 검증 빌드다. 다만 이번 작업부터는 작은 테스트 박스 느낌을 줄이는 쪽으로 방향을 바꿨고, 맵/배경을 실제 출시 준비용 월드 베이스에 가깝게 키우기 시작했다.

## 2. 오늘 바뀐 것

- 새 맵 스프라이트를 추가했다:
  - `tile_lethe_stone_01.png`
  - `tile_lethe_stone_02.png`
  - `tile_lethe_stone_03.png`
  - `tile_lethe_stone_04.png`
  - `spr_lethe_arena_backdrop_01.png`
- 같은 이미지를 다시 만들 수 있게 `scripts/generate_world_sprites.ps1`를 추가했다.
- 플레이어 이동 범위를 기존 `x +/-12`, `y -8.5..8.5`에서 `x +/-24`, `y +/-16`으로 키웠다.
- 바닥 타일 배치도 `11x9`에서 `21x15`로 늘렸다.
- 카메라 크기를 `6.1`에서 `6.8`로 키우고, 커진 맵 안에서만 따라가도록 클램프했다.
- 적 스폰 반경을 늘려서 커진 맵에서 바로 몸에 붙는 느낌을 줄였다.
- 배경, 경계선, 균열, 외곽 표식을 더 큰 맵 기준으로 다시 배치했다.

## 3. 테스트 결과와 근거

- 새 타일과 배경 PNG를 로컬에서 직접 열어 비어 있지 않고 어두운 전투 배경으로 읽히는지 확인했다.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console error count: `0`.

## 4. 결정한 것

이제 배경/맵 작업은 단순한 임시 장식이 아니라 출시 준비형 월드 베이스로 본다. 먼저 맵 크기와 카메라 감각을 잡고, 그 다음에 배경 스프라이트를 더 강한 아트 방향으로 교체하는 순서가 맞다.

## 5. 문제 또는 리스크

이번 배경은 직접 제작한 1차 생성 이미지라 최종 아트는 아니다. 맵이 커진 만큼 전투가 시원해질 수 있지만, 반대로 초반 180초가 더 루즈하게 느껴질 수도 있다. 직접 플레이로 크기, 밀도, 이동 시간, 적 접근감까지 봐야 한다.

## 6. GPT/Claude 인계 요약

Codex가 `Dev_Prototype_v1`의 월드 크기를 약 두 배로 확장하고, 새 바닥/배경 스프라이트를 연결했다. 다음 판단은 "이 크기가 출시 준비용 기본 전투장으로 맞는가"이며, 맞다면 배경은 생성형 1차 이미지에서 더 의도된 랜드마크/경계/테마 아트로 업그레이드하면 된다.

## 7. 다음 Codex 작업

1. jaewoo가 대검/쌍검으로 커진 맵을 직접 플레이한다.
2. 맵 크기가 맞으면 배경 스프라이트 2차 제작으로 넘어간다.
3. 맵이 너무 넓거나 루즈하면 적 스폰 밀도, 카메라 크기, 이동 속도, 보스 타이밍을 같이 조정한다.
4. 큰 맵에서 적/보스 스프라이트가 충분히 읽히는지 다시 확인한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 프로토타입 맵이 작고 배경이 임시 장식처럼 보여 실제 출시 준비 단계로 보기 어려웠다.
- 방향: 먼저 플레이 공간과 카메라 기준을 키워 실제 게임 공간의 기본 감각을 잡는다.
- 행동: 새 맵 스프라이트를 만들고, 플레이 범위/타일 수/카메라/스폰 반경을 함께 확장했다.
- 결과: 기술 검증상 빌드와 Unity Play Mode가 통과했고, 이제 직접 플레이로 맵 크기와 배경 방향을 판단할 수 있다.

# 2026-06-24-02 - 레테 지형 스프라이트 교체

## 1. 현재 빌드 상태

`Dev_Prototype_v1`은 커진 맵 위에 새 자연 지형 타일을 깔도록 바뀌었다. 이전 1차 배경은 인공적인 필드/의식장 느낌이 강했기 때문에, 이번에는 뱀서처럼 반복 배치돼도 지형으로 읽히는 방향으로 바꿨다.

## 2. 오늘 바뀐 것

- 레테 컨셉 지형 소스 시트를 생성하고 프로젝트에 저장했다:
  - `LETHE/Assets/_dev/Art/Source/spr_lethe_terrain_sheet_01_source.png`
- 새 지형 타일 8개를 생성했다:
  - 젖은 검은 돌
  - 진흙 둑
  - 얕은 레테 물길
  - 갈라진 슬레이트
  - 기억 조각 자갈
  - 죽은 뿌리
  - 재빛 흙
  - 낡은 습지 길
- `V1GameManager`가 기존 `tile_lethe_stone_*.png` 대신 `tile_lethe_terrain_01..08.png`를 쓰게 했다.
- 배경도 `spr_lethe_terrain_backdrop_01.png`로 교체했다.
- 인공적인 외곽 원형 마커를 제거하고, 습지 가장자리 / 레테 물길 / 죽은 뿌리 / 기억 자갈 장식으로 바꿨다.

## 3. 테스트 결과와 근거

- 대표 타일 3장과 새 배경 PNG를 직접 열어 지형으로 읽히는지 확인했다.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console error count: `0`.

## 4. 결정한 것

LETHE의 기본 전투 배경은 인공 아레나보다 자연 지형 쪽이 맞다. 레테 강바닥, 검은 습지, 젖은 돌, 뿌리, 기억 조각이 기본 언어가 되고, 마법진/완전한 원형 장식은 전투 VFX나 특수 이벤트에만 쓰는 편이 낫다.

## 5. 문제 또는 리스크

타일 단독 이미지는 좋아졌지만 실제 게임에서는 반복감, 밝기, 적 실루엣, VFX와의 충돌을 봐야 한다. 특히 뿌리 타일은 멋있지만 너무 자주 나오면 바닥이 복잡해 보일 수 있다.

## 6. GPT/Claude 인계 요약

Codex가 배경 방향을 인공 필드에서 자연 지형으로 전환했다. 다음 리뷰는 "타일 자체가 예쁜가"보다 "움직이는 전투 중 지형으로 읽히면서도 적과 VFX를 방해하지 않는가"를 기준으로 보면 된다.

## 7. 다음 Codex 작업

1. jaewoo가 커진 맵에서 쌍검/대검을 모두 플레이한다.
2. 배경이 여전히 복잡하면 타일 밝기와 뿌리/자갈 빈도를 낮춘다.
3. 배경이 너무 밋밋하면 큰 지형 랜드마크를 추가한다.
4. 적/보스 스프라이트가 새 지형 위에서 잘 보이는지 다시 본다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 1차 배경이 지형이 아니라 인공 필드처럼 보여 출시 준비용 월드 감각이 부족했다.
- 방향: 뱀서식 반복 지형 읽기와 LETHE의 망각 강/습지 컨셉을 결합한다.
- 행동: 새 지형 시트를 만들고, 8개 타일과 배경으로 잘라 Unity 런타임에 연결했다.
- 결과: 빌드와 Unity Play Mode 검증을 통과했고, 이제 직접 플레이로 반복감과 전투 판독성을 판단할 수 있다.

# 2026-06-24-03 - 지형 연결감 보정

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 배경은 자연 지형 방향을 유지하되, 타일들이 따로 노는 문제를 줄이도록 다시 보정했다.

## 2. 오늘 바뀐 것

- `tile_lethe_terrain_01..08.png`를 서로 다른 지형 묶음이 아니라 같은 젖은 검은 돌 베이스에서 파생되게 다시 생성했다.
- 런타임 바닥 타일 선택은 대부분 연결된 기본 타일을 쓰고, 특징 강한 변형은 드물게 나오게 했다.
- 물길, 죽은 뿌리, 기억 자갈 장식 개수를 줄였다.
- 바닥 타일 크기를 살짝 키워서 타일 경계가 덜 드러나게 했다.

## 3. 테스트 결과와 근거

- 재생성된 대표 타일과 배경 PNG를 직접 열어 타일 간 성격 차이가 줄었는지 확인했다.
- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity `Assets/Refresh`: 성공.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console error count: `0`.

## 4. 결정한 것

기본 맵은 다양한 지형을 한 칸씩 섞는 것보다 하나의 연결된 바닥 언어가 먼저다. 물길, 뿌리, 기억 조각은 바닥 타일 자체가 아니라 장식과 랜드마크로 올리는 편이 더 자연스럽다.

## 5. 문제 또는 리스크

연결감은 좋아졌지만 반복감이 생길 수 있다. 다음 직접 플레이에서는 "따로 논다"가 해결됐는지와 동시에 "너무 반복된다"는 새 문제가 생겼는지를 같이 봐야 한다.

## 6. GPT/Claude 인계 요약

타일 다양성보다 연결감을 우선하는 방향으로 바꿨다. 다음 판단 기준은 한 화면에서 바닥이 하나의 젖은 레테 강바닥처럼 이어지는지, 그리고 VFX/적 실루엣을 방해하지 않는지다.

## 7. 다음 Codex 작업

1. 직접 플레이에서 타일 경계가 아직 보이는지 확인한다.
2. 반복감이 강하면 큰 랜드마크나 부드러운 장식 패치를 추가한다.
3. 여전히 따로 놀면 개별 타일 방식을 줄이고 큰 단일 배경/청크 방식으로 전환한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 자연 지형 타일은 생겼지만 각 타일이 서로 다른 지형처럼 보여 맵이 이어지지 않았다.
- 방향: 바닥은 하나의 연결된 지형으로 통일하고, 특색은 장식 레이어로 분리한다.
- 행동: 타일 생성과 런타임 선택 규칙을 연결감 우선으로 수정했다.
- 결과: 기술 검증은 통과했고, 직접 플레이로 연결감과 반복감의 균형을 판단할 수 있다.

# 2026-06-24-04 - 8잔향 VFX 판독성 보강

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에는 8잔향 로직과 VFX 호출이 모두 들어가 있었다. 다만 실제 전투 속도에서는 몇몇 잔향이 너무 작거나 짧거나 색 대비가 약해서 "발동했는지 모르겠다"는 문제가 있었다.

## 2. 오늘 바뀐 것

- 칼무리 잔향: 범위 링을 키우고 밝게 만들었고, 내부 플래시 링과 더 크고 오래 남는 칼날 무리를 추가했다. +5 칼날 발사도 더 크게 보이게 했다.
- 혈반 잔향: 피 표식에 붉은 펄스 링을 추가했고, 피꽃 폭발 크기/유지시간/범위를 키웠다.
- 처형 잔향: 금색 처형 프롬프트를 키우고 할로 링과 더 큰 폭발감을 추가했다.
- 추적 잔향: 연두색 유도탄과 타겟락 링을 더 크고 오래 보이게 했다.
- 파문 잔향: 충격파 필드의 발동률, 크기, 유지시간, 링/균열선 밝기를 올렸다.
- 정지 잔향: 금색 시계장판의 발동률, 범위, 유지시간을 올리고 정지 시간을 살짝 늘렸다.
- 잿빛 잔향: 플레이어 주변 보호/회복 링이 더 크게 남도록 했다.
- 낙인 잔향: 보라색 낙인 프롬프트를 키우고 추가 보라 베기 액센트를 넣었다.
- 잔향만 테스트하는 Echo Only 디버그를 추가했다:
  - `Echo A`: 처형/추적/정지 잔향 +5, 궁극 억제.
  - `Echo B`: 파문/잿빛/낙인 잔향 +5, 궁극 억제.
  - `Echo All` 또는 `F10`: 8잔향 전체 +5, 궁극 억제.
  - `Ult 3`: 궁극 테스트용으로 따로 유지.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console error count: `0`.
- Echo Only 디버그 추가 후 `dotnet build` 재확인: 통과, error 0개.

## 4. 결정한 것

잔향은 디버그 텍스트나 설명보다 먼저 전투 VFX로 읽혀야 한다. 앞으로 8잔향은 각자 색, 형태, 유지시간이 달라야 하고, "발동했네"가 아니라 "아, 이 잔향이 터졌네"까지 보여야 한다.

## 5. 문제 또는 리스크

직접 플레이 판독성은 아직 jaewoo 확인이 필요하다. 특히 여러 잔향이 동시에 +5가 되면 화면이 시끄러워질 수 있고, 새 지형 배경 위에서 빨강/금색/초록/보라 VFX가 충분히 분리되는지도 봐야 한다.

## 6. GPT/Claude 인계 요약

Codex가 8잔향의 기존 VFX 존재 여부를 확인했고, 로직은 유지하되 발동 판독성을 키웠다. 다음 리뷰 기준은 밸런스 수치보다 "각 잔향을 텍스트 없이 구분할 수 있는가"다.

## 7. 다음 Codex 작업

1. jaewoo가 직접 플레이하면서 8잔향 중 아직 안 보이는 것, 너무 빠른 것, 너무 비슷한 것을 체크한다.
2. 피드백에 따라 개별 잔향의 색/크기/지속시간/발동 빈도를 다시 좁혀 튜닝한다.
3. 맵 배경과 적/보스 스프라이트가 VFX를 가리는지 함께 확인한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 기능은 구현되어 있었지만 플레이어가 잔향 발동을 체감하기 어려웠다.
- 방향: 잔향을 시스템 텍스트가 아니라 전투 이벤트로 보이게 만든다.
- 행동: 8잔향별 색/형태/크기/유지시간/보조 링을 보강했다.
- 결과: 기술 검증은 통과했고, 이제 직접 플레이로 잔향별 판독성을 판단할 수 있다.

# 2026-06-24-05 - 잔향 정체성 분리와 대검 VFX 즉발화

## 1. 현재 빌드 상태

8잔향은 보이도록 보강됐지만, 일부 잔향은 아직 기억 VFX와 비슷하게 읽힐 수 있었다. 특히 파문/정지/잿빛/낙인은 독립 스킬 장판처럼 보일 위험이 있었다. 대검은 베기 VFX가 무기 움직임보다 늦게 붙어 손맛이 둔했다.

## 2. 오늘 바뀐 것

- 대검 베기 VFX 딜레이를 `0.18s`에서 `0.045s`로 줄여 거의 즉시 나오게 했다.
- 잔향을 "기억 스킬"보다 "무기 타격 후 남는 현상"으로 읽히게 바꿨다.
- 칼무리: 타격 지점에 청색 베기 흔적을 추가했다.
- 혈반: 적 몸에 붉은 상처선을 추가했다.
- 처형: 금색 처형 절단선을 추가했다.
- 추적: 타격 지점 표식과 유도 대상까지 이어지는 조준선을 추가했다.
- 파문: 장판 전에 타격점 균열선을 추가했다.
- 정지: 적 몸 주변에 작은 금색 시계 고리를 추가하고, 지원 장판은 조금 더 타이트하게 했다.
- 잿빛: 적중 지점 표식, 플레이어로 돌아오는 회색 실, 더 작은 보호 잔광으로 바꿨다.
- 낙인: 적 몸에 보라 낙인 고리와 낙인선을 추가했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity compile error count: `0`.
- Unity Play Mode 진입: `isPlaying=true` 확인.
- Unity console error count: `0`.

## 4. 결정한 것

잔향은 큰 독립 스킬처럼 보이기보다, 무기 타격점/적 몸에 남는 후속 흔적으로 먼저 읽혀야 한다. 기억은 전장이나 플레이어 중심의 능력이고, 잔향은 방금 벤 자리에 박히는 잔상이다.

## 5. 문제 또는 리스크

가시성은 더 좋아졌지만, 실제 플레이 속도에서는 새 상처선/링/실이 너무 짧거나 너무 많이 겹칠 수 있다. Echo Only 모드에서 쌍검과 대검을 모두 확인해야 한다.

## 6. GPT/Claude 인계 요약

Codex가 8잔향을 기억과 분리하기 위해 타격점 중심 VFX를 추가했다. 다음 판단 기준은 "잔향이 기억처럼 보이는가"보다 더 구체적으로 "방금 무기 타격 때문에 생긴 후속 현상처럼 보이는가"다.

## 7. 다음 Codex 작업

1. `Echo All` 또는 `F10`으로 8잔향을 켜고 쌍검/대검을 직접 플레이한다.
2. 잔향별로 너무 안 보이는 것, 기억과 여전히 비슷한 것, 너무 시끄러운 것을 표시한다.
3. 문제가 남은 잔향만 색/형태/유지시간을 다시 좁혀 튜닝한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 잔향 VFX가 보이더라도 기억 VFX와 역할이 겹쳐 보일 수 있었다.
- 방향: 잔향을 무기 타격의 후속 흔적으로 재정의한다.
- 행동: 8잔향에 타격점 중심의 상처선, 조준선, 균열, 시계 고리, 회귀 실, 낙인선을 추가했다.
- 결과: 기술 검증은 통과했고, 이제 Echo Only 직접 플레이로 잔향 정체성을 평가할 수 있다.

# 2026-06-24-06 - 단일 잔향 디버그 리뷰 플로우

## 1. 현재 빌드 상태

`Echo All`은 8잔향을 한 번에 볼 수 있지만, 어떤 잔향이 안 보이는지 또는 기억과 비슷한지 하나씩 찍어내기에는 부족했다. 다음 리뷰는 잔향 전체가 아니라 개별 잔향 단위로 해야 한다.

## 2. 오늘 바뀐 것

- 오른쪽 디버그 패널에 현재 선택 잔향을 표시했다.
- `Prev`, `Echo One`, `Next` 버튼을 추가했다.
- `Echo One`은 선택된 잔향 하나만 +5로 켜고 궁극은 억제한다.
- 단축키를 추가했다:
  - `F10`: 8잔향 전체 +5, 궁극 억제.
  - `F11`: 현재 선택 잔향 하나만 +5, 궁극 억제.
  - `F12`: 다음 잔향 선택.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 통과, legacy warning 7개, error 0개.
- Unity compile error count: `0`.
- Unity console error count: `0`.

## 4. 결정한 것

다음 리뷰 순서는 `Echo One`으로 잔향을 하나씩 보는 것이다. 전체가 시끄러운지보다 먼저 각 잔향이 단독으로 "무슨 잔향인지" 보이는지 확인한다.

## 5. 문제 또는 리스크

단독으로 잘 보여도 실제 2~3개 조합에서는 시끄러울 수 있다. 그래서 단일 리뷰 후 `Echo A/B`, 마지막에 `Echo All` 순서로 다시 봐야 한다.

## 6. GPT/Claude 인계 요약

Codex가 8잔향 단독 테스트 플로우를 추가했다. 이제 jaewoo는 잔향 하나씩 확인하면서 가시성/정체성/과밀도를 표시할 수 있다.

## 7. 다음 Codex 작업

1. jaewoo가 `Echo One`으로 8개를 하나씩 본다.
2. 문제가 있는 잔향만 이름을 적는다.
3. Codex가 해당 잔향만 색, 크기, 지속시간, 발동 빈도를 다시 튜닝한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 전체 잔향 테스트만으로는 어떤 잔향이 문제인지 분리하기 어려웠다.
- 방향: 8잔향을 하나씩 독립 리뷰할 수 있게 한다.
- 행동: 선택 잔향 표시와 `Prev/Echo One/Next`, `F11/F12` 디버그를 추가했다.
- 결과: 기술 검증은 통과했고, 이제 문제 잔향을 정확히 특정할 수 있다.
