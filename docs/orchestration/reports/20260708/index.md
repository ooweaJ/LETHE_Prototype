> 2026-07-08 LETHE 개발 보고서

# 2026-07-08-01 - 기억/잔향 왕귀 VFX와 판정 보강

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에 기억/잔향/궁극 잔향의 왕귀형 보상과 VFX 판정을 1차 구현했다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- 처형은 즉사 직전 예고 VFX와 처형/망각 연계 폭발력을 추가했다.
- 추적은 가까운 적만 보지 않고 보스/힐러/위협 몬스터를 우선 노리게 했다.
- 파쇄는 밀집 몹과 보스에게 균열 보너스가 생겼다.
- 정지는 시간 정지 후 균열 폭발이 붙어 제어만 하는 루트가 아니게 했다.
- 잿빛 방패는 막은 피해와 잔향 타격으로 충전되고, 저장된 방어력을 파동으로 터뜨린다.
- 망각 낙인은 +5에서 전이/폭발하는 방향으로 보강했다.
- 피의 칼폭풍 외 궁극인 처형 균열, 정지 추적, 잿빛 망각의 피해와 VFX를 강화했다.
- 밀집 쌍검 QA는 처치 연쇄가 아니라 실제 밀집 히트/잔향 억제를 측정하도록 정리했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- Unity compilation errors: `0`.
- VFX Matrix: PASS, `previewMemory=8`, `previewEcho=8`, `fracture=1`, `stasis=1`, `ashen=1`.
- Echo Matrix Dual Blades: PASS, `total=226`, `state=78`.
- Echo Matrix Greatsword: PASS, `total=207`, `state=57`.
- Passive Memory Matrix: PASS, `blood=17`, `ash=6`, `stopped=8`, `oblivion=62`.
- Utility Ultimate Matrix Dual Blades: PASS, `fracture=28`, `stasis=11`, `ashen=47`.
- Utility Ultimate Matrix Greatsword: PASS, `fracture=49`, `stasis=22`, `ashen=14`.
- Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=45`, `activeVfx=26`, `ms=57.58`.

## 4. 결정한 것

혈반/피의 칼폭풍만 강하게 보상하는 구조를 피하고, 약하거나 수동적인 기억도 잔향과 궁극 잔향에서 폭발적으로 변형될 수 있게 잡았다. 특히 잿빛 방패는 방어형 기억이지만 저장 후 방출하는 공격형 보상으로 방향을 틀었다.

## 5. 문제 또는 리스크

자동 QA는 수치와 오브젝트 존재를 확인하지만, 실제 손맛과 가시성은 직접 플레이가 최종 판단이다. 특히 잿빛 파동이 너무 약하거나 반대로 너무 공짜 딜처럼 느껴지는지 확인이 필요하다.

## 6. GPT/Claude 인계 요약

이번 패스는 밸런스 수치만 올린 작업이 아니라, 각 기억/잔향이 몹에게 어떤 판정을 남기고 어떤 VFX 언어로 읽히는지 보강한 작업이다. 다음 리뷰는 텍스트 설명 없이 전투 화면만 보고 루트 차이를 느낄 수 있는지에 집중하면 된다.

## 7. 다음 Codex 작업

jaewoo 직접 플레이 후 가장 약한 루트 하나를 골라 세부 조정한다. 우선 후보는 잿빛 방패 충전량/방출 반경, 정지 균열 타이밍, 망각 전이 수, 처형 예고 가시성, 비혈반 궁극 피해량이다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: Blood Reflection이 효과, 회복, VFX, 궁극 연결을 모두 가져 다른 선택지가 약해 보였다.
- 방향: 약한 기억도 잔향/궁극 단계에서 강하게 변형되는 왕귀 구조를 만든다.
- 행동: 처형, 추적, 파쇄, 정지, 잿빛, 망각의 판정과 VFX를 보강하고 비혈반 궁극을 상향했다.
- 결과: 주요 Unity QA가 모두 통과했고, 이제 직접 플레이로 손맛과 선택지를 평가할 수 있는 상태가 됐다.

# 2026-07-08-02 - 칼무리 잔향 무기별 개성 분리

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에서 칼무리 잔향의 쌍검/대검 분기를 다시 잡았다. 쌍검은 빠른 사냥떼, 대검은 큰 판결검이 내려찍는 방향이다.

## 2. 오늘 바뀐 것

- 대검 칼무리 잔향을 기존 공통 클램프/물어뜯기 패턴에서 분리했다.
- 대검은 낙하하는 큰 칼날, 낙하선, 지면 찢기, 처형 균열, 충격 코어를 사용한다.
- 쌍검 칼무리는 기존처럼 빠른 칼날 군집/물어뜯기 언어를 유지했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 단독 재실행 기준 warning 0개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- Unity compilation errors: `0`.
- Echo Matrix Greatsword: PASS, `total=335`, `K=136`, `state=58`.
- Echo Matrix Dual Blades: PASS, `total=230`, `K=8`, `state=85`.
- Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=94`, `activeVfx=30`, `ms=87.85`.

## 4. 결정한 것

잔향은 무기와 무관한 동일 효과가 아니라, 같은 잔향이라도 무기 성격을 보존해야 한다. 칼무리부터 쌍검은 빠르고 여럿, 대검은 적고 무겁게 갈라냈다.

## 5. 문제 또는 리스크

대검 칼무리 카운트가 크게 늘었다. 직접 플레이에서 통쾌하면 유지하고, 화면을 너무 덮으면 낙하선/지면 이빨 수를 줄이는 쪽이 맞다.

## 6. GPT/Claude 인계 요약

이번 수정은 칼무리 하나만 무기별 액션 문법을 분리한 사례다. 다음에는 처형, 추적, 정지, 잿빛 같은 유틸 잔향도 같은 기준으로 쌍검/대검 개성을 점검해야 한다.

## 7. 다음 Codex 작업

jaewoo가 대검 칼무리의 낙하검 연출을 확인한 뒤, 너무 과하면 VFX 수를 줄이고 약하면 충격/히트스톱을 더 조정한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 같은 잔향이 무기만 바뀌어도 거의 같은 화면 언어로 보였다.
- 방향: 잔향의 정체성은 유지하되 무기별 액션 문법을 다르게 만든다.
- 행동: 대검 칼무리 잔향을 낙하 판결검/지면 균열 패턴으로 분리했다.
- 결과: 대검/쌍검 Echo Matrix와 Dense 쌍검 성능 QA가 모두 통과했다.

# 2026-07-08-03 - 칼무리 잔향 상처 반응 수정

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에서 칼무리 잔향 +5 동작을 다시 수정했다. 이제 칼이 플레이어 몸에서 추가로 발사되는 느낌이 아니라, 무기 공격이 박힌 지점에서 상처/검흔/체인이 반응하는 쪽으로 바뀌었다.

## 2. 오늘 바뀐 것

- 각성 칼무리 체인의 시작점을 플레이어 위치가 아니라 맞은 적의 상처 위치로 옮겼다.
- 상처 폭발, 검흔, 체인 라인, 상처 체인 투사체 이름과 색을 추가했다.
- 밀집 쌍검 상황에서는 고비용 각성 체인을 억제해서 렉 회귀를 막고, 기본 칼무리 타격은 유지했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: error 0개.
- Unity compilation errors: `0`.
- Echo Matrix Dual Blades: PASS, `total=230`, `K=8`, `state=86`.
- Echo Matrix Greatsword: PASS, `total=335`, `K=136`, `state=58`.
- Dense Dual Blades Perf Matrix: PASS, `hits=18`, `suppressed=15`, `transient=87`, `activeVfx=25`, `ms=80.59`.

## 4. 결정한 것

칼무리 잔향은 “몸에서 추가 칼 발사”가 아니라 “공격 지점에서 굶주린 칼들이 상처를 물고 다음 대상으로 이어지는 반응”이어야 한다. 이후 칼무리 튜닝도 이 기준을 따른다.

## 5. 문제 또는 리스크

자동 QA는 통과했지만 실제 플레이에서 상처 반응의 타이밍, 크기, 체인 라인이 충분히 맛있는지는 직접 확인이 필요하다.

## 6. GPT/Claude 인계 요약

칼무리의 핵심 설계 기준이 바뀌었다. 다음 리뷰에서는 수치보다 “공격 지점에서 상호작용한다”는 느낌이 화면에서 읽히는지를 우선 판단하면 된다.

## 7. 다음 Codex 작업

jaewoo가 직접 플레이하면서 +5 칼무리가 상처 폭발 -> 검흔/체인 -> 다음 대상 연결로 읽히는지 확인한다. 부족하면 스프라이트 추가보다 타이밍, 알파, 체인 지속시간, 생성 위치부터 조정한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 잔향이 무기 공격과 분리된 추가 발사체처럼 보여 설계 의도가 어긋났다.
- 방향: 잔향을 플레이어 몸이 아니라 타격 지점의 상호작용으로 재정의했다.
- 행동: 각성 칼무리 체인의 시작 위치와 VFX 언어를 상처 중심으로 바꿨다.
- 결과: 쌍검/대검 잔향 QA와 밀집 성능 QA가 모두 통과했다.

# 2026-07-08-04 - 칼무리 콘셉트 프리뷰 디버거

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에 칼무리 잔향 후보를 직접 비교하는 F12 디버그 프리뷰가 추가됐다. 아직 최종 전투안 확정이 아니라, jaewoo가 화면에서 어떤 방향이 가장 칼무리답게 읽히는지 고르는 단계다.

## 2. 오늘 바뀐 것

- F12 디버그 패널에 `K1`, `K2`, `K3`, `K4` 버튼을 추가했다.
- `K1`: 상처 포식형. 여러 칼날이 타격 상처로 모여든다.
- `K2`: 궤적 증식형. 공격 궤적 잔상이 늦게 추가 절단으로 번진다.
- `K3`: 군집 교차참격형. 대상 주변에서 칼들이 나타나 안쪽으로 교차 절단한다.
- `K4`: 표식 폭주형. 굶주린 검흔 표식이 주변 적에게 갈라진다.
- 각 버튼은 같은 더미 적 배치를 다시 만들고 해당 콘셉트만 보여줘 비교 조건을 맞춘다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- Unity console errors: `0`.
- Unity `EditorApplication.isCompiling` 값이 refresh 이후 오래 `true`로 유지되어, 실제 버튼 클릭 검증은 직접 플레이에서 이어서 확인해야 한다.

## 4. 결정한 것

칼무리 잔향은 바로 하나로 확정하지 않고, 네 가지 화면 언어를 같은 조건에서 비교한 뒤 최종안을 고른다.

## 5. 문제 또는 리스크

현재 프리뷰는 비교용 샘플이다. 최종 전투 판정, 밀집 최적화, 쌍검/대검 분리 적용은 선택 이후 다시 구현해야 한다.

## 6. GPT/Claude 인계 요약

칼무리의 핵심 이슈는 수치가 아니라 화면 문법이다. K1~K4 중 어떤 것이 “칼무리 잔향”으로 가장 잘 읽히는지 선택한 뒤, 그 방향을 실제 잔향 로직에 이식하면 된다.

## 7. 다음 Codex 작업

jaewoo가 Play Mode에서 F12를 누르고 `K1`~`K4`를 차례로 눌러 본다. 선택 결과가 나오면 Codex가 해당 후보 또는 하이브리드를 실제 칼무리 잔향으로 변환한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 기존 칼무리 잔향이 단일 투사체처럼 보여 콘셉트가 약했다.
- 방향: 확정 구현 전에 여러 VFX 문법을 직접 비교한다.
- 행동: F12 디버그 패널에 네 가지 칼무리 콘셉트 프리뷰를 추가했다.
- 결과: 이제 플레이 화면에서 후보를 비교하고 최종 방향을 고를 수 있다.

# 2026-07-08-05 - 칼무리 콘셉트 프리뷰 식별성 분리

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 F12 `K1`~`K4` 칼무리 프리뷰가 다시 수정됐다. 첫 버전은 전부 비슷한 청록 칼날로 보여서 비교 도구로 부족했고, 이번 수정은 색과 형태부터 다르게 갈랐다.

## 2. 오늘 바뀐 것

- `K1`: 붉은/주황 상처 포식형. 상처 원반, 물어뜯는 링, 안쪽으로 수축하는 이빨, 붉은 검흔을 사용한다.
- `K2`: 파란 궤적 증식형. 긴 공격 리본 잔상과 지연 절단선을 사용한다.
- `K3`: 보라/흰 군집 교차참격형. 방사형으로 칼이 모이고 큰 X자 충격을 만든다.
- `K4`: 보라 표식 폭주형. 표식 링과 주변 적으로 갈라지는 링크/보조 표식을 사용한다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.

## 4. 결정한 것

프리뷰 후보는 같은 칼날 스프라이트만 바꿔 쓰면 비교가 안 된다. 최종 후보를 고르기 전에는 색, 실루엣, 움직임, 판정 형태가 서로 다르게 보여야 한다.

## 5. 문제 또는 리스크

아직 최종 잔향 구현이 아니라 비교용 프리뷰다. jaewoo가 직접 플레이로 어느 방향이 가장 칼무리답게 읽히는지 골라야 한다.

## 6. GPT/Claude 인계 요약

칼무리 후보는 이제 네 가지 화면 언어로 분리됐다. 다음 판단은 수치가 아니라 직관적 식별성, 칼무리다움, 쾌감 기준으로 한다.

## 7. 다음 Codex 작업

jaewoo가 `F12 -> K1~K4`를 다시 눌러 보고, 마음에 드는 후보 또는 섞을 조합을 알려주면 Codex가 실제 칼무리 잔향 로직으로 이식한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 네 후보가 모두 같은 청록 칼날처럼 보여 비교가 어려웠다.
- 방향: 후보마다 색과 실루엣을 다르게 설계한다.
- 행동: K1~K4의 VFX 문법을 붉은 상처, 푸른 궤적, 보라 교차, 보라 표식으로 분리했다.
- 결과: 직접 플레이에서 후보 차이를 훨씬 빠르게 구분할 수 있는 상태가 됐다.

# 2026-07-08-06 - 칼무리 프리뷰 고HP 더미 적용

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 F12 칼무리 프리뷰가 다시 보강됐다. 이제 `K1`~`K4`를 누르면 고HP 더미가 생성되고, 화면에 최신 버전 라벨이 뜬다.

## 2. 오늘 바뀐 것

- 칼무리 프리뷰 전용 더미 적 HP를 `9999`로 올렸다.
- 프리뷰 데미지는 `1`로 제한했다.
- `K1`~`K4`를 누르면 `K Preview v2 / high HP dummies` 라벨이 뜨게 했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.

## 4. 결정한 것

프리뷰는 전투 밸런스가 아니라 VFX 비교 도구이므로, 더미가 죽지 않고 반복 확인 가능해야 한다.

## 5. 문제 또는 리스크

만약 `K Preview v2 / high HP dummies` 라벨이 안 뜨면 Play Mode가 이전 스크립트 어셈블리를 쓰고 있는 것이다. Play Mode를 나갔다가 다시 들어가야 한다.

## 6. GPT/Claude 인계 요약

칼무리 후보 비교는 이제 고HP 더미 기준으로 봐야 한다. 최신 반영 확인 신호는 `K Preview v2 / high HP dummies` 라벨이다.

## 7. 다음 Codex 작업

jaewoo가 F12에서 `K1`~`K4`를 다시 눌러 보고, 라벨이 보이는지와 각 후보가 충분히 다르게 보이는지 판단한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 더미가 빨리 죽어 VFX를 보기 어려웠고 업데이트 적용 여부도 헷갈렸다.
- 방향: 디버그 프리뷰를 전투가 아닌 관찰용으로 분리한다.
- 행동: 고HP 더미, 데미지 제한, v2 라벨을 추가했다.
- 결과: 프리뷰를 반복해서 확인할 수 있는 상태가 됐다.

# 2026-07-08-07 - 칼무리 VFX 하드 리셋 인계

## 1. 현재 빌드 상태

현재 `K1`~`K4` 프리뷰는 비교 도구로 충분하지 않다. 더미 HP나 색 조정 문제가 아니라, 기존 칼무리 칼날/청록/짧은 폭발 언어를 계속 재사용해서 jaewoo가 보기에는 거의 안 바뀐 것처럼 느껴진다.

## 2. 오늘 바뀐 것

- 다음 작업 방향을 문서에 고정했다.
- 현재 `K1`~`K4` 프리뷰를 더 다듬는 대신, 칼무리 VFX를 하드 리셋하기로 했다.
- 다음 세션에서는 기존 칼무리 스프라이트와 헬퍼를 재사용하지 않는 새 후보를 만들어야 한다.

## 3. 테스트 결과와 근거

이번 단위는 코드 구현이 아니라 인계/작업 방향 기록이다. 이전 검증 기준으로 현재 빌드는 컴파일 에러 0, Unity 콘솔 에러 0 상태다.

## 4. 결정한 것

다음 작업은 “색/수명/크기 조정”이 아니다. 기존 칼무리 VFX를 없애고 완전히 다른 실루엣으로 다시 만든다.

## 5. 문제 또는 리스크

지금 프리뷰는 계속 기존 칼날 느낌을 끌고 와서, 플레이어가 후보 차이를 판단할 수 없다. 이 상태로 실제 잔향에 이식하면 같은 문제가 반복된다.

## 6. GPT/Claude 인계 요약

다음 세션은 `V1GameManager.cs`의 현재 칼무리 프리뷰 헬퍼를 삭제하거나 우회하는 것부터 시작한다. 새 후보는 `KalmuriBladeSprite()`를 쓰지 않는다. 후보 실루엣은 아래처럼 완전히 달라야 한다.

- `K1`: 상처 입/톱니 검흔.
- `K2`: 긴 리본 궤적/잔상 띠.
- `K3`: 큰 기하학적 X/교차 폭발.
- `K4`: 저주 표식/사슬 링크 네트워크.

## 7. 다음 Codex 작업

1. `docs/orchestration/state/CURRENT_TASK.md`의 `Kalmuri VFX Hard Reset Handoff`부터 읽는다.
2. 현재 `DebugPreviewKalmuriWoundFeast`, `DebugPreviewKalmuriTrailBloom`, `DebugPreviewKalmuriCrossSwarm`, `DebugPreviewKalmuriMarkFrenzy`를 삭제하거나 새 구현으로 완전히 교체한다.
3. `KalmuriBladeSprite()`와 기존 칼무리 헬퍼를 새 프리뷰 후보에서 사용하지 않는다.
4. F12 프리뷰 라벨을 `K Preview HARD RESET`으로 바꾼다.
5. 각 후보가 최소 0.8초 이상 남아 화면에서 비교 가능하게 한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 칼무리 프리뷰가 기존 VFX 재사용 때문에 거의 바뀌지 않은 것처럼 보였다.
- 방향: 기존 칼날 언어를 버리고 완전히 다른 시각 문법으로 재설계한다.
- 행동: 다음 세션용 하드 리셋 지시를 상태/작업 문서에 기록했다.
- 결과: 집에서 이어 할 때 바로 현재 프리뷰 폐기와 새 실루엣 제작부터 시작할 수 있다.

# 2026-07-08-08 - 칼무리 VFX 하드 리셋 구현

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 F12 칼무리 미리보기 `K1`부터 `K4`까지 하드 리셋 버전으로 교체했다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- `K1`: 상처 입 / 톱니 흉터 방향.
- `K2`: 리본 궤적 / 매장 깃발 방향.
- `K3`: 기하학적 X / 십자 폭발 방향.
- `K4`: 저주 표식 / 체인 네트워크 방향.
- 쌍검은 빠르고 얇은 다단 히트 느낌으로, 대검은 넓고 무거운 단발 충격 느낌으로 분리했다.
- 미리보기 라벨을 `K Preview HARD RESET / HP 9999`로 바꿔 최신 버전 적용 여부를 바로 확인할 수 있게 했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.
- `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `totalKalmuri=268`.

## 4. 결정한 것

이번 단계에서는 실제 칼무리 Echo를 바로 교체하지 않고, 먼저 네 개 후보를 눈으로 비교하는 선택 도구로 만든다. jaewoo가 하나를 고르거나 섞을 방향을 정하면 그때 실제 전투 동작에 이식한다.

## 5. 문제 또는 리스크

자동 QA는 컴파일과 기본 성능만 보장한다. 최종 판단은 직접 플레이에서 “칼무리답게 보이는가”, “쌍검과 대검의 차이가 바로 읽히는가”를 보고 내려야 한다.

## 6. GPT/Claude 인계 요약

칼무리 후보는 이제 색만 다른 칼날 묶음이 아니라 완전히 다른 실루엣 네 개다. 다음 리뷰는 설명 없이 화면만 보고 K1-K4가 구분되는지, 그리고 쌍검/대검 분리가 유지되어야 하는지를 판단하면 된다.

## 7. 다음 Codex 작업

jaewoo가 F12에서 K1-K4를 직접 본 뒤 승자 또는 하이브리드를 지정하면, Codex는 해당 시각 문법을 실제 칼무리 Echo 런타임으로 변환한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 기존 칼무리 미리보기는 후보끼리 너무 비슷해 선택 기준이 생기지 않았다.
- 방향: 칼날 재사용을 줄이고 상처, 리본, 십자, 표식 네 가지 서로 다른 실루엣으로 재설계한다.
- 행동: F12 K1-K4 후보를 하드 리셋하고 쌍검/대검 변형을 각각 만들었다.
- 결과: 빌드와 Unity QA가 통과했고, 이제 직접 플레이로 후보를 고를 수 있는 상태가 됐다.

# 2026-07-08-09 - 칼무리 잔향 플레이어블 후보 정정

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에서 F12 `K1`~`K4`가 이제 단순 VFX 미리보기가 아니라 실제 칼무리 잔향 프로토타입 선택 버튼으로 동작한다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- jaewoo가 지적한 핵심을 반영했다: 목적은 “다르게 보이는 VFX”가 아니라 `굶주린 칼무리` 기억이 잔향으로 변했을 때의 재미와 납득감이다.
- F12 `K1`~`K4`를 누르면 실제 칼무리 Echo 후속타가 해당 후보 판정으로 발동된다.
- `K1`: 상처/입이 물어뜯는 원형-입 판정.
- `K2`: 무기 궤적을 따라 긋는 리본/띠 판정.
- `K3`: X/십자 교차 폭발 판정.
- `K4`: 표식에서 주변으로 이어지는 저주 체인 판정.
- 프리뷰용 1데미지와 실제 Echo 데미지 스케일을 분리했다.
- 디버그 UI에 `K real echo prototype: K#` 상태를 표시한다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning 7개, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.
- `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `totalKalmuri=268`.

## 4. 결정한 것

K1~K4는 더 이상 “보고 고르는 장식”이 아니다. 후보를 선택하고 실제로 적을 때려 보면서 VFX, 판정, 타격감, 무기별 어울림을 함께 판단한다.

## 5. 문제 또는 리스크

자동 QA는 기본 경로가 깨지지 않았음을 확인할 뿐, 네 후보의 재미와 정체성은 직접 플레이로 판단해야 한다. 특히 대검은 무겁고 한 방이 있어야 하고, 쌍검은 빠른 다단 감각이 살아야 한다.

## 6. GPT/Claude 인계 요약

칼무리 잔향 후보는 이제 실제 전투 후속타에 연결되어 있다. 다음 리뷰는 F12에서 K1~K4를 각각 켠 뒤 쌍검/대검으로 때려 보고, `굶주린 칼무리`가 잔향으로 변했다는 감각이 가장 강한 방향을 고르는 것이다.

## 7. 다음 Codex 작업

jaewoo가 하나를 고르거나 하이브리드를 지정하면, Codex는 해당 후보를 실제 최종 칼무리 Echo로 정리하고 기본 경로에 통합한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 이전 구현은 VFX 프리뷰에 머물러 실제 잔향의 재미를 판단하기 어려웠다.
- 방향: 후보를 실제 전투 판정과 연결해 플레이 감각으로 고를 수 있게 한다.
- 행동: K1~K4를 실제 Echo 프로토타입 선택 모드로 연결했다.
- 결과: 이제 플레이어가 후보별 잔향 손맛을 직접 비교할 수 있는 상태가 됐다.

# 2026-07-08-10 - 칼무리 후보 기존 투사체 억제

## 1. 현재 빌드 상태

`Dev_Prototype_v1`에서 F12 K1~K4 칼무리 후보를 켠 상태에서는 기존 +5 각성 칼날 투사체가 추가로 날아가지 않게 했다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- jaewoo가 “전에 있던 VFX가 날아가는 게 보인다”고 지적한 문제를 처리했다.
- K1~K4 후보 모드가 켜져 있으면 기존 `LaunchKalmuriBlade` 각성 투사체를 막는다.
- 후보 VFX 위에 옛 칼무리 칼날 언어가 겹쳐 보이는 상황을 줄였다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.

## 4. 결정한 것

K1~K4 후보 리뷰 중에는 기존 칼무리 +5 투사체를 섞지 않는다. 후보 자체의 콘셉트, VFX, 판정만 보고 판단한다.

## 5. 문제 또는 리스크

기존 투사체는 막았지만, jaewoo가 네 후보 모두 마음에 들지 않는다고 판단했으므로 다음 단계는 후보 polish가 아니라 칼무리 잔향의 핵심 이미지 재정의가 맞다.

## 6. GPT/Claude 인계 요약

현재 K1~K4는 기술적으로 플레이어블 후보지만, 디자인 방향은 재검토가 필요하다. `굶주린 칼무리`에서 떠오르는 이미지와 잔향 변환 원리를 다시 잡은 뒤 새 후보를 만드는 쪽이 맞다.

## 7. 다음 Codex 작업

jaewoo와 칼무리의 핵심 이미지를 다시 합의한 뒤, 기존 네 후보를 폐기하거나 크게 재구성한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 후보 VFX 위에 기존 칼무리 투사체가 겹쳐 보였다.
- 방향: 후보 리뷰 중에는 옛 칼날 언어를 차단한다.
- 행동: 프로토타입 모드에서 +5 각성 투사체 발동을 억제했다.
- 결과: 이제 후보 자체만 보고 판단할 수 있는 상태에 가까워졌다.

# 2026-07-08-11 - 칼무리다운 잔향 후보 재구성

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 F12 K1~K4 칼무리 후보를 다시 구성했다. 이번 버전은 “서로 다른 VFX”가 아니라 `굶주린 칼무리`라는 기억이 잔향으로 남았을 때의 이미지에 맞춘 네 가지 후보이다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- 기존 리본/십자/저주 체인 느낌의 후보를 칼무리 이미지와 맞지 않는 방향으로 보고 재구성했다.
- `K1`: 상처 포식 / 물어뜯는 칼무리.
- `K2`: 피냄새 추적 / 첫 상처가 냄새 표식이 되어 칼날 잔향이 끌려감.
- `K3`: 칼무리 식탁 / 적 주변에 이빨 같은 칼날이 차려졌다가 안쪽으로 접힘.
- `K4`: 씹힌 궤적 / 무기 궤적 위에 물린 자국과 파인 흉터가 이어짐.
- 쌍검은 빠른 다단 물어뜯기, 대검은 큰 상처와 무거운 포식감으로 분리했다.
- K2/K3/K4의 실제 판정 영역도 새 컨셉에 맞게 바꿨다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.
- `LETHE/V1 QA/Kalmuri Perf Matrix`: PASS, `totalKalmuri=268`.

## 4. 결정한 것

칼무리는 예쁜 궤적이나 추상 표식보다 상처, 피냄새, 굶주림, 이빨, 안쪽으로 닫히는 포식감이 핵심이다. 이후 후보도 이 언어에서 벗어나면 폐기한다.

## 5. 문제 또는 리스크

아직 직접 플레이 감각은 jaewoo 판단이 필요하다. 네 후보가 모두 별로라면, 이번 구현을 최종 후보가 아니라 칼무리 언어를 찾는 다음 출발점으로 본다.

## 6. GPT/Claude 인계 요약

K1~K4는 이제 모두 칼무리다운 포식 언어를 공유한다. 다음 리뷰는 어떤 후보가 가장 “기억이 잔향으로 바뀌었다”는 감각을 주는지, 그리고 쌍검/대검 분리가 충분한지 보는 것이다.

## 7. 다음 Codex 작업

jaewoo가 선택하거나 탈락 이유를 주면, Codex는 선택 후보를 최종 칼무리 Echo로 정리하거나 다음 후보 세트를 다시 만든다. 동시에 혈반 외 잔향과 무기별 개성도 같은 기준으로 점검해야 한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 기존 네 후보는 칼무리보다 일반 스킬처럼 보였다.
- 방향: 상처와 포식이라는 핵심 이미지로 후보를 다시 만든다.
- 행동: K1~K4 VFX와 판정 의미를 wound/scent/feast/trail로 재구성했다.
- 결과: 칼무리다운 잔향 후보를 직접 플레이로 다시 비교할 수 있는 상태가 됐다.

# 2026-07-08-12 - 쌍검 칼무리 빨간 원 판독 수정

## 1. 현재 빌드 상태

`Dev_Prototype_v1`의 쌍검 칼무리 후보에서 크게 보이는 빨간 원형 표식을 줄였다. 아직 `_dev` 검증 단계이며 `Assets/Lethe` 승격은 하지 않았다.

## 2. 오늘 바뀐 것

- 쌍검 칼무리가 빨간 원으로 읽히는 문제를 의도와 맞지 않는 것으로 판단했다.
- 쌍검 후보에서 큰 링/디스크를 줄이고 짧은 상처선, 이빨 스냅, 작은 물림/파편 표식으로 바꿨다.
- 대검은 큰 상처/식탁/파인 흉터처럼 무거운 판독이 필요하므로 일부 큰 실루엣을 유지했다.

## 3. 테스트 결과와 근거

- `dotnet build LETHE/Assembly-CSharp.csproj --nologo`: 기존 legacy warning, error 0개.
- `dotnet build LETHE/Assembly-CSharp-Editor.csproj --nologo`: 순차 재실행 기준 warning 0개, error 0개.
- Unity compilation errors: `0`.
- Unity console errors: `0`.

## 4. 결정한 것

쌍검 칼무리는 원형 범위 표시가 아니라 빠른 다단 물림과 짧은 상처가 중심이어야 한다. 원형 판독은 대검의 큰 포식/식탁 후보에서만 제한적으로 허용한다.

## 5. 문제 또는 리스크

쌍검 쪽 원형 판독은 줄였지만, 직접 플레이에서 여전히 빨간 덩어리처럼 보이면 색/알파보다 형태 자체를 더 잘게 찢어야 한다.

## 6. GPT/Claude 인계 요약

쌍검 칼무리는 큰 빨간 원이 아니라 빠른 물림 흔적이어야 한다. 다음 리뷰는 K1~K4 쌍검 후보가 원형 표식이 아니라 다단 포식으로 읽히는지 확인하면 된다.

## 7. 다음 Codex 작업

jaewoo가 다시 확인한 뒤 여전히 원처럼 보이는 후보가 있으면 해당 후보의 링/디스크류를 완전히 제거하고 선형 상처/작은 이빨 파편으로 대체한다.

## 8. 포트폴리오 메모: 문제, 방향, 행동, 결과

- 문제: 쌍검 칼무리 후보가 빨간 원으로 보였다.
- 방향: 쌍검은 빠른 다단 물림, 대검은 큰 포식 실루엣으로 분리한다.
- 행동: 쌍검의 큰 링/디스크를 줄이고 물림 자국 계열로 바꿨다.
- 결과: 쌍검 칼무리 판독이 원형 표식에서 상처/이빨 방향으로 이동했다.
