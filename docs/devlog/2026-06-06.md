# 2026-06-06 Devlog

## v0.12 Browser First Boss TTK Terminal

- Goal: stabilize browser `first_boss_ttk` accepted sample collection at first boss HP `2800`.
- Changed `src/game.js` so `balanceScenario=first_boss_ttk` exits as `complete` as soon as the first boss TTK sample is recorded.
- Changed `scripts/run_browser_balance_qa.js` so timeout wrappers preserve the last page QA payload when available.
- Changed `scripts/run_browser_balance_qa.js` so the `first_boss_ttk` scenario uses first-boss-only checks instead of full-run clear, level-up, and slot-fill checks.
- Verification:
  - `node --check src/game.js`: pass.
  - `node --check scripts/run_browser_balance_qa.js`: pass.
  - `node scripts\run_browser_balance_qa.js --scenario first_boss_ttk --runs 3 --run-sec 230 --timeout-ms 60000 --report docs\balance\2026-06-06-v012-browser-first-boss-ttk-terminal.md --out alpha_test\outputs\balance-browser-ttk-terminal`: `GO_BALANCE_BASELINE`, 3/3 accepted, first boss clear `100%`, TTK median `25.76s`.
  - `npm run qa:balance`: `ITERATE_BALANCE`, first boss clear `80%`, death `20%`, first boss TTK median `27.79s`, level-ups before first boss median `11`, full clear `0%`.
- Decision: first boss TTK sampling is no longer the blocker. Keep HP `2800`; move the next balance task to post-boss/full-run flow.
- Risk: full browser balance still fails because full clear remains `0%`, and one run died around `156.09s` during the forget-warning phase.
- Report/Discord:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - `npm run report:discord:unit:dry`: pass.
  - Next trusted-local command: `npm run report:discord:unit`.

## v0.12 Post-Boss Balance Baseline

- Goal: move from first-boss TTK stability to post-boss/full-run flow.
- Fixed `resolveBalanceInterrupts()` so the cycle result continue button is clicked before the zero-active-memory guard can stop the QA loop in `questions` mode.
- Added post-boss spawn caps:
  - deficit breath `16`,
  - deficit trial `22`,
  - later-cycle default `58`.
- Changed browser balance QA default run window from `608s` to `690s`, because the scheduled final boss spawns at `600s`.
- Changed first boss HP from `2800` to `2500` after full QA post-boss flow passed but first boss TTK median was still above the 30s target.
- Verification:
  - `node --check src/game.js`: pass.
  - `node --check scripts/run_boss_ttk_harness.js`: pass.
  - `node --check scripts/run_browser_balance_qa.js`: pass.
  - `node scripts\run_boss_ttk_harness.js --report docs\balance\2026-06-06-v012-boss-ttk-hp2500.md --out alpha_test\outputs\boss-ttk-hp2500`: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK median `15.62s`.
  - `node scripts\run_browser_balance_qa.js --scenario first_boss_ttk --runs 3 --run-sec 230 --timeout-ms 60000 --report docs\balance\2026-06-06-v012-browser-first-boss-ttk-hp2500.md --out alpha_test\outputs\balance-browser-ttk-hp2500`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK median `21.05s`.
  - `npm run qa:balance`: `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `40%`, death `60%`, first boss TTK median `22.24s`.
- Decision: v0.12 has an automated balance baseline. Next step is reviewer/GPT interpretation of whether deficit-trial death concentration is acceptable tension or needs another design pass.
- Risk: death remains high at `60%`, concentrated in deficit trial. This passes the current automated clear-rate gate but still needs design judgment before treating it as human-ready.
- Report/Discord:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - `npm run report:discord:unit:dry`: pass.
  - Next trusted-local command: `npm run report:discord:unit`.

## v0.12 Deficit Trial Survival Tuning

- Goal: reduce death from `60%` to `<= 40%` while keeping full clear in the automated `35-80%` target band and first boss TTK in `15-30s`.
- Changed deficit duration from `75s` to `60s`.
- Changed deficit trial cap from `22` to `16`.
- Changed pre-boss XP multiplier from `1.75` to `1.95`.
- Refill now restores HP to at least `85%` and grants shield `18`.
- Survival stat now gives max HP `+16`, heal `+28`, and damage reduction `+5%`.
- Low-HP level-ups now force-offer survival if it has not already been taken.
- Balance QA now chooses survival below `78%` HP.
- Verification:
  - `node --check src/game.js`: pass.
  - `node --check scripts/run_boss_ttk_harness.js`: pass.
  - `node --check scripts/run_browser_balance_qa.js`: pass.
  - `node scripts\run_boss_ttk_harness.js --report docs\balance\2026-06-06-v012-boss-ttk-survival-choice.md --out alpha_test\outputs\boss-ttk-survival-choice`: `GO_BOSS_TTK_SAMPLE`, 5/5 accepted, TTK median `15.62s`.
  - `node scripts\run_browser_balance_qa.js --scenario first_boss_ttk --runs 3 --run-sec 230 --timeout-ms 60000 --report docs\balance\2026-06-06-v012-browser-first-boss-ttk-survival-choice.md --out alpha_test\outputs\balance-browser-ttk-survival-choice`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK median `21.04s`.
  - `npm run qa:balance`: `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `80%`, death `20%`, first boss TTK median `27.84s`.
- Decision: death target is met, but full clear is exactly at the automated upper bound.
- Risk: this may now be too forgiving for the intended tension; reviewer/GPT should decide whether to accept or reintroduce small late pressure.
- Report/Discord:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - `npm run report:discord:unit:dry`: pass.
  - Next trusted-local command: `npm run report:discord:unit`.
