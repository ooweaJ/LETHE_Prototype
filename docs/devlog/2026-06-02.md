# 2026-06-02

## Summary

Normalized the Codex operating workflow for LETHE so longer tasks can be handed off, paused, resumed, and reported more safely.

## Built

- Added `scripts/send_codex_notice.js` for short Discord status notices.
- Added `docs/CODEX_RUNBOOK.md` with the standard Codex work loop.
- Added `docs/checkpoints/README.md` with the interruption checkpoint template.
- Added `npm run codex:notice` and `npm run codex:notice:dry`.

## Decisions

- Discord can notify the user that Codex may need attention, but approval still happens in the Codex UI.
- GPT is the default planning reviewer; Claude is optional for emotional/narrative review.
- Significant interruptions should be recorded in `docs/checkpoints/YYYY-MM-DD.md`.

## Verification

- `node --check scripts/send_codex_notice.js`: passed.
- `node scripts/send_codex_notice.js --dry-run --type=approval ...`: verified the custom notice output shape.
- `node scripts/build_report.js docs/reports/2026-06-02.md`: generated the daily HTML report.
- `node scripts/send_codex_notice.js --type=done ...`: sent the workflow normalization completion notice to Discord.

## Next

- Run syntax checks and notice dry-run.
- Generate the daily report HTML.
- Send a completion notice to Discord if verification passes.

## Addendum: GPT v0.2 Planning Answer

- Recorded the GPT answer in `docs/review_responses/2026-06-02-gpt-v02.md`.
- Verdict: `ITERATE_BEFORE_TEST`.
- Converted the answer into Korean Codex tasks in `docs/NEXT_TASKS.md`.
- Updated the review prompt template so future GPT/Claude prompts ask for Korean output and a clear "앞으로 해야 할 일" section.
- Updated Discord report summary generation to prefer Korean headings and Korean labels.
- Updated report commands to use the latest dated Markdown report by default.
- Rewrote `docs/DISCORD_REPORTING.md` in Korean.
- Sent the updated Korean Discord report with `npm run report:discord`.

## Addendum: Claude Code Review Automation

- Added root `CLAUDE.md` with Claude Code role rules.
- Added `scripts/ask_claude_review.js`.
- Added `npm run review:claude` and `npm run review:claude:dry`.
- Verified dry-run path selection for `docs/review_prompts/2026-06-01.md`.
- Ran Claude Code non-interactively with tools disabled.
- Saved the Claude response to `docs/review_responses/2026-06-01-claude.md`.
- Merged Claude's additions into `docs/NEXT_TASKS.md`, including echo preset comparison and a memory-name free response check.
- Sent a Discord completion notice for Claude automation.

## Addendum: v0.2 Narrow Tuning Implementation

- Delayed first forgetting pacing to the 8-10 minute target window by moving the boss spawn and AI encounter timing to a 9 minute average.
- Changed the experiment default to `echo=0.50`, `ui=0.62`.
- Reduced deletion dominance for `처형자의 섬광` and lightly tuned other deletion weights so one memory does not represent the whole test.
- Added deletion score/weight logging to the browser JSON export and AI raw logs.
- Added selected/predicted/deleted memory names to the browser log for easier review.
- Improved the forgetting result screen so it shows the lost memory, prediction result, deletion weight, echo effect, and next direction.
- Added a Q3 free-response field asking whether the player can recall the forgotten memory name.
- Updated AI test criteria with prediction upper bound, recovery target, deletion distribution checks, and v0.2 defaults.

## Addendum Verification

- `node --check src/game.js`: passed.
- `node --check alpha_test/src/config.js`: passed.
- `node --check alpha_test/src/simulator.js`: passed.
- `node --check alpha_test/src/metrics.js`: passed.
- `node --check alpha_test/src/run_alpha.js`: passed.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.7603`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.
- `npm run ai:sweep`: completed and wrote sweep outputs.

## Addendum Result

- v0.2 is now an AI-tested human-test candidate.
- Main residual risk: post-forgetting power drop is `29.6%`, slightly below the `30%` target.
- Next practical step: browser visual QA, then a 5-8 player human test focused on regret vs irritation.

## Addendum: Local Pull Follow-up

- Pulled the latest `main` changes into this local workspace with a fast-forward update.
- Re-ran `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`, regret `76.3%`, irritation `1.1%`, prediction `76.3%`.
- Re-ran `npm run report` and regenerated `docs/reports/2026-06-02.html`.
- Browser QA confirmed the start screen and run start flow; result screen and JSON download still need a full browser pass.
- Added `docs/HUMAN_PLAYTEST_GUIDE.md` for the 5-8 player v0.2 human test.

## Addendum: v0.3 Claude Planning Prompt

- Added `docs/review_prompts/2026-06-02.md` for v0.3/version-up planning review.
- Verified `npm run review:claude:dry` now selects `docs/review_prompts/2026-06-02.md` and writes to `docs/review_responses/2026-06-02-claude.md`.
- Attempted `npm run review:claude`, but this local shell does not currently have the `claude` executable in PATH.
- Next step: run the same command from a local environment where Claude Code is installed, or add Claude Code to PATH before rerunning.

## Addendum: Browser QA And Human-Test Gate

- Fixed browser-facing v0.2 labels in `index.html`.
- Aligned the browser boss/forgetting timing with the v0.2 9-minute target.
- Added `?qa=fast` so the question/result/JSON path can be verified without waiting 9 minutes.
- Fixed result-screen wording that produced awkward particles such as `추적자의 맹세이`.
- Changed the result summary to a two-column desktop layout so the survey and download button fit without panel scroll in the QA viewport.
- Added cache-busting query strings to the static CSS/JS references.
- Browser QA result: question screen reached, result screen displayed, survey enabled download, and JSON payload contained selected memory names, prediction/protection names, forgotten memory, deletion weights, survey, echo, and experiment fields.
- Re-ran `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.
- Human playtest is deferred until combat spectacle and LETHE-system readability are stronger.

## Addendum: Codex CLI Fallback And v0.3 Polish

- Started v0.3 pre-human-test polish without adding new content scope:
  - floating memory names,
  - damage numbers,
  - hit sparks and projectile trails,
  - boss spawn/phase impact feedback,
  - `레테의 시선` and dependency percent on memory slots.
- Browser QA confirmed `HTML Alpha Prototype v0.3`, question/result flow, no console errors, result panel without internal scroll, and JSON payload with `experiment.version: v0.3`.
- `node --check src/game.js`: passed.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.7798`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.

## Addendum: Codex CLI Review Fallback

- Confirmed the local `codex` executable is available at `/Applications/Codex.app/Contents/Resources/codex`.
- Confirmed `codex exec --output-last-message <file>` can write the final terminal answer to a file.
- Added `scripts/ask_codex_review.js`.
- Added `npm run review:codex` and `npm run review:codex:dry`.
- The Codex CLI fallback writes to `docs/review_responses/YYYY-MM-DD-codex.md`.
- This is the preferred GPT/Codex terminal path when the user wants to avoid direct OpenAI API key setup.

## Addendum: v0.4 Human-Test Candidate Polish

- Upgraded the browser prototype label and experiment version to v0.4.
- Raised the default UI clarity test value from `0.62` to `0.78` to match the stronger in-game dependency and forgetting-risk UI.
- Split the result screen into clearer loss and echo-transformation messaging:
  - `사라진 행동`,
  - `잔향 변형`,
  - echo stat badges,
  - next direction.
- Added `echoTransformation` to the JSON payload.
- Browser QA confirmed v0.4 labels, result cards, no console errors, no internal result-panel scroll, and payload fields including `echoTransformation`.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8190`, regret `86.2%`, irritation `0.0%`, prediction `87.0%`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8261`, regret `85.6%`, irritation `0.4%`, prediction `85.8%`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.8369`, regret `87.6%`, irritation `0.3%`, prediction `87.7%`.
- `npm run ai:sweep`: confirmed `echo=0.50`, `ui=0.78` remains the best current `GO_CANDIDATE` preset.
- Remaining soft warning: post-forgetting power drop is still just below target at roughly `29.4-29.6%`.

## Addendum: Claude Automation Offline Verification

- Confirmed `npm run review:claude:dry` still selects `docs/review_prompts/2026-06-02.md`.
- Confirmed the expected real Claude output path is `docs/review_responses/2026-06-02-claude.md`.
- Confirmed the local `claude` command is installed.
- Attempted the real `npm run review:claude` path, but Codex was not allowed to send repository prompt content to the external Claude service from this session.
- Added `--mock-response` to `scripts/ask_claude_review.js` so the automation write path can be verified offline.
- Verified `node --check scripts/ask_claude_review.js`.
- Verified `node scripts/ask_claude_review.js --output alpha_test/outputs/claude-review-mock.md --mock-response ...` writes a response file without external transmission.
- Current conclusion: Claude automation plumbing is healthy; actual Claude planning review should be run only when the user explicitly approves external prompt transmission from a suitable local environment.

## Addendum: v0.5 Core-Fun Pass

- User playtest feedback changed the priority: the prototype felt too loose before memory/forgetting mattered.
- Updated `docs/review_prompts/2026-06-02.md` to ask for core-fun/loguelike/Vampire-Survivors-style early loop review.
- Attempted real Claude automation, but external prompt transmission was blocked in this Codex session.
- Implemented the same narrowed direction in Codex:
  - denser early enemy packs,
  - kill XP,
  - run-only level-up choices,
  - stat choices for attack speed, damage, range, survival, and XP gain.
- Added `runGrowth` to the browser JSON payload.
- Added AI early-fun gates:
  - `Early Fun Score`,
  - `earlyKillTempo`,
  - `earlyLevelUps`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8531`, Early Fun `0.8669`, kill tempo `0.9620`, level-ups `4.08`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.8509`, Early Fun `0.8672`, kill tempo `0.9636`, level-ups `4.08`.
- Browser plugin QA was attempted, but the in-app browser failed to start because of a Windows sandbox startup error.
- Current conclusion: v0.5 is AI-ready for human testing focused first on early fun, then on forgetting emotion.

## Addendum: Review Automation Cleanup

- Investigated why no Discord notice arrived after v0.5:
  - `codex:notice:dry` generated the expected Discord message.
  - The actual notice was not sent earlier because the send command was not called after the v0.5 commit.
  - A real Discord send attempt from this session was blocked because it would transmit project status to an external webhook.
- Checked Claude properly:
  - `Get-Command claude`: found local Claude CLI.
  - `claude --version`: `2.1.153 (Claude Code)`.
  - `npm run review:claude:dry`: prompt/output path works.
  - minimal non-project prompt through Claude failed with `401 Invalid authentication credentials`.
- Removed the OpenAI API fallback by request:
  - deleted `scripts/ask_openai_review.js`,
  - removed `review:openai` and `review:openai:dry`,
  - updated runbook/status/review-prompt docs.
- Current review order: Claude Code first, then Codex CLI fallback. No third OpenAI API fallback.

## Addendum: Work-Unit Reporting

- Clarified that Markdown/HTML reports are still stored by date, but Discord can send either daily summaries or work-unit summaries.
- Added `--section` and `--latest-section` support to `scripts/send_discord_report.js`.
- Added `npm run report:discord:unit` and `npm run report:discord:unit:dry`.
- Verified work-unit dry-run for `v0.5 초반 재미 보완 보고`.
- Updated `docs/DISCORD_REPORTING.md` and `docs/CODEX_RUNBOOK.md` with the daily vs work-unit distinction.
- Updated `scripts/ask_claude_review.js` so Claude `401 Invalid authentication credentials` errors tell the user to run `claude` locally, complete login/authentication, and retry `npm run review:claude`.

## Addendum: Claude v0.5 Evaluation

- Saved the first Claude core-fun planning response to `docs/review_responses/2026-06-02-claude.md`.
- Updated `docs/review_prompts/2026-06-02.md` into a v0.5 implementation evaluation prompt.
- The local Claude evaluation was saved to `docs/review_responses/2026-06-02-claude-v05-eval.md`.
- Claude verdict: `ITERATE_BEFORE_TEST`, but only because real-browser verification of the v0.5 level-up UI and `runGrowth` JSON is still missing.
- Claude says the build can move to human testing once:
  - level-up 3-choice UI appears,
  - choosing an upgrade resumes combat,
  - JSON `runGrowth` matches the actual selected upgrades,
  - level-up pause/resume does not break input, timer, or spawning.
- Updated `docs/NEXT_TASKS.md` and `docs/CODEX_STATUS.md` with this gate.

## Addendum: v0.5 Level-Up Browser Gate

- Added `?qa=fast,levelup` automated browser QA mode to `src/game.js`.
- Because the in-app Browser plugin still fails with a Windows sandbox startup error, ran the gate through installed Chrome headless.
- Chrome headless QA result:
  - `status: complete`,
  - `levelUpSeen: true`,
  - `resumedAfterUpgrade: true`,
  - `hasRunGrowthPayload: true`,
  - selected upgrade appears in both runtime `choicesTaken` and payload `runGrowth.choicesTaken`.
- `node --check src/game.js`: passed.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8607`, Early Fun `0.8660`.
- Claude's remaining v0.5 gate is now passed; v0.5 can move to human testing.

## Addendum: Project Goal And Claude Role Clarification

- Clarified the project goal across agent/runbook/status docs:
  - validate LETHE's core fun in an HTML prototype first,
  - move to Unity only after AI and human tests show enough promise.
- Reframed Claude/GPT from generic reviewers into planning partners that interpret AI/human test results, revise the design direction, and decide what Codex should implement next.
- Updated `docs/NEXT_TASKS.md` with Unity transition gates based on early fun, level-up choice quality, forgetting emotion, restart intent, and player belief that the idea is worth expanding.
- Kept Codex responsible for implementation, verification, reports, commits, and turning Claude/GPT answers into concrete tasks.

## Addendum: Test-Result Planning Pipeline

- Added `scripts/run_planning_pipeline.js`.
- Added npm scripts:
  - `planning:pipeline`,
  - `planning:pipeline:dry`,
  - `planning:pipeline:prompt`,
  - `planning:pipeline:codex`,
  - `planning:pipeline:claude`.
- The pipeline runs a quick AI test by default, creates `docs/review_prompts/YYYY-MM-DD-pipeline.md`, asks Claude first, and falls back to Codex CLI if Claude fails.
- `planning:pipeline:prompt` runs the safe prompt-generation path without external model transmission.
- Verified `npm run planning:pipeline:prompt`: quick AI test passed and `docs/review_prompts/2026-06-02-pipeline.md` was written.
- Verified `review:claude:dry` and `review:codex:dry` now select the latest pipeline prompt and write to pipeline-specific response paths.
- The actual external provider step was blocked in this Codex session because it would export repository prompt content; the user can run `npm run planning:pipeline` from a trusted local terminal.
- Updated Claude/Codex planning prompt wrappers so they describe the role as test-result interpretation and planning revision, not generic review.
- Updated prompt discovery to accept dated prompt suffixes such as `YYYY-MM-DD-pipeline.md`.
- Updated Discord work-unit reports to attach the latest same-date planning prompt, including suffix prompts.

## Addendum: Claude Pipeline Response And Human-Test Logging

- Read `docs/review_responses/2026-06-02-pipeline-claude.md`.
- Claude verdict: `GO_TO_HUMAN_TEST`.
- Added tester ID and session number inputs to the start screen.
- Added `playtest.testerId` and `playtest.sessionId` to JSON logs.
- Updated JSON log filenames to include tester/session metadata when provided.
- Updated `docs/HUMAN_PLAYTEST_GUIDE.md` to v0.5 and expanded `docs/PLAYTEST_NOTES.md` for early fun, growth choice, forgetting reaction, restart intent, and Unity possibility notes.
- Verified `node --check src/game.js`.
- Verified `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8607`.
- Verified Chrome headless QA with `?qa=fast,levelup&tester=T01&session=S01`: level-up/runGrowth passed and payload included `playtest.testerId: T01`, `playtest.sessionId: S01`.

## Addendum: AI Collaboration Portfolio Docs And Local Doctor

- Added `docs/ai/README.md`.
- Added `docs/ai/ai-collaboration.md` to record AI roles, workflow, evidence rules, prompt rules, and developer control rules.
- Added `docs/ai/ai-failure-cases.md` to record blocked or wrong AI/automation cases and developer fixes.
- Added ADRs:
  - `docs/adr/ADR-001-html-prototype-before-unity.md`,
  - `docs/adr/ADR-002-test-result-planning-pipeline.md`.
- Added `docs/portfolio/LETHE-case-study.md`.
- Added `scripts/check_local_pipeline.js`.
- Added `npm run doctor` and `npm run doctor:deep` so another local machine can check tools, npm scripts, required role/rule docs, environment hints, and safe dry-runs.
- Verified `npm run doctor`: 24 pass, 0 warn, 0 fail.
- Verified `npm run doctor:deep`: 30 pass, 0 warn, 0 fail.

## Addendum: Human Playtest Summary Automation

- Added `playtest_logs/README.md`.
- Added `docs/playtest_summaries/README.md`.
- Added `scripts/summarize_playtests.js`.
- Added npm scripts:
  - `playtest:summary`,
  - `playtest:summary:dry`.
- Raw human JSON logs under `playtest_logs/` are ignored by git.
- `npm run playtest:summary` reads human JSON logs, writes `docs/playtest_summaries/YYYY-MM-DD.md`, and creates `docs/review_prompts/YYYY-MM-DD-human-playtest.md`.
- Updated `docs/HUMAN_PLAYTEST_GUIDE.md`, `docs/CODEX_RUNBOOK.md`, `docs/ai/ai-collaboration.md`, and `docs/adr/ADR-002-test-result-planning-pipeline.md`.
- Verified `node --check scripts/summarize_playtests.js`.
- Verified `npm run playtest:summary:dry`.
- Verified `npm run playtest:summary`: generated a 0-log preparation summary and human-test prompt.
- Verified `npm run doctor`: 26 pass, 0 warn, 0 fail.
- Verified `npm run doctor:deep`: 34 pass, 0 warn, 0 fail.

## Addendum: Human Playtest Package Automation

- Added `scripts/prepare_playtest_build.js`.
- Added npm scripts:
  - `playtest:package`,
  - `playtest:package:dry`.
- Added `dist/` to `.gitignore`.
- `npm run playtest:package` writes `dist/lethe-v0.5-playtest` with static game files and playtest instructions.
- Updated `docs/HUMAN_PLAYTEST_GUIDE.md`, `docs/CODEX_RUNBOOK.md`, `docs/CODEX_STATUS.md`, and `docs/NEXT_TASKS.md`.
- Verified `node --check scripts/prepare_playtest_build.js`.
- Verified `npm run playtest:package:dry`.
- Verified `npm run playtest:package`.
- Verified `npm run doctor:deep`: 38 pass, 0 warn, 0 fail.

## Addendum: Run Structure Redesign Prompt

- User direct playtest overrode the previous v0.5 human-test gate.
- Main finding: the boss/forgetting event arrives too late, so current testing would mostly measure boredom before LETHE's core loop appears.
- Added `docs/review_prompts/2026-06-02-run-structure-redesign.md`.
- The prompt asks Claude/GPT to choose between:
  - a 25-minute run with boss/forgetting every 5 minutes,
  - a 20-minute run with larger late-run forgetting and a final memory engraving,
  - or a hybrid structure.
- Updated `docs/NEXT_TASKS.md` and `docs/CODEX_STATUS.md` so the next gate is v0.6 run-structure planning, not broad human testing.

## Addendum: Autopilot Preflight Rules

- Added `scripts/autopilot_preflight.js`.
- Added npm scripts:
  - `autopilot:preflight`,
  - `autopilot:preflight:local`,
  - `autopilot:preflight:dry`.
- Preflight checks git cleanliness, required npm scripts, doctor deep, Claude/Codex dry-runs, Discord work-unit dry-run, Claude command availability, Codex fallback readiness, Discord env hints, and optional live Claude auth.
- Updated `AGENTS.md`, `docs/CODEX_RUNBOOK.md`, `docs/ai/ai-collaboration.md`, and `docs/CODEX_STATUS.md` so known auth/permission/tooling risks must be checked before unattended automation starts.
- Added preflight dry-run and syntax checks to the local doctor deep path.

## Addendum: v0.6 Core Run Structure

- Ran `npm run autopilot:preflight`: 17 pass, 0 warn, 0 fail.
- Implemented v0.6 core cycle:
  - first boss at 4 minutes,
  - boss schedule at 4 / 8 / 12 / 16 / 20 minutes,
  - boss defeat -> dependency-based memory loss,
  - 2-memory deficit survival,
  - memory refill from 3 candidates,
  - `runTimeline` payload with cycles and refill choices.
- Added `?qa=fast,v06` browser QA mode.
- Updated AI simulation targets from 8-10 minute first forgetting to 3.5-4.5 minute first cycle.
- Added AI metrics:
  - first cycle completion,
  - two-memory survival,
  - echo pivot score,
  - refill reached rate.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.9093`, first cycle completion `82.4%`, two-memory survival `80.5%`, echo pivot `0.7456`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.9095`, first cycle completion `81.8%`, two-memory survival `80.2%`, echo pivot `0.7415`.
- Chrome headless `?qa=fast,v06`: status `complete`, boss -> forgetting -> deficit -> refill payload passed.
- Chrome headless `?qa=fast,levelup&tester=T01&session=S01`: level-up regression still passed.
- Added `docs/review_prompts/2026-06-02-v06-cycle-eval.md` for Claude v0.6 evaluation.
- Attempted the actual Claude v0.6 call, but outbound project-summary transfer was blocked in this Codex session.
- Saved the block note to `docs/review_responses/2026-06-02-v06-cycle-claude-blocked.md`.

## Addendum: v0.7 Automated Balance Pass

- User solo feedback after v0.6: after memory loss, the player becomes too weak and cannot reliably kill normal mobs.
- Ran `npm run autopilot:preflight`: 17 pass, 0 warn, 0 fail.
- Implemented v0.7 as an automated balance/design iteration:
  - buffed `절단쌍검` baseline damage/range/interval,
  - buffed `장송대검` baseline damage/range/interval,
  - added weapon-facing echo effects when memories are lost,
  - added side-panel labels for weapon echo residue,
  - updated AI simulator weapon baseDps and weapon residue echo proxy.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.9339`, first cycle completion `82.1%`, two-memory survival `81.6%`, echo pivot `0.7363`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.9131`, first cycle completion `84.1%`, two-memory survival `81.5%`, echo pivot `0.7349`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.9136`, first cycle completion `82.7%`, two-memory survival `81.1%`, echo pivot `0.7286`.
- Chrome headless cycle QA passed with version `v0.7` and runTimeline `v0.7`.
- Chrome headless level-up regression QA still passed.
- Added `docs/review_prompts/2026-06-02-v07-balance-eval.md`.
- Added local Codex judgment at `docs/review_responses/2026-06-02-v07-balance-codex.md`.

## Addendum: v0.7 Claude Loop Closure

- Sent the v0.7 work-unit report to Discord with the real `npm run report:discord:unit` path.
- Ran the actual Claude v0.7 balance evaluation:
  - prompt: `docs/review_prompts/2026-06-02-v07-balance-eval.md`,
  - output: `docs/review_responses/2026-06-02-v07-balance-claude.md`.
- Claude verdict: `GO_TO_SOLO_PLAYTEST`.
- Claude's main reason: v0.7 directly addressed the v0.6 post-loss mob-clearing failure, and the remaining questions are human-feel questions that AI simulation cannot settle.
- Created `docs/playtest/2026-06-02-solo.md` so the next run records:
  - the first 30 seconds after forgetting,
  - post-loss mob clearing,
  - weapon echo visibility,
  - prediction causes,
  - restart desire.
- Updated `scripts/prepare_playtest_build.js` so `npm run playtest:package` includes `SOLO_PLAYTEST_SHEET.md`.
- Verified `npm run playtest:package`, `npm run report:discord:unit`, and `npm run doctor:deep`.
- Decision: do not auto-create v0.7.1 until the user solo test shows whether the problem is balance, visibility, or core feel.

## Addendum: v0.7 Balance Verdict Correction

- User live feedback invalidated the v0.7 balance judgment: the balance is not close enough.
- Corrected the interpretation of the Claude response:
  - it was based on AI proxy metrics and implementation summary,
  - it was not real balance feedback from actual play.
- Added `docs/review_prompts/2026-06-02-v071-balance-reality-check.md`.
- Added an AI failure case: balance proxy was mistaken for real balance feedback.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, and the daily report so the next gate is balance reality checking before v0.7.1 tuning.
- Root lesson: AI simulator pass, browser flow QA pass, and human balance pass must be reported as separate evidence.

## Addendum: v0.8 Double Check And Gate A Start

- Changed the planning pipeline default provider to `double`, so major design decisions ask both Claude and Codex CLI.
- Added `planning:pipeline:double` and preserved the older fallback route as `planning:pipeline:auto`.
- Fixed custom prompt handling in `scripts/run_planning_pipeline.js` so `--prompt` uses the existing prompt instead of overwriting it.
- Updated Codex CLI review invocation for the current CLI option set.
- Added v0.8 redesign prompt: `docs/review_prompts/2026-06-02-v08-core-redesign.md`.
- Received v0.8 Claude and Codex CLI responses and summarized them in `docs/review_responses/2026-06-02-v08-core-redesign-double-check.md`.
- Started v0.8 Gate A:
  - version label moved to v0.8,
  - HP 1 death-prevention removed,
  - actual death/run-end overlay added,
  - `death` and `danger` log payloads added,
  - `?qa=fast,death` QA mode added.
- Chrome headless QA passed for:
  - `?qa=fast,v06` cycle flow with v0.8 payload,
  - `?qa=fast,levelup&tester=T01&session=S01`,
  - `?qa=fast,death`.

## Addendum: v0.8 Gate B Automated Balance Loop

- Continued the automation loop from v0.8 Gate A into Gate B.
- Implemented the v0.8 core structure:
  - 9-minute prototype run,
  - first mini-boss and first forgetting at 90 seconds,
  - later bosses at 210 / 360 / 510 seconds,
  - memory tag badges,
  - three minimal synergies:
    - `area + control` -> 압축 파문,
    - `dot + control` -> 느린 출혈,
    - `burst + survival` -> 피 묻은 결의,
  - tag echoes after forgetting,
  - reduced generic stat refund from lost-memory echoes,
  - real death/failure-rate checks in AI alpha metrics.
- First Gate B quick loop failed:
  - `NO_GO_FIX_CORE`,
  - regret `26.7%`,
  - prediction `28.3%`.
- Diagnosis: probabilistic deletion made the lost memory feel random, so the core "you lose what you depended on" fantasy weakened.
- Restored deterministic highest-dependence deletion.
- Corrected the post-forget drop target from `30-40%` to `20-35%` because the user's live feedback was already that post-loss combat was too weak.
- A 5000-run heavy loop then failed by a small margin:
  - death/fail `70.7%`,
  - target max `70%`.
- Softened stage/boss difficulty:
  - simulator boss difficulty now uses `94 + stageIndex * 13`,
  - browser late-boss HP scaling now uses `18%` instead of `22%` per late boss step.
- Final AI loop results:
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, death/fail `40.0%`.
  - `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8909`, death/fail `45.1%`.
  - `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.8893`, death/fail `67.7%`.
- Verified:
  - `node --check src/game.js`,
  - `node --check alpha_test/src/config.js`,
  - `node --check alpha_test/src/simulator.js`,
  - `node --check alpha_test/src/metrics.js`,
  - `node --check alpha_test/src/report.js`,
  - `node --check alpha_test/src/run_alpha.js`,
  - `npm run doctor:deep`: 45 pass, 0 warn, 0 fail.
- Browser plugin QA could not run in this Codex session because `iab` was unavailable.
- Chrome CLI also returned no `--dump-dom` output because it handed off to an existing local browser session.
- Decision: v0.8 Gate B is AI-pass, but v0.8 Gate C must add a stable browser combat QA runner before asking for user 1-person playtest.

## Addendum: v0.9 Release-Feel Loop Preparation

- User rejected a straight v0.8 Gate C path because browser QA alone would likely lead to a not-fun human test.
- Reframed the next step as v0.9 release-feel preparation:
  - research successful roguelike/survivor-like fun structures,
  - translate them into LETHE's memory-loss core,
  - build an overnight loop so the project can continue planning/verification while the user sleeps.
- Added reference research:
  - `docs/research/2026-06-02-roguelike-reference.md`.
- Added v0.9 planning prompt:
  - `docs/review_prompts/2026-06-02-v09-release-feel-loop.md`.
- Added overnight loop runner:
  - `scripts/run_overnight_loop.js`,
  - `npm run overnight:loop`,
  - `npm run overnight:loop:dry`.
- Updated `doctor`, `doctor:deep`, and autopilot preflight checks so a new session can detect whether the loop command exists.
- Updated runbook and prompt README with loop usage and blocker behavior.
- Verified:
  - `node --check scripts/run_overnight_loop.js`,
  - `node --check scripts/check_local_pipeline.js`,
  - `node --check scripts/autopilot_preflight.js`,
  - `npm run overnight:loop:dry`,
  - `npm run doctor`: 36 pass, 0 warn, 0 fail,
  - `npm run doctor:deep`: 50 pass, 0 warn, 0 fail,
  - safe smoke loop with `--provider none --test none`.
- Smoke loop log:
  - `docs/loop_runs/2026-06-02-overnight-163600.md`.
- Ran one actual v0.9 overnight loop iteration with Claude + Codex CLI:
  - loop log: `docs/loop_runs/2026-06-02-overnight-163806.md`,
  - Claude response: `docs/review_responses/2026-06-02-v09-release-feel-loop-claude.md`,
  - Codex CLI response: `docs/review_responses/2026-06-02-v09-release-feel-loop-codex.md`,
  - synthesis: `docs/review_responses/2026-06-02-v09-release-feel-loop-double-check.md`.
- Both planning partners chose `BUILD_V09_RELEASE_FEEL_LOOP`.
- Selected next implementation scope is v0.9 Work Package 1: make the existing 6-memory build identity readable within 90 seconds.
- Decision: broad human testing stays paused until v0.9 produces a stronger release-like prototype target and at least one visually verified playable build.

## Addendum: Overnight Loop Discord Progress Fixed

- User noticed the v0.9 loop work did not send a Discord notification because the verification run used dry-run/skip flags.
- Changed `scripts/run_overnight_loop.js` so real overnight loops send Discord progress by default:
  - start notice,
  - per-iteration status notice,
  - per-iteration checkpoint notice,
  - blocked notice on required step failure,
  - final done notice,
  - latest work-unit report upload.
- Added `--discord-dry-run` to preview notices without sending.
- Added `--no-discord` for deliberate local debugging only.
- Verified:
  - `node --check scripts/run_overnight_loop.js`,
  - `npm run overnight:loop:dry`,
  - `node scripts/run_overnight_loop.js --allow-dirty --skip-preflight --provider none --test none --skip-post-ai --skip-report --discord-dry-run --iterations 1`.
- Notification smoke log:
  - `docs/loop_runs/2026-06-02-overnight-164700.md`.

## Addendum: Autonomous Dev Loop Added

- User clarified that the desired loop is not a three-step planning/report loop.
- Desired loop:
  - read task/NEXT_TASKS,
  - implement,
  - verify,
  - report to Discord,
  - get Claude + Codex feedback,
  - update task/status/report Markdown,
  - continue into the next implementation until the prototype reaches the target.
- Added `scripts/run_autonomous_dev_loop.js`.
- Added npm scripts:
  - `npm run dev:loop:dry`,
  - `npm run dev:loop`.
- Default dev loop budget:
  - 6 iterations,
  - 360 minutes,
  - Codex implementation with `codex exec --sandbox workspace-write`,
  - `npm run doctor`,
  - `npm run ai:test:quick`,
  - report regeneration and Discord work-unit upload,
  - Claude + Codex CLI feedback,
  - docs-only Codex task update,
  - commit and push after successful verified iterations.
- Safety behavior:
  - clean git tree required by default,
  - failed verification stops the loop,
  - blocked loop writes `docs/review_prompts/YYYY-MM-DD-autodev-blocker-*.md`,
  - `--no-push`, `--no-commit`, `--discord-dry-run`, and `--allow-dirty` are available for controlled runs.
- Verified:
  - `node --check scripts/run_autonomous_dev_loop.js`,
  - `npm run dev:loop:dry`,
  - `npm run doctor`: 38 pass, 0 warn, 0 fail,
  - `npm run doctor:deep`: 54 pass, 0 warn, 0 fail.
- Important note: the actual long-running implementation loop has not been started in this turn; only the structure and dry-run readiness were verified.

## Addendum: v0.9 Work Package 1 Identity Hook

- Implemented the first v0.9 Work Package 1 unit without adding new memories, slots, shops, meta progression, regions, or weapons.
- Updated browser label and experiment version to `v0.9`.
- Added build identity calculation from the existing six memories and current tag/synergy rules:
  - build name,
  - primary tag,
  - active synergy list,
  - most-dependent memory,
  - active memory names.
- Added the build identity card to the setup side panel and combat HUD:
  - current build,
  - active synergy,
  - dependent memory.
- Memory selection cards now show tag chips next to the existing role and short combat description.
- Added JSON/event hooks:
  - `buildIdentity`,
  - `buildIdentitySeenBy90Sec`,
  - `run_start.buildIdentity`,
  - `memory_forgotten.buildIdentity`,
  - `memory_refilled.buildIdentity`,
  - `build_identity_updated`.
- Added matching AI raw-run payload fields under `stage.build`:
  - `buildName`,
  - `primaryTag`,
  - `activeSynergyDetails`,
  - `mostDependentMemory`,
  - `memoryNames`.
- Added `?qa=fast,identity` dataset hook through `data-lethe-identity-qa`.
- Verification:
  - `npm run autopilot:preflight:local`: failed before implementation because the working tree already had untracked loop-run files:
    - `docs/loop_runs/2026-06-02-devloop-170139-iteration-1-implement-prompt.md`,
    - `docs/loop_runs/2026-06-02-devloop-170139.md`.
    - Next command before unattended automation: clean, commit, stash, ignore, or intentionally run with `--allow-dirty` from a trusted local terminal.
  - `node --check src/game.js`: passed.
  - `node --check alpha_test/src/simulator.js`: passed.
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `80.8%`, irritation `1.0%`, prediction `85.5%`, death/fail `40.0%`.
  - `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8909`, regret `81.6%`, irritation `0.7%`, prediction `85.1%`, death/fail `45.1%`.
  - Quick raw-run payload confirmed `stage.build.buildName`, `stage.build.mostDependentMemory`, and `stage.build.activeSynergyDetails`.
  - Static hook check confirmed `buildIdentity`, `letheIdentityQa`, v0.9 labels, and build-card CSS are present.
- Browser QA status:
  - in-app Browser failed because `iab` is unavailable in this session,
  - local Chrome headless `--dump-dom` returned empty output again,
  - therefore visual browser QA for `?qa=fast,identity` remains a required next-loop task.

## Addendum: Auto Dev Loop Iteration 1 Recovery

- Started the real autonomous dev loop with `node scripts/run_autonomous_dev_loop.js --iterations 1 --duration-minutes 90`.
- The loop successfully sent Discord start/status notices, passed preflight with dirty loop logs allowed, and entered Codex implementation.
- Nested Codex implemented v0.9 WP1 identity hook but did not return cleanly from the parent loop.
- Recovered manually:
  - read implementation result from `docs/loop_runs/2026-06-02-devloop-170139-iteration-1-implement-result.md`,
  - re-ran `node --check src/game.js`,
  - re-ran `node --check alpha_test/src/simulator.js`,
  - re-ran `npm run ai:test:quick`,
  - re-ran `npm run ai:test`.
- Added `--codex-timeout-minutes` to `scripts/run_autonomous_dev_loop.js`, default `20`, so future nested Codex calls stop instead of waiting indefinitely.
- Collected feedback:
  - Claude: `docs/review_responses/2026-06-02-autodev-feedback-1-claude.md`,
  - Codex CLI: `docs/review_responses/2026-06-02-autodev-feedback-1-codex.md`,
  - synthesis: `docs/review_responses/2026-06-02-autodev-feedback-1-double-check.md`.
- Selected next task:
  - keep WP1 open,
  - add a stable `?qa=fast,identity` browser/headless smoke test before WP2.

## Addendum: v0.9 Identity QA Passed

- Second autonomous dev-loop iteration added a dedicated identity QA runner:
  - `scripts/run_browser_identity_qa.js`,
  - `npm run qa:identity`,
  - doctor/deep checks for the new runner.
- Initial `qa:identity` failed because Chrome DevTools pipe input/output were reversed and the game could overwrite `status: complete` with `status: visible` after the core identity fields were already visible.
- Fixed the runner:
  - use fd 3 for Chrome pipe input and fd 4 for output,
  - treat the QA as complete when build name, active synergy, dependent memory, payload, and 90-second flag are all visible.
- Verified:
  - `node --check scripts/run_browser_identity_qa.js`,
  - `npm run qa:identity`: `status: complete`, failures `[]`,
  - `npm run doctor`: 39 pass, 0 warn, 0 fail.
- WP1 identity QA is now complete. Next candidate is memory card text compression before deciding whether to close WP1 and move to WP2.

## Addendum: v0.9 Work Package 1 Identity QA Runner

- Added `scripts/run_browser_identity_qa.js`.
- Added `npm run qa:identity`.
- Updated local doctor checks so the identity QA script entry and syntax are tracked.
- The runner targets `index.html?qa=fast,identity&tester=QA&session=IDENTITY` and checks `data-lethe-identity-qa` for:
  - `status: complete`,
  - `version: v0.9`,
  - visible build name,
  - visible active synergy,
  - visible dependent memory,
  - `buildIdentity` payload,
  - `buildIdentitySeenBy90Sec`.
- Verification:
  - `node --check scripts/run_browser_identity_qa.js`: passed.
  - `node --check scripts/check_local_pipeline.js`: passed.
  - `node --check src/game.js`: passed.
  - `npm run doctor`: 39 pass, 0 warn, 0 fail.
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`.
  - `npm run autopilot:preflight:local`: failed as expected because the working tree is dirty and existing loop-run files are untracked.
- Browser QA status:
  - `npm run qa:identity` could not complete inside this Codex sandbox.
  - in-app Browser reports `Browser is not available: iab`.
  - Chrome CDP localhost access fails with `connect EPERM`.
  - Chrome remote-debugging pipe times out waiting for `Target.getTargets`.
- Decision:
  - WP1 remains open.
  - The next trusted-local command is `npm run qa:identity`.
  - WP2 should not start until identity QA produces a real `status: complete` result.

## Addendum: v0.9 WP1 Memory Copy Compression

- Implemented the next unfinished v0.9 WP1 task only: compress the existing 6 memory descriptions so build identity is easier to scan inside the first 90 seconds.
- Scope guard held:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion.
- Code changes:
  - shortened all six memory `desc` strings in `src/game.js`,
  - setup/refill memory cards now share `memoryChoiceHtml(memory)`,
  - card summaries use `역할 · 짧은 설명`,
  - `.choice-summary` caps the visible summary to two lines.
- Static copy check:
  - `처형자의 섬광`: 22 chars,
  - `굶주린 칼무리`: 23 chars,
  - `추적자의 맹세`: 21 chars,
  - `파쇄의 파문`: 26 chars,
  - `피의 반사`: 23 chars,
  - `멈춘 초침`: 20 chars.
- Verification:
  - `npm run autopilot:preflight:local`: failed because the working tree already had untracked loop-run files:
    - `docs/loop_runs/2026-06-02-devloop-173350-iteration-1-implement-prompt.md`,
    - `docs/loop_runs/2026-06-02-devloop-173350.md`,
  - `node --check src/game.js`: passed,
  - `node --check scripts/run_browser_identity_qa.js`: passed,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `80.8%`, irritation `1.0%`, prediction `85.5%`, death/fail `40.0%`.
  - `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8909`, regret `81.6%`, irritation `0.7%`, prediction `85.1%`, death/fail `45.1%`.
- Browser QA limitation:
  - `npm run qa:identity`: failed in this Codex session because Chrome CDP pipe timed out waiting for `Target.getTargets`,
  - Browser plugin fallback was unavailable because `iab` was not available in this session.
- Decision:
  - v0.9 WP1 implementation tasks are complete in code/docs,
  - trusted-local `npm run qa:identity` should be rerun before a long unattended loop or before treating WP1 as fresh browser evidence,
  - next implementation candidate is v0.9 WP2: pressure rhythm and post-loss challenge.

## Addendum: Devloop Feedback Task Update

- Read the devloop implementation prompt/result and the three feedback files:
  - `docs/review_prompts/2026-06-02-devloop-173350-feedback-1.md`,
  - `docs/review_responses/2026-06-02-devloop-173350-feedback-1-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-173350-feedback-1-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-173350-feedback-1-double-check.md`.
- Filled the double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and required tests.
- Common conclusion:
  - WP1 memory-copy compression is implemented and AI-stable,
  - Alpha Fun Score stayed at `0.8883/0.8909`,
  - low irritation/confusion supports leaving WP1 copy work,
  - preflight and browser QA evidence are still the blockers.
- Main conflict:
  - Claude recommends pressure high/low first and delaying post-loss challenge,
  - Codex CLI recommends a minimal post-loss challenge as the next implementation.
- Selected order:
  - docs-only update in this pass,
  - clean or intentionally track/ignore the devloop `docs/loop_runs/` files,
  - rerun trusted-local `npm run qa:identity`,
  - start WP2 with pressure rhythm as Slice A,
  - keep post-loss challenge as a minimal WP2 follow-up, not a new system.
- Updated `docs/NEXT_TASKS.md` with the synthesis and split WP2 into pressure rhythm first, post-loss challenge second.
- Updated `docs/CODEX_STATUS.md` with the latest loop feedback verdict and blockers.
- No code feature files were edited in this docs-only pass.

## Addendum: Autonomous Dev Loop Prompt Scope Cleanup

- Implemented the next smallest v0.9 automation cleanup task without changing game content.
- Problem:
  - WP1 identity hook, identity QA runner, and copy compression were already implemented,
  - but `scripts/run_autonomous_dev_loop.js` still generated nested implementation prompts that hard-coded v0.9 WP1 as the selected scope,
  - this made future loops likely to keep re-selecting completed WP1 work instead of the current first unfinished `NEXT_TASKS.md` item.
- Code change:
  - removed the hard-coded WP1 scope line from `buildImplementationPrompt`,
  - added explicit guidance that the selected scope is the foremost unfinished v0.9 item in `docs/NEXT_TASKS.md`,
  - added a guard that WP1 completion should lead first to preflight cleanup and trusted-local identity QA before WP2,
  - updated the generated feedback prompt to judge the current v0.9 task and scope guard instead of WP1 specifically.
- Scope guard held:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion,
  - no combat parameter change.
- Documentation updates:
  - `docs/NEXT_TASKS.md` now records the prompt cleanup as complete,
  - the stale `173350` untracked-file blocker was corrected because those files are now git-tracked,
  - the current dirty-tree blocker is the wrapper-generated `2026-06-02-devloop-175642*` output until the wrapper records it,
  - `docs/CODEX_STATUS.md` now points future loops at the current `NEXT_TASKS.md` item instead of WP1.
- Verification:
  - `node --check scripts/run_autonomous_dev_loop.js`: passed,
  - `npm run dev:loop:dry`: passed,
  - `npm run doctor`: passed,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `80.8%`, irritation `1.0%`, prediction `85.5%`, death/fail `40.0%`.
- Decision:
  - WP1 remains implemented and should not receive more code work in the next loop unless trusted-local identity QA exposes a concrete issue,
  - next loop should let the wrapper record the current loop-run files, then run trusted-local `npm run qa:identity`,
  - only after that should Codex start WP2 Slice A pressure rhythm.

## Addendum: Devloop 175642 Feedback Task Update

- Completed the requested docs-only task update for `2026-06-02-devloop-175642-feedback-1`.
- Read and synthesized:
  - `docs/review_prompts/2026-06-02-devloop-175642-feedback-1.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-1-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-1-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-1-double-check.md`.
- Filled the double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and required tests before balance reporting.
- Common conclusion:
  - prompt cleanup was appropriate,
  - WP1 should not be reopened for new gameplay/content work,
  - AI quick remains planning-stable at `GO_CANDIDATE`, Alpha Fun Score `0.8883`,
  - current loop-run outputs and trusted-local identity QA remain blockers.
- Main conflict:
  - Claude recommends `GO_TO_HUMAN_TEST` after `npm run qa:identity` passes and asks for a human-test checklist,
  - Codex CLI recommends gate cleanup first, then WP2 Slice A pressure rhythm before people testing.
- Selected order:
  - this pass is docs-only,
  - record/track the current `2026-06-02-devloop-175642*` outputs,
  - rerun trusted-local `npm run qa:identity`,
  - keep the existing `NEXT_TASKS.md` order for WP2 Slice A unless the user overrides it.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, and `docs/reports/2026-06-02.md`.
- No game code or feature scope was changed in this pass.

## Addendum: Autonomous Dev Loop Preflight Ordering Cleanup

- Implemented the next gate-cleanup unit from the v0.9 queue without changing game content.
- Problem:
  - the autonomous dev loop required a clean tree before it started,
  - but after that it created `docs/loop_runs/...` logs before running autopilot preflight,
  - therefore preflight saw the loop's own new log as dirty and the default command hid that with `--allow-dirty`.
- Code change:
  - changed `scripts/run_autonomous_dev_loop.js` default preflight from `node scripts/autopilot_preflight.js --allow-dirty` to `npm run autopilot:preflight:local`,
  - moved preflight execution before loop log creation,
  - copied successful preflight stdout/stderr into the generated loop log header after the log is created,
  - kept `--allow-dirty` available only as an explicit local smoke-test option by switching that dry-run path to `node scripts/autopilot_preflight.js --allow-dirty`.
- Scope guard held:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion,
  - no combat parameter change.
- Verification:
  - `node --check scripts/run_autonomous_dev_loop.js`: passed,
  - `npm run dev:loop:dry`: passed and now prints `npm run autopilot:preflight:local`,
  - `node scripts/run_autonomous_dev_loop.js --dry-run --allow-dirty --no-commit --no-push --discord-dry-run`: passed and prints `node scripts/autopilot_preflight.js --allow-dirty`,
  - `npm run doctor`: passed, 39 pass, 0 warn, 0 fail,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `80.8%`, irritation `1.0%`, prediction `85.5%`, death/fail `40.0%`,
  - `git diff --check`: passed,
  - `npm run autopilot:preflight:local`: failed as expected because the current session still has dirty wrapper outputs and this code change.
- Decision:
  - the code-side preflight ordering cleanup is complete,
  - the existing `docs/loop_runs/2026-06-02-devloop-175642*` dirty-tree blocker is not complete until wrapper records/cleans those files,
  - after cleanup, rerun trusted-local `npm run qa:identity` before WP2 Slice A.

## Addendum: Devloop 175642 Feedback-2 Task Update

- Completed the requested docs-only task update for `2026-06-02-devloop-175642-feedback-2`.
- Read and synthesized:
  - `docs/review_prompts/2026-06-02-devloop-175642-feedback-2.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-2-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-2-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-2-double-check.md`.
- Filled the double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and required tests.
- Common conclusion:
  - preflight-order cleanup is correct and code-complete,
  - WP1 official completion is still blocked by dirty loop-run outputs,
  - clean-tree `npm run autopilot:preflight:local` and trusted-local `npm run qa:identity` must pass before WP2 or unattended automation.
- Planning evidence:
  - quick AI test remains `GO_CANDIDATE`,
  - Alpha Fun Score `0.8883`,
  - irritation `0.0104`,
  - but this remains AI planning evidence rather than real player emotion or balance proof.
- Watch points:
  - echoPivotScore `0.656`,
  - earlyChoiceInterest `0.654`.
- Main conflict:
  - Claude limits the next executable unit to WP1 gate cleanup and forbids WP2 before clean preflight plus identity QA,
  - Codex CLI agrees on cleanup first but points back to WP2 Slice A pressure rhythm after the gate passes.
- Selected order:
  - this pass is docs-only,
  - record or clean `docs/loop_runs/2026-06-02-devloop-175642*.md`,
  - rerun clean-tree `npm run autopilot:preflight:local`,
  - rerun trusted-local `npm run qa:identity`,
  - start WP2 Slice A only after those pass.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, `docs/reports/2026-06-02.md`, and the feedback-2 double-check summary.
- No game code or feature scope was changed in this pass.

## Addendum: Loop-run Preflight Blocker Diagnosis

- Implemented the next smallest v0.9 WP1 gate-cleanup unit without changing game content.
- Problem:
  - the next blocker is still the dirty `docs/loop_runs/2026-06-02-devloop-175642*.md` output set,
  - but `npm run autopilot:preflight:local` previously returned a generic dirty-tree fix,
  - this made it less clear whether the next action was wrapper artifact recording, abandoned artifact cleanup, or a normal code-change commit.
- Code change:
  - updated `scripts/autopilot_preflight.js`,
  - dirty-tree summaries are capped to five entries,
  - when dirty files include `docs/loop_runs/*.md`, the fix now says these are pending loop-run artifacts,
  - the message tells the operator to let wrapper result files finish, then run `git add docs/loop_runs && git commit -m "docs: 자동 개발 루프 산출물 기록"` or remove abandoned artifacts,
  - the next check is explicitly `npm run autopilot:preflight:local`.
- Scope guard held:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion,
  - no combat parameter change.
- Verification:
  - `node --check scripts/autopilot_preflight.js`: passed,
  - `npm run doctor`: passed, 39 pass, 0 warn, 0 fail,
  - `npm run autopilot:preflight:local`: failed as expected because the working tree remains dirty, but now prints the loop-run-artifact-specific fix.
- Decision:
  - this is a diagnostic cleanup only,
  - the actual loop-run artifact blocker remains open because this cycle does not commit, push, or delete wrapper outputs,
  - next execution should let the wrapper finish/write the missing iteration result, then record or remove the `docs/loop_runs/2026-06-02-devloop-175642*` artifacts before rerunning clean preflight.

## Addendum: Devloop 175642 Feedback-3 Task Update

- Completed the requested docs-only task update for `2026-06-02-devloop-175642-feedback-3`.
- Read and synthesized:
  - `docs/review_prompts/2026-06-02-devloop-175642-feedback-3.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-3-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-3-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-3-double-check.md`.
- Filled the feedback-3 double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and tests required before balance reporting.
- Common conclusion:
  - AI proxy evidence is positive enough for planning: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `0.8083`, irritation `0.0104`, restart `0.90`,
  - this is still planning evidence, not human emotion or real balance proof,
  - the latest implementation is infrastructure/gate cleanup, not new gameplay,
  - WP1 should not be reopened for new feature work.
- Remaining blocker:
  - record/clean `docs/loop_runs/2026-06-02-devloop-175642*`,
  - rerun clean-tree `npm run autopilot:preflight:local`,
  - rerun trusted-local `npm run qa:identity`.
- Conflict:
  - Claude recommends human-test checklist and `GO_TO_HUMAN_TEST` after cleanup and identity QA,
  - Codex CLI recommends cleanup first, then trusted-local identity QA, then existing WP2 Slice A pressure rhythm.
- Selected order:
  - this cycle stays docs-only,
  - next executable scope is gate cleanup only,
  - do not start WP2, human-test checklist, UI/tutorial changes, or balance changes until the cleanup checks pass.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, and `docs/reports/2026-06-02.md`.
- No game code or feature scope was changed in this pass.

## Addendum: Loop-run Missing Result Diagnosis

- Implemented the next smallest v0.9 gate-cleanup diagnostic without changing game content.
- Problem:
  - the current dirty loop-run artifact set includes the iteration 4 implementation prompt,
  - the matching `docs/loop_runs/2026-06-02-devloop-175642-iteration-4-implement-result.md` is not present yet,
  - the result file is expected to be written by the wrapper after this Codex turn, so this cycle should not fabricate or commit it manually.
- Code change:
  - updated `scripts/autopilot_preflight.js`,
  - when dirty loop-run prompt files are present, preflight now calculates the expected `*-result.md` path,
  - missing result paths are printed directly in the loop-run artifact fix text.
- Verification:
  - `node --check scripts/autopilot_preflight.js`: passed,
  - `npm run autopilot:preflight:local`: failed as expected because the working tree remains dirty, and now reports `docs/loop_runs/2026-06-02-devloop-175642-iteration-4-implement-result.md` as the missing wrapper result.
- Decision:
  - the artifact cleanup blocker remains open,
  - WP2, human-test checklist, UI/tutorial changes, and balance changes stay blocked until wrapper output recording/cleanup, clean preflight, and trusted-local identity QA pass.

## Addendum: Devloop 175642 Feedback-4 Task Update

- Completed the requested docs-only task update for `2026-06-02-devloop-175642-feedback-4`.
- Read and synthesized:
  - `docs/review_prompts/2026-06-02-devloop-175642-feedback-4.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-4-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-4-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-175642-feedback-4-double-check.md`.
- Filled the feedback-4 double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and tests required before balance reporting.
- Common conclusion:
  - AI proxy evidence remains a positive planning pass: `GO_CANDIDATE`, Alpha Fun Score `0.8883`, regret `0.8083`, irritation `0.0104`, restart `0.90`,
  - this is still not human emotion, real balance evidence, or Unity-transition evidence,
  - the latest implementation was missing-result preflight diagnosis, not gameplay scope expansion,
  - WP1 should not be reopened for new feature work.
- Current blocker:
  - the wrapper result file now exists,
  - record/clean `docs/loop_runs/2026-06-02-devloop-175642*`,
  - rerun clean-tree `npm run autopilot:preflight:local`,
  - rerun trusted-local `npm run qa:identity`.
- Conflict:
  - Claude frames the build as people-test preparation candidate after cleanup and highlights `멈춘 초침` deletion-rate, earlyChoiceInterest, and echoPivotScore as observation targets,
  - Codex CLI limits the next executable unit to loop-run artifact 정합성 정리 and warns the current evidence is not Unity-transition proof.
- Selected order:
  - this cycle stays docs-only,
  - next executable scope is artifact 정합성 정리 only,
  - after clean preflight and identity QA, resume the existing order with v0.9 WP2 Slice A pressure high/low rhythm.

## Post-loop gate closure

- Ran the 4-iteration autonomous dev loop: `node scripts/run_autonomous_dev_loop.js --iterations 4 --duration-minutes 240 --codex-timeout-minutes 20`.
- The loop completed and pushed:
  - `3f227a4 feat: 자동 개발 루프 1차 반영`,
  - `d7b4ca9 feat: 자동 개발 루프 2차 반영`,
  - `13fa101 feat: 자동 개발 루프 3차 반영`,
  - `f6ee83f feat: 자동 개발 루프 4차 반영`.
- Post-loop gate checks:
  - `git status --short`: clean,
  - `npm run autopilot:preflight`: 21 pass, 0 warn, 0 fail,
  - `npm run qa:identity`: `status: complete`, failures `[]`, `buildIdentitySeenBy90Sec: true`.
- Result:
  - loop-run artifact cleanup, clean preflight, and trusted-local identity QA blockers are closed,
  - the next executable scope is v0.9 Work Package 2 Slice A, pressure high/low rhythm,
  - people testing remains paused until WP2 pressure rhythm and later post-loss challenge evidence exist.

## v0.9 WP2 Slice A pressure rhythm

- Implemented pressure high/low pacing in `src/game.js`.
- Browser combat now transitions through:
  - `숨 고르기`: lower pressure so players can read the build and take early level-ups,
  - `압박 상승`: normal pressure where the selected build starts being tested,
  - `망각 전조`: denser packs and a richer enemy pool before the next memory-loss gate.
- Added run logging:
  - `runTimeline.pressureSegments`,
  - `danger.pressureLullTime`,
  - `danger.pressureRiseTime`,
  - `danger.pressureClimaxTime`,
  - `pressure_phase` events.
- Added AI proxy support:
  - `pressureRhythm` option,
  - `earlyLoop.pressureRhythm`,
  - headline `pressureContrast`,
  - CLI output line for pressure crowd/contrast.
- Added browser QA:
  - `?qa=fast,pressure`,
  - `npm run qa:pressure`,
  - `scripts/run_browser_pressure_qa.js`.
- Verification:
  - `npm run qa:pressure`: `status: complete`, failures `[]`, segments `lull/rising/climax`,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.885`, pressureContrast `0.4417`.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, and `docs/reports/2026-06-02.md`.
- No game code or feature scope was changed in this pass.

## Addendum: v0.9 WP2 Slice B minimal post-loss challenge

- Implemented the foremost unfinished v0.9 task: WP2 Slice B.
- Scope guard held:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion.
- Browser gameplay change:
  - after memory loss, the 2-memory deficit segment now moves through `결손 정비` and `결손 압박`,
  - this uses only existing enemies and spawn parameters,
  - the refill gate still restores the third active memory after the deficit segment.
- Logging and payload change:
  - added `runTimeline.postLossChallenges`,
  - added `danger.deficitBreathTime`,
  - added `danger.deficitChallengeTime`,
  - added `danger.deficitChallengeLowHpTime`,
  - added `danger.postLossChallengeCompletions`,
  - added `post_loss_challenge_phase` and `post_loss_challenge_completed` events.
- QA/tooling change:
  - added `?qa=fast,postloss`,
  - added `npm run qa:postloss`,
  - extended `scripts/run_browser_pressure_qa.js` to support `--mode postloss`,
  - updated doctor script checks for `qa:postloss`.
- AI proxy change:
  - added `postLossChallenge` simulation option,
  - added headline `postLossChallengeScore`,
  - added headline `postLossChallengeContrast`,
  - exposed those values in CLI output, markdown/html AI reports, and CSV.
- Verification:
  - `node --check src/game.js`: passed,
  - `node --check scripts/run_browser_pressure_qa.js`: passed,
  - `node --check alpha_test/src/simulator.js`: passed,
  - `node --check alpha_test/src/metrics.js`: passed,
  - `node --check alpha_test/src/run_alpha.js`: passed,
  - `node --check alpha_test/src/report.js`: passed,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8846`, post-loss challenge `0.6687`, contrast `0.3134`, 2-memory survival `79.0%`,
  - `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8879`, post-loss challenge `0.6692`, contrast `0.3135`, 2-memory survival `78.8%`,
  - `npm run doctor`: 43 pass, 0 warn, 0 fail.
- Browser QA blocker:
  - `npm run qa:postloss -- --timeout-ms 15000` failed at Chrome/CDP `Target.getTargets`,
  - `npm run qa:pressure -- --timeout-ms 15000` failed at the same point,
  - in-app Browser was unavailable (`iab` not available),
  - this indicates the current local browser automation channel is blocked; trusted-local browser QA should be rerun before treating Slice B as browser-proven.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, `docs/devlog/2026-06-02.md`, and `docs/reports/2026-06-02.md`.

## Addendum: Devloop 193946 Feedback-1 Task Update

- Completed the requested docs-only task update for `2026-06-02-devloop-193946-feedback-1`.
- Read and synthesized:
  - `docs/review_prompts/2026-06-02-devloop-193946-feedback-1.md`,
  - `docs/review_responses/2026-06-02-devloop-193946-feedback-1-claude.md`,
  - `docs/review_responses/2026-06-02-devloop-193946-feedback-1-codex.md`,
  - `docs/review_responses/2026-06-02-devloop-193946-feedback-1-double-check.md`.
- Filled the feedback-1 double-check synthesis fields with common recommendations, conflicts, selected vNext scope, and tests required before balance reporting.
- Common conclusion:
  - WP2 Slice B is implementation-complete and scope-valid,
  - it used only existing combat parameters and did not add memories, slots, shops, meta progression, regions, enemies, or weapons,
  - AI proxy evidence remains planning-stable with `GO_CANDIDATE`, low irritation, high restart intent, and about `79%` 2-memory survival.
- Browser QA blocker:
  - `qa:postloss` and `qa:pressure` both failed at Chrome/CDP `Target.getTargets`,
  - this is recorded as a local automation-channel blocker rather than Slice B-specific proof of failure,
  - trusted-local `npm run qa:postloss` must pass before Slice B is treated as browser-proven.
- Conflict:
  - Claude recommends a stage-entry two-choice "기억 집중" moment after browser QA passes,
  - Codex CLI recommends HUD/number-key active-memory focus during combat,
  - both agree the next agency work must stay inside the current active-memory/combat loop.
- Selected order:
  - rerun trusted-local `npm run qa:postloss`,
  - retry once with `--timeout-ms 30000` only if the same CDP timeout recurs,
  - then start WP3 Slice A as one minimal tactical agency hook using an existing active memory.
- Updated `docs/NEXT_TASKS.md`, `docs/CODEX_STATUS.md`, `docs/devlog/2026-06-02.md`, `docs/reports/2026-06-02.md`, and the feedback-1 double-check summary.
- No game code or feature scope was changed in this pass.
