# 2026-06-07 Devlog

## v0.12 Human Playtest Package

- Goal: continue from the accepted automated balance baseline into human-test packaging instead of another blind balance pass.
- Updated `docs/HUMAN_PLAYTEST_GUIDE.md` for v0.12 controlled 5-8 player sessions.
- Updated `docs/PLAYTEST_NOTES.md` with a v0.12 session template that separates early loop, first boss, forgetting, deficit, and refill observations.
- Added `docs/playtest/2026-06-07-v012-human.md`.
- Updated `scripts/prepare_playtest_build.js` so `npm run playtest:package` includes `V012_HUMAN_PLAYTEST_SHEET.md` instead of the old v0.7 solo sheet.
- Verification:
  - `node --check scripts/prepare_playtest_build.js`: pass.
  - `npm run playtest:package:dry`: pass, output target `dist\lethe-v0.12-playtest`.
  - `npm run playtest:package`: pass, generated `dist\lethe-v0.12-playtest`.
  - Package file check: `HUMAN_PLAYTEST_GUIDE.md`, `PLAYTEST_NOTES_TEMPLATE.md`, `V012_HUMAN_PLAYTEST_SHEET.md`, `PLAYTEST_SUMMARY_README.md`, `README.md`, `index.html`, `style.css`, and `src/`.
- Decision: v0.12 is packaged as the current controlled human-test candidate.
- Risk: human sessions may still reject the `40%` death / post-loss pressure feel; do not tune again without human evidence.
- Report/Discord:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - `npm run report:discord:unit:dry`: pass.
  - Next trusted-local command: `npm run report:discord:unit`.

## v0.12 Balance Loop Gate Fix

- Goal: run the next balance pass through the QA loop and fix the mismatch where loop/default checks did not match the current v0.12 balance target.
- `npm run autopilot:preflight:local`: 20 pass / 1 warn / 0 fail.
- Found that `npm run balance:loop -- --runs ...` was reaching the script as positional args in this PowerShell/npm path, so the loop silently used the old default `608s` run window.
- Updated `scripts/run_balance_loop.js`:
  - default run window `608s -> 690s`,
  - positional args accepted,
  - dry-run now prints run-sec, timeout, out, and report path.
- Updated `scripts/run_browser_balance_qa.js` so death rate max `<= 40%` is part of the automated gate.
- Balance changes:
  - first boss HP `2500 -> 2050`,
  - deficit duration `60s -> 54s`,
  - enemy damage scaling softened,
  - first-cycle rising and later-cycle caps lowered,
  - refill HP floor and shield increased.
- Verification:
  - `node --check src/game.js`: pass.
  - `node --check scripts/run_browser_balance_qa.js`: pass.
  - `node --check scripts/run_balance_loop.js`: pass.
  - `npm run balance:loop:dry`: pass and now shows `run-sec 690`.
  - Browser `first_boss_ttk`: `GO_BALANCE_BASELINE`, 3/3 accepted, TTK median `18.61s`.
  - Final `npm run balance:loop -- 5 690 60000 alpha_test\outputs\balance-loop-2026-06-07-final-hp2050 docs\balance\2026-06-07-v012-balance-loop.md`: `GO_BALANCE_BASELINE`, first boss clear `100%`, full clear `60%`, death `40%`, first boss TTK median `20.73s`.
- Regenerated `dist\lethe-v0.12-playtest` after the balance changes with `npm run playtest:package`.
- Report/Discord:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit`: blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - `npm run report:discord:unit:dry`: pass.
  - Next trusted-local command: `npm run report:discord:unit`.
