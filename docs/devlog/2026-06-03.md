# Devlog - 2026-06-03

## Report Unit File Delivery Fix

- Confirmed the previous report change only numbered top-level sections inside `docs/reports/2026-06-02.md`.
- The user expected work-unit reports to be separable by task, so the previous loop result was incomplete for reporting ergonomics.
- Updated `scripts/build_report.js`:
  - keeps the daily Markdown/HTML report,
  - splits each `# YYYY-MM-DD-NN - 작업 제목` section into `docs/reports/units/YYYY-MM-DD/*.md` and `*.html`,
  - writes `docs/reports/units/YYYY-MM-DD/latest.json`.
- Updated `scripts/send_discord_report.js`:
  - `--latest-section` still summarizes the latest section,
  - attachment now uses the generated latest unit HTML when available instead of the full daily HTML.
- Updated `scripts/check_report_units.js`:
  - verifies numbered top-level headings,
  - verifies generated unit Markdown/HTML files,
  - verifies `latest.json`.
- Updated `scripts/run_autonomous_dev_loop.js`:
  - removed hard-coded `2026-06-02` report/devlog paths from nested prompts,
  - uses the loop date variable,
  - tells nested Codex that `npm run report` generates per-unit report files.
- Generated 57 unit reports for `docs/reports/2026-06-02.md`.
- Verified:
  - `node --check scripts/build_report.js`,
  - `node --check scripts/send_discord_report.js`,
  - `node --check scripts/check_report_units.js`,
  - `node --check scripts/run_autonomous_dev_loop.js`,
  - `npm run report`,
  - `npm run report:check`,
  - `node scripts/send_discord_report.js --dry-run --latest-section`.

## Project Status

- v0.9 WP2 Slice A pressure rhythm is implemented.
- v0.9 WP2 Slice B minimal post-loss challenge is implemented.
- The project is still blocked before WP3 because browser proof is missing.
- Latest trusted post-loss gate result remains `status: blocked`, `transportFailure: true` in this managed sandbox.
- Next gameplay step remains blocked until sandbox-outside trusted local `npm run qa:postloss:trusted` passes or the environment-blocker decision is made.

## Report Unit Size Policy Fix

- User clarified the unit files were useful, but the unit size was too small.
- Updated the autonomous dev loop so it no longer sends report/Discord output immediately after implementation.
- The loop now reports once after implementation, verification, Claude/Codex feedback, and task update are folded together.
- Updated implementation prompts:
  - routine implementation cycles should not create standalone report units,
  - report units are reserved for completed feature/gate/decision bundles.
- Updated task-update prompts:
  - one report unit should include implementation, verification, feedback, and next decision,
  - procedural titles like `Feedback-N 태스크 갱신` are not acceptable report units.
- Updated `scripts/check_report_units.js` to reject too-small procedural report titles.

## 2026-06-02 Report Consolidation

- User pointed out that the previous 57 report units were still too many for roughly two days of work.
- Rewrote `docs/reports/2026-06-02.md` from 57 small sections into 13 feature/gate/decision-sized sections.
- Preserved the detailed development trail in git history and `docs/loop_runs/`.
- Regenerated `docs/reports/2026-06-02.html`.
- Regenerated `docs/reports/units/2026-06-02/` so the generated unit count is now 13 instead of 57.

## Post-loss Browser Gate Closure

- Reran `npm run qa:postloss:trusted` before continuing gameplay loops.
- The first rerun reached browser QA `status: complete` with failures `[]`, but the process failed while deleting a Chrome temp profile directory.
- Updated `scripts/run_browser_pressure_qa.js` so Chrome is stopped before temp profile cleanup and cleanup is retried.
- Reran `npm run qa:postloss:trusted` successfully:
  - result `status: passed`,
  - browser QA `status: complete`,
  - failures `[]`,
  - confirmed `deficit_breath` and `deficit_trial`,
  - confirmed post-loss challenge completion and refill back to 3 active memories.
- WP2 Slice B is now browser-proven.
- Added autonomous-loop completion criteria support so large loops can stop when target quality is met instead of only when iteration/time limits expire.

## v0.9 WP3 Slice A Tactical Focus

- Implemented the smallest tactical agency hook inside the existing combat loop.
- Added `전술 집중` to current active memory slots:
  - slot click focuses that active memory,
  - `Digit1`-`Digit3` focuses the corresponding current active slot,
  - no new memory, slot, shop, meta progression, region, enemy, or weapon was added.
- Focus behavior:
  - cooldown memories have their next trigger pulled forward,
  - `굶주린 칼무리` gets a short existing aura range/damage boost,
  - `피의 반사` gets a short existing on-hit chance/damage boost.
- Added payload/QA support:
  - `tacticalFocus` in JSON logs,
  - `runTimeline.tacticalFocus`,
  - `?qa=fast,tactical`,
  - `npm run qa:tactical`,
  - doctor script-entry coverage for `qa:tactical`.
- Verified:
  - `node --check src/game.js`,
  - `node --check scripts/run_browser_pressure_qa.js`,
  - `node --check scripts/check_local_pipeline.js`,
  - `npm run doctor`: 45 pass / 0 warn / 0 fail,
  - `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8846`, irritation `1.0%`, post-loss challenge contrast `0.3134`.
- Browser automation note:
  - `npm run qa:tactical` failed before gameplay evaluation with Chrome CDP `Target.getTargets` timeout and port fallback `listen EPERM: operation not permitted 127.0.0.1`,
  - in-app Browser was also unavailable in this session (`iab` not available),
  - next gate is trusted-local `npm run qa:tactical` or an explicit environment-blocker decision.

## WP3 Tactical Focus Feedback Synthesis

- Read the devloop feedback prompt and both Claude/Codex responses for `2026-06-03-devloop-050050-feedback-1`.
- Common conclusion:
  - WP3 Slice A `전술 집중` is scope-valid because it only uses existing memories, existing active slots, and existing combat systems.
  - `npm run ai:test:quick` is still a positive planning pass: `GO_CANDIDATE`, Alpha Fun Score `0.8846`, low irritation.
  - `npm run qa:tactical` did not produce browser gameplay evidence because the managed sandbox hit Chrome/CDP `Target.getTargets` timeout and `127.0.0.1 listen EPERM`.
  - Browser combat evidence outranks AI proxy metrics, so people testing and further gameplay expansion remain blocked.
- Conflict summary:
  - There is no material next-scope conflict.
  - Claude emphasizes `ITERATE_BEFORE_TEST` and observing the low `멈춘 초침` deletion frequency.
  - Codex CLI narrows the next action to tactical QA transport resolution and result recording.
- Selected next task:
  - Run trusted-local `npm run qa:tactical`.
  - If it passes, record WP3 Slice A as browser-proven.
  - If the same transport failure repeats outside the sandbox, create an environment-blocker decision prompt before adding scope.
- Reporting note:
  - `npm run report` should split this feature/decision-sized unit into `docs/reports/units/2026-06-03/`.
  - Discord latest-section attachment should use the generated unit HTML, not the full daily cumulative HTML.

## Tactical QA Gate Logging

- Kept the next v0.9 scope to the foremost unfinished gate: tactical browser proof for WP3 Slice A.
- Reran `npm run qa:tactical` in this managed sandbox.
- Result:
  - failed before gameplay evaluation,
  - CDP pipe timed out at `Target.getTargets`,
  - remote-debugging-port fallback failed with `listen EPERM: operation not permitted 127.0.0.1`.
- Added `scripts/run_trusted_tactical_gate.js` and `npm run qa:tactical:trusted`.
- The trusted tactical wrapper:
  - runs standard tactical QA first,
  - retries once with `--timeout-ms 30000` only for Chrome/CDP transport failures,
  - writes `alpha_test/outputs/tactical-trusted-gate/latest.json` for automated loop handoff.
- Verified:
  - `node --check scripts/run_trusted_tactical_gate.js`,
  - `node --check scripts/check_local_pipeline.js`.
- Reran `npm run qa:tactical:trusted`.
- Current managed-sandbox result:
  - `status: blocked`,
  - `transportFailure: true`,
  - standard run and 30000 ms retry both stopped before gameplay evaluation.
- This is gate evidence cleanup only, not browser proof.
- Next executable gate is sandbox-outside trusted-local `npm run qa:tactical:trusted`.
- No new memory, slot, shop, meta progression, region, weapon, balance change, UI expansion, or gameplay scope was added.

## Tactical Trusted Gate Feedback Synthesis

- Read the devloop feedback prompt and both Claude/Codex responses for `2026-06-03-devloop-050050-feedback-2`.
- Common conclusion:
  - WP3 Slice A `전술 집중` and `qa:tactical:trusted` gate logging are scope-valid.
  - AI proxy evidence remains positive but limited: `GO_CANDIDATE`, Alpha Fun Score `0.8846`, regret `0.8073`, irritation `0.0104`.
  - The current managed-sandbox result is still transport-blocked, not gameplay-proven: `alpha_test/outputs/tactical-trusted-gate/latest.json` records `status: blocked`, `transportFailure: true`.
  - Browser combat evidence still outranks aggregate AI proxy metrics before people testing or balance reporting.
- Conflict summary:
  - Claude recommends one line of echo-pivot hint text on the forgetting event screen to improve `echoPivotScore 0.6554` and random bot prediction clarity.
  - Codex CLI recommends no new UI/gameplay/balance change until trusted-local tactical browser proof or an explicit environment-blocker decision.
- Selected next task:
  - Keep this cycle docs-only and do not widen implementation scope.
  - Run sandbox-outside trusted-local `npm run qa:tactical:trusted`.
  - If it passes, record WP3 Slice A as browser-proven.
  - If the same transport blocker repeats outside the sandbox, record an environment-blocker decision before adding hint text, changing `멈춘 초침`, or starting people testing.
- Reporting note:
  - The report unit remains feature/decision-sized by folding implementation, verification, feedback, and next decision into `2026-06-03-05`.
  - `npm run report` should keep generating the Discord attachment source under `docs/reports/units/2026-06-03/`, not only the daily cumulative report.

## Tactical Trusted Gate Handoff Cleanup

- Kept the foremost unfinished v0.9 item on WP3 Slice A browser proof.
- Reran `npm run qa:tactical:trusted` in the managed sandbox.
- Result:
  - standard tactical QA failed before gameplay evaluation,
  - the 30000 ms retry also failed before gameplay evaluation,
  - both attempts hit Chrome/CDP `Target.getTargets` timeout and remote-debugging-port `127.0.0.1 listen EPERM`.
- This is still transport-blocked gate evidence, not tactical-focus gameplay failure or browser proof.
- Added the WP3-specific blocker prompt:
  - `docs/review_prompts/2026-06-03-tactical-browser-transport-blocker.md`.
- Updated `scripts/run_trusted_tactical_gate.js` so repeated transport failures point to the tactical prompt instead of the older post-loss prompt.
- Scope stayed unchanged:
  - no new memory,
  - no new slot,
  - no shop,
  - no meta progression,
  - no new region,
  - no weapon expansion,
  - no hint, UI, balance, or gameplay expansion.
- Next executable gate remains sandbox-outside trusted-local `npm run qa:tactical:trusted`.

## Tactical Blocker Feedback Synthesis

- Read the devloop feedback prompt and both Claude/Codex responses for `2026-06-03-devloop-050050-feedback-3`.
- Common conclusion:
  - WP3 Slice A `전술 집중` remains code-complete and scope-valid.
  - The WP3-specific blocker prompt split is valid gate/handoff cleanup, not gameplay expansion.
  - `npm run ai:test:quick` remains positive planning evidence: `GO_CANDIDATE`, Alpha Fun Score `0.8846`, regret `0.8073`, irritation `0.0104`.
  - `npm run qa:tactical:trusted` still reports `status: blocked`, `transportFailure: true` in the managed sandbox, so WP3 Slice A is not browser-proven.
  - Browser combat evidence still outranks aggregate AI proxy metrics before people testing or balance reporting.
- Conflict summary:
  - There is no material next-scope conflict.
  - Claude flags `멈춘 초침` deletion frequency and echo-pivot/post-loss scores as observation risks.
  - Codex CLI narrows the executable next step to sandbox-outside trusted-local `npm run qa:tactical:trusted`.
- Selected next task:
  - Run trusted-local `npm run qa:tactical:trusted`.
  - If it passes, record WP3 Slice A as browser-proven.
  - If the same transport failure repeats outside the sandbox, use `docs/review_prompts/2026-06-03-tactical-browser-transport-blocker.md` for an environment-blocker decision.
- Reporting note:
  - This update stays folded into the existing feature/decision report unit `2026-06-03-05`.
  - `npm run report` should keep generating per-unit Markdown/HTML under `docs/reports/units/2026-06-03/`, and Discord latest-section attachments should use the generated latest unit HTML rather than the full daily cumulative HTML.

## Tactical Trusted Gate Managed-Sandbox Rerun

- Kept the foremost unfinished v0.9 item on WP3 Slice A browser proof.
- Reran `npm run qa:tactical:trusted` in this managed sandbox.
- Result:
  - standard tactical QA failed before gameplay evaluation,
  - the 30000 ms retry also failed before gameplay evaluation,
  - both attempts hit Chrome/CDP `Target.getTargets` timeout and remote-debugging-port `127.0.0.1 listen EPERM`.
- `alpha_test/outputs/tactical-trusted-gate/latest.json` was refreshed with:
  - `status: blocked`,
  - `transportFailure: true`,
  - blocker prompt `docs/review_prompts/2026-06-03-tactical-browser-transport-blocker.md`.
- This is transport-blocked gate evidence only, not browser proof and not a tactical-focus gameplay failure.
- No new memory, slot, shop, meta progression, region, weapon, hint, balance change, UI expansion, or gameplay scope was added.
- Next executable gate remains sandbox-outside trusted-local `npm run qa:tactical:trusted`, or an explicit environment-blocker decision if the same transport failure repeats outside the sandbox.

## Tactical Trusted Rerun Feedback Synthesis

- Read the devloop feedback prompt and both Claude/Codex responses for `2026-06-03-devloop-050050-feedback-4`.
- Common conclusion:
  - WP3 Slice A `전술 집중` remains code-complete and scope-valid, but it is not browser-proven.
  - The managed-sandbox tactical trusted rerun is valid gate evidence, not gameplay proof.
  - AI quick `GO_CANDIDATE`, Alpha Fun Score `0.8846`, regret `0.8073`, irritation `0.0104`, and restart `0.90` remain planning evidence only.
  - Browser combat evidence still outranks aggregate AI proxy metrics before people testing, balance reporting, or Unity-transition judgment.
  - `멈춘 초침` deletion frequency, echo-pivot score, and post-loss challenge score stay observation risks rather than immediate tuning scope.
- Conflict summary:
  - There is no material next-scope conflict.
  - Claude emphasizes trusted-local execution and observation risks.
  - Codex CLI narrows the next executable task to trusted-local tactical gate result recording.
- Selected next task:
  - Run sandbox-outside trusted-local `npm run qa:tactical:trusted`.
  - If it passes, record WP3 Slice A as browser-proven.
  - If the same transport failure repeats outside the sandbox, use `docs/review_prompts/2026-06-03-tactical-browser-transport-blocker.md` for an explicit environment-blocker decision before adding scope.
- Reporting note:
  - This feedback stays folded into report unit `2026-06-03-05`, not a new procedural unit.
  - `npm run report` should keep generating per-unit Markdown/HTML under `docs/reports/units/2026-06-03/`; Discord attachments should use the generated latest unit HTML.
