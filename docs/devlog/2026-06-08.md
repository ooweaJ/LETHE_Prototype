# 2026-06-08 Devlog

## v0.12 Playtest Package Rerun

- Goal: finish the next queued step after the balance loop gate fix by regenerating the controlled human-test package.
- Confirmed `docs/HUMAN_PLAYTEST_GUIDE.md` and `docs/PLAYTEST_NOTES.md` are already v0.12-specific.
- Verification:
  - `npm run playtest:package:dry`: pass, output target `dist\lethe-v0.12-playtest`.
  - `npm run playtest:package`: pass, regenerated `dist\lethe-v0.12-playtest`.
  - `npm run report`: pass, generated `docs\reports\2026-06-08.html` and one unit report under `docs\reports\units\2026-06-08`.
  - `npm run report:check`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `npm run report:discord:unit:dry`: pass.
- Decision: the latest package is ready for controlled human sessions.
- Risk: generated `dist/` output is ignored by git, so it should be regenerated when sharing a fresh build.
- Discord:
  - `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - Next trusted-local command: `npm run report:discord:unit`.
- Next task: run controlled sessions, place downloaded JSON logs in `playtest_logs/`, then run `npm run playtest:summary`.

## Orchestration Interface Adoption

- Goal: test the shared Codex orchestration standard inside the active LETHE project.
- Added `docs/orchestration/` with core files for project brief, status, current task, next tasks, prompt context, runbook, scope guard, and decision log.
- Added extension folders and seed files for devlog, reports, review prompts/responses, evidence, and templates.
- Updated `AGENTS.md` with an Orchestration Interface section while preserving the existing LETHE-specific workflow, report, git, scope, Claude, and preflight rules.
- Fixed `EXISTING_PROJECT_MIGRATION_PROMPT.md` so it now describes orchestration adoption rather than blind document migration.
- Verification:
  - Read `docs/orchestration/README.md`: pass.
  - Read `docs/orchestration/STATUS.md`: pass.
  - Read `docs/orchestration/CURRENT_TASK.md`: pass.
  - Read `docs/orchestration/NEXT_TASKS.md`: pass.
  - `npm run report`: pass.
  - `npm run report:check`: first parallel run failed because it raced report generation; sequential rerun passed.
  - `npm run report:discord:unit:dry`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Discord:
  - `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - Next trusted-local command: `npm run report:discord:unit`.
- Decision: old docs remain detailed archives; `docs/orchestration/` is the quick resume and operating interface.
- Not migrated: full historical reports, large QA outputs, and every old review prompt/response were not copied into orchestration; they remain linked or referenced from legacy locations.
- Next: test this interface in the next Codex session, then decide whether to add a generated HTML orchestration dashboard.

## Orchestration Dashboard Current-State Refresh

- Goal: make `docs/orchestration/index.html` useful as the quick human-readable view.
- Updated the dashboard top summary cards:
  - current stage: v0.12 controlled human-test gate,
  - latest verification: `balance:loop GO`, full clear `60%`, death `40%`,
  - blocker: missing human reaction evidence.
- Updated `docs/orchestration/STATUS.md` to record that the dashboard exists.
- Updated `docs/orchestration/NEXT_TASKS.md` so dashboard work now means automation/refresh, not initial dashboard creation.
- Decision: Markdown remains the source of truth; the dashboard is a readable view until a generator exists.
- Verification:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run report:discord:unit:dry`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Discord:
  - `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - Next trusted-local command: `npm run report:discord:unit`.
- Next: run controlled human sessions, collect logs, and run `npm run playtest:summary`.

## Korean Human-Facing Dashboard Normalization

- Goal: make the orchestration HTML usable as a Korean project report screen for deciding the next prompt.
- Rebuilt `docs/orchestration/index.html` around:
  - current status,
  - next user action,
  - copyable recommended prompt,
  - last completed report,
  - current task and done criteria,
  - next task candidates,
  - evidence,
  - scope guard,
  - development document links.
- Added `docs/orchestration/reports/index.html` for report browsing.
- Added `docs/orchestration/devlog/index.html` for devlog browsing.
- Updated orchestration Markdown docs to define HTML as a Korean readable view and Markdown as the source of truth.
- Decision: the dashboard shows last completed state, not live in-progress Codex thoughts.
- Verification:
  - `npm run report`: pass.
  - `npm run report:check`: pass.
  - `npm run report:discord:unit:dry`: pass.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Discord:
  - `npm run report:discord:unit` was blocked by the approval reviewer because sending workspace-generated reports/attachments to an external Discord webhook is treated as potential private data exfiltration in this Codex session.
  - Next trusted-local command: `npm run report:discord:unit`.
- Next: add a Markdown-to-dashboard generator once the HTML shape is validated.

## HTML Interface Contract Alignment

- Goal: update the current orchestration HTML changes to match the revised `EXISTING_PROJECT_MIGRATION_PROMPT.md`.
- Changed:
  - restored `docs/orchestration/index.html` as the main Korean dashboard,
  - added `docs/orchestration/command.html` for the compact next instruction,
  - added `docs/orchestration/runbook.html` for repeated operating procedures,
  - added a user-facing report HTML under `docs/orchestration/reports/`,
  - updated `AGENTS.md` and orchestration Markdown docs so HTML is the human-facing project interface and Markdown remains the source of truth.
- Decision: `reports/index.html` and `devlog/index.html` remain browse pages; the required interface files are `index.html`, `command.html`, and `runbook.html`.
- Verification:
  - `npm run report`: pass, generated 5 unit reports.
  - `npm run report:check`: pass, 5 report units.
  - `npm run report:discord:unit:dry`: pass, latest unit 05 summary generated.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Discord:
  - `npm run report:discord:unit` was rejected by the approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook without explicit user approval for that exfiltration.
  - Next trusted-local command: `npm run report:discord:unit`.

## Project Dashboard Compaction

- Goal: reduce the project dashboard to a readable 30-second summary.
- Changed:
  - simplified `docs/orchestration/index.html` around state, blocker, next gate, recent completion, and primary links,
  - removed content that belongs in `command.html`, `runbook.html`, or `reports/`,
  - updated orchestration Markdown docs to preserve the role split.
- Decision: the dashboard should not explain the whole system; it should tell the user where the project stands and where to click next.
- Verification:
  - `npm run report`: pass, generated 6 unit reports.
  - `npm run report:check`: pass, 6 report units.
  - `npm run report:discord:unit:dry`: pass, latest unit 06 summary generated.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
- Discord:
  - `npm run report:discord:unit` was rejected by the approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook without explicit user approval for that exfiltration.
  - Next trusted-local command: `npm run report:discord:unit`.

## Project Dashboard Surface Trim

- Goal: make `docs/orchestration/index.html` feel like a compact dashboard rather than an interface explanation page.
- Changed:
  - heading now reads `상태 요약`,
  - removed explanatory subtitle,
  - removed the bottom command/runbook/report link cards,
  - kept only status-focused dashboard sections.
- Decision: dashboard UI should carry core state only; reports and other interface pages can carry detail.
- Verification:
  - `npm run report`: pass, generated 7 unit reports.
  - `npm run report:check`: pass, 7 report units.
  - `npm run report:discord:unit:dry`: pass, latest unit 07 summary generated.
  - `npm run doctor`: pass, 50 pass / 0 warn / 0 fail.
  - `Select-String` dashboard check: only `상태 요약` remained; removed target strings were absent.
- Discord:
  - `npm run report:discord:unit` was rejected by the approval reviewer because it would send workspace-generated report content and attachments to an external Discord webhook, which is untrusted by default and not explicitly approved by the user.
  - Next trusted-local command: `npm run report:discord:unit`.
