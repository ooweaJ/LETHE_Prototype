# LETHE 개발 보고서 - 2026-06-04

# 2026-06-04-01 - 기획서 v0.9 동기화

## 1. Current build status

- LETHE HTML Alpha remains v0.9.
- Current game and the old v6 Word 기획서 were not aligned.
- The release-feel prototype currently includes build identity, pressure rhythm, post-loss challenge, memory refill, and tactical focus.
- AI/browser gate evidence remains sufficient for controlled 1-person browser playtest preparation, not Unity transition.

## 2. What changed today

- Added `docs/LETHE_망각의_군주_프로토타입_기획서_v0.9.md` as the current Markdown planning source.
- Updated `docs/LETHE_망각의_군주_완성형_기획서_v6.docx` from the Markdown source.
- Added `scripts/update_design_doc_from_markdown.ps1` for repeatable DOCX regeneration.
- Updated `README.md` to reference the v0.9 planning source and current implemented scope.
- Updated `docs/CODEX_STATUS.md` with the planning-document sync note.

## 3. Test results and evidence

- DOCX text extraction after regeneration confirmed the v0.9 title, current loop, tactical focus, post-loss challenge, and scope guard sections.
- `node --check src/game.js`: passed.
- `npm run report`: generated daily HTML and unit files.
- `npm run report:check`: passed.
- DOCX visual render QA was skipped because LibreOffice/`soffice` is not installed in this environment.

## 4. Decisions made

- Treat the Markdown v0.9 planning file as the source of truth.
- Keep the existing Word 기획서 filename for continuity, but update its contents to v0.9.
- Record that the older complete-game/meta-progression sections are not current prototype scope.
- Do not add gameplay while syncing the document.

## 5. Problems or risks

- The DOCX could not be visually rendered here, so formatting was validated structurally rather than by page PNG inspection.
- The DOCX filename still says `v6`; its contents now identify the document as the v0.9 prototype planning update.
- Future design changes should update the Markdown source first, then regenerate the DOCX.

## 6. GPT handoff summary

- 기존 기획서와 현재 게임은 달랐다.
- 최신 기준은 v0.9 HTML prototype: 2 weapons, 6 memories, 3 active slots, boss cycles, dependency forgetting, echo, 75-second deficit survival, refill, build identity, and tactical focus.
- Unity 전환은 아직 자동 지표만으로 판단하지 않는다. 다음 근거는 controlled 1-person browser playtest다.

## 7. Next Codex tasks

- Use `docs/LETHE_망각의_군주_프로토타입_기획서_v0.9.md` for future planning diffs.
- If planning changes again, run `pwsh scripts/update_design_doc_from_markdown.ps1`.
- Continue with final preflight/report/Discord and controlled 1-person playtest preparation when the user is ready.

## 8. Portfolio notes

- Problem: the project had a working v0.9 prototype but an older planning document, creating ambiguity about what was actually current.
- Direction: make the latest HTML prototype design explicit and diffable.
- Action: created a Markdown source, regenerated the Word planning document, and updated project references.
- Result: implementation, planning, and handoff docs now describe the same v0.9 scope.

# 2026-06-04-02 - v0.10 망각 갈림길 구현

## 1. Current build status

- LETHE HTML Alpha is now `v0.10`.
- The v0.10 target document is `docs/LETHE_망각의_군주_프로토타입_기획서_v0_10.md`.
- The prototype now implements the first v0.10 target slice: top-2 forgetting fork, echo unlock routes, 8 memories, 4 synergies, 180-second first boss, and simplified dependency scoring.

## 2. What changed today

- Changed the default run schedule to 600 seconds with bosses at 180 / 340 / 490 / 600 seconds.
- Added `잿빛 보호막` and `망각의 낙인`.
- Added `각인 연쇄` synergy.
- Replaced automatic most-dependent forgetting with `망각 갈림길`.
- Added strong vs safe echo unlock paths depending on whether the player releases rank-1 or rank-2 dependency.
- Added real-time dependency/omen meter wording and tactical-focus overheat/dependency tradeoff.
- Added `forkChoice`, `predictionAccuracy`, and `echoUnlocks` logs.
- Regenerated the Word planning document from the v0.10 Markdown source.

## 3. Test results and evidence

- `node --check src/game.js`: passed.
- `node --check scripts/run_browser_identity_qa.js`: passed.
- `node --check scripts/run_browser_pressure_qa.js`: passed.
- `node --check scripts/run_autonomous_dev_loop.js`: passed.
- `npm run report`: generated daily HTML and 2 unit files.
- `npm run report:check`: passed.
- `npm run doctor`: passed, 46 pass / 0 warn / 0 fail.
- `npm run qa:identity`: blocked before browser assertion because Chrome/Chromium was not found and `CHROME_PATH` is not set.
- `npm run qa:tactical:trusted`: blocked before tactical gameplay assertion for the same `CHROME_PATH` reason.
- AI simulator recalibration is still pending for v0.10; old AI scores should not be used as fork-choice evidence.

## 4. Decisions made

- Implement only the v0.10 target slice, not broader content expansion.
- Keep active memory slots at 3.
- Do not add shop, meta progression, new region, new weapon, new enemy, final boss completion, or multi-region structure.
- Treat AI fun metrics as weak planning evidence for this change; browser QA and 1-person playtest are the next meaningful gates.

## 5. Problems or risks

- Existing browser QA scripts were version-updated, but v0.10-specific fork-choice browser assertions still need to be added or run manually.
- Browser QA cannot run in the current environment until Chrome is installed or `CHROME_PATH` points to Chrome/Chromium.
- The alpha simulator does not yet model fork choice and echo unlock routes, so old AI scores should not be used as v0.10 proof.
- Balance of the new memories and strong/safe echo paths is provisional until browser/playtest evidence.

## 6. GPT handoff summary

- v0.10 changes the emotional core from "the game takes your best memory" to "you choose what to release at a top-2 fork."
- The player-facing bet is now prediction, agency, and stronger pivot reward for releasing the rank-1 dependency.
- GPT/Claude should review whether the fork and echo unlock framing solves the "optimal play punishment" risk before broader testing.

## 7. Next Codex tasks

- Add or run v0.10 browser QA for the fork-choice path.
- Set `CHROME_PATH` or run the browser QA on a trusted local environment with Chrome/Chromium available.
- Recalibrate AI simulator only as planning evidence, not emotion proof.
- Prepare controlled 1-person browser playtest focused on first-forgetting agency, prediction accuracy, and whether rank-1 release feels like a stronger pivot.

## 8. Portfolio notes

- Problem: v0.9 risked reading as punishment for optimal play.
- Direction: convert loss into an authored fork with meaningful pivot rewards.
- Action: implemented top-2 release choice, echo unlock routes, expanded memory/synergy space, and dependency visibility.
- Result: the prototype now tests whether "letting go" can feel like agency instead of confiscation.

# 2026-06-04-03 - v0.11 기억 획득과 무기 진화 구현

## 1. 현재 빌드 상태

- LETHE HTML Alpha가 `v0.11`로 올라갔다.
- 현재 기획 원본은 `docs/LETHE_망각의_군주_프로토타입_기획서_v0_11.md`다.
- 프로토타입은 이제 시작 기억 1개, 사냥/레벨업 기억 획득, 보유 기억 강화, 가중 랜덤 망각, 무기 잔향, 무기 진화 2종을 구현한다.

## 2. 오늘 바뀐 것

- 시작 세팅을 시작 기억 3개에서 시작 기억 1개로 바꿨다.
- 레벨업 선택지를 새 기억 획득, 보유 기억 강화, 런 성장, `잔향 증폭`으로 개편했다.
- 능동 기억 레벨과 레벨별 피해/범위/지속/쿨다운 보정을 추가했다.
- v0.10의 `망각 갈림길`을 제거하고, 예측만 입력한 뒤 가중 랜덤으로 실제 망각이 결정되게 했다.
- 전조/질문 단계에서 망각 확률 UI를 표시한다.
- 레벨이 누적되는 `echoState`와 무기 진화 2종을 추가했다:
  - `피의 늪`,
  - `파쇄 각인`.
- 결과 설문에 Q5 성장 체감 문항을 추가했다.
- README, 상태 문서, 다음 작업, Discord 보고 문서, v0.11 기획서를 갱신했다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `node --check scripts/run_browser_identity_qa.js`: 통과.
- `node --check scripts/run_browser_pressure_qa.js`: 통과.
- `powershell -ExecutionPolicy Bypass -File scripts/update_design_doc_from_markdown.ps1`: v0.11 Markdown 기준 Word 기획서 재생성 완료.
- `npm run report`: 일일 HTML과 단위 보고서 4개 생성.
- `npm run report:check`: 통과.
- `npm run doctor`: 46 pass / 0 warn / 0 fail.
- `npm run report:discord:unit:dry`: 최신 보고 단위 미리보기 통과.
- `node scripts/send_discord_report.js docs/reports/2026-06-04.md --dry-run --section "2026-06-04-03 - v0.11 기억 획득과 무기 진화 구현"`: v0.11 구현 단위 요약과 첨부 확인.
- `npm run qa:identity`: Chrome/Chromium 실행 파일을 찾지 못했고 `CHROME_PATH`가 없어 브라우저 검증 전 차단.
- `npm run qa:pressure`: 같은 Chrome/`CHROME_PATH` 사유로 차단.

## 4. 결정한 것

- v0.10의 선택형 망각 갈림길보다 v0.11의 가중 랜덤 망각을 따른다.
- 기억 8종, 무기 2종, 활성 슬롯 3개, 600초 런, 180 / 340 / 490 / 600초 보스 스케줄을 유지한다.
- 프로토타입 무기 진화는 `피의 늪`, `파쇄 각인` 2종만 구현한다.
- AI 지표는 v0.11 루프를 시뮬레이터가 반영하기 전까지 기획 참고 근거로만 본다.

## 5. 문제 또는 리스크

- 기억 획득/강화와 무기 진화가 새로 들어가서 밸런스는 임시값이다.
- 기존 브라우저 QA 스크립트는 v0.11 버전 확인까지만 맞췄고, 획득/망각/진화 전용 assertion은 아직 부족하다.
- 현재 관리 환경은 Chrome/Chromium을 찾지 못해 브라우저 QA가 막힌다.
- 알파 시뮬레이터는 기억 획득, 가중 망각, 무기 진화 모델을 다시 맞춰야 한다.

## 6. GPT/Claude 인계 요약

- v0.11은 망각을 선택 갈림길이 아니라 무기 성장으로 재정의한다.
- 핵심 검증 질문은 “내가 키운 빌드에서 의존하던 능동 기억을 잃었지만, 무기가 그 기억을 받아 새 몸이 됐다”로 느껴지는지다.
- GPT/Claude는 브라우저/플레이테스트 로그에서 성장 체감, 확률 이해, 잔향/진화 이해, 망각 후 짜증 정도를 봐야 한다.

## 7. 다음 Codex 작업

- Chrome/Chromium 또는 `CHROME_PATH`가 준비된 환경에서 브라우저 QA를 실행한다.
- 획득/강화, 가중 망각, 잔향 상태, 무기 진화 전용 v0.11 브라우저 assertion을 추가한다.
- v0.11 루프를 반영하도록 AI 시뮬레이터를 재보정한다.
- 1인 브라우저 플레이테스트로 성장감과 망각 후 피벗 감정을 확인한다.

## 8. 포트폴리오 노트

- 문제: v0.10은 선택권은 좋아졌지만, 망각 전에 빌드를 키우는 성장 판타지가 아직 약했다.
- 방향: 기억을 사냥으로 얻고 강화하는 빌드로 만들고, 상실을 무기 진화로 전환한다.
- 행동: 기억 획득, 능동 레벨, 가중 망각, 레벨 잔향, 무기 진화 2종을 구현했다.
- 결과: 프로토타입은 이제 망각이 처벌이 아니라 변형과 빌드 피벗으로 읽히는지 검증할 수 있다.

# 2026-06-04-04 - 수동 보고와 Discord 기록 경로 정리

## 1. 현재 빌드 상태

- 보고 스크립트는 일일 보고, 작업 단위 보고, 최신 섹션 첨부, Discord dry-run을 이미 지원한다.
- 자동 루프는 Discord 작업 단위 보고를 호출하지만, 일반 수동 Codex 구현 턴은 마지막에 보고/Discord 명령을 명시적으로 실행해야 한다.
- v0.11 구현 보고는 `dry-run`만 됐던 상태에서 실제 Discord 전송까지 보정 완료했다.

## 2. 오늘 바뀐 것

- `docs/DISCORD_REPORTING.md`에 수동 보고 절차를 문서화했다.
- `docs/ai/`는 매 작업 자동 누적 로그가 아니라 AI 협업 규칙/실패 사례를 보관하는 정적 문서라고 기록했다.
- 작업별 AI 활용 기록은 devlog, daily report, review prompt, review response에 남긴다고 정리했다.
- `## 2. 오늘 바뀐 것`처럼 번호가 붙은 섹션도 Discord 요약이 인식하도록 `scripts/send_discord_report.js`를 고쳤다.
- v0.11 구현 보고와 수동 보고 경로 정리 보고서를 한국어로 다시 작성했다.

## 3. 테스트 결과와 근거

- 스크립트 검색으로 `run_autonomous_dev_loop.js`가 `sendWorkUnitReport()`를 호출하는 것을 확인했다.
- `report:discord:unit:dry`와 `report:discord:unit` 명령이 이미 존재하는 것도 확인했다.
- `node scripts/send_discord_report.js docs/reports/2026-06-04.md --dry-run --section "2026-06-04-03 - v0.11 기억 획득과 무기 진화 구현"`이 실제 작업/상태/문제/인계 요약을 뽑고 v0.11 단위 HTML을 첨부하는 것을 확인했다.
- `node scripts/send_discord_report.js docs/reports/2026-06-04.md --section "2026-06-04-03 - v0.11 기억 획득과 무기 진화 구현"`로 v0.11 구현 보고 실제 Discord 전송을 완료했다.

## 4. 결정한 것

- 수동 구현 후 Discord 업로드가 없었다고 해서 스크립트 자체가 자동 실패한 것으로 보지 않는다.
- Codex 관리 세션에서는 기본 증거로 `npm run report:discord:unit:dry`를 실행한다.
- 실제 webhook 전송은 의도된 trusted local 환경에서 `npm run report:discord:unit` 또는 특정 섹션 전송으로 수행한다.
- v0.11 구현처럼 사용자가 완료 알림을 기대하는 큰 단위는 `dry-run`에서 멈추지 않고 실제 전송 결과까지 확인한다.

## 5. 문제 또는 리스크

- 실제 Discord 업로드는 `.env`의 `DISCORD_WEBHOOK_URL`과 네트워크 접근이 필요하다.
- 수동 작업 후 Codex가 dry-run에서 멈추면 Discord/latest-section 증거가 구현보다 뒤처진다.
- `docs/ai/`를 작업별 로그로 기대하면 문서가 오래된 것처럼 보일 수 있다.
- latest-section은 항상 마지막 보고 단위를 잡기 때문에, v0.11 구현 보고처럼 특정 단위를 보내려면 `--section`을 명시해야 한다.

## 6. GPT/Claude 인계 요약

- 보고 시스템은 존재하지만 자동 전송 경로는 자동 루프에 묶여 있다.
- 수동 구현은 보고서 생성, 보고서 체크, Discord dry-run 또는 실제 전송까지 닫아야 한다.
- 작업별 AI 활용 증거는 reports/devlogs와 review prompt/response 파일에 남긴다.
- 이번 누락은 dry-run을 실제 전송으로 착각한 Codex 운용 실수이며, v0.11 구현 보고는 실제 전송으로 보정됐다.

## 7. 다음 Codex 작업

- 매 수동 구현 단위 후 보고서 생성/체크/dry-run 또는 실제 전송을 실행한다.
- 같은 실수가 반복되면 수동 종료용 wrapper 명령을 추가한다.
- Discord 실제 전송은 명시적인 trusted-local 작업으로 유지한다.
- 큰 기능 구현 단위는 Discord 전송 후 `Uploaded ... to Discord.` 출력까지 확인한다.

## 8. 포트폴리오 노트

- 문제: 수동 Codex 작업 후 완료 보고와 AI 활용 기록이 자동으로 움직인다고 기대했지만 실제 구조는 그렇지 않았다.
- 방향: 자동 루프의 자동화와 수동 작업의 종료 절차를 분리해 명확히 한다.
- 행동: 실제 명령 경로와 기록 원본 위치를 문서화했다.
- 결과: 앞으로 Discord 업로드가 암묵적으로 일어난다고 가정하지 않고도 작업 흔적을 명확히 남길 수 있다.

# 2026-06-04-05 - v011 밸런스 수치표 정리

## 1. 현재 빌드 상태

- LETHE HTML Alpha는 v0.11 구조 구현 이후 첫 밸런스 점검 단계다.
- 별도 밸런스 수치표는 없었고, 실제 수치는 `src/game.js`와 기획서에 흩어져 있었다.
- 현재 코드 기준 수치표는 `docs/BALANCE_TABLE_v0_11.md`에 새로 정리했다.

## 2. 오늘 바뀐 것

- `docs/BALANCE_TABLE_v0_11.md`를 추가했다.
- 수치표에 런 구조, 플레이어, 무기, 기억, 적, 스폰, 보스, 시너지, 망각 확률, 잔향, 무기 진화를 정리했다.
- README, `docs/CODEX_STATUS.md`, `docs/NEXT_TASKS.md`에 밸런스 수치표 위치와 다음 밸런스 범위를 연결했다.
- 첫 번째 의심 원인으로 `굶주린 칼무리`가 프레임마다 피해를 주는 문제를 기록했다.

## 3. 테스트 결과와 근거

- 코드 검색으로 현재 실제 수치가 독립 문서가 아니라 `src/game.js`에 흩어져 있음을 확인했다.
- `src/game.js` 기준으로 무기/기억/적/보스/잔향/진화 수치를 수치표에 옮겼다.
- `굶주린 칼무리`는 `dt` 없이 업데이트마다 7.4 피해를 적용하고 있어, 60fps 기준 단일 대상 약 444 DPS까지 튈 수 있음을 확인했다.
- `npm run report`: 일일 HTML과 단위 보고서 5개 생성.
- `npm run report:check`: 통과.
- `node scripts/send_discord_report.js docs/reports/2026-06-04.md --dry-run --section "2026-06-04-05 - v011 밸런스 수치표 정리"`: 한국어 Discord 본문과 수치표 보고 단위 HTML 첨부를 확인.
- 실제 Discord 전송은 이번 새 밸런스 수치표 작업에 대한 사용자 명시 요청이 없어 보류했다.

## 4. 결정한 것

- 밸런스 조정 기준 문서는 `docs/BALANCE_TABLE_v0_11.md`로 둔다.
- 다음 구현은 새 기능보다 첫 밸런스 패스를 우선한다.
- 첫 밸런스 패스는 `굶주린 칼무리`, 첫 180초 성장 속도, `피의 늪`, 보스 HP를 우선 본다.

## 5. 문제 또는 리스크

- 현재 수치는 v0.11 구조 검증용 임시값이라 실제 플레이 감각과 맞지 않을 가능성이 높다.
- `굶주린 칼무리`가 다른 기억 대비 압도적으로 강하면 기억 획득/망각/진화 테스트가 왜곡된다.
- `피의 늪`은 쌍검 발동률과 틱 피해가 높아 후반 화면 장악으로 이어질 수 있다.
- Chrome/`CHROME_PATH` 문제 때문에 자동 브라우저 QA는 아직 막혀 있다.

## 6. GPT/Claude 인계 요약

- 기획 구조는 v0.11로 어느 정도 잡혔지만, 수치 밸런스는 아직 정리/검증 전이었다.
- 현재 가장 먼저 의심할 수치는 `굶주린 칼무리`의 프레임 단위 피해다.
- GPT/Claude는 밸런스 리뷰 시 수치표를 기준으로 기억별 역할 대비 DPS/유틸/리스크가 맞는지 봐야 한다.

## 7. 다음 Codex 작업

- `굶주린 칼무리`를 초당 피해 기준으로 고친다.
- 첫 보스 전 목표를 평균 레벨업 2-3회, 기억 2-3개 완성으로 맞춘다.
- `피의 늪` 발동률/틱 피해를 낮춰 기준선을 만든다.
- v0.11 전용 브라우저/AI 밸런스 체크를 추가한다.

## 8. 포트폴리오 노트

- 문제: 게임 구조는 잡혔지만 수치가 흩어져 있어 밸런스 판단 기준이 없었다.
- 방향: 코드 기준 실제 수치를 표로 모아 조정 가능한 기준선을 만든다.
- 행동: v0.11 수치표를 만들고 최우선 밸런스 위험을 표시했다.
- 결과: 다음 작업은 감으로 튜닝하는 대신 수치표 기준으로 밸런스 패스를 시작할 수 있다.

# 2026-06-04-06 - v012 밸런스 기준선 1차 구현

## 1. 현재 빌드 상태

- LETHE HTML Alpha는 `v0.12` 첫 밸런스 기준선 구현 상태다.
- v0.11의 기억 획득, 가중 망각, 무기 잔향, 무기 진화 구조는 유지했다.
- 이번 패스는 새 기능 확장이 아니라 `굶주린 칼무리`, 적 스케일링, telemetry, 전술 집중 가중치, `피의 늪` 폭주 위험을 먼저 정리했다.

## 2. 오늘 바뀐 것

- 브라우저 라벨과 QA 기대 버전을 `v0.12`로 올렸다.
- `굶주린 칼무리`를 `28 DPS * dt`로 교정하고 가까운 타깃 우선 soft cap을 적용했다.
- 일반 적 HP/피해에 시간+레벨 하이브리드 스케일링을 추가했다.
- JSON 로그에 `balance`와 `telemetry`를 추가했다.
- telemetry는 피해원별 누적 피해/DPS, 보스 TTK/집중 DPS, 레벨업 시각, 3슬롯 완성 시각, 적 스케일링, 5초 샘플을 남긴다.
- 전술 집중의 망각 가중치 반영을 12배에서 3배로 낮췄다.
- `피의 늪`은 쌍검 30%, 대검 20% 발동률로 낮추고, 지속 시간/틱/피해를 줄였으며 최대 활성 장판 5개 제한을 넣었다.
- `docs/CODEX_STATUS.md`, `docs/NEXT_TASKS.md`, `docs/devlog/2026-06-04.md`를 v0.12 기준으로 갱신했다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `node --check scripts/run_browser_identity_qa.js`: 통과.
- `node --check scripts/run_browser_pressure_qa.js`: 통과.
- `npm run doctor`: 46 pass / 0 warn / 0 fail.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8904`.
- `npm run qa:identity`: Chrome/Chromium 실행 파일을 찾지 못했고 `CHROME_PATH`가 없어 브라우저 assertion 전에 차단.
- `npm run report:discord:unit`: `2026-06-04-06-v012-밸런스-기준선-1차-구현.html` 실제 Discord 전송 완료.

## 4. 결정한 것

- 보스 HP는 이번 패스에서 올리지 않는다.
- 먼저 실제 브라우저 로그의 `telemetry.bossFights[].ttk`, `focusedDps`, `dpsBySource`를 보고 첫 보스 HP 목표를 역산한다.
- 순수 레벨 비례 스케일링은 성장 벌칙처럼 느껴질 수 있어 시간+레벨 하이브리드로 간다.
- 이후 의미 있는 구현/밸런스/문서화 단위는 dry-run에서 멈추지 않고 실제 Discord 작업 단위 보고까지 완료한다.

## 5. 문제 또는 리스크

- 브라우저 QA는 현재 환경에서 Chrome/`CHROME_PATH` 문제로 막혀 있어 실제 플레이 assertion은 아직 없다.
- AI 시뮬레이터는 v0.12 코드 telemetry를 직접 실행하는 검증이 아니므로 밸런스 증거로 과신하면 안 된다.
- `굶주린 칼무리` DPS 교정으로 초반 체감이 급격히 약해질 수 있다.
- 보스 HP와 XP 곡선은 telemetry 로그를 본 뒤 2차 패스로 조정해야 한다.

## 6. GPT/Claude 인계 요약

- v0.12 1차 패스는 세 리뷰의 공통 우선순위를 반영했다.
- Claude/GPT 계열 제안의 구조화된 수치표와 안전장치, Gemini의 엔진/하이브리드 스케일링 관점을 합쳤다.
- 다음 리뷰는 추상 수치표가 아니라 실제 JSON의 `telemetry` 필드를 기준으로 봐야 한다.
- 특히 보스 TTK, 기억별 DPS, 첫 180초 레벨업 시각, 3슬롯 완성 시각을 기준으로 XP/보스/시너지 2차 조정을 판단한다.

## 7. 다음 Codex 작업

- Chrome/`CHROME_PATH`가 준비된 trusted local 환경에서 v0.12 브라우저 로그를 수집한다.
- 수집한 JSON의 `telemetry.bossFights[].ttk`와 `telemetry.dpsBySource`로 보스 HP를 역산한다.
- `telemetry.levelUpTimestamps`와 `slotsFilledAt`로 첫 180초 성장 페이스를 조정한다.
- 그 뒤 시너지 배율과 XP 곡선을 v0.12 2차 패스로 조정한다.

## 8. 포트폴리오 노트

- 문제: 구조는 잡혔지만 한 기억의 dt 버그와 진화 장판 폭주가 전체 밸런스 판단을 오염시킬 수 있었다.
- 방향: 감으로 보스 HP를 올리기 전에 피해원별 DPS와 보스 TTK를 기록하는 기준선을 만든다.
- 행동: dt 교정, 하이브리드 스케일링, telemetry, 망각 가중치 완화, 장판 cap을 구현했다.
- 결과: 다음 밸런스 패스는 실제 로그를 기준으로 보스/XP/시너지를 조정할 수 있다.
