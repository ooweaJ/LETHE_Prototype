# 2026-06-02-13 - v0.5 레벨업 브라우저 gate 통과 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha는 v0.5 core-fun human-test ready 상태다.
- Claude가 요구한 레벨업 UI/`runGrowth` 브라우저 gate를 Chrome headless로 통과했다.
- 다음 단계는 5-8명 사람 테스트다.

## 2. 오늘 바뀐 것

- `src/game.js`에 `?qa=fast,levelup` 자동 QA 모드를 추가했다.
- QA 모드는 실제 브라우저 JS에서 레벨업 3택 화면을 띄우고, 선택 후 전투 재개와 `runGrowth` payload를 DOM dataset에 남긴다.
- `docs/NEXT_TASKS.md`와 `docs/CODEX_STATUS.md`를 human-test ready 상태로 갱신했다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- Chrome headless:
  - URL: `file:///C:/jaewoo/LETHE_Prototype/index.html?qa=fast,levelup`
  - `status: complete`
  - `levelUpSeen: true`
  - `resumedAfterUpgrade: true`
  - `hasRunGrowthPayload: true`
  - `choicesTaken`과 `runGrowth.choicesTaken`에 같은 성장 선택 기록.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8607`, Early Fun `0.8660`, kill tempo `0.9570`, level-ups `4.11`.

## 4. 결정 사항

- Claude가 막았던 이유였던 브라우저 검증 gate는 통과로 본다.
- 새 기능 추가 없이 사람 테스트로 간다.
- 기억별 투자-망각 상호작용과 28% power drop 보정은 사람 테스트 후 판단한다.

## 5. 문제 또는 리스크

- in-app Browser 플러그인은 여전히 Windows sandbox 오류로 실패한다.
- Chrome headless는 실제 Chrome 실행이지만 사람 눈으로 보는 visual QA는 아니다. 첫 사람 테스트 전에 로컬에서 한 번 열어보는 정도는 권장한다.

## 6. GPT/Claude 인계 요약

- Claude verdict는 조건부 `ITERATE_BEFORE_TEST`였고, 조건은 v0.5 브라우저 동작 검증이었다.
- 해당 조건을 Chrome headless로 통과했으므로 실무 판단은 `GO_TO_HUMAN_TEST`다.

## 7. 다음 Codex 작업

- `docs/HUMAN_PLAYTEST_GUIDE.md` 기준으로 5-8명 테스트를 진행한다.
- 테스트 중 초반 1-3분 재미, 성장 선택 고민, 망각 후회/짜증 반응을 기록한다.

## 8. 포트폴리오 메모

- 문제: AI 지표와 Claude 평가가 좋아도 신규 레벨업 UX가 실제 브라우저에서 깨지면 테스트가 무효가 된다.
- 방향: Browser 플러그인 실패를 Chrome headless QA로 보완한다.
- 실행: `?qa=fast,levelup` 자동 QA 모드를 만들고 Chrome에서 레벨업 UI/재개/log payload를 검증했다.
- 결과: v0.5는 사람 테스트 진입 준비가 끝났다.

---
