# 2026-06-02-18 - 사람 테스트 요약 자동화 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha v0.5는 사람 테스트 준비 상태다.
- 사람 테스트 자체는 아직 필요하지만, 테스트 후 JSON 로그를 요약하고 Claude/GPT에 넘기는 자동화 라인은 준비됐다.

## 2. 오늘 바뀐 것

- `playtest_logs/README.md`를 추가했다.
- `docs/playtest_summaries/README.md`를 추가했다.
- `scripts/summarize_playtests.js`를 추가했다.
- `playtest:summary`, `playtest:summary:dry` npm 명령을 추가했다.
- `playtest_logs/**/*.json`을 gitignore에 추가해 원본 사람 테스트 JSON이 추적되지 않게 했다.
- `docs/HUMAN_PLAYTEST_GUIDE.md`에 테스트 후 요약 절차를 추가했다.
- `docs/CODEX_RUNBOOK.md`, `docs/ai/ai-collaboration.md`, `docs/adr/ADR-002-test-result-planning-pipeline.md`에 human-test result loop를 반영했다.

## 3. 테스트 결과와 근거

- `node --check scripts/summarize_playtests.js`: 통과.
- package.json JSON parse 검사: 통과.
- `npm run playtest:summary:dry`: 로그 0개 상태에서 안전하게 요약 preview 생성.
- `npm run playtest:summary`: `docs/playtest_summaries/2026-06-02.md`와 `docs/review_prompts/2026-06-02-human-playtest.md` 생성.
- `npm run doctor`: 26 pass, 0 warn, 0 fail.
- `npm run doctor:deep`: 34 pass, 0 warn, 0 fail.

## 4. 결정 사항

- 원본 사람 테스트 JSON은 `playtest_logs/`에 두고 git에는 올리지 않는다.
- 추적 가능한 산출물은 요약 Markdown과 human-test prompt만 남긴다.
- 사람 테스트 후에는 바로 기능 구현하지 않고, summary prompt를 Claude/GPT에 보내 방향을 다시 결정한다.

## 5. 문제 또는 리스크

- 현재 생성된 summary는 로그 0개 기준이므로 최종 판단 자료가 아니다.
- 사람 테스트가 5개 미만이면 script가 preparation output으로 표시한다.
- 정성 관찰은 JSON만으로 충분하지 않으므로 `docs/PLAYTEST_NOTES.md`를 함께 작성해야 한다.

## 6. GPT/Claude 인계 요약

- 다음 외부 기획 판단에는 `docs/review_prompts/2026-06-02-human-playtest.md` 같은 사람 테스트 summary prompt를 사용한다.
- Claude/GPT는 Unity groundwork, HTML v0.6, FIX_CORE, NEED_MORE_HUMAN_DATA 중 하나를 판단해야 한다.

## 7. 다음 Codex 작업

- 5-8명 사람 테스트를 진행한다.
- JSON 로그를 `playtest_logs/`에 넣는다.
- `docs/PLAYTEST_NOTES.md`에 관찰 기록을 적는다.
- `npm run playtest:summary` 후 생성된 human-test prompt로 planning pipeline을 실행한다.

## 8. 포트폴리오 메모

- 문제: 사람 테스트 결과가 생겨도 수동 요약만 하면 AI 기획 파이프라인으로 이어지는 근거가 흐려진다.
- 방향: 원본 로그, 정성 노트, 요약 Markdown, Claude/GPT 프롬프트를 분리한다.
- 실행: playtest summary script와 문서 절차를 추가했다.
- 결과: 사람 테스트 이후에도 “테스트 결과 -> AI 기획 판단 -> Codex 구현” 흐름을 반복할 준비가 됐다.

---
