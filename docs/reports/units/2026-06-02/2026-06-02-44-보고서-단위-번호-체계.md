# 2026-06-02-44 - 보고서 단위 번호 체계

## 1. Current build status

- Daily report sections are now split into numbered work units.
- New report headings must use `# 2026-06-02-NN - 작업 제목`.
- Discord `--latest-section` now sends a clearly named work-unit section instead of an ambiguous daily tail.

## 2. What changed today

- Added `scripts/check_report_units.js`.
- Added `npm run report:check`.
- Updated `doctor` and `doctor:deep` to validate report unit headings.
- Updated autonomous dev loop prompts so future Codex task reports use numbered work-unit headings.
- Renumbered existing `docs/reports/2026-06-02.md` top-level work sections from `2026-06-02-01` through `2026-06-02-44`.

## 3. Test results and evidence

- `npm run report:check`: validates numbered report units.
- `npm run doctor`: will fail if the latest report uses ambiguous top-level headings.

## 4. Decisions made

- Keep one daily Markdown file, but divide it by numbered work-unit headings.
- Use the work-unit title as the Discord report title.
- Commit history and report sections should describe the same work unit.

## 5. Problems or risks

- Old content is still long, but it is now searchable by numbered work unit.
- Future automation must not append unnumbered `# 제목` sections.

## 6. GPT handoff summary

- Reporting format is now enforceable.
- Next work should be gameplay implementation, not more report-format cleanup.

## 7. Next Codex tasks

- Implement v0.9 WP2 Slice A pressure high/low rhythm.
- Record the gameplay work under the next numbered report unit.

## 8. Portfolio notes

- Problem: one daily report made individual work units hard to identify.
- Direction: align report units, Discord reports, and commits.
- Action: added heading validation and renumbered the current report.
- Result: future work reports should be readable by feature/task unit.
