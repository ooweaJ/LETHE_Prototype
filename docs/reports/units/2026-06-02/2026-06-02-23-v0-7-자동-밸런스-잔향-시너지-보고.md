# 2026-06-02-23 - v0.7 자동 밸런스/잔향 시너지 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha는 v0.7 weapon/echo balance solo-test candidate 상태다.
- v0.6 1인 체감 피드백에서 나온 “기억을 잃은 뒤 너무 약해져 쫄몹을 못 잡는다” 문제를 자동 밸런스 루프 입력으로 삼았다.
- 넓은 사람 테스트가 아니라, 사용자 1인 체감 테스트로 다시 확인할 단계다.

## 2. 오늘 바뀐 것

- 브라우저 표기와 experiment version을 `v0.7`로 올렸다.
- 기존 무기 2종의 기본 전투력을 보강했다:
  - `절단쌍검`: damage `12 -> 15`, range `74 -> 86`, interval `0.42 -> 0.36`.
  - `장송대검`: damage `34 -> 42`, range `112 -> 128`, interval `1.18 -> 1.02`.
- 잃은 기억이 무기에 남는 잔향 효과를 추가했다:
  - `처형자의 섬광`: 무기 섬광 연쇄.
  - `굶주린 칼무리`: 무기 출혈 잔향.
  - `추적자의 맹세`: 무기 보조 유도탄.
  - `파쇄의 파문`: 무기 파문.
  - `피의 반사`: 무기 출혈/온힛 잔향 보강.
  - `멈춘 초침`: 무기 둔화.
- 사이드 패널 잔향 목록에 무기 잔향 라벨을 추가했다.
- AI 시뮬레이터의 무기 baseDps와 weapon residue echo proxy를 v0.7 방향에 맞췄다.
- v0.7 결과 판단 프롬프트 `docs/review_prompts/2026-06-02-v07-balance-eval.md`를 추가했다.
- 로컬 Codex 판단을 `docs/review_responses/2026-06-02-v07-balance-codex.md`에 저장했다.

## 3. 테스트 결과와 근거

- `npm run autopilot:preflight`: 17 pass, 0 warn, 0 fail.
- `node --check src/game.js`: 통과.
- `node --check alpha_test/src/config.js`: 통과.
- `node --check alpha_test/src/simulator.js`: 통과.
- `node --check alpha_test/src/run_alpha.js`: 통과.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.9339`, Early Fun `0.8772`, Kill Tempo `0.9815`, first cycle completion `82.1%`, two-memory survival `81.6%`, echo pivot `0.7363`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.9131`, Early Fun `0.8793`, Kill Tempo `0.9847`, first cycle completion `84.1%`, two-memory survival `81.5%`, echo pivot `0.7349`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.9136`, Early Fun `0.8787`, Kill Tempo `0.9854`, first cycle completion `82.7%`, two-memory survival `81.1%`, echo pivot `0.7286`.
- Chrome headless cycle QA:
  - version `v0.7`,
  - runTimeline version `v0.7`,
  - status `complete`,
  - `refilled: true`,
  - `hasTimelinePayload: true`.
- Chrome headless level-up regression QA:
  - version `v0.7`,
  - status `complete`,
  - `hasRunGrowthPayload: true`.

## 4. 결정 사항

- v0.7은 로컬 Codex 판단 기준 `GO_TO_SOLO_PLAYTEST_CANDIDATE`다.
- 신규 무기/무기 특수기/상점/메타 진행은 아직 넣지 않았다.
- 먼저 기존 2무기와 잃은 기억의 무기 잔향 시너지로 결손 구간이 버틸 만해졌는지 확인한다.
- 1인 테스트에서 여전히 쫄몹 처리가 무너지면 v0.7.1 자동 밸런스 루프를 실행한다.

## 5. 문제 또는 리스크

- AI 지표상 초반 처치 템포가 크게 좋아졌지만, 실제로 너무 쉬워졌는지는 모른다.
- Echo pivot score는 v0.6보다 살짝 낮아졌다. 무기 바닥 성능이 문제를 해결한 대신, 잔향 변형의 체감이 약해질 위험이 있다.
- 예측율은 여전히 높다.
- 무기 특수기와 신규 무기는 아직 기획 후보일 뿐 구현하지 않았다.

## 6. GPT/Claude 인계 요약

- v0.7 결과 판단 프롬프트는 준비됐다: `docs/review_prompts/2026-06-02-v07-balance-eval.md`.
- 현재 Codex 세션에서는 프로젝트 요약의 외부 Claude 전송이 막히므로, 필요하면 사용자의 신뢰된 로컬 터미널에서 실행한다.
- 이번 자동 루프에서는 로컬 Codex 판단으로 v0.7을 1인 체감 테스트 후보로 둔다.

## 7. 다음 Codex 작업

- 사용자가 v0.7을 1인 테스트한다.
- 특히 기억 상실 후 쫄몹 처리, 무기 바닥 성능, 잔향 무기 시너지, 결손 구간 난이도, 다시 하고 싶은지를 본다.
- 여전히 약하면 v0.7.1에서 결손 구간 적 밀도, 무기 잔향 강도, 무기 특수기 중 하나를 좁게 보정한다.

## 8. 포트폴리오 메모

- 문제: v0.6은 구조는 개선됐지만 실제 플레이에서 상실 후 전투력이 급격히 무너졌다.
- 방향: 새 콘텐츠를 크게 늘리기보다 기존 무기와 잃은 기억의 관계를 강화해 코어 루프를 안정시킨다.
- 실행: 사용자 피드백을 자동 루프 입력으로 받아 v0.7 밸런스/잔향 시너지 구현, AI 테스트, 브라우저 QA, 문서 보고까지 진행했다.
- 결과: LETHE의 자동화는 v0.6에서 멈추지 않고, 피드백 기반 v0.7 밸런스 버전업까지 재현됐다.

---
