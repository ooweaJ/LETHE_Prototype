# LETHE 개발 보고서 - 2026-06-02

## 요약

- Codex 작업 운영 규칙을 정리했다.
- Discord 상태 알림 스크립트를 추가했다.
- GPT v0.2 기획 답변을 한국어 작업 목록으로 변환했다.
- Claude Code 자동 기획 검토 흐름을 구현하고 실제 호출까지 테스트했다.
- 앞으로 GPT/Claude에게 보내는 프롬프트와 Discord 보고 요약은 한국어 중심으로 작성한다.

## 현재 상태

- LETHE HTML alpha v0.1은 실행 가능하다.
- v0.2 방향은 `ITERATE_BEFORE_TEST`로 결정됐다.
- GPT와 Claude 모두 좁은 v0.2 튜닝 후 사람 테스트를 권장했다.
- 다음 작업은 새 콘텐츠 추가가 아니라 망각 루프를 해석 가능한 상태로 보정하는 것이다.
- 보고서 명령은 `docs/reports/`의 최신 날짜 Markdown을 기본으로 사용한다.

## 오늘 한 일

- `scripts/send_codex_notice.js`를 추가해 짧은 Discord 상태 알림을 보낼 수 있게 했다.
- `docs/CODEX_RUNBOOK.md`에 Codex 작업 루프와 권한/중단 대응 규칙을 정리했다.
- `docs/checkpoints/README.md`에 중단 체크포인트 템플릿을 추가했다.
- GPT 답변을 `docs/review_responses/2026-06-02-gpt-v02.md`에 저장했다.
- 루트 `CLAUDE.md`를 추가해 Claude Code 역할과 답변 규칙을 정리했다.
- `scripts/ask_claude_review.js`를 추가했다.
- `npm run review:claude`와 `npm run review:claude:dry`를 추가했다.
- Claude Code를 비대화형으로 호출해 `docs/review_responses/2026-06-01-claude.md`를 생성했다.
- `docs/NEXT_TASKS.md`를 GPT/Claude 답변을 합친 한국어 v0.2 작업 목록으로 다시 정리했다.
- `docs/review_prompts/README.md`와 `docs/review_prompts/2026-06-01.md`를 한국어 프롬프트 형식으로 바꿨다.
- `docs/DISCORD_REPORTING.md`를 한국어 기준으로 다시 정리했다.
- `scripts/send_discord_report.js`가 한국어 제목과 섹션을 우선 읽도록 수정했다.
- `scripts/build_report.js`와 `scripts/send_discord_report.js`가 최신 날짜 보고서를 기본으로 찾도록 바꿨다.

## 테스트 결과와 근거

- `claude --version`: `2.1.153 (Claude Code)`.
- `claude --help`: `-p/--print` 비대화형 출력 지원 확인.
- `node --check scripts/send_codex_notice.js`: 통과.
- `node --check scripts/send_discord_report.js`: 통과.
- `node --check scripts/build_report.js`: 통과.
- `node --check scripts/ask_claude_review.js`: 통과.
- `npm run review:claude:dry`: 프롬프트와 출력 경로 확인.
- `npm run review:claude`: Claude 답변 파일 생성 성공.
- `npm run report`: `docs/reports/2026-06-02.html` 생성 확인.
- `npm run report:discord:dry`: Discord 한국어 요약 형식 확인.
- `npm run report:discord`: 한국어 요약 보고서를 Discord에 전송했다.
- `node scripts/send_codex_notice.js --type=done ...`: Codex 운영 규칙 완료 알림을 Discord에 전송했다.
- `node scripts/send_codex_notice.js --type=done ...`: Claude 자동 검토 완료 알림을 Discord에 전송했다.

## 결정 사항

- GPT/Claude에게 보내는 프롬프트는 기본적으로 한국어로 작성한다.
- 답변 형식에는 반드시 `앞으로 해야 할 일` 섹션을 넣는다.
- Discord 보고는 긴 원문 대신 `작업`, `완료`, `문제`, `기획질문`만 짧게 보여준다.
- GPT는 구현 우선순위와 테스트 기준 검토에 사용한다.
- Claude는 감정선, 문구, 플레이어 경험 검토에 사용한다.
- Claude 자동 검토 때는 `--tools ""`로 도구를 비활성화해 파일을 직접 수정하지 못하게 한다.
- Claude 답변 원문은 Discord가 아니라 `docs/review_responses/`에 저장한다.

## 문제와 리스크

- 예전 문서 일부에는 깨진 한국어 인코딩이 남아 있다.
- 앞으로 새 문서는 깨끗한 UTF-8 한국어로 작성해야 한다.
- Discord는 권한 승인을 대신 누를 수 없고, Codex UI 승인이 필요하면 작업이 멈춘다.
- Claude Code 인증이나 네트워크 문제가 있으면 자동 검토가 멈출 수 있다.
- PowerShell `Get-Content` 출력에서는 Claude 응답 한국어가 깨져 보일 수 있지만, Node로 읽으면 UTF-8 파일은 정상이다.
- 아직 v0.2 구현은 시작하지 않았다.

## 기획 검토 요약

- GPT verdict: `ITERATE_BEFORE_TEST`.
- Claude verdict: `ITERATE_BEFORE_TEST`.
- v0.1은 바로 사람 테스트로 보내기보다 좁은 v0.2 튜닝 후 테스트하는 것이 좋다.
- 첫 망각 타이밍을 8-10분, 가능하면 8.0-9.5분 범위로 늦춘다.
- `처형자의 섬광` 삭제율을 25-35%로 낮춘다.
- JSON 로그에 선택 기억, 예측 기억, 실제 삭제 기억, 삭제 weight 또는 score를 남긴다.
- `echo=0.50`과 기존 `echo=0.60`을 비교 가능하게 로그를 남긴다.
- 망각 후 피드백 화면은 “왜 잃었고 무엇이 남았는지” 쉽게 이해되게 만든다.
- 사람 테스트에는 사라진 기억 이름을 말할 수 있는지 확인하는 자유응답을 추가한다.

## 다음 Codex 작업

- `docs/NEXT_TASKS.md` 기준으로 v0.2 narrow tuning을 구현한다.
- 첫 망각 타이밍을 조정한다.
- 삭제 분포와 JSON 로그를 개선한다.
- echo/ui 실험값과 로그를 정리한다.
- 망각 후 피드백 화면을 명확하게 만든다.
- 사람 테스트용 자유응답 문항을 추가한다.
- 구현 후 AI 테스트와 sweep을 다시 실행한다.

## 포트폴리오 메모

- 문제: 기획 답변을 사람이 수동으로 복사해 붙여넣고, 영어/한국어가 섞인 답변을 다시 작업화해야 했다.
- 방향: GPT/Claude 기획 검토를 Markdown으로 받고, Codex가 바로 작업 목록과 구현으로 이어가게 만든다.
- 실행: Claude Code 자동 호출 스크립트와 `CLAUDE.md`를 추가하고, 실제 Claude 답변 파일 생성을 검증했다.
- 결과: 기획 검토는 파일로 저장되고, Discord는 상태 알림만 맡는 구조가 완성됐다.

---

# v0.2 구현 추가 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha는 v0.2 narrow tuning 후보 상태다.
- AI 1000런 기준 verdict는 `GO_CANDIDATE`다.
- 사람 테스트 전에 브라우저 화면 QA만 한 번 더 보면 된다.

## 2. 오늘 바뀐 것

- 첫 망각 타이밍을 평균 9분으로 늦췄다.
- 기본 실험값을 `echo=0.50`, `ui=0.62`로 바꿨다.
- `처형자의 섬광` 삭제 쏠림을 낮추고 삭제 weight를 로그에 남기게 했다.
- 망각 결과 화면에 사라진 기억, 예측 결과, 삭제 weight, 잔향, 다음 방향을 한 번에 보여주게 했다.
- Q3 자유응답으로 “방금 사라진 기억 이름을 떠올릴 수 있는지”를 추가했다.
- AI 테스트 기준에 예측 상한, 삭제 분포, 회복률 기준을 추가했다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `node --check alpha_test/src/config.js`: 통과.
- `node --check alpha_test/src/simulator.js`: 통과.
- `node --check alpha_test/src/metrics.js`: 통과.
- `node --check alpha_test/src/run_alpha.js`: 통과.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.
- 주요 지표: regret `76.3%`, irritation `1.1%`, prediction `76.3%`, restart `70.0%`, first forgetting `9.00 min`.

## 4. 결정 사항

- 새 기억, 새 보스, 상점, 메타 성장 같은 확장은 아직 하지 않는다.
- v0.2는 “더 만들기”가 아니라 “망각이 이해 가능한 상실인지 확인하는 버전”으로 둔다.
- `echo=0.50`, `ui=0.62`를 기본값으로 유지한다.

## 5. 문제 또는 리스크

- 망각 직후 전투력 딥은 `29.6%`라 목표 `30%`보다 아주 조금 낮다.
- sweep에서는 `ui=0.78` 조합이 더 높은 점수를 냈다. 다만 이번 구현 범위에서는 기획 답변의 기본값을 유지했다.
- 아직 실제 브라우저 화면 QA는 하지 않았다.

## 6. GPT/Claude 인계 요약

- GPT와 Claude 모두 `ITERATE_BEFORE_TEST`를 권장했다.
- Codex는 그 답변을 기준으로 타이밍, 삭제 분포, 피드백, 로그, 자유응답 문항만 구현했다.
- 추가 기획 질문은 지금 당장 필요하지 않다.

## 7. 다음 Codex 작업

- 브라우저에서 v0.2 결과 화면을 직접 확인한다.
- 화면 QA가 통과하면 5-8명용 human playtest 가이드를 만든다.
- 사람 테스트에서는 “아까움 / 짜증 / 무감정” 중 어떤 반응인지와 그 이유를 가장 중요하게 본다.

## 8. 포트폴리오 메모

- 문제: v0.1은 감정 루프는 좋았지만 첫 망각이 빠르고 삭제 결과 해석이 어려웠다.
- 방향: 기능 추가 없이 테스트 결과를 해석 가능한 상태로 보정했다.
- 실행: 타이밍, 삭제 weight, 결과 피드백, JSON 로그, 자유응답 문항을 좁게 구현했다.
- 결과: AI 기준 human test 후보까지 올라왔고, 다음 단계는 실제 플레이어 감정 반응 검증이다.

---

# 로컬 pull 이후 추가 보고

## 1. 현재 빌드 상태

- 원격 `main`을 로컬에 fast-forward로 반영했다.
- LETHE v0.2는 계속 AI 기준 human-test candidate 상태다.
- 브라우저 QA는 시작 화면과 런 시작 흐름까지 확인했고, 결과 화면과 JSON 다운로드는 아직 남아 있다.

## 2. 오늘 바뀐 것

- `docs/HUMAN_PLAYTEST_GUIDE.md`를 추가했다.
- `docs/PLAYTEST_NOTES.md`에 가이드 링크와 반응 라벨 기록 항목을 추가했다.
- `docs/NEXT_TASKS.md`에서 human playtest 가이드 작성 항목을 완료로 표시했다.

## 3. 테스트 결과와 근거

- `npm run ai:test`: `GO_CANDIDATE`.
- Alpha Fun Score `0.7737`.
- regret `76.3%`, irritation `1.1%`, prediction `76.3%`.
- `npm run report`: `docs/reports/2026-06-02.html` 생성 성공.

## 4. 결정 사항

- 사람 테스트는 5-8명 규모로 진행한다.
- 테스트 목적은 콘텐츠 평가가 아니라 망각이 아쉬운지, 납득되는지, 회복 가능한지 확인하는 것이다.
- AI 테스트 결과와 사람 플레이테스트 노트는 섞지 않는다.

## 5. 문제 또는 리스크

- 결과 화면과 JSON 다운로드는 아직 실제 브라우저에서 끝까지 확인하지 못했다.
- 컨텍스트가 길어질 경우 Codex 대화는 요약될 수 있으므로, 진행 상태는 문서에 남긴다.

## 6. GPT 인계 요약

- 추가 GPT 질문은 아직 필요하지 않다.
- 사람 테스트 후 `regret`, `irritation`, `neutral`, `unclear` 반응 분포를 기준으로 v0.3 방향을 물어보면 된다.

## 7. 다음 Codex 작업

- v0.2 결과 화면 브라우저 QA를 완료한다.
- JSON 다운로드에 선택 기억, 예측 기억, 실제 삭제 기억, 삭제 weight가 들어가는지 확인한다.
- 사람 테스트 결과가 모이면 v0.3 방향을 정리한다.

## 8. 포트폴리오 메모

- 문제: AI 지표만으로는 실제 상실감과 짜증을 구분할 수 없다.
- 방향: 5-8명 테스트에서 관찰 질문과 로그 필드를 맞춰 감정 반응을 수집한다.
- 실행: 플레이테스트 가이드와 노트 항목을 문서화했다.
- 결과: v0.2는 사람 테스트를 실행할 준비 문서까지 갖췄고, 남은 것은 결과 화면/JSON 브라우저 QA다.

---

# v0.3 Claude 기획 프롬프트 추가 보고

## 1. 현재 빌드 상태

- LETHE v0.2는 AI 기준 human-test candidate 상태를 유지한다.
- v0.3/version-up 판단은 아직 실행된 Claude 답변이 아니라, 새 검토 프롬프트 준비 단계다.

## 2. 오늘 바뀐 것

- `docs/review_prompts/2026-06-02.md`를 추가했다.
- 프롬프트는 v0.3를 사람 테스트 전에 시작할지, 아니면 v0.2 QA 후 사람 테스트로 갈지 묻는다.
- 프롬프트는 콘텐츠 추가 없이 감정선, 결과 피드백, 테스트 질문만 좁게 검토하도록 제한한다.

## 3. 테스트 결과와 근거

- `npm run review:claude:dry`: 최신 프롬프트로 `docs/review_prompts/2026-06-02.md` 선택 확인.
- 예상 출력 경로: `docs/review_responses/2026-06-02-claude.md`.
- `npm run review:claude`: 실패. 현재 로컬 셸에서 `claude` 실행 파일을 찾지 못했다.

## 4. 결정 사항

- Claude Code 자동 검토 흐름은 유지한다.
- 실제 Claude 호출은 Claude Code가 설치되어 있거나 PATH에 잡힌 환경에서 다시 실행한다.

## 5. 문제 또는 리스크

- 현재 로컬 환경의 PATH에는 `claude`가 없다.
- 아직 Claude v0.3 답변 파일은 생성되지 않았다.

## 6. GPT/Claude 인계 요약

- Claude에게 넘길 질문은 준비됐다.
- 핵심 질문은 “v0.3를 만들기 전에 사람 테스트로 가도 되는가”와 “사람 테스트 전 감정선/피드백에서 꼭 고칠 것은 무엇인가”다.

## 7. 다음 Codex 작업

- Claude Code가 가능한 환경에서 `npm run review:claude`를 다시 실행한다.
- 답변이 생성되면 `docs/review_responses/2026-06-02-claude.md`를 읽고 `docs/NEXT_TASKS.md`에 작업화한다.
- 그 전에는 결과 화면/JSON 브라우저 QA를 계속 우선한다.

## 8. 포트폴리오 메모

- 문제: v0.3를 바로 만들지, 사람 테스트를 먼저 할지 판단 기준이 필요하다.
- 방향: Claude Code에 감정선 중심 검토를 맡기되, 구현과 파일 변경은 Codex가 담당한다.
- 실행: v0.3/version-up 기획 프롬프트와 자동화 dry-run을 준비했다.
- 결과: 자동화 입력은 준비됐지만, 현재 셸에서 Claude Code 실행 파일이 없어 실제 응답 생성은 보류됐다.

---

# v0.2 브라우저 QA 및 사람 테스트 게이트 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha v0.2는 브라우저 QA 기준으로 결과 화면과 JSON payload 검증을 통과했다.
- 사람 테스트는 바로 진행하지 않는다. 전투 연출과 LETHE 고유 시스템 체감이 더 강해진 뒤 진행한다.

## 2. 오늘 바뀐 것

- 브라우저 표기를 `v0.2`로 고쳤다.
- 브라우저 실제 보스/망각 타이밍을 v0.2 목표인 9분 기준으로 맞췄다.
- `?qa=fast` 모드를 추가해 결과 화면과 JSON payload를 빠르게 검증할 수 있게 했다.
- 결과 화면 문구의 조사 오류를 피하도록 문장을 고쳤다.
- 결과 카드 레이아웃을 데스크톱 2열로 바꿔 다운로드 버튼이 QA viewport 안에 보이게 했다.
- 정적 CSS/JS에 버전 query를 붙여 캐시된 파일이 남는 문제를 줄였다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.7798`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.
- 브라우저 QA:
  - `?qa=fast`에서 질문 화면까지 도달했다.
  - 결과 화면 문구가 `처형자의 섬광 기억이 망각되었습니다.`처럼 표시됐다.
  - 결과 패널은 QA viewport에서 내부 스크롤 없이 표시됐다.
  - 설문 입력 후 다운로드 버튼이 활성화됐다.
  - JSON payload에 `memoryNames`, `questionNames`, `forgottenName`, `deletionWeights`, `survey`, `echo`, `experiment`가 들어갔다.

## 4. 결정 사항

- AI 기준으로는 사람 테스트 후보지만, 아직 사람에게 보여줄 완성감은 부족하다고 판단한다.
- 사람 테스트 전 다음 단계는 콘텐츠 확장이 아니라 전투 연출과 망각/잔향 시스템 가시성 강화다.
- `?qa=fast`는 QA용으로만 사용하고, 기본 빌드는 9분 망각 타이밍을 유지한다.

## 5. 문제 또는 리스크

- 현재 공격 연출은 기능 확인 수준이고, “화려한 공격”이나 기억별 개성이 충분히 강하지 않다.
- 자동 기억 발동은 작동하지만, 플레이어가 어떤 기억이 빌드를 이끌고 있는지 순간적으로 알아보기 어렵다.
- 사람 테스트를 지금 하면 시스템보다 프로토타입의 밋밋함에 반응이 섞일 수 있다.

## 6. GPT/Claude 인계 요약

- v0.2 QA는 통과했다.
- 다음 기획 질문은 “사람 테스트 전 전투 연출/시스템 가시성을 어디까지 올릴 것인가”다.
- Claude Code 실제 호출은 로컬 로그인 후 다시 실행해야 한다.

## 7. 다음 Codex 작업

- 기억별 공격 이펙트를 더 구분되게 만든다.
- 의존도 상승, 망각 위험, 잔향 변형이 플레이 중 더 눈에 띄게 만든다.
- QA fast 모드로 결과 화면과 JSON payload를 계속 회귀 검증한다.

## 8. 포트폴리오 메모

- 문제: AI 지표는 통과했지만, 실제 유저에게 보여줄 프로토타입 완성감은 아직 낮다.
- 방향: 사람 테스트를 서두르지 않고, LETHE의 핵심 체감인 화려한 자동 기억 공격과 상실 후 변형을 먼저 강화한다.
- 실행: 브라우저 QA에서 타이밍 불일치, 표기, 결과 문구, 레이아웃, JSON 검증 경로를 고쳤다.
- 결과: v0.2는 검증 가능한 상태가 됐고, v0.3 전 단계의 목표는 사람 테스트용 완성감 확보다.

---

# v0.3 기획 자동화 fallback 및 전투 가시성 개선 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha는 v0.3 pre-human-test polish 후보 상태다.
- AI 1000런 기준 verdict는 계속 `GO_CANDIDATE`다.
- 사람 테스트는 아직 진행하지 않는다. 전투 연출과 시스템 가시성을 먼저 더 끌어올리는 단계다.

## 2. 오늘 바뀐 것

- `scripts/ask_openai_review.js`를 추가해 Claude Code가 안 될 때 GPT 기획 검토를 터미널에서 호출할 수 있게 했다.
- `npm run review:openai`와 `npm run review:openai:dry`를 추가했다.
- `scripts/ask_codex_review.js`를 추가해 Codex CLI로도 같은 기획 답변을 파일로 받을 수 있게 했다.
- `npm run review:codex`와 `npm run review:codex:dry`를 추가했다.
- 브라우저 표기를 `v0.3`으로 올렸다.
- 기억 발동 시 기억 이름이 전투 화면에 플로팅 텍스트로 뜨게 했다.
- 피해 숫자, 타격 스파크, 투사체 trail, 보스 등장/페이즈 impact를 추가했다.
- 기억 슬롯에 `레테의 시선` 태그와 의존도 퍼센트를 표시해 어떤 기억이 삭제 위험에 가까운지 더 잘 보이게 했다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `node --check scripts/ask_codex_review.js`: 통과.
- `npm run review:codex:dry`: 최신 v0.3 프롬프트와 출력 경로 확인.
- `npm run review:openai:dry`: 최신 v0.3 프롬프트와 출력 경로 확인.
- `npm run review:openai`: `OPENAI_API_KEY` 없음으로 중단. 실패 원인이 명확하게 출력된다.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.7798`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.7737`.
- 브라우저 QA:
  - `HTML Alpha Prototype v0.3` 표시 확인.
  - `?qa=fast`에서 질문 화면과 결과 화면까지 도달했다.
  - `레테의 시선` 태그와 의존도 퍼센트가 기억 슬롯에 표시됐다.
  - 결과 패널은 내부 스크롤 없이 표시됐다.
  - JSON payload에 `memoryNames`, `questionNames`, `forgottenName`, `deletionWeights`, `survey`, `echo`, `experiment`가 들어갔다.
  - payload의 `experiment.version`은 `v0.3`으로 기록됐다.

## 4. 결정 사항

- Claude Code는 설치됐지만 로컬 로그인/승인이 필요하므로, 내일 로컬 Mac에서 `claude auth login`을 진행해야 한다.
- Claude가 막힐 때는 먼저 Codex CLI fallback을 사용한다.
- 직접 API key 방식이 필요할 때만 OpenAI API fallback을 사용한다.
- OpenAI fallback도 실제 호출에는 `OPENAI_API_KEY`가 필요하다.
- 외부 기획 검토가 막혀도, 현재 범위에서는 새 콘텐츠를 추가하지 않고 전투 가시성/시스템 전달력만 개선한다.

## 5. 문제 또는 리스크

- 현재 로컬에는 OpenAI API key가 없어 밤새 GPT 기획 검토 자동 실행은 아직 불가능하다.
- Codex CLI 방식도 로컬 Codex 로그인 세션과 플랜 사용 한도에 의존한다.
- Claude Code는 설치됐지만 인증 흐름에서 멈추므로, 원격으로는 브라우저 승인까지 진행할 수 없다.
- 공격 연출은 개선됐지만, 잔향이 “상실 후 변형”처럼 느껴지는지는 아직 결과 화면 중심이라 추가 개선 여지가 있다.

## 6. GPT/Claude 인계 요약

- v0.3/version-up 기획 질문 Markdown은 준비되어 있다.
- Claude 실행은 로컬 로그인 후 가능하다.
- Codex CLI 실행은 `npm run review:codex`로 가능하며, 답변은 `docs/review_responses/YYYY-MM-DD-codex.md`에 저장된다.
- GPT 실행은 `.env` 또는 셸 환경에 `OPENAI_API_KEY`를 넣은 뒤 `npm run review:openai`로 가능하다.

## 7. 다음 Codex 작업

- 잔향 보상이 결과 화면뿐 아니라 다음 플레이 감각으로 더 잘 이어지게 만든다.
- 공격 이펙트가 기억별로 더 구분되는지 브라우저에서 계속 확인한다.
- 사람 테스트는 v0.3 polish가 충분히 보인 뒤 `docs/HUMAN_PLAYTEST_GUIDE.md`로 진행한다.

## 8. 포트폴리오 메모

- 문제: Claude 인증이 막히면 기획 자동화가 멈추고, v0.2는 기능은 통과했지만 전투 체감이 약했다.
- 방향: OpenAI fallback으로 기획 검토 경로를 하나 더 만들고, 사람 테스트 전 핵심 전투 체감을 강화한다.
- 실행: GPT 호출 스크립트와 v0.3 전투 가시성 개선을 추가하고 브라우저 QA로 회귀를 확인했다.
- 결과: 외부 기획 검토는 키/로그인만 남았고, v0.3 프로토타입은 기억 의존도와 망각 위험이 더 눈에 띄는 상태가 됐다.

---

# v0.4 사람 테스트 후보 보고

## 1. 현재 빌드 상태

- LETHE HTML alpha는 v0.4 human-test candidate 상태다.
- AI 1000런과 heavy 5000런/3스테이지 모두 `GO_CANDIDATE`다.
- 다음 큰 단계는 더 많은 기능 추가가 아니라 5-8명 사람 테스트다.

## 2. 오늘 바뀐 것

- 브라우저 표기를 `v0.4`로 올렸다.
- 기본 AI 테스트의 UI 선명도 값을 `ui=0.78`로 올렸다.
- 결과 화면에서 `사라진 행동`과 `잔향 변형`을 분리했다.
- 잔향 변형 카드에 요약 문장과 stat badge를 추가했다.
- JSON payload에 `echoTransformation`을 추가했다.
- AI 테스트 기본 명령과 alpha test 문서 예시를 `echo=0.50`, `ui=0.78` 기준으로 맞췄다.

## 3. 테스트 결과와 근거

- `node --check src/game.js`: 통과.
- `node --check alpha_test/src/config.js`: 통과.
- `node --check alpha_test/src/run_alpha.js`: 통과.
- `npm run ai:test:quick`: `GO_CANDIDATE`, Alpha Fun Score `0.8190`.
- `npm run ai:test`: `GO_CANDIDATE`, Alpha Fun Score `0.8261`, regret `85.6%`, irritation `0.4%`, prediction `85.8%`.
- `npm run ai:test:heavy`: `GO_CANDIDATE`, Alpha Fun Score `0.8369`, regret `87.6%`, irritation `0.3%`, prediction `87.7%`.
- `npm run ai:sweep`: `echo=0.50`, `ui=0.78`이 현재 가장 안정적인 `GO_CANDIDATE` 프리셋임을 확인했다.
- 브라우저 QA:
  - `HTML Alpha Prototype v0.4` 표시 확인.
  - `?qa=fast`에서 질문 화면과 결과 화면까지 도달했다.
  - `잔향 변형` 카드와 echo badge가 표시됐다.
  - 결과 패널은 내부 스크롤 없이 표시됐다.
  - JSON payload에 `echoTransformation`, `uiClarity: 0.78`, `experiment.version: v0.4`가 들어갔다.

## 4. 결정 사항

- 지금부터는 Codex가 더 큰 콘텐츠를 추가하기보다 사람 테스트를 위한 안정화 상태로 본다.
- 상점, 메타 진행, 최종 보스, 기억 추가, 다중 지역 구조는 여전히 만들지 않는다.
- 남은 `29.4-29.6%` power drop soft warning은 코드로 과보정하기보다 사람 테스트에서 실제 체감으로 확인한다.

## 5. 문제 또는 리스크

- Alpha Fun Score는 올랐지만 목표 `0.89+`에는 아직 못 미친다.
- 망각 직후 전투력 딥은 목표 30%보다 아주 조금 낮다.
- AI 지표가 좋아져도 실제 사람이 잔향을 “상실 후 변형”으로 느낄지는 테스트가 필요하다.

## 6. GPT/Claude 인계 요약

- 외부 기획 검토 없이도 v0.4는 사람 테스트 후보까지 도달했다.
- Claude/GPT에게 다음에 물을 질문은 “사람 테스트 결과를 어떻게 해석해 v0.5 방향을 잡을지”다.
- Codex CLI fallback은 준비되어 있지만, 최종 기획 판단은 사람 테스트 후 GPT/Claude에 다시 묻는 것이 좋다.

## 7. 다음 Codex 작업

- `docs/HUMAN_PLAYTEST_GUIDE.md` 기준으로 5-8명 테스트를 진행한다.
- JSON 로그와 관찰 메모를 분리해서 수집한다.
- 테스트 후 regret/irritation/neutral/unclear 비율과 발화 내용을 요약한다.

## 8. 포트폴리오 메모

- 문제: v0.3는 전투 가시성은 좋아졌지만, 잔향이 단순 보상처럼 보일 위험이 있었다.
- 방향: 결과 화면에서 “잃은 행동”과 “남은 변형”을 분리해 LETHE의 핵심 감정을 명확히 한다.
- 실행: v0.4 결과 화면, 로그 payload, AI 테스트 기본값, 브라우저 QA를 한 번에 맞췄다.
- 결과: AI 기준 주요 게이트를 통과했고, 이제 실제 사람 테스트만 남은 후보 상태가 됐다.
