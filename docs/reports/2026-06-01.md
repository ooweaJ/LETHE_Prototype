# LETHE 개발 보고서 - 2026-06-01

## Executive Summary

- 오늘 한 일: LETHE HTML 알파 프로토타입 v0.1을 구현하고, AI 알파테스트 시스템을 통합했으며, GitHub 레포에 초기 버전을 push했다. 이후 Codex-GPT 협업 문서와 포트폴리오용 보고서 생성 구조까지 추가했다.
- 현재 상태: 브라우저에서 실행 가능한 정적 HTML 프로토타입이며, “보스 처치 → 예측 질문 → 의존도 기반 망각 → 잔향 획득 → Q1/Q2 설문 → JSON 로그 다운로드”까지 한 번의 코어 루프가 동작한다.
- 핵심 결과: AI 테스트 기준으로 망각 루프는 가능성이 있다. 다만 사람 테스트 전에 첫 망각 타이밍, `처형자의 섬광` 삭제 쏠림, 잔향 강도를 기획적으로 판단해야 한다.
- 다음 판단: GPT에게 v0.2의 우선순위를 물어보고, Codex는 그 결과에 따라 구현/테스트/보고서 갱신을 진행한다.

## 오늘 만든 것

### 1. HTML 알파 프로토타입 v0.1

구현 파일:

- `index.html`
- `style.css`
- `src/game.js`

구현 기능:

- 무기 2종: 절단쌍검, 장송대검
- 기억 6종 중 3슬롯 선택
- 자동 기본 공격
- 자동 기억 발동
- 적 4종
- 보스 1종 3페이즈: 기억을 씹는 자
- 의존도 기반 망각
- 망각 시 잔향 스탯 획득
- 보스 처치 후 질문 UI
- 망각 결과 화면
- Q1/Q2 설문
- JSON 로그 다운로드

제외한 기능:

- 메타 진행
- 상점
- 최종보스
- 기억 12종 이상
- 다영역 런 구조

### 2. AI 알파테스트 시스템 통합

`LETHE_alpha_ai_test_system_v01.zip`을 `alpha_test/`로 통합했다.

목적:

- 실제 사람 감정을 대체하는 것이 아니라, 사람 테스트 전에 구조적 위험을 거르는 사전 필터로 사용한다.
- “망각됐을 때 아쉬운가, 짜증나는가”를 봇 기반 프록시 지표로 확인한다.

실행 명령:

```bash
npm run ai:test
npm run ai:test:quick
npm run ai:sweep
```

### 3. GitHub 레포 정리 및 push

레포:

- `https://github.com/ooweaJ/LETHE_Prototype.git`

오늘 push된 주요 커밋:

- `810c567 Initial LETHE HTML alpha prototype`
- `7525441 Add development workflow docs`
- `e2fadec Add report generation workflow`

Git 작성자 설정:

- `ooweaJ <121000013+ooweaJ@users.noreply.github.com>`

### 4. Codex-GPT 협업 문서 구조

추가 문서:

- `docs/WORKFLOW.md`
- `docs/CODEX_STATUS.md`
- `docs/GPT_REVIEW_PROMPT.md`
- `docs/NEXT_TASKS.md`
- `docs/DECISIONS.md`
- `docs/PLAYTEST_NOTES.md`
- `docs/devlog/2026-06-01.md`

운영 방향:

- Codex는 구현, 테스트, Git, 상태 보고를 담당한다.
- GPT는 기획 검토, 우선순위 결정, 테스트 결과 해석을 담당한다.
- Codex는 작업 후 상태 보고서를 갱신하고, GPT에게 넘길 질문을 명확히 남긴다.

### 5. 개발 보고서 생성 구조

추가 파일:

- `docs/reports/report-template.md`
- `docs/reports/2026-06-01.md`
- `docs/reports/2026-06-01.html`
- `scripts/build_report.js`

실행 명령:

```bash
npm run report
```

방향:

- 보고서는 Markdown을 먼저 작성한다.
- HTML은 Markdown을 기준으로 자동 생성한다.
- 토큰이 끊겨도 Markdown 원본을 기준으로 이어갈 수 있게 한다.

### 6. 프로젝트 에이전트 규칙

루트에 `AGENTS.md`를 추가했다.

포함한 규칙:

- 구현 전 현재 문서/파일 확인
- 변경 후 테스트 실행
- `CODEX_STATUS.md`, devlog, report 갱신
- 보고서 필수 8항목 유지
- GPT에게 넘길 내용 명시
- 스코프 밖 기능 추가 금지

## 테스트 결과

### 실행한 명령

```bash
node --check src/game.js
npm run ai:test
npm run ai:sweep
npm run report
```

### AI 기본 테스트 결과

- Verdict: `ITERATE`
- Playability: `튜닝 후 재검증`
- Risk Level: `MEDIUM`
- Alpha Fun Score: `0.8647`
- 아쉬움 분면: `92.7%`
- 짜증 분면: `0.6%`
- 예측 일치율: `92.7%`
- 삭제 직후 즉시 종료율: `0.9%`
- 런 재시작 의향: `70.7%`
- 망각 직후 전투력 딥: `29.9%`
- 새 기억 후 회복률: `95.9%`
- 첫 망각 전 사용 시간: `6.83분`

### 경고 지표

- 첫 망각 전 시간이 목표보다 짧다. 목표는 8~10분, 현재 평균은 6.83분이다.
- `처형자의 섬광` 삭제율이 46.4%로 높다.
- 망각 직후 전투력 딥이 29.9%로 목표 하한 30%에 약간 못 미친다.

### 스윕 결과

가장 좋은 후보:

- `echo=0.5`
- `ui=0.62`
- Verdict: `GO_CANDIDATE`
- Alpha Fun Score: `0.9053`

해석:

- 현재 기본값 `echo=0.60`보다 `echo=0.50`이 더 나은 후보일 수 있다.
- 다만 AI 테스트는 사람 감정의 대체가 아니므로 GPT 기획 검토와 사람 테스트가 필요하다.

## GPT에게 반드시 전달할 내용

### 현재 상태

- LETHE HTML alpha v0.1은 실행 가능하다.
- 코어 루프는 구현됐다.
- AI 테스트 기준으로 망각 루프는 가능성이 있지만, 사람 테스트 전 튜닝 판단이 필요하다.
- 메타 진행, 상점, 최종보스, 기억 확장은 아직 만들지 않았다.

### GPT에게 물어볼 질문

1. 첫 망각 시간을 8~10분 쪽으로 늦겨야 하는가?
2. `처형자의 섬광`이 자주 삭제되는 것을 게임의 감정적 중심으로 봐야 하는가, 아니면 밸런스 문제로 봐야 하는가?
3. 잔향 기본 강도를 `0.60`에서 `0.50`으로 낮춰야 하는가?
4. v0.2의 목표는 상실감 강화, 예측성 강화, 피벗 명확화 중 무엇이어야 하는가?
5. 지금 사람 테스트로 넘겨도 되는가, 아니면 v0.2 튜닝 후 넘겨야 하는가?

### GPT에게 요구할 출력

- Verdict: `GO_TO_HUMAN_TEST` / `ITERATE_BEFORE_TEST` / `FIX_CORE`
- Codex가 구현할 다음 작업 1~3개
- 다음 AI 테스트 성공 기준
- 사람 플레이테스트 관찰 기준
- 이번 라운드에서 만들지 말아야 할 것

## 오늘의 결정

| 결정 | 이유 | 영향 |
|---|---|---|
| v0.1은 코어 망각 검증에만 집중한다 | 첫 검증 질문이 “아쉬운가, 짜증나는가”이기 때문이다 | 상점, 메타, 최종보스, 콘텐츠 확장은 보류 |
| AI Q1/Q2는 감정 프록시로만 본다 | 봇은 실제 상실감과 납득감을 느낄 수 없다 | 최종 판단은 사람 테스트로 확인 |
| Codex와 GPT 역할을 분리한다 | 구현/검증과 기획 판단은 다른 종류의 피드백이다 | Codex는 실행, GPT는 우선순위 판단 |
| 개발 보고서를 Markdown 먼저 작성한다 | 작업 중 토큰이 끊겨도 원본 기록이 남아야 한다 | HTML 보고서는 명령어로 생성 |
| 루트 `AGENTS.md`를 둔다 | 저장소를 다시 열어도 작업 규칙을 유지하기 위해서다 | Codex가 프로젝트 규칙을 자동 참고 가능 |

## 문제와 리스크

- 현재 전투는 검증용 단순 모델이다.
- 브라우저 시각 검증은 제한적으로만 진행됐다.
- AI 테스트 수치는 사람 감정 검증을 대체하지 않는다.
- 첫 망각 타이밍이 짧아 실제 플레이어가 기억에 충분히 애착을 갖지 못할 수 있다.
- `처형자의 섬광` 삭제 쏠림이 의도된 감정 설계인지, 밸런스 문제인지 아직 판단이 필요하다.

## 다음 Codex 작업

- [ ] GPT에게 이 보고서의 “GPT에게 반드시 전달할 내용” 섹션을 전달한다.
- [ ] GPT 답변을 `docs/NEXT_TASKS.md`에 반영한다.
- [ ] v0.2 우선순위가 확정되면 해당 작업만 구현한다.
- [ ] 구현 후 `npm run ai:test`, `npm run ai:sweep`, `npm run report`를 실행한다.
- [ ] `docs/CODEX_STATUS.md`, devlog, report를 갱신한다.

## 포트폴리오용 정리

### 문제

로그라이크 게임에서 강한 능력을 삭제하는 시스템은 쉽게 “아쉬움”이 아니라 “짜증”으로 받아들여질 수 있다. LETHE의 핵심 문제는 의존한 기억을 잃는 경험이 납득 가능한 상실로 작동하는지 검증하는 것이었다.

### 방향

완성 게임을 만들기보다, 하나의 코어 루프를 검증 가능한 형태로 좁혔다. 플레이어가 기억 3개를 구성하고, 보스를 처치한 뒤, 가장 의존한 기억을 잃고, 잔향으로 피벗하는 흐름만 구현했다.

### 실행

HTML 프로토타입을 만들고, AI 알파테스트 도구를 통합했다. 자동 테스트는 수백~수천 런을 돌려 삭제 기억 분포, 예측 일치율, 아쉬움/짜증 프록시, 망각 직후 전투력 하락, 잔향 후 회복률을 측정하게 했다.

### 결과

AI 테스트 기준으로 아쉬움 프록시는 92.7%, 짜증 프록시는 0.6%로 나왔다. 코어 루프는 가능성이 있지만, 첫 망각 시간이 짧고 `처형자의 섬광` 삭제 쏠림이 높다는 구체적인 튜닝 과제가 드러났다.

### 포트폴리오 문장 초안

추상적인 게임 기획 질문인 “강한 능력을 잃는 것이 짜증이 아니라 아쉬움으로 작동하는가?”를 HTML 프로토타입과 AI 알파테스트 시스템으로 검증 가능한 문제로 전환했다. 구현 범위를 코어 루프 하나로 제한하고, 자동화된 테스트 지표를 통해 다음 기획 판단에 필요한 근거를 만들었다.

## 형상관리 메모

### 사용한 커밋 규칙

한국 개발팀에서도 많이 쓰는 Conventional Commits 형식을 따른다.

예:

- `feat: 기억 자동 발동 구현`
- `fix: 보스 처치 후 질문 UI 오류 수정`
- `docs: 오늘 개발 보고서 갱신`
- `test: AI 알파테스트 시나리오 추가`
- `chore: gitignore 정리`

### 올바른 커밋 타이밍

- 기능 하나가 동작하고 검증됐을 때 커밋한다.
- 문서/보고서처럼 독립적으로 의미 있는 변경은 코드와 분리해서 커밋한다.
- 실험성 변경은 테스트 결과까지 확인한 뒤 커밋한다.
- 원격 push는 작업 단위가 끝나고 로컬 상태가 깨끗할 때 한다.
- 생성 산출물은 필요한 경우만 커밋하고, 재생성 가능한 대용량 출력은 `.gitignore`로 제외한다.

## Changelog

- Added: HTML alpha prototype
- Added: AI alpha test tooling
- Added: GitHub repository setup
- Added: Codex-GPT workflow docs
- Added: daily devlog structure
- Added: report Markdown/HTML generation
- Added: root `AGENTS.md`
- Changed: README에 실행/테스트/기록 방법 추가
- Commit: `810c567 Initial LETHE HTML alpha prototype`
- Commit: `7525441 Add development workflow docs`
- Commit: `e2fadec Add report generation workflow`

